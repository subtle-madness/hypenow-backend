# 결정 기록

> 이 파일은 `ARCHITECTURE.md` §7(결정 기록)을 분리한 것이다. ARCHITECTURE.md는 매 세션
> 자동으로 통째 로드되는데, 결정 기록이 누적되며 전체 74%(177KB)를 차지해 세션마다
> 불필요한 토큰을 태우고 있었다(2026-07-30). **이 파일이 결정 기록의 정본**이며, 새 결정은
> 여기에 추가한다.

> 새 결정은 맨 위에 추가. 전말은 링크된 dated 문서에.

| 날짜 | 결정 | 근거/상세 |
|---|---|---|
| 2026-09-04 | **자체크롤(self) 건전성은 self 시리즈 유무가 아니라 Hiker `fallback:*` 비율로 판정한다 - 폴백 무음 로그·서킷 무관측 공백을 메우고, 프로필·피드 self 확장은 보류** - 09-04 "self 라우팅 사망" 의심은 오진이었다(원인: self 1순위 빈을 쓰는 기동 백필 워크로드 소진, 설정·서킷·프록시 전부 정상). 오진의 직접 원인은 관측 구조: self 실패 → Hiker 폴백이 메트릭 `instagram_source_route_total{backend="hiker",outcome="fallback:<클래스>"}`로만 남고 로그가 없으며, `SurfaceCircuitBreaker`(60초 쿨다운·half-open은 이미 구현)에 트립·복구 로그도 게이지도 없고, Grafana에 이 메트릭 패널이 없었다. 수정: ① 폴백 경로 rate-limited WARN(키 (path, 에러클래스)별 30초 1건 + 억제 건수 합산, `SelfCrawlException`에 surface 부착) ② 서킷 트립·half-open(사이클당 1회)·복구 로그 + monitoring 게이지 `instagram.source.self.circuit.open{surface,source=batch|sync}`(모듈은 순수 JDK라 게이지 등록은 monitoring `SurfaceCircuitGauges`) ③ `[흐름] 비용·외부 의존` 대시보드에 self 건전성 행. **self-paths 확장은 보류**: `fetchRecentPosts`의 `api/v1/feed/user`가 운영 프록시(레지덴셜·모바일) 경유 실측 3/3 전부 401 `require_login`(`igweb_rollout:true`)로, 09-02부터의 wpi 차단과 같은 로그아웃 API 계열 차단. 같은 프록시로 embed는 200이라 프록시 문제 아님. 2단계(`ig-source.self-user-triggered=true`)는 4흐름 모두 응답 후 백그라운드라 동기 지연 0, 영향은 self-paths의 fetchPost·fetchComments뿐이며 롤백은 app_setting 한 줄(5초 반영) - 켜기 전 24h 폴백률 10% 미만·서킷 게이지 0·partial 1% 미만 확인 후 사용자 승인으로 토글 | [트랙 RR](docs/tracks/RR-인스타-수집-하이브리드.md), [스펙 §8](docs/superpowers/specs/2026-08-31-instagram-hiker-selfcrawl-hybrid-design.md) |
| 2026-09-04 | **공동구매 규칙에 부정·회의 문맥 애매 분류 추가 - `공구❌`·`공구 아님`·`공구 안 해요`·`공구템`은 LLM으로** - 운영 검수에서 맨몸 공구 확정 참 4,788건 중 38건이 부정 표현·리뷰. 진짜 공구가 섞여 있어 확정 거짓 대신 애매 | [스펙 §3 정정 2](docs/superpowers/specs/2026-09-03-group-purchase-judgment-design.md), `GroupPurchaseRule` |
| 2026-09-04 | **공동구매 규칙 도구 어휘 축소 - `드릴`은 명사형만, `없이`는 `공구 없이`만** - 운영 실측 후보 4,808건 중 애매 1,717건의 원인이 "드릴게요"(995)·"부담없이"(957)라 LLM 콜 1,700건·오탐 2건을 유발. "LLM이 걸러주니 무해" 전제 폐기, 좁힌 뒤 애매 239건 | [스펙 §3 정정](docs/superpowers/specs/2026-09-03-group-purchase-judgment-design.md), `GroupPurchaseRule` |
| 2026-09-03 | **브랜드 해시태그 자동 시드 재설계 - 계정명 절삭 폐기, monitoring은 계산만 / was가 유일한 작성자** - 자동 태그 재료를 "계정명 소문자화 후 첫 무효 문자 앞까지 절삭"(2026-08-17 be39cbd7, 08-27 §4로 was에 이식)에서 **3단 계산**으로 바꾼다: ① 그 브랜드에 태그된 게시물 캡션의 해시태그 빈도(최다 태그의 등장 게시물 수 ≥ 7) ② 미달이면 AI가 **IG 표시명(brand_account.full_name)** + 계정명에서 브랜드 상호를 뽑아 1개 ③ 그래도 없으면 계정명에서 점·언더스코어를 뺀 값. **결과는 브랜드당 항상 1개**이고 경쟁사도 같다. 절삭은 점이 든 계정명에서 계정과 무관한 일반어를 만들었다(`dr.piel_official` → `#dr` 전량 스윕 = 무관 게시물 대량 유입 + Hiker 콜 낭비) - 점 든 계정명은 그 자체로 해시태그가 될 수 없어 문자열을 어떻게 잘라도 결과가 추측이고, 실제 소비자가 쓰는 태그는 `#닥터피엘` 같은 상호다. AI 입력에서 **회사명(app.users.company_name)·바이오는 제외** - 회사명은 법인명이라 소비자가 태그에 쓰는 브랜드 상호와 다르고, 경쟁사 연결에는 아예 없다(사용자 결정). **역할 분담: monitoring은 계산만 하고 DB에 쓰지 않는다**(내부 조회 API `GET /api/brands/{username}/hashtag-suggestion` 1개, 응답 `{path, tag, topCount, candidatePosts}`) - **08-28 "태그 생성 권한 was 일원화" 결정을 그대로 유지**한다. 1차 초안이던 A안(monitoring이 직접 `brand_hashtag`에 심고 was도 자기 장부에 쓰는 구조)은 **같은 태그를 두 모듈이 각자 쓰게 만들어** 폐기했다(사용자 지적) - 쓰기 주체가 갈리면 "누가 심었나"가 불명확해지고 08-28에 정리한 권한 경계가 도로 무너진다. **시점은 링크 생성이 아니라 초기 백필 완료 뒤**(그때야 캡션 모수가 존재한다) - was 단건 폴링(수집 완료 시)·태그 목록 조회·해시태그 게시물 목록/개수 조회에 같은 훅(`ensureAutoSeeded`). 중복·부활 방지는 브랜드당 시드 기록 1행(`app.brand_hashtag_seed`, 계산 1회) + 링크별 `hashtag_seeded_at`(장부 삽입 사용자당 1회) - 사용자가 지운 자동 태그는 되살아나지 않고, 이미 사용자 관리 태그가 있는 브랜드는 SKIP만 기록한다. 임계·stoplist·AI 킬스위치는 monitoring app_setting 런타임 토글. FE 계약 변화 없음(자동 태그가 "등록 즉시"가 아니라 "수집 완료 뒤 첫 조회"에 나타나는 타이밍만 바뀜 - FE 통지 1건). 운영 정리는 이미 심긴 절삭 태그를 monitoring·was 양쪽에서 삭제하는 것뿐이고, **재시드는 훅이 자동으로 한다**(스크립트·replay 호출 불요). | [spec 2026-09-03](docs/superpowers/specs/2026-09-03-brand-hashtag-auto-seed-redesign-design.md), [plan](docs/superpowers/plans/archive/2026-09-03-brand-hashtag-auto-seed-redesign.md) |
| 2026-09-04 | **발굴 카드 최근 썸네일은 분석 여부와 무관하게 노출 - #749의 "분석 완료만" 필터 원복** - 인플루언서 상세 최근 콘텐츠와 동일 규칙, 2단계 분리로 6.3 드로어가 D+1부터 200이라 죽은 썸네일 우려는 당일 게시물뿐(수용) | 핫픽스 PR 참조 |
| 2026-09-03 | **PR #741 후속 감사 결함 4건 수정** — ① 후행(댓글·광고 판정) 완료 시점에 `touchProgress`가 안 찍혀 was 캐시(ETag·`BrandIndexCache`)가 회전하지 않던 결함 수정 — `BrandCollectService.enrich`의 후행 태스크 finally에 `brands.touchProgress(brand.id())` 추가(계약 §11을 "이후 폴링 없이 조용히 채워진다"에서 실제 메커니즘으로 정정, v2.18) ② 재등록(`insertOrReactivate`)·기간확장(`expandWindow`)이 `backfill_attempts`·`backfill_attempted_at`을 리셋하지 않아, 상한 소진 상태에서 재가입하면 첫 실패가 재시도 없이 곧장 exhausted 문구로 떨어지던 결함 수정 — 두 UPDATE에 `backfill_attempts = 0, backfill_attempted_at = NULL` 추가 ③ `@SpringBootTest` 컨텍스트에서 백필 재시도 스케줄러(5분 틱)가 함께 도는 문제 — test resources에 `application.yml`을 신설하는 안은 기각(실측: Gradle 테스트 클래스패스에서 `build/resources/test`가 `build/resources/main`보다 먼저 잡혀, Spring Boot의 단일 리소스 로딩이 test 쪽만 읽고 main `application.yml` 전체를 가린다 — `ClassLoader.getResources("application.yml")`로 직접 확인), 대신 영향받는 7개 `@SpringBootTest` 클래스에 `monitoring.brand.backfill-retry.enabled=false`를 `properties`로 개별 주입(기존 `SweepControllerTest` 관용구와 동형) ④ compose 킬 스위치 `MONITORING_BRAND_BACKFILL_RETRY_ENABLED`(기본 true) 노출 + `deploy/README.md`에 끄는 법 기록 — staging(`compose.test.yaml`)은 운영 `compose.yaml`을 상속하지 않는 완전 별도 서비스 정의라 이 키가 없으면 이미지 기본값(true)으로 켜진 채 배포되는데, 기존 유사 킬 스위치(예: 브랜드 광고 판정 `monitoring.brand.ad-disclosure.enabled`)도 compose 배선이 아예 없는 선례를 따라 staging 쪽 오버라이드는 추가하지 않기로 결정(판단만, 끄지 않음) | `monitoring-was-contract.md` v2.18, `docs/tracks/MON-BT-브랜드-태그-모니터링.md` |
| 2026-09-03 | **브랜드 초기 백필 열거 실패에 5분 주기 복구 스케줄러 신설(최대 3회·선형 백오프 5·10·15분), 시도 횟수는 DB 컬럼으로 영속화** — 종전에는 `backfill_error`만 찍고 재시도 주체가 없어 복구가 익일 KST 02:00 정기 스윕뿐이었는데, 사용자에게 내려가는 문구는 "자동으로 재시도 중이에요"라 사실과 달랐고 FE는 그 사이 `collectionCompletedAt == null`로 목록 전체를 계속 폴링했다. 후보는 `status=ACTIVE AND last_swept_on IS NULL AND backfill_error IS NOT NULL`(정상 진행 중은 `backfill_error`가 null이라 자연 제외) + 나이 창 6시간(앵커는 `registered_at`이 아니라 등록·재등록·기간확장 모두가 갱신하는 `collection_started_at` — was의 FE 폴링 앵커와 같은 값) + 틱당 배치 상한. **시도 횟수는 메모리 카운터가 아니라 Flyway expand 컬럼(`backfill_attempts`·`backfill_attempted_at`)**: monitoring은 롤링이 아닌 재기동 배포라(deploy/README §5-1) 메모리 카운터는 배포마다 리셋되는데, 백필이 무더기로 실패하는 상황은 대개 벤더 장애(08-27 Hiker 503)라 그 와중의 재배포가 장애 중 벤더에 재시도를 다시 3배 쏟는 증폭 구조가 된다. 관측성(왜 재시도가 멈췄나를 SQL로 즉답)과 `backfill_error`와의 상태 동거도 근거. 중복 `sweepCore` 방지는 3중 — 틱 AtomicBoolean, `BrandSweepScheduler`의 private 가드를 공유 컴포넌트 `BrandSweepGuard`로 추출해 야간 스윕 중 틱 전체 스킵, 브랜드 단위 in-flight 셋(프로세스 로컬 — DB에 두면 프로세스 사망 시 영구 락). 등록·확장 제출 경로에는 in-flight 가드를 넣지 않는다(확장 요청이 조용히 스킵되는 행동 변화 방지). 시도 카운트는 제출 직전 증가(완료 시 증가는 재시도 중 프로세스 사망 시 예산 환불 = 무한 재시도), 성공 시 `touchSwept`가 `backfill_error`와 함께 0으로 클리어. 상한 소진 시 문구를 "다음 새벽 정기 수집에서 다시 시도해요"로 교체. 킬 스위치(`monitoring.brand.backfill-retry.enabled`, 기본 true)가 꺼져 있으면 즉시 실패 문구도 재시도 예산을 약속하지 않도록 곧장 exhausted 문구를 쓴다 | `BrandBackfillRetryJob` javadoc, 마이그레이션 `V20260903084414__brand_account_backfill_retry.sql`, 계약 `docs/contracts/monitoring-was-contract.md` §11 |
| 2026-09-03 | **브랜드 등록 백필 완주 스탬프(`backfill_completed_at` = FE `collectionCompletedAt`)의 의미를 "전 페이지 게시물·게시자 보강 정산 완료"로 좁히고, 댓글 수집·광고 표기 판정을 스탬프 밖 후행 단계로 분리** — 종전 계약("댓글·판정까지 포함한 완주")은 2,000건급 브랜드에서 스탬프를 수십 분 미뤄, FE가 `collectionCompletedAt == null`을 미완주로 읽고 계정 목록 전체(2,281건)를 60초마다 재조회하게 만들었다. 좁히는 근거 3가지: ① was는 이 값을 해석 없이 통과시킬 뿐이라 "판정 포함 완주"에 의존하는 소비자가 없다(`BrandAccountAssembler`는 `last_swept_on`·`last_swept_at`·`backfill_error`만 본다 — 별도 스탬프 컬럼 신설은 그래서 기각) ② 두 후행 단계는 이미 실패를 삼켜 스탬프를 막지 못하므로("실패해도 찍힘") "느리면 미룸"은 비대칭이었다 ③ 각자 백스톱이 있다 — 광고 판정은 `backfillUnjudged`가 전역 `ad_verdict IS NULL`을 기동 즉시·매일 밤 상한 없이(180일 창 밖 포함) 흡수하고, 댓글은 `comments_collected_count` 워터마크가 다음 스윕에서 연다. 진행 관측은 게시물별 `adDisclosure`·`commentsCollectedCount`로 가능하다. 배선은 신설 전용 `brandFollowupExecutor`(기본 1스레드) detached 제출 — 기존 `brandEnrichExecutor` 재사용은 08-18 사고(LLM이 보강 큐를 점유) 재현이라 기각. 전역 Hiker 동시 콜 상한 14는 불변(후행 콜도 `brandEnrichWorkerPool` 10을 통해 나간다). **야간 스윕·해시태그·direct 동기 경로는 무변** — 후행 실행기를 파라미터로 받아 그 경로들은 direct executor(`Runnable::run`)를 넘겨 현행과 동일 의미를 유지한다 | `BrandCollectService.enrich`·`BrandRepository.touchSwept` javadoc, 계약 `docs/contracts/monitoring-was-contract.md` §11 |
| 2026-09-03 | **브랜드 태그 스윕 브랜드 단위 병렬화(동시성 4) — 6시간 44분 → 105~140분 기대** — 139브랜드 직렬 스윕이 8일 만에 4배(추세 유지 시 04:30 미러 침범). Loki 마커로 브랜드별 소요를 역산하니 직렬 합계 404분 중 상위 13개가 343분이고, 본체는 Hiker 대기(컨테이너 CPU 평균 0.03코어)라 브랜드 간 병렬이 정답. `brandSweepExecutor`(`monitoring.brand.sweep-concurrency:4`)에 브랜드 1건=태스크 1건, 전날 `brand_call_count` 내림차순 LPT 배정(`findActiveHeaviestFirst`), 3단계 격리·touchSwept 불변. 동반: direct 2단계 겹침 가드를 서비스 전역 AtomicBoolean → 브랜드 키 집합 `busyBrands`(전역이면 병렬 브랜드가 서로의 2단계를 매일 건너뛴다), Hikari 풀은 10 유지(초안 20은 실측 기각 — 직렬 스윕 중 활성 커넥션 최대 1/10·대기 0, yml에 두면 테스트 컨텍스트가 물려받아 CI가 깨지는 부작용도 확인), 전역 Hiker 동시 콜 예산 14→17(램프 실측 20 이내), 브랜드별 단계 소요 로그 신설. 롤백은 동시성 1. 스윕 뒤 아카이브 57분은 범위 밖(전체 잡 종료 KST 04:45~05:20). | [설계](docs/superpowers/specs/2026-09-03-brand-sweep-parallel-design.md) |
| 2026-09-03 | **콘텐츠 AI 분석 2단계 분리 - 파트 A(사실: 광고 판정·카테고리·브랜드, 캡션만 의존)는 D+1 새벽 배치로 먼저, 파트 B(해석 5필드·기준선 인용)만 성숙(D+4) 후 배치** - 09-03 운영 점검으로 "업로드 후 4일간 광고 판정·카테고리 공백"의 원인이 품질이 아니라 후보 뷰 04의 성숙 게이트 위치임을 확인. 통합 1콜을 phase로 나눠(`ContentAnalysisJob` FACTS/SYNTHESIS, `GeminiContentSynthesizer` 프롬프트를 배치 제출), `v_fact_candidates`(성숙 무관) 신설 + 기존 `v_analysis_candidates`는 그 위의 `WHERE mature`로 의미 고정. 파트 A 시점 `metric_timeliness`는 신규 어휘 `pending`(NULL은 랭킹이 레거시 timely로 노출, immature는 종결 상태라 기각) → 파트 B 수거 시 timely/late_backfill 확정. `content_batch_jobs.kind`로 수거 파서 분기. 롤백은 `analytics.analyze-mode=unified` 한 줄. 무LLM D+0 라벨·파트 B 템플릿화는 안 함. 비용 실측(countTokens): 파트 B 856 in/244 out ≈ $1.02/일(3,500건), 파트 A 축소 상쇄 시 순증 ≈ $0.2/일. 부수 효과: 제때창 놓친 게시물도 파트 A 채움, 계정 카피·발굴 MV 3일 가속. 구현 완료(브랜치 feat/content-analysis-two-phase-split, 운영 미배포·토글 기본 unified) - 구현 중 정정: 파트 B 배치 쓰기는 pending 가드(`updateSynthesisPending`), 스윕은 resolveTargets 앞으로, 커버리지 19행도 파트 B 분모, 수거 스윕 락 없음(수용) | [스펙](docs/superpowers/specs/2026-09-03-content-analysis-two-phase-split-design.md) |
| 2026-09-03 | **공동구매 판정은 규칙 우선 + 애매분만 LLM — analytics 소형 판정기(브랜드 광고 판정과 같은 계층 원칙)** — 발굴 카드·상세의 공동구매 시그널 규칙 `/공동구매|#공구/`(FE 동일)가 운영 실측에서 공동구매 게시물의 72%를 놓쳤다(맨몸 `공구` 3,620건은 표본 50건 전부 공동구매, 도구 의미는 코퍼스 전체 20건 안팎). "오탐은 0이어야 한다"는 요구라 정규식 확장(잔여 오탐 1건)도 탈락, 기존 콘텐츠 분석 프롬프트에 얹는 안은 새벽 배치 지연·95% 커버·확실한 99.5%까지 LLM 오판에 노출·13만 행 백필로 세 축 모두 열위라 기각. 채택: `공동구매`·`#공구`·도구 어휘 없는 `공구`는 규칙 확정, `공구`+도구 어휘 동반만 LLM 보류 판정, `group_purchase_judgments` 테이블(analysis DB)을 was가 직접 읽고 미판정은 미포함. FE는 클라이언트 정규식을 버리고 6.4 게시물 플래그·6.21 카운트를 서버 값으로 | [스펙](docs/superpowers/specs/2026-09-03-group-purchase-judgment-design.md) |
| 2026-09-03 | **발굴 카드 recentThumbs는 분석 완료 게시물만 — FE 피드백(09-01) #1·#3 회신** — ① 6.4 상세 `influencer.email`이 항상 null이던 결함은 컨트롤러 null 상수(07-15 회신표 #2 "미수집" 잔재)였다 — `account_summaries.email`(V46, 목록 6.21·유사 6.23과 같은 소스)로 채운다. 6.4 인증은 FE 스펙의 Optional이 아니라 **Required가 의도**(SecurityConfig — 상세는 잠김 유지, 목록만 공개)라 스펙을 구현에 맞추라고 회신. ② `/v1/contents/{id}/ai-report` 404(30일 199건)는 썸네일 소스(`account_content_series`, 미러 직후 최신 게시물 포함)와 6.3 소스(`contents ⋈ content_analyses`, 게시 후 ~3일 뒤 야간 배치)의 시차 — PostHog 404 표본 60건 전량이 404 시점 미분석·현재 40/60 분석 완료로 확인. FE가 제시한 두 안 중 was만으로 끝나는 ②(리포트 없는 콘텐츠를 썸네일에서 제외 = `findThumbs`를 content_analyses INNER JOIN)를 채택. 트레이드오프: 최신 1~3일치가 카드 썸네일에서 빠진다(창 지표·6.4 recentContents는 불변). 기각: `hasAiReport` 플래그 추가(FE 변경 동반, 이번 요청 범위 밖) | FE 백엔드 요청(2026-09-01) #1·#3. 근거 SQL·PostHog 질의는 PR 본문 |
| 2026-09-03 | **유사 인플루언서(6.23)도 서비스 층 + Redis 캐시(TTL 6h)** — similar는 v1에 없던 v2 전용 엔드포인트라 어느 버전에서도 캐시된 적이 없고, 유사도 쿼리가 후보 풀 전체 점수화라 운영 1.46초(09-01 수리 후)인데 상세 패널마다 ai-report와 함께 호출된다. `V2SimilarInfluencerService` `@Cacheable`(캐시명 `influencer-similar`, 기준 계정 키, 6h — 재료가 리포트와 같은 새벽 배치 산출). 캐시 값은 List 최상위가 아니라 record 래퍼 `SimilarInfluencers` — 불변 List를 값 최상위로 두면 직렬화기가 구현 클래스명을 박아 역직렬화가 깨진다(발굴 DiscoveryPage와 같은 구조). 빈 목록도 정상 결과라 캐시, 404는 예외라 미적재. 컨트롤러는 두 엔드포인트 모두 레이트리밋·동시성 permit만 남김 | 발굴 리포트 v2 캐시(#744) 후속, 같은 날 결정 |
| 2026-09-03 | **발굴 리포트 v2(6.22)에 서비스 층 + Redis 캐시(TTL 6h) 추가** — v2는 v1(6.5)과 병존하며 만들어질 때 캐시 층 없이 컨트롤러가 리포지토리를 직접 불렀고 응답 헤더도 Spring Security 기본 `no-store`라 어느 계층에도 캐시가 없었다(프론트 09-01 실측: 같은 계정 연속 2회 8,018ms·7,678ms — 운영 헤더 직접 확인). 지연 본체는 PR #734(findBrandCollabs 단일 스캔+GIN)가 해결하므로 이 캐시는 지연 대책이 아니라 부하 대책 — v1과 같은 관용구(`V2InfluencerReportService` `@Cacheable` sync, 캐시명 `influencer-report-v2` 분리: 값 타입이 다름, TTL 등급은 v1과 동일 6h). 무효화 연동 없이 TTL 백스톱만(2026-07-28 Redis 캐싱 설계 그대로 — 재료가 새벽 배치 후 하루 불변). 프론트가 요청한 "수집 배치 갱신 시 재생성"·`no-store` 해제는 미채택/미결: 전자는 레포 캐시 설계에 없는 무효화 연동, 후자는 보안 헤더 정책이라 별도 결정. 동시성 permit은 캐시 히트도 잡지만 ms 단위 점유라 유지 | 프론트 요청서 2-a, [스펙](docs/superpowers/specs/2026-07-28-redis-caching-design.md) §4 등급 |
| 2026-09-03 | **발굴 리포트 v2 브랜드 협업 조회를 단일 스캔 + GIN 식 인덱스로** — `/v2/influencers/{h}/ai-report` 꼬리 지연(PERF 알람 13.8초)의 본체는 `findBrandCollabs`(단계 로그 13,671/13,685ms): "같은 브랜드 협업 계정 상위 5"가 브랜드별 상관 서브쿼리라 플래너가 브랜드마다 content_analyses(309MB) 전체 순차 스캔 + jsonb 전개를 반복(EXPLAIN SubPlan loops=브랜드 수). 브랜드 21개 계정이 상시 7.3초, 동시 2건이면 13.7초 — 운영 브랜드 20개 이상 계정 61·10개 이상 2,024. 수리 ① 내 브랜드명 배열을 `?|`로 한 번에 짚는 GIN 식 인덱스(`jsonb_path_query_array(detected_brands, '$[*].name')`, 협찬 부분 인덱스, `fastupdate = off` — 켜두면 조회가 VACUUM 전까지 누적되는 펜딩 리스트(최대 4MB)를 선형으로 훑어 읽기 지연이 적재량에 흔들리므로 읽기를 결정론적으로 고정, 쓰기 비용은 행당 브랜드 1.08개라 5,000행 INSERT 벤치에서 인덱스 유무 차이 없음 — analytics V20260903083555) ② 브랜드×계정 집계를 MATERIALIZED CTE + 조인으로(단일 참조 CTE는 플래너가 도로 상관 서브쿼리로 인라인 — 실측 무효과). 로컬 스냅샷(협찬 6.5만 행) 실측 브랜드 49개 3,168ms → 8ms(인덱스 없이 885ms), 두 핸들 결과 전량 동일. 부수 발견: 운영 analysis DB `jit=on`이라 추정 비용 2,190만짜리 이 플랜은 매 실행 JIT 컴파일(로컬 ~260ms)까지 물고 있었다 — 인덱스 플랜은 비용이 낮아 JIT 미발동. Spring named-param + pgjdbc 경유 `?|`는 `??|` 이중 이스케이프(레포 첫 사례, 테스트 DB에 같은 인덱스를 둬 검증) | 09-03 PERF 알람(x-request-id oiswnmui) 단계 로그·운영 EXPLAIN·perf-diag 스냅샷 EXPLAIN ANALYZE. 구현 PR 참조 |
| 2026-09-03 | **PII DEK 에스크로 — PR 3(평문 DROP) 전에 DEK 평문 1부를 비밀번호 관리자에 보관(방안 A)** — 키 계열 비대칭 때문: 백업 age 비밀키는 맥·비밀번호 관리자·Vault 3중이지만 DB 컬럼 DEK는 OCI Vault KEK로만 풀려서, 테넌시 유실 시 B2 백업이 있어도 암호화 컬럼(이메일·이름·전화)은 복구 불가가 된다. PR 1·2 동안은 평문 컬럼이 남아 age 키만으로 완전 복원되지만 PR 3부터 의존이 실재. 대안 B(의존 수용 — 연락처 재수집 가능)는 "최악의 경우가 백업의 존재 이유인데 그때 개인정보만 잃는 구조"라 기각. "DB 키를 사람이 들지 않는다"는 설계를 한 곳 깨지만 보관처가 비밀번호 관리자라 위험 제한적. 절차는 코드 변경 없이 관리자 계정으로 `oci kms crypto decrypt`(트랙 QQ §미해결 4). DEK 로테이션 시 에스크로 갱신, 스테이징 DEK는 대상 아님 | [트랙 QQ §미해결 4](docs/tracks/QQ-개인정보-봉투-암호화.md) |
| 2026-09-03 | **개인정보 봉투 암호화(트랙 A) — 대칭 KEK + DEK 자동 부트스트랩, 세션은 형상 유지 전환, signup_events는 삭제 아닌 보존** — `app.users`/`inquiries`/`password_resets`/`signup_events`의 이메일 등 개인정보 컬럼을 AES-256-GCM DEK로 암호화, DEK는 OCI Vault KEK로 래핑해 `app.encryption_keys`에 저장(비대칭 KEK 대비 대칭 채택 — 이 워크로드는 암복호화 주체가 was 하나뿐이라 공개키 분리의 이득이 없고 대칭이 더 저렴·단순). 등가 검색은 HMAC 블라인드 인덱스(`*_bidx`)로 대체. **DEK 자동 부트스트랩**(계획 결함 수정, Task 11): 최초 계획은 로컬에서 `openssl rand`로 DEK를 만들어 수동 등록하는 절차였으나 vault 모드 첫 부팅이 `app.encryption_keys` 행 부재로 죽는 결함이 있었다 — `DekStore`가 행이 없으면 스스로 생성·래핑·`INSERT … ON CONFLICT DO NOTHING`·재조회까지 수행(롤링 배포 동시 부트스트랩은 먼저 커밋한 쪽으로 수렴, 진 쪽도 재조회로 동일 값에 도달). **세션 principal은 이메일→userId 문자열로 교체하되 직렬화 형상(필드·serialVersionUID)은 유지**해 기존 세션 재로그인 없이 전환(email 필드는 항상 null로 잔존). **signup_events는 즉시 삭제가 아니라 90일 보존 후 삭제 배치**(`SignupEventRetentionScheduler`)로 파기 — 비회원 이메일이 섞인 로그라 암호화와 별개로 법적 파기 의무가 있지만, 즉시 삭제 대신 어뷰징 추적 목적을 살리는 보유 기간을 뒀다. 이로써 트랙 NN(삭제 데이터 아카이브) 설계 §4-4가 지적한 "signup_events는 삭제 경로가 없어 탈퇴 유저의 가명화 아카이브를 무기한 재식별할 수 있다"는 갭이 90일로 한정된다 — **재식별 편의보다 개인정보 최소 보존을 우선한 의도된 선택**이며(비회원 이메일을 무기한 붙들 근거가 없다), 90일 이내 재식별이 필요한 법무·어뷰징 조사 용도는 그대로 유지된다. 운영 fail-closed 가드(prod 프로파일 + crypto.mode=local 조합은 기동 차단) 동반 | [스펙](docs/superpowers/specs/2026-09-03-pii-envelope-encryption-design.md), [트랙 QQ](docs/tracks/QQ-개인정보-봉투-암호화.md) |
| 2026-09-03 | **유사 인플루언서(6.23)도 서비스 층 + Redis 캐시(TTL 6h)** — similar는 v1에 없던 v2 전용 엔드포인트라 어느 버전에서도 캐시된 적이 없고, 유사도 쿼리가 후보 풀 전체 점수화라 운영 1.46초(09-01 수리 후)인데 상세 패널마다 ai-report와 함께 호출된다. `V2SimilarInfluencerService` `@Cacheable`(캐시명 `influencer-similar`, 기준 계정 키, 6h — 재료가 리포트와 같은 새벽 배치 산출). 캐시 값은 List 최상위가 아니라 record 래퍼 `SimilarInfluencers` — 불변 List를 값 최상위로 두면 직렬화기가 구현 클래스명을 박아 역직렬화가 깨진다(발굴 DiscoveryPage와 같은 구조). 빈 목록도 정상 결과라 캐시, 404는 예외라 미적재. 컨트롤러는 두 엔드포인트 모두 레이트리밋·동시성 permit만 남김 | 발굴 리포트 v2 캐시(#744) 후속, 같은 날 결정 |
| 2026-09-03 | **발굴 리포트 v2(6.22)에 서비스 층 + Redis 캐시(TTL 6h) 추가** — v2는 v1(6.5)과 병존하며 만들어질 때 캐시 층 없이 컨트롤러가 리포지토리를 직접 불렀고 응답 헤더도 Spring Security 기본 `no-store`라 어느 계층에도 캐시가 없었다(프론트 09-01 실측: 같은 계정 연속 2회 8,018ms·7,678ms — 운영 헤더 직접 확인). 지연 본체는 PR #734(findBrandCollabs 단일 스캔+GIN)가 해결하므로 이 캐시는 지연 대책이 아니라 부하 대책 — v1과 같은 관용구(`V2InfluencerReportService` `@Cacheable` sync, 캐시명 `influencer-report-v2` 분리: 값 타입이 다름, TTL 등급은 v1과 동일 6h). 무효화 연동 없이 TTL 백스톱만(2026-07-28 Redis 캐싱 설계 그대로 — 재료가 새벽 배치 후 하루 불변). 프론트가 요청한 "수집 배치 갱신 시 재생성"·`no-store` 해제는 미채택/미결: 전자는 레포 캐시 설계에 없는 무효화 연동, 후자는 보안 헤더 정책이라 별도 결정. 동시성 permit은 캐시 히트도 잡지만 ms 단위 점유라 유지 | 프론트 요청서 2-a, [스펙](docs/superpowers/specs/2026-07-28-redis-caching-design.md) §4 등급 |
| 2026-09-03 | **발굴 리포트 v2 브랜드 협업 조회를 단일 스캔 + GIN 식 인덱스로** — `/v2/influencers/{h}/ai-report` 꼬리 지연(PERF 알람 13.8초)의 본체는 `findBrandCollabs`(단계 로그 13,671/13,685ms): "같은 브랜드 협업 계정 상위 5"가 브랜드별 상관 서브쿼리라 플래너가 브랜드마다 content_analyses(309MB) 전체 순차 스캔 + jsonb 전개를 반복(EXPLAIN SubPlan loops=브랜드 수). 브랜드 21개 계정이 상시 7.3초, 동시 2건이면 13.7초 — 운영 브랜드 20개 이상 계정 61·10개 이상 2,024. 수리 ① 내 브랜드명 배열을 `?|`로 한 번에 짚는 GIN 식 인덱스(`jsonb_path_query_array(detected_brands, '$[*].name')`, 협찬 부분 인덱스, `fastupdate = off` — 켜두면 조회가 VACUUM 전까지 누적되는 펜딩 리스트(최대 4MB)를 선형으로 훑어 읽기 지연이 적재량에 흔들리므로 읽기를 결정론적으로 고정, 쓰기 비용은 행당 브랜드 1.08개라 5,000행 INSERT 벤치에서 인덱스 유무 차이 없음 — analytics V20260903083555) ② 브랜드×계정 집계를 MATERIALIZED CTE + 조인으로(단일 참조 CTE는 플래너가 도로 상관 서브쿼리로 인라인 — 실측 무효과). 로컬 스냅샷(협찬 6.5만 행) 실측 브랜드 49개 3,168ms → 8ms(인덱스 없이 885ms), 두 핸들 결과 전량 동일. 부수 발견: 운영 analysis DB `jit=on`이라 추정 비용 2,190만짜리 이 플랜은 매 실행 JIT 컴파일(로컬 ~260ms)까지 물고 있었다 — 인덱스 플랜은 비용이 낮아 JIT 미발동. Spring named-param + pgjdbc 경유 `?|`는 `??|` 이중 이스케이프(레포 첫 사례, 테스트 DB에 같은 인덱스를 둬 검증) | 09-03 PERF 알람(x-request-id oiswnmui) 단계 로그·운영 EXPLAIN·perf-diag 스냅샷 EXPLAIN ANALYZE. 구현 PR 참조 |
| 2026-09-02 | **브랜드 AI 날조 차단은 "가벼운 층"으로 - 표는 서버 렌더 안 함, 고지 문구·스크럽 없음** — 툴 0회 날조(가상 계정명 표)를 프롬프트로 두 번 막아도 재현돼 구조 대안 3층을 비교: ① 재시도 턴 데이터 툴 강제(Gemini toolConfig mode ANY, 탈출용 빈 함수 없음 - 재시도는 표·계정명을 그렸을 때만 걸리므로 조회 없는 정당한 경우가 없음) + 답변 계정명을 이번 실행 툴 결과 집합과 정확 대조(entity exact-set, aggregate author key 포함) ② 결정론 스크럽(행 제거) ③ 표·수치를 서버가 툴 페이로드에서 렌더. **①만 채택**: ③은 표 날조를 원천 차단하지만 파생 열·조인·주석 등 유연성 손실과 재설계 공수가 크고 문장형 날조는 못 막는데, 실측 발생률이 104턴 중 표 1건(재시도로 해소)이라 비용 대비 불리 - FE가 표 행 클릭 구조 데이터를 요구하거나 ①로도 재발하면 재검토. ②와 "(자동 검증)" 고지 문구는 사용자 결정으로 제거 - 날조 표에 경고를 달아 내보내는 건 방향이 틀렸고, 재시도 소진 후 잔여는 warn 로그만(사용자가 잔여 수용). 한계 명시: 표·@계정명 형태만 100% 포착, 문장형(`@` 없는 계정명·불릿 수치) 날조는 사전 억제뿐(체인 2턴 실측 1건). 같은 날 👍👎 피드백 저장 API 신설(PUT/DELETE /ai/messages/{id}/feedback, ai_chat_logs 컬럼 3개 expand, 대화 상세에 messageId·feedback 노출) - 골드셋 발굴 필터용 | 커밋 메시지, `BrandAiGroundednessGuard` javadoc |
| 2026-09-02 | **브랜드 AI 품질 운영 2라운드 — 리서치 후속 결정 + 마케터 104턴 스윕 수확** — (리서치) ① DSPy·MIPROv2·GEPA류 프롬프트 자동 최적화는 **보류**: 골드셋 22건은 실전 권고 최소 구간(20~100) 하한이고 GEPA 프로덕션 사례에서 500건도 과적합(프롬프트 길이 +75%·성능 -2%) 실측, reflection엔 프론티어급 모델이 필수라 flash-lite 가성비 결정과 충돌 — 재검토 조건은 실사용 로그 기반 골드셋 30~50건+train/val 분리. ② LLM-as-judge는 결정론 채점을 대체하지 않는 **2단계 비게이트 애드온**으로만, 생성 모델(Gemini)과 **다른 계열**을 judge로(self-preference bias 10~25pt), rubric은 간결성·한계 고지·과잉확신 3축 binary 체크리스트+0-5 종합 — 게이트 승격은 사람 라벨 kappa 0.7+ 이후(미착수). ③ groundedness 검증기는 **로깅 우선·차단 최후**(Langfuse·Bedrock 공통: 정규식 대조는 라우팅 신호) — 툴 0회+수치 답변(tool boundary hallucination)이 최우선 신호, 계정명은 툴 결과 exact-set 대조. 오프라인은 `sweep-screen.sh` B규칙으로 즉시 도입, 인라인 검증기·툴 페이로드 로깅은 미착수(승인 대기). ④ 멀티턴 열화는 프롬프트로 못 고친다(arXiv 2505.06120·2605.24432, 단일턴 대비 -39%, 개입 전부 실패) — 기대치를 "관측·완화"로 두고 골드셋에 체인 케이스를 추가(러너 `turns` 스키마). 이력 요약 압축은 캐시 프리픽스 파괴로 비용 역전 실측이라 현 원본 재전송 구조 유지, 후속은 툴 응답 크기 캡·observation masking. (스윕) 마케터 말투 93항목 104턴 실측, 결정론 스크리닝 후보 27→진짜 실패 5부류 9턴(≈9%): 툴 0회 가상 계정명 표 날조 1·메타 질문("뭘 체크할 수 있어?")에서 내부 툴명 대량 유출 2·"뒷광고" 도메인 밖 오거절 1·팔로워 추이 현재값 미제공 1·`###` 헤더 4. 경쟁사 5건은 사전 안내 문구대로 정상, 체인 8건 지칭 해소 전부 정상. 수정은 전부 사전(BrandAiGlossary 4줄)+규칙 11 헤더 명확화+전역 denylist `###`, 골드셋 17→22(체인 1건 포함). 스크리닝 오탐 2종(브랜드 컨텍스트 선주입 수치, 사전 지시 안내 문구) README 기록 | `was/eval/README.md` §대량 스윕, 커밋 메시지 |
| 2026-09-02 | **오프사이트 백업 암호화(age) 도입 — B2로 나가는 스트림만, 키는 서버 밖** — 덤프에 사용자 이메일 등 개인정보가 담기는데 B2 계정 유출 시 그대로 노출되던 구멍을 막는다. backup.sh가 B2 업로드 직전 age 공개키 암호화(`.sql.zst.age`), 서버 로컬 사본은 평문 유지(서버 침해 시엔 라이브 DB가 어차피 읽혀 이득 없음 + 로컬 즉시 복원 경로 보존). 복호화 비밀키는 서버에 없다 — OCI Vault 시크릿 `hypenow-backup-age-key`(Always Free 범위, 서버 dynamic group에 읽기 권한 없음 — 주면 무의미) + 로컬 맥 `~/.config/age/`. age 미설치면 업로드 생략(기밀성 fail-closed, 가용성은 기존 로컬 폴백). 서버 암호화→Vault 키 인출→복호화 왕복 리허설 검증 완료. 컬럼 암호화보다 이걸 먼저 한 이유: 디스크 층은 OCI 블록 볼륨 기본 암호화로 커버, 앱 경로는 방화벽·접근제어 담당, 남은 실질 노출면이 오프사이트 덤프였다 | [deploy/README.md §6-2](deploy/README.md), backup.sh 상단 09-02 주석 |
| 2026-09-02 | **서빙 이미지 720px·q70 축소 — 잡 데코레이터 + 기존분 백필** — GCS `hypenow-images` 50GB 초과 알람 대응(수명주기 배제 확정): FE 최대 렌더 488px(플레인 img — 저장 바이트가 1:1 서빙) 실측 + 상세 패널 마크업 복제 A/B 화질 승인 후, 최장변 720 초과분만 JPEG q70 축소(동일 해상도 재인코딩은 실측 +30% 역효과라 금지, 커지거나 실패 시 원본 유지). `ImageDownloader` 데코레이터라 잡 무수정 — analytics·monitoring 복제(공유 금지 전례). 기존분 48.8GB는 로컬 맥 sips 백필(자연 멱등·드라이런 게이트, **비가역** — CDN 원본 만료). 실측 예상: 버킷 51→28GB(−46%), 증가 3→1.5GB/일 | [스펙](docs/superpowers/specs/2026-09-02-image-resize-720-design.md) |
| 2026-09-02 | **해시태그 감시 세트 2,000 롤링 전환** — 편입 하드스톱(1,000, 도달 시 신규 드랍) 폐기 → 게시일 최신 2,000 롤링 세트(세트 밖은 동결 touch·저장 유지). 2단계 재수집 상한 300→2,000(모수를 세트로 한정). was 노출 컷 2,000 폐지. 근거: 09-02 미처리 1,828건 진단(캠페인 브랜드 일일 due가 상한을 매일 초과 + celimax 2종 편입 상한 포화). 비용: 캠페인 피크 브랜드당 월 $20~40 승인. 스펙: docs/superpowers/specs/archive/2026-09-02-hashtag-monitoring-set-2000-design.md | 09-02 사용자 구두 승인. 구현 PR 참조 |
| 2026-09-02 | **브랜드 완주 시각을 진행 워터마크에서 분리(`sweep_completed_at`)** — 08-31 백필 캐시 고착 수리(touchProgress)가 `last_swept_at`을 "완주 시각"에서 "진행 워터마크"(페이지 정산마다 전진)로 넓히면서, 그 컬럼으로 완주를 재던 Grafana 패널들이 오염됐다(09-02 실측: 야간 수집 02:00~06:24 ≈ 4.4h가 오전 해시태그 스윕 정산 1건에 9h로 표시). 수리: touchSwept(전량 수집 완주)만 찍는 `sweep_completed_at` 컬럼 신설(expand, 기존 행은 last_swept_at로 초기 백필 — 다음 스윕이 자연 교정), 대시보드의 완주 용례 전부 전환([흐름] 브랜드 신선도·오늘 수집 소요·미처리 브랜드별·브랜드별 처리 현황, ops 신선도·오늘 수집 소요, 홈 신선도). ops '오늘 수집 소요'에는 [흐름] 장과 같은 등록 당일 제외도 추가. was 캐시 버전키·lastCollectedAt 표기는 워터마크(last_swept_at) 유지 — 계측과 캐시 무효화의 관심사 분리. 검토했다 접은 안: 시간대 창 클램프(야간 런 안에서도 해시태그 단계가 브랜드마다 워터마크를 전진시켜 신호 자체가 소실 — 복원 불가), 런 단위 기록 테이블(브랜드별 패널 오염은 못 고침·스코프 과대) | 계기: 09-02 Grafana 실측. 구현 PR 참조 |
| 2026-09-02 | **버킷 메트릭 OCI 병행 게시 제거 + 알람 본문 실체화** — `bucket_used_gb`에 OCI(동결 스냅샷 18.4GB, 5분 결)·GCS(실쓰기, 정시 1회) 두 스트림이 같은 `bucketName`으로 병행 게시되던 것을 GCS 단일로 정리(§5-2 9단계 실행). 계기: 09-02 `hypenow-bucket-high` 알람이 GCS 50GB 초과로 울렸는데 본문은 컷오버 전 문구("15GB 초과·무료 티어 20GiB")여서 OCI 무료 한도 문제로 오독됨 — 실제 임계는 50GB(GCS는 과금형, 한도 삭제 없음). 본문을 GCS 기준으로 갱신. 참고: GCS 사용량 41→50GB/일 ~9GB 증가 중(아카이브 잡) — 비용 추이 감시 대상 | [deploy/README §5-2·§9](deploy/README.md) |
| 2026-09-01 | **브랜드 AI 구조적 품질 개선 — 조용한 근사를 구조로 차단** — 실측 실패 3건(모델이 "광고 게시물"을 캡션 keyword로 조용히 근사해 "광고 아님" 명시 게시물이 광고 랭킹 1위, 정성 질문을 소표본 도달배수 1위로 환원, 표본 수 미표기)의 공통 패턴이 조용한 근사(silent approximation)임을 확인하고, 개별 패치가 아니라 업계 리서치(dbt 2026 벤치마크: 자유 생성 90% vs 시맨틱 레이어 98.2%, 실패 모드가 "조용한 오답"→"명시적 거절"로 질적 전환 / "Knowing but Not Showing": 고지·확인질문을 프롬프트 재량에 맡기면 컨텍스트가 많을수록 오히려 덜 지켜짐)가 수렴한 구조를 도입. ① sponsorship 축 인자(list/search/aggregate 공유, BrandSponsorshipClassifier 바인딩) + minSample ② 서버 강제 caveats 배열(keyword=캡션 매칭 고지·소표본 경고를 모델 재량이 아니라 페이로드에 강제) ③ 용어 사전 BrandAiGlossary 상시 주입(용어→축 매핑 단일 정본, Genie BUSINESS DEFINITIONS 패턴) ④ 프리셋 verified 플랜 선실행 주입(검증된 호출을 루프 진입 전 실행·주입 — 08-31 기각한 B(툴 콜링 우회 별도 경로)와 달리 루프 유지, 리서치의 검증 경로 이원화 패턴이 재검토 근거) ⑤ eval 골드셋 13케이스+러너(`was/eval/run.sh`, 결정론 채점: 툴 궤적 부분매치·forbid·SQL 정답 대조). 첫 가동에서 러너가 과잉 거절 회귀 2건을 잡아 사전 2줄로 수렴, 최종 13/13 PASS 실측. **후속**: 온라인 judge 샘플링·모호성 분류 파이프라인은 운영 배포 후, 모델 실험(flash+thinking/pro)은 이 러너를 자로 사용 | `docs/superpowers/specs/archive/2026-09-01-brand-ai-structural-quality-design.md` |
| 2026-09-01 | **플래너 오추정에 노출된 집계는 InitPlan·MATERIALIZED로 평가 횟수를 구조로 고정** — 축 인지화 배포 직후 유사 추천 API 전면 50~60초(양축 공통, 운영 장애): CTE 행 수 오추정(est 121 vs 실제 12,055)이 ①`account_peer_axis_stats`의 gmed 조인을 집계-안쪽 네스티드 루프로(중앙값 12,055회 재계산 ≈46s), ②was `me`(1행 CTE)를 후보별 재평가로(≈12s) 풀었다. 수리: gmed를 비상관 스칼라 서브쿼리(InitPlan — 추정 무관 1회 평가, 축별 gmed 동치라 값 불변)로, `me`는 `AS MATERIALIZED`. 운영 실측 59.2s→1.46s, 결과 10건 전량 동일. 부수 발견: postgres `/dev/shm` 64MB(도커 기본) 94% 포화로 병렬 해시 DSM 확장 실패 500 — `shm_size: 256m` 별도 핫픽스(#688). 교훈: 조인 상대가 "작은 집계 결과"라도 플래너가 바깥을 1행으로 오추정하면 재실행 루프가 된다 — 뷰가 CTE 다중 참조 구조면 특히 | 09-01 운영 장애(유사 탭 스켈레톤 고착·PERF 알람 51.4s). 실측·플랜 분석은 PR 본문 |
| 2026-09-01 | **유사 인플루언서 추천 축 인지화** — F&B 리포트의 유사 추천이 '미분류' 풀 잡탕이던 것(muk_gyumato 실측)을 해소: `account_category_stats`에 axis 컬럼(게이트 제거, 뷰티 투영 동치), `account_peer_axis_stats` 신설(계정×축 피어, 구 이름은 뷰티 투영 유지 — 롤링 안전), was는 `findFnbAxis`(F&B 단독→fnb, 혼합·레거시→beauty)로 축을 파생해 유사 핸들·카드 비중에 전파. F&B 축 후보 게이트는 `a.fnb`(뷰티 비율 게이트는 뷰티 축만). 부수: 계정 카피 LLM 입력에 F&B 카테고리 분포 유입 시작. 피어 퍼센타일 컬럼은 소비자 0건(죽은 컬럼) 확인 — 축별 준비만, 서빙은 FE 명세 후. 콘텐츠 리포트 카테고리 비교(findCategoryContext)도 같은 불변식으로 is_beauty 게이트 제거 — F&B 게시물 '현재 준비중' 해소(운영 불변식 위반 0건·구신 전량 대조 0건 실측). | [스펙](docs/superpowers/specs/2026-09-01-similar-influencer-fnb-axis-design.md), 트랙 [LL2](docs/tracks/LL2-fnb-콘텐츠-분류.md) |
| 2026-09-01 | **아카이브 잡은 선정·처리 분리 — 선정은 커서 스트리밍 1회 스캔 + 만료 임박 순 상한 힙, 다운로드는 raw 커넥션 점유 0** — 대상 선정이 `v_contents` 전 행(45.6만)을 리스트로 적재하던 전량 축적(미러 08-31 OOM과 동일 패턴)을 제거: 읽기전용 트랜잭션 + fetchSize 500 커서로 행마다 필터 후 상한(archive-batch-limit) 크기 최대 힙만 유지, 커서·트랜잭션을 닫은 뒤 수 시간짜리 다운로드 루프 시작(analysis 건별 커밋 = 체크포인트, 기존 그대로 — 중단 시 다음 실행이 미기록분 자연 재대상). **키셋 페이지네이션 불채택**: ① archived 대조가 크로스 DB(analysis `image_assets` ↔ raw 뷰)라 SQL 술어 불가 + raw에 상태 컬럼은 경계 위반 ② `v_contents`가 다중 조인+LATERAL 뷰라 페이지마다 조인 스택 재실행 ③ 만료 임박 top-N은 URL `oe` 파싱(Java)이라 어차피 전 행 스캔. 검증: 3000행×100KB(≈300MB) 시드에서 구코드 테스트 JVM(512m) OOM 재현 → 신코드 통과(회귀 테스트로 상주) | 09-01 힙 91% 원인 조사(04:50 선정 단계가 피크 주범, 잡 9h 상주 중 확인). 선정·처리 분리 원칙은 09-01 리뷰 피드백 반영 |
| 2026-09-01 | **발굴 카드 categoryShares를 축 인지로** — `findShares`가 뷰티 게이트(`is_beauty IS TRUE`) 고정이라 F&B 카드가 전부 "카테고리 정보 준비 중"이었다(운영 F&B 분류 보유 4,660계정 전멸 — 08-31 서빙 개방이 "FE 명세 후"로 이연한 항목). 어휘 축 조인(`beauty_taxonomy.axis`, `account_category_share` 재정의와 같은 패턴)으로 요청 축의 비중만 반환: 뷰티 축은 구 게이트와 동치(불변식 — 운영 전량 EXCEPT 대조 0건 실측), F&B 축은 F&B 분류분이 분모. 축은 `V1InfluencerDiscoveryQuery.fnbAxis()`(F&B 대분류 필터 ∨ `vertical=fnb`)로 파생 — 발굴 게이트 분기와 동일 판정. 유사 카드(6.23)는 후보 자체가 뷰티 코퍼스 기반(`account_peer_stats`)이라 뷰티 축 고정 유지, F&B 유사 추천 개방 시 대상 계정 축 파생으로 전환 | FE 피드백(2026-09-01), 트랙 [LL2](docs/tracks/LL2-fnb-콘텐츠-분류.md) |
| 2026-09-01 | **버티컬 파라미터 `vertical=beauty\|fnb` 신설(랭킹·발굴) + 서브 라벨은 확정 대분류로 정합 필터** — FE F&B 피드백 반영: ① 대분류 6종 fanout 대신 축 전체를 쿼리 1번으로(`vertical=fnb` ⇒ `main_category IN (F&B 6종)` / 발굴은 `COALESCE(a.fnb,false)`만, 비중 게이트 없음 — total도 축 단위 정확), `vertical=beauty`는 무필터와 동치(is_beauty 게이트 그대로 — 대분류 미도출 레거시 포함해 기존 화면 보존), `mainCategory`와 동시 지정은 400. ② 상위 없는 `subCategory`는 조용한 무시 → 400(필터 건 줄 알았는데 전체가 나오는 화면 버그 차단). ③ `meta.distributors`는 요청 축의 어휘로(뷰티 고정 해제). ④ sanitize가 서브·제품 라벨을 확정 대분류 소속으로 재필터 + 프롬프트에 소분류 필수 지시 — 교차 오염·중분류 단독 태깅 완화 | FE 피드백 6건(2026-09-01), 트랙 [LL2](docs/tracks/LL2-fnb-콘텐츠-분류.md) |
| 2026-08-31 | **브랜드 AI 툴·한계 재설계 — 조합형 aggregate_posts로 일반화 + 한계를 뿌리 자원에서 재도출** — 프리셋 1번("팔로워 상위 10명" 류) 요청이 툴 8회 호출 상한에 걸려 5명만 반환되고, 모델이 정렬을 암산하다 오류를 낸 사례(ai_chat_logs id=20)가 발단. `aggregate_posts`를 groupBy·orderBy·keyword·limit 조합형으로 일반화(파생지표는 서버 계산), `get_comments`를 배치화, 브랜드 컨텍스트를 시스템 프롬프트에 선주입해 첫 왕복(list_brands)을 제거. 한계는 대리 지표(툴 호출 횟수)가 아니라 뿌리 자원(토큰·시간)에서 재도출 — 누적 프롬프트 토큰 60,000→100,000, 벽시계 예산 55초→~85초, 툴 호출 상한 8회→24회(안전망으로 강등), 컨트롤러 응답 계약 60초→90초, 분당 요청 5회→10회(사용자 지시). `limitReached: "time"\|"budget"\|null` 구조화 원인 고지 필드 추가(FE는 무시 가능한 additive 변경). 5개 프리셋 지시문을 새 툴 경로 기준으로 재작성. **기각**: 신규 툴 분리(query_posts 신설), 기존 툴 전면 통합, 프리셋 전용 서버 조립 경로(설계안 B), 모델 상향 선행 — 전부 기각. **후속**: eval 골드셋 구축 → 모델 설정 실험 순서로 진행 예정 | `docs/superpowers/specs/archive/2026-08-31-brand-ai-tool-limits-redesign-design.md` |
| 2026-08-31 | **미러는 스트리밍(커서 fetch 500 + 배치 단위 즉시 INSERT)으로 — 전량 리스트 축적 불채택** | 08-31 운영 analytics OOM(힙덤프 분석: v_contents 28.5만 행 미러에서 PgResultSet 503MB + record 리스트 251MB 이중 상주 = 힙 768m 소진, 어드민 수동 트리거 중 사망). pgjdbc 기본 fetchSize 0은 결과셋 전체를 클라이언트에 선적재하므로 raw 읽기를 읽기전용 트랜잭션(autoCommit=false)으로 감싸 커서 모드를 켜고, 500행 버퍼가 찰 때마다 analysis 트랜잭션 안에서 즉시 batchUpdate — 상주 메모리가 행 수와 무관해진다(-Xmx192m에서 12만 행×207MB 실증: 구코드 OOM, 신코드 2초 완주). TRUNCATE+INSERT 한 트랜잭션(읽는 쪽 공백 없음)은 불변. **수용한 트레이드오프**: TRUNCATE의 ACCESS EXCLUSIVE 락이 미러 전체 시간 동안 유지된다(테이블별) — 새벽 크론(KST 04:30) 전제이며, 트래픽 시간대 수동 트리거는 해당 테이블 조회를 세운다(락 창까지 없애려면 스테이징 테이블+스왑이 후속 선택지). Hikari는 사용 중 커넥션을 회수하지 않으므로 장기 커서 점유는 무해 |
| 2026-08-31 | **crawler·analytics JVM 지표 계측 확장** — 08-22 monitoring 스펙이 이연한 백로그를 같은 패턴 반복으로 해소: 두 모듈에 actuator+micrometer(health·prometheus만), 관리 포트는 compose env `MANAGEMENT_SERVER_PORT`(앱 포트 +1000: crawler 9080·analytics 9082), prometheus 잡 2개 추가(job 이름은 ops-metrics 대시보드 `$module` 값과 일치). 대시보드 noValue "스크레이프 미설정" 문구 제거. 이로써 4모듈 전부 JVM 시야 확보 | [08-22 스펙](docs/superpowers/specs/2026-08-22-monitoring-jvm-metrics-design.md)의 "범위 밖" 이연분 |
| 2026-08-31 | **브랜드 게시물 해시태그 필터는 조회 시 정규식 추출로 처리한다 — AI 판단·수집 시 저장은 불채택** | 규칙 `#[\p{L}\p{N}_]+`(전각 ＃ 제외 — 인스타 링크화 검증 기준)로 캡션에서 즉석 추출하며, 인덱스 SQL은 withCaptions 분기로 성과 대시보드 경로를 면제한다. 근거·갭은 [spec](docs/superpowers/specs/archive/2026-08-31-brand-post-hashtag-filter-design.md) |
| 2026-08-31 | **F&B 서빙 개방 — 기본 화면 무변경, 명시 필터에서만** — FE 필터 UI 없이 백엔드를 먼저 연다(사용자 확정: F&B는 쿼리 파라미터로만 나오므로 무필터 기본 화면이 불변이면 안전). 뷰 01/02 모수를 뷰티∪F&B로 확장(20 랜딩은 의도적 뷰티 유지 — 랜딩 숫자는 기본 화면), `accounts`에 축 컬럼 `beauty`·`fnb`(nullable — 롤링 창 구 미러 무해, was는 COALESCE(beauty,true)) + 계약 3종 동기. **랭킹**: mainCategory 필터가 있으면 `is_beauty=true` 게이트를 제거하고 `main_category=:main`만 — 불변식 "main 있음 ⇒ 축 확정"으로 뷰티 필터는 동치·F&B 필터는 자연 동작(축 조회 코드 자체가 불필요). 무필터는 is_beauty=true 유지. **발굴**: 무필터·뷰티축 `COALESCE(a.beauty,true)` / F&B축 `COALESCE(a.fnb,false)` + 뷰티 비율 게이트(V45) 미적용(F&B 계정은 뷰티 비율 0이라 걸면 전멸 — 방어는 F&B 비중 20% 게이트가 동일 원리로 대체, 사용자 확정 대칭 20%). allowlist는 공용 상수 `MainCategories`(뷰티 7+F&B 6)로 일원화 — 구조상 두 쿼리 홀더가 각자 하드코딩하던 것. `account_category_share` matview를 **축별 분모**(PARTITION BY handle, axis)로 재정의 — 뷰티 pct는 기존과 동치 증명, F&B 행 신설. account_category_stats(V35)·findShares는 무필터 카드 표시값이라 뷰티 게이트 유지. **수용 비용**: 미러 +10만 게시물(~60%), 이미지 아카이브 +10만, 계정 카피 LLM +5,575계정(일회성). FE 명세 후 후속: meta.distributors 축 선택·카드 F&B 비중 표시·무필터 의미 재정의 | [설계](docs/superpowers/specs/2026-08-31-fnb-serving-open-design.md) · 마이그레이션 `V20260831101007__accounts_axis_columns.sql`·`V20260831101100__category_share_axis_denominator.sql` |
| 2026-08-31 | **대시보드 조립 8초의 본체는 쿼리가 아니라 Spring `SimplePropertyRowMapper`의 행 매핑이다 — 실측 3개 쿼리만 수동 RowMapper로 교체(응급)** | 스테이징 리더 8.4초 계층 분해(단계 로그 × DB 직접 실측): JVM 행 매핑 ~5.4s(71%) + DB 실행 ~2.0s(그중 협찬 정규식 1.35s) + 전송·직렬화 ~0.2s. `JdbcClient.query(Class)`의 `SimplePropertyRowMapper`가 행마다 camelCase `findColumn` 실패 예외(요청당 ~43만 개)·값당 ConversionService·리플렉션 생성을 물어 raw JDBC의 **20배**(로컬 47µs/행 vs 2µs/행 실측). 풀 대기 아님(같은 요청 `findAccount` 14콜 8ms)·전송 아님(80ms/12MB)·브랜드당 DB 고정비 아님(작은 브랜드는 네스티드 루프). 응급은 `BrandReadRepository`의 `findBrandPostIndex`·`findLatestSnapshotsForBrand`·`findAuthors` 3개만 컬럼명 기반 수동 람다로 — SQL·계약 무변경, 기대 8.4s→~3.5s, 브랜드 표면도 동일 수혜. findAuthors IN 6,633개 바인드 전개(~0.4s)는 이번에 안 고침(변경 표면 최소화). 정규식·스코핑은 후속 본체. [설계](docs/superpowers/specs/archive/2026-08-31-dashboard-manual-rowmapper-design.md) |
| 2026-08-31 | **백필·스윕은 페이지 정산마다 `brand_account.last_swept_at`을 전진시킨다(`touchProgress`) — 의미가 "완주 시각"에서 "마지막 수집 활동 시각"으로 확장** | was 인덱스 캐시(`BrandIndexCache`, 08-28 도입)의 버전키가 brand_account 워터마크만 보는데, 백필 한 사이클 동안 그 행은 첫 페이지(markServing)·완주(touchSwept) 두 번만 바뀌어 그 사이 폴링이 전부 첫 페이지 단면 캐시에 붙었다(08-31 skinfood 실측: 90초간 21건 동결 후 247건 일괄 노출). 기존 "수용된 지연" 분석은 하루 상한·야간 스윕 전제라 유저가 지켜보는 유일한 스윕(등록 백필)이 사각지대였다. 전진은 반드시 markEnriched **뒤**(앞이면 새 키가 미정산 단면을 캐시), 가드 없이 무조건(재가입·기간 확장 재백필도 같은 고착). 완주 판정(last_swept_on·backfill_completed_at)·FE ready 파생은 불변. 대안이던 버전키에 brand_tagged_post 지문 추가는 핫패스 쿼리+인덱스 신설 비용 대비 방어 시나리오가 가설이라 보류 (PR #651) | PR #651 |
| 2026-08-31 | **유저별 기능 플래그는 `app.users.feature_overrides` jsonb 한 컬럼에 두고 키 어휘는 백엔드가 검증하지 않는다** — 기능 목록·기본값의 정본이 프론트라, DB나 was가 키를 열거하면 기능이 늘 때마다 마이그레이션·배포가 프론트 릴리스에 앞서 따라붙는다. 백엔드는 값 타입만 `boolean | string[]`로 좁히고(그 외 400 VALIDATION_FAILED) 나머지는 그대로 저장한다. `PUT /v1/admin/users/{id}/features`는 전체 교체(PATCH 병합 아님) — 부분 병합은 "지운 키"를 표현할 방법이 없어 프론트가 삭제를 못 한다. 감사 기록은 **컬럼 추가 없이 기존 `app.admin_audit_logs` 재사용**: `ActAsUserFilter`가 `/v1/admin/*`를 기록하지 않으므로 path로 두 종류가 구분되고, 변경과 기록은 한 트랜잭션(상태를 바꾸는 요청이라 act-as 조회의 best-effort 기록과 다르다). 컬럼은 `NOT NULL DEFAULT '{}'` — "미설정"이 null과 `{}` 둘로 쪼개지면 응답 계약(항상 객체·null 금지)을 표현 계층에서 매번 방어해야 한다 | [track](docs/tracks/MM-어드민-백엔드-api.md) |
| 2026-08-31 | **성과 대시보드 26초 꼬리는 자기 경합이다 — 보관 없는 동시 조립 합류(single-flight)로 접는다** | 운영 growth 26.5초(mbx0prz2) 단계 분해: 앱 CPU는 123ms이고 26.4초가 리포지토리 대기다. 경합 전 같은 엔드포인트가 8.4초(`findBrandPostIndex` 6.2초/6콜 + `findLatestSnapshotsForBrand` 1.4초/6콜)이므로 **8.4초는 고정비, 나머지 18초는 7중 자기 경합**이다(값싼 `lastSuccessfulSweepAt`이 689ms로 부푼 것이 증거). FE가 계정마다 growth를 병렬로 쏘는데 `accountIds`가 조립 **후** 메모리 필터라 N요청이 각자 N브랜드를 만든다. ETag는 URL이 갈려 못 막는다. **캐시(보관)는 비채택** — 콜드 8.4초를 못 고치고 유저당 ~13MB를 무는데, 후속 구조 개편(화면은 7일 724행이면 되는데 180일 15,177행을 만든다 — 범위 스코핑 + DB 집계)이 오면 보관할 이유가 사라진다. FE의 계정별 N요청 수정도 같은 중복을 없애는 처방이라 8.4초를 못 줄인다. [설계](docs/superpowers/specs/archive/2026-08-31-dashboard-index-coalescing-design.md) |
| 2026-08-31 | **F&B 콘텐츠 분류 — 축을 어휘에서 유도, 서빙은 무변경** — F&B 계정은 이미 매일 크롤링되는데(운영 `fnb.pipeline-enabled=true` — 트랙 LL 문서의 "기본 off"는 토글 on이 런타임 UPDATE라 커밋에 안 남아 갱신 누락) LLM 분석이 **0건**이었다. 08-31 실측: F&B 단독 계정 5,575개 / ENUMERATION 콘텐츠 101,932건(캡션 보유 98.9%) / 분석 후보 61,619건 / `content_analyses` 0건·미러 0건 — 크롤링 비용만 쓰고 산출물이 없는 구간. 막고 있던 문이 **셋**이었다: ①`04_analysis_candidates`가 서빙 뷰 `v_contents`(02) 위에 얹혀 뷰티 모수를 상속 ②`sanitize()`의 "비뷰티면 `main_category=null`"이라 어휘를 넣어도 분류가 지워짐 ③`resolveTargets()`의 미러 미도달 게이트 — F&B는 미러(`contents`)에 없어 100% 스킵(①②만 고치면 여전히 0건). **해법**: 축 소속을 `content_analyses` 컬럼이 아니라 **어휘 테이블이 알게** 한다(`beauty_taxonomy.axis`·`beauty_distributors.axis`, `DEFAULT 'beauty'`) — `main_category`가 있으면 그 대분류의 axis가 곧 콘텐츠의 축이고, `is_beauty`는 "axis=beauty"의 **파생값**으로 계속 채워 was 소비처 5곳(랭킹·V35 카테고리 믹스·발굴 matview·`findShares`·`account_beauty_ratio`)을 무접촉으로 뒀다. **홈/리빙 추가 = 어휘 INSERT 한 번 + 04 모수에 OR 한 항.** LLM 응답 필드는 `isBeauty`→`isRelevant`(분류표 어느 대분류에든 해당하는가)로 축 중립화하고, 프롬프트 2벌에 규칙 3개 추가(섭취 제품은 뷰티 목적이어도 F&B 우선 · 공동구매는 sponsored · 유통사는 mainCategory와 같은 축만). 04는 `v_analysis_source`(신설, 모수 = 뷰티 ∪ F&B) 위로 이설 — 미러가 `SELECT * FROM v_contents`라 02를 넓히면 랭킹이 즉시 열린다. 분석 재료도 미러가 아니라 후보 뷰에서 읽어 미러 게이트를 제거(콘텐츠당 조회 1회 감소 + 미러 지연으로 뷰티 후보가 하루 밀리던 스킵도 해소). 배치 제출은 청크 분할(`analytics.batch-chunk-size`, 기본 3000) — 6만 건을 한 배치로 밀면 `sidecar_jsonl` 한 컬럼에 수십 MB. **어휘는 피처링 콘텐츠 랭킹 필터 트리 정본**(대분류 6·소분류 24, slug는 URL 파라미터 그대로. 요리/레시피의 '음료'는 중분류 '음료'와 정확 일치 충돌이라 '음료 레시피'로 분리 — 에스테틱 '필링 시술' 선례). **was 변경은 `findDistributorOptions` 축 필터 1줄뿐이며 서빙 확장이 아니라 오염 차단**(F&B 유통사 11행이 시드되면 뷰티 랭킹 드롭다운에 편의점이 뜬다). **기각한 대안**: `is_fnb` 컬럼 추가(축마다 컬럼·LLM 필드·마이그레이션이 늘어 4번째 카테고리에서 반복) / "main_category 있음 ⇒ 뷰티" 불변식 폐기(`main_category IS NOT NULL`에만 의존하는 경로가 F&B를 흡수) / 테이블 rename(운영 DB·소비처 다수, 에스테틱 때와 같은 판단) / 어휘만 먼저 추가(문 ①③ 때문에 무효). **함정**: `in_window`(01 위임)뿐 아니라 `timely` EXISTS도 `content_id`를 얻으려 `v_serving_content`를 조인해 뷰티 게이트에 묶여 있었다 — 하나만 고치면 F&B의 timely가 영원히 false. 시드 격리 DELETE의 하드코딩 ID 범위가 범위 밖 새 픽스처의 raw 원형을 조용히 지워 "시드했는데 뷰에 안 보임"으로 나타났다(유도식으로 교체). 검증: 4모듈 테스트 클래스 361개 실패 0 + SQL 하니스 15개 ALL GREEN(프레시 PG16 + crawler 마이그레이션 27개). 서빙 무변경은 F&B 계정을 **공용 시드**에 두고 서빙 뷰 테스트(01·02·20)가 통과하는 것으로 보증 | [설계](docs/superpowers/specs/2026-08-31-fnb-content-taxonomy-design.md) · 트랙 [LL2](docs/tracks/LL2-fnb-콘텐츠-분류.md) · 마이그레이션 `V20260831032349__taxonomy_axis.sql`·`V20260831032411__fnb_taxonomy.sql` |
| 2026-08-30 | **브랜드 AI 어시스턴트를 FE 계약(2026-08-28 변경요청서)으로 개편 — `/ai/chat`→`/ai/messages`, 대화 영속화·scope 서버 강제** — 미배포 상태라 하위호환 없이 교체. `POST /v1/brand-monitoring/ai/messages`(conversationId·accountIds[1개 고정, 2개↑ 400]·presetId·text·scope) + 대화 목록/상세/삭제(soft delete — 유저가 지운 대화가 분석 가치 최고라 서버 보존) + `GET /ai/usage`. **응답은 완결 JSON**(SSE는 요청서 §12.1이 허용한 폴백 경로 — 동기 툴 루프 구조상 첫 delta 3초가 불가능해 완결 우선, 스트리밍은 후속). scope(기간·mediaType·sponsorship·source·팔로워·작성자 검색)는 프롬프트가 아니라 **ToolSession 인덱스 필터로 강제** — 모델이 어떤 파라미터를 넘겨도 scope 밖·요청 계정 밖 데이터는 툴 결과에 나타나지 않는다(인덱스 SQL에 content_type·author_username 2컬럼 승격, q·팔로워 축은 지정 시에만 author 배치 조회). 프리셋은 서버 지시문 맵 + **미등록 presetId는 자유 질의 폴백**(FE 단독 실험 허용). followUps는 답변 후 구조화 출력 1콜(kind deepen/action 서버 강제, 실패 시 빈 배열), references는 post 타입만(brandPostId=shortcode, influencer는 기존 이슈 종속 제외). 차감 규칙: 성공·타임아웃=차감(토큰 소모 기준 — "유저 중단도 차감" 논리와 동일), 순수 LLM 실패만 미차감, 풀 포화는 로그 자체 미생성. `INSUFFICIENT_DATA`는 에러 코드가 아니라 답변 본문으로 처리. 대화는 계정 종속 — 불일치 시 409 `CONVERSATION_SCOPE_MISMATCH`. 기존 `V20260827110158` 마이그레이션은 develop 채번 역전으로 `V20260828120000` 재채번(미배포 브랜치라 안전) | FE 변경요청서(프론트 전달분) · `was/…/brandmonitoring/ai/` · `V20260830041012__ai_conversations.sql` |
| 2026-08-29 | **발굴을 조건 기반 자동화로 전환 — 발굴~판정을 한 잡(harvest)으로 통합하고 qualify·beauty 크론은 수동으로 내린다** — 파이프라인 중 발굴만 수동으로 남아 있었고, 해시태그 축은 이미 사실상 죽어 있었다(`search_keyword` 93개 중 활성 1개, `influencer_discovery` 출처가 해시태그 33건 대 suggested 2,951건 — 현 DB는 suggested 단일 축의 산물). 원인은 **워터마크 부재** — `/v2/hashtag/medias/recent` 첫 페이지를 재호출하면 24건 중 24건이 동일해 매일 같은 게시물만 읽는다(첫 페이지 ≈ 84분치). 발굴 항목에 워터마크(마지막 읽은 `taken_at`)를 두고 첫 페이지부터 읽어 워터마크에서 중단하면 활발한 태그는 많이·한산한 태그는 적게 읽어 콜 배분이 자기조절된다. **밴드 조준 레버는 태그 규모가 아니라 top/recent 표면 선택**(#메이크업 top 작성자 팔로워 중앙값 14,115 vs recent 1,180 — 12배, 표본 각 22명). 발굴 시점에 프로필 조회·산업군 3축 동시 판정까지 완결해 "목표 인원"을 종료 조건으로 쓸 수 있게 하고(예산 콜 수와 둘 중 먼저 도달하는 쪽에서 종료), 그 결과 DISCOVERED 적체가 구조적으로 사라져 07-24 유형(대량 유입→qualify 폭주→IG IP 차단) 재발 경로가 제거된다. 팔로워 밴드 밖도 EXCLUDED로 저장(기준 변경·계정 성장 시 재발굴 없이 재분류). 판정 LLM은 Vertex로 전환하고 `common-llm` 모듈에 의존한다(복제하면 Vertex 인증·재시도가 analytics·monitoring·crawler 3벌이 된다 — 기존 `ClaudeClientFactory` 복제 전례를 뒤집는 결정). 크론은 harvest 신설(KST 00:00, collect 앞이라 신규 계정이 같은 밤에 게시물까지 수집돼 04:30 미러에 완전한 데이터로 진입)·qualify·beauty는 `"-"`. **기각한 대안(전부 실측으로 반증)**: 팔로잉 교차(겹침률 91.9%·df≥8은 0명·df≥4의 48명 중 24개가 화장품 브랜드 공식계정 — 인플루언서가 공통 팔로우하는 건 동료가 아니라 소비 대상), 위치 지오태그(뷰티 살롱 3곳 게시물 0건·도시 태그 작성자는 고철상·책방·좋아요 중앙값 27), 좋아요 프리필터(좋아요 500배 차이에 팔로워 중앙값 1.7배·하한 컷은 유해), 태그 규모 조준(단조 관계 없음·니치 태그는 콜당 작성자 1/3), `explore/businesses`(뷰티 계정에 사진작가·정치 추천), 무료 디렉토리(남의 인덱스에 커버리지 종속·열거 불가). 실측 940콜/$0.65, 뷰티 인플루언서 1명당 ≈14콜(100명 ≈ 1,200~1,500콜) | [spec](docs/superpowers/specs/2026-08-29-auto-discovery-design.md) · [track](docs/tracks/FF-발굴-자동화.md) |
| 2026-08-28 | **태그 생성 권한 was 일원화 — monitoring 등록 자가 시드 제거** — 계정명 유도 태그가 monitoring(등록·replay 시 `BrandRegistrationService.seedHashtagsSafely`)과 was(링크 생성 시 사용자 장부 시딩) 두 곳에서 각자 만들어지던 구조를 폐기. monitoring은 이제 `brand_hashtag`에 아무것도 스스로 심지 않는다 — 자동 시드 태그는 was가 링크 생성 시 **일반 태그 add**로 push한다(재활성 의미론까지 포함 — 사용자 등록과 완전 동일 경로, `commandClient.addHashtagTags`). push는 monitoring push 먼저 시도 후 실패해도 사용자 장부 쓰기는 그대로 진행(둘 다 best-effort, 등록 실패로 이어지지 않는다). 초기 백필이 아직 안 끝난 브랜드는 태그 추가 즉시 스윕을 스킵한다(`triggerHashtagSweepIfNonEmpty` — `row.lastSweptOn() == null` 가드) — 백필 꼬리의 `triggerHashtagSweep`이 그 태그를 곧 쓸어주므로 전역 콜 예산(14) 경합을 피한다(순서 무관 안전 — push가 touchSwept 전/후 어느 쪽에 도착해도 한쪽이 커버). **근거**: 생성 권한이 두 시스템에 분산되면 정합성 추론 부담이 커지고(어느 쪽이 먼저 쓰는지, 실패 시 누가 재시도하는지), monitoring의 `BrandHashtagTags.derive`가 was와 같은 규칙을 중복 구현하며, replay 경로가 매번 죽은 장부 태그를 재시드하는 낭비 엣지가 있었다 | `monitoring/…/service/BrandRegistrationService.java` · `was/…/brandmonitoring/V1BrandAccountService.java` |
| 2026-08-28 | **모수 불일치 2건 — 참여율 분자에 `ratedComments` 신설 + 수집 상한 컷을 `/posts`·`/influencers` 공용 함수로 분리** ([설계](docs/superpowers/specs/2026-08-28-fe-parameter-unification-design.md)) | 둘 다 "같은 화면의 두 숫자가 서로 다른 게시물 집합을 센다"는 한 종류의 결함이고, 둘 다 **FE가 우회할 수 없다**(우회하려면 방금 없앤 13.19MB 전량 응답을 다시 받아야 한다). <br><br>**① `/growth` 댓글**: `foldOne`에서 `comments`만 좋아요 숨김 게이트 **바깥**에 있었다 — `likes`·`followersSum`은 `else` 안인데 `comments`만 밖이라 숨김 게시물 댓글이 분자에 남았다(FE 실측 8/22~28 249건: comments 2,738 vs 숨김 제외 2,317, **421건 과대**. likes·followersSum은 일치). 08-27 ①이 이걸 못 잡은 이유는 그 작업이 **분모를 분자에 맞추는** 일이었고, 분자의 두 항이 서로 다른 위치에서 더해진다는 사실은 시야 밖이었기 때문이다. 기존 `comments`는 08-06 계약(관측 전량)대로 두고 **`ratedComments`를 additive 신설** — 제자리 수정하면 그 값을 쓰는 소비자 화면이 그날 바뀐다. 게이트는 `followersSum`이 아니라 **`likes`와 문자 그대로 같은 위치**(같은 else 블록)로 뒀다: `likes` 합이 이미 팔로워 미상 게시물을 포함하므로 `ratedComments`만 더 좁히면 **분자 안에서** 두 항의 모수가 갈린다. 분자·분모 잔차는 08-27에 수용된 기존 비대칭이고 `followersMissingCount`로 이미 보인다. **같은 블록에 두는 것이 설계의 요점** — 떨어진 자리에서 더하면 모수가 갈렸는지 읽어서 알 수 없다(이번 결함이 정확히 그 형태). <br><br>**② 수집 상한**: `/posts`는 08-27 ③에서 모수를 최신순 2,000으로 선컷했는데 `/influencers`가 그 컷을 안 탔다 — 같은 화면의 앞뒤(목록 → 상세)라 숫자가 모순됐다(marynmay 관련 인원 2,800 vs 1,607, harthbeauty 게시물 14 vs 10 — "14개"를 눌러 들어가면 10개). 컷을 **`BrandCollectionCap`으로 분리**하고 둘이 같은 것을 부른다. **값만 맞추고 각자 인라인으로 두지 않은 이유**: 정렬 타이브레이크가 갈리는 순간 같은 상한을 쓰고도 상한 경계에서 **서로 다른 2,000개**를 고른다(같은 종류의 버그 재발). 그래서 순서(업로드일 내림차순 + shortcode 타이브레이크의 **전순서**)도 같은 클래스에 뒀고, 목록의 기본 정렬 `uploaded_desc`가 컷과 같은 순서인 것도 우연이 아니라 "최근 것부터"라는 한 규칙이라 정의를 공유한다. **호출 위치가 계약의 일부**다 — 링크 창 뒤·기간 필터 앞(뒤로 밀면 기간을 좁힐 때마다 상한 밖 게시물이 되살아나 모수가 필터에 따라 달라진다), 그리고 **계정 단위**(상한이 브랜드 계정별 수집 정책이라 계정을 합쳐 자르면 요청 계정 수에 따라 계정별 모수가 달라진다). 상한 도달은 `meta.collectionCapped`(계정 OR)로 내리고, `/influencers`의 `meta.limit`은 이미 페이지 크기라 상한값 자체는 안 싣는다. <br><br>**알고도 남긴 것**: `/comparison`은 08-27 ①의 `followersSum` 수정이 **반영되지 않았다** — 팔로워를 무조건 더하는 구 규칙이고, `PerformanceComparisonAssemblerTest`가 그 구 규칙을 단언 2건으로 고정하고 있어 `/growth`만 고쳤을 때 아무것도 깨지지 않았다(두 집계기가 별개 파일의 별개 private 메서드라 타입 시스템이 잡을 지점이 없다). `comments`도 숨김 포함이다. 기존 소비자가 있는 필드라 고치면 값이 바뀌므로, **FE가 `/comparison`을 아직 호출하는지** 확인 후 결정한다(호출자가 없으면 고치는 게 아니라 표면 제거가 답이다). || 2026-08-28 | **캐시가 인덱스를 장기 보관하면서 드러난 힙 유지 결함 수정 + LRU 상한 16→32(메모리 다이얼이 아니라 작업 집합 기준으로 재산정)** | 인덱스 캐시를 넣고 힙을 실측하다 발견: `indexForBrand`가 `poolByCode.keySet()`(LinkedHashMap **뷰**)를 그대로 담아 **원시 행 맵이 통째로 함께 살아남았다** — 로컬 풀GC 후 히스토그램에서 `BrandPostIndexRow` 11,438개 잔존 확인. 인덱스가 요청마다 버려지던 시절엔 무해했지만 캐시가 이를 **장기 상주로 승격**시켰다. `Set.copyOf`로 끊으니 히스토그램에서 원시 행이 완전히 사라졌다(바이트 절감은 8%로 크지 않다 — Strings는 PostRef와 공유라 실제로 풀리는 건 행 객체·OffsetDateTime 4개·맵 노드뿐. **더 큰 값어치는 죽은 객체 1만여 개를 old gen에 붙잡아 두지 않는 것**). <br><br>**상한은 한 번 8로 낮췄다가 32로 되돌렸다 — 그 과정이 이 결정의 핵심이다.** 8로 낮춘 근거였던 "엔트리 하나 ~9MB × 16 = 최악 160MB"는 **모든 엔트리가 운영 최대 브랜드(계정 120)라는 불가능한 가정**이었다(브랜드 120을 연결한 유저는 3명뿐이고 한 유저는 한 번만 담는다). 활성 링크 132개를 전수로 놓고 가능한 엔트리 528개를 크기순으로 더하니 실제는 **8개 62MB · 16개 91MB · 32개 116MB · 528개 전부여도 170MB**다 — 큰 브랜드가 앞쪽에 다 들어와 곡선이 금방 평평해진다. 반대로 8은 **작업 집합을 못 덮는다**: 인플루언서 조회가 브랜드마다 2엔트리(인덱스+스냅샷), 게시물 상세는 그 유저의 전 브랜드를 훑는데 브랜드 8개 보유 유저가 실재해서, 한 요청이 자기 엔트리를 밀어내 **적중률이 0**이 된다. 그래서 **`MAX_ENTRIES`는 메모리 다이얼이 아니라 폭주 방지 백스톱**으로 규정하고, 값은 작업 집합(최대 12엔트리 유저 + 여유)으로 고른다. 힙 근거: 2GB에 Old Gen 최대 887MB(Prometheus 7일)라 최악 116MB를 얹어도 1,003MB로 절반이 남는다. **재검토 트리거는 "브랜드 창 안 총 행수"**(상주량이 여기에 선형) — 08-28 기준 156개 브랜드 69,972행, 3배가 되면 다시 볼 것. <br><br>**알고도 남긴 것**: `cached()`가 락 밖에서 계산하므로 콜드 구간 동시 진입 시 각자 인덱스를 만든다(계정 120에 10명이면 ~90MB 순간 스파이크, 회수됨) — 락 안에서 1.6초 조립을 돌리면 무관한 조회까지 줄 서는 쪽이 더 나쁘다고 판단했다. |
| 2026-08-28 | **브랜드 목록 고정비의 본체는 캡션이 아니라 모수 그 자체 — 버전키 기반 인덱스 캐시로 재조립을 없앤다(성과 대시보드와 같은 무효화 계약 재사용)** | FE가 브랜드 계열만 남은 요청당 고정비 2~2.5초를 "캡션 전송 제거가 브랜드 쪽에 미적용"으로 진단했는데 **오진이다** — 두 표면은 이미 같은 `findBrandPostIndex`(캡션 대신 `caption_marker` boolean, 08-27 P0)를 쓴다. 스테이징 단계 로그 실측(계정 119·창 안 5,111행): 목록 1.72초 중 `findBrandPostIndex` 단독 **1.59초**이고, 그 안은 DB 실행 430ms(협찬 정규식 350ms 포함) + psql 전송까지 505ms, **나머지 ~1.1초가 JDBC 행 매핑**(5,111행 × 16컬럼, 2코어 호스트)이다. 즉 남은 비용은 캡션이 아니라 **모수 전량을 매 요청 다시 만드는 것**이고, 같은 쿼리를 쓰는 성과 대시보드도 200 응답은 3.7초로 동일하다(FE가 본 0.28초는 **ETag 304 경로**였다 — 08-13 ETag 설계가 브랜드 표면엔 없었을 뿐). **ETag만으로는 이 문제가 안 풀린다**: FE는 2,000건을 100건씩 20회로 받으므로 페이지마다 URL이 달라 `If-None-Match`가 없다. 그래서 **인덱스 자체를 캐시**한다(`BrandIndexCache`) — 키는 성과 대시보드가 이미 쓰는 `DashboardVersion.compute(userId)`(스윕 워터마크·유저 쓰기 지문 5종·KST 날짜·배포 세대)라 **무효화 규칙을 새로 만들지 않고 검증된 계약을 상속**한다(수용된 지연 3건도 그대로 상속 — 상한 하루·자가 치유). Redis가 아닌 인프로세스인 이유: 값이 5천 행 객체 그래프라 JSON 직렬화 왕복이 아끼려는 비용보다 크고, 수명이 "한 유저의 페이징 버스트" 단위다. LRU 16개 상한, 계산은 맵 락 밖(1.6초 조립을 락 안에서 돌리면 무관한 조회까지 줄 선다). **로컬 실데이터 실측**(브랜드 34·2,014행): 목록 첫 요청 296ms → 이후 22ms, 2,000건 20페이지 전량 **0.95초**(첫 페이지 244ms + 이후 평균 37ms). 브랜드 연결 INSERT 직후 227ms로 재조립되는 것까지 확인(무효화 실동작). 게시물·상세·인플루언서 세 표면이 같은 인덱스를 공유한다. 후속 후보: 협찬 정규식의 `lower(caption) ~` → `caption ~*`(08-27 행), 인덱스 프로젝션에서 목록이 안 쓰는 게시자 URL 2컬럼 분리. |
| 2026-08-28 | **growth의 `followersSum`은 참여율 분모다 — 좋아요를 아는 게시물만 담는다(분자·분모 모수 일치)** | `PerformanceGrowthAggregator.foldOne`이 팔로워를 알면 무조건 합산해, 분자(likes)에서 빠지는 좋아요 숨김·미상 게시물의 팔로워가 분모에 남았다. 분모만 부풀어 **참여율이 과소 표시**된다(스테이징 계정 119 실측: 2026-05 11,352,313 → 9,238,679, **19% 과대 분모**. 06월 15,325,546→11,627,445, 07월 12,199,300→9,896,830 — FE가 화면 규칙으로 계산한 값과 세 구간 모두 정확히 일치). 게이트를 **숨김 아님 + likes 있음 + 팔로워 있음**으로 좁혀, 같은 서버에서 이미 올바르던 `PerformanceInfluencerAggregator.ratedFollowers`와 규칙을 통일했다. `followersMissingCount`의 의미("팔로워 미상 건수")는 게이트와 무관하게 불변이다 — 그쪽까지 좁히면 결측 사유 카운트가 "합산 안 된 건수"로 뜻이 바뀐다. 요청서의 결측 규칙("좋아요 숨김: 합계에서 빼고 `likesHiddenCount` 증가")과도 이제 일치. |
| 2026-08-28 | **브랜드 게시물 `total`·`counts`·`facets`를 수집 상한(2,000) 모수로 통일 + `meta.collectionCapped` 신설** | 상한을 필터·정렬 **뒤에** 걸고 있어 `total`(≤2000)과 `counts`·`facets`(상한 없는 전량)가 서로 다른 모수를 말했다(계정 119 실측: `total` 2,000 vs `facets.contentType.all` 4,256 — 화면이 "릴스 3,961"을 띄워도 실제로는 2,000건 안에서만 나온다). **모수 자체를 최신 업로드순 2,000으로 선컷**해 이후 모든 계산이 같은 모수를 보게 했다. 컷 순서를 요청 정렬이 아니라 **항상 최신순**으로 둔 이유: 이 상한은 "최근 2,000개까지 수집"이라는 수집 정책의 표면이지 정렬별 상위 2,000이 아니다(정렬을 바꿀 때마다 모수가 갈리면 counts가 흔들린다). 전부 2,000으로 통일하면 "상한 도달"과 "마침 정확히 2,000건"을 구분할 수 없어 FE의 안내 문구 판정이 깨지므로, FE가 제시한 두 선택지 중 **정확한 쪽(`meta.collectionCapped` 불리언)** 을 골랐다 — `total === limit` 추정은 경계에서 조용히 틀리는 계약이라 남기지 않는다. 실데이터 검증(브랜드 34·2,014행): `total`·`counts.all`·`facets.*.all` 전부 2,000, sponsored 519 + organic 1,481 = 2,000, reels 1,236 + feed 764 = 2,000, `offset=1999` 1건·`offset=2000` 0건, 필터 적용 `total`이 해당 패싯 값과 일치. 상한 미달 브랜드(1,838행)는 `collectionCapped: false`. |
| 2026-08-27 | **브랜드 모니터링 AI 어시스턴트 PoC — was에 툴 콜링 에이전트로 구현** — `POST /v1/brand-monitoring/ai/chat`을 열어 사용자가 자기 브랜드 모니터링 데이터(등록 브랜드·태그 게시물·해시태그 발견 게시물·댓글·게시자 프로필)에 자유 질의하면 읽기 전용 툴 6종(`list_brands`·`list_posts`·`get_post`·`get_comments`·`list_hashtag_posts`·`get_author`)을 가진 Vertex Gemini 에이전트 루프가 답한다. `common-llm`은 무수정 — 기존 `VertexHttpTransport.post(path, jsonBody)`가 임의 JSON 본문을 그대로 통과시켜 function calling 페이로드를 얹는 데 확장이 불필요했다(설계 §3 확인). 킬 스위치는 `BRAND_AI_ENABLED` env(기본 false)로 표면 자체를 없애는 방식 — 폴백 경로 없이 미설정 시 API가 404다. 일일 질문 상한 기준값은 `app_setting` `ai.chat.daily-limit`=30(마이그레이션 시드, 런타임 조정만 UPDATE). 툴 소유 검증(brandId·shortCode가 요청 유저 것인지)은 `BrandAiToolbox` 안에서 강제하며, `BrandReadRepository` 자체는 여전히 brandId를 검증하지 않는다는 기존 전제를 유지한다. | [설계](docs/superpowers/specs/2026-08-27-brand-monitoring-ai-assistant-poc-design.md) · [계획](docs/superpowers/plans/archive/2026-08-27-brand-monitoring-ai-assistant-poc.md) |
| 2026-08-27(구현 08-28 완료) | **해시태그 직접 수집 전환 — 감지 구조 폐기, tagged 풀에 편입** — 해시태그 파이프라인을 별도 감지 테이블(`brand_hashtag_post`) + LLM 관련성 판정 구조에서 `brand_tagged_post` 풀 직접 편입으로 전환. `hashtag_detected_at` 성분 컬럼 추가(tag_detected_at·direct_registered_at과 병존), 판정 없이 전부 편입하되 브랜드 본인 계정 게시물은 규칙 기반 제외, 브랜드당 1000 상한(tagged 2000과 별도 카운터), 수집 기간은 고정 90일 대신 브랜드 collectionMonths 적용. 매칭 태그 원장 `brand_post_matched_tag` 신설. `/posts` 통합 목록에 source=hashtag로 합류 + 사용자 격리(장부∩매칭 방식, 기존 fail-open 폐기). 구 `/hashtag-posts`는 새 풀로 리라우팅 후 다음 릴리스 제거, 구 감지 테이블(`brand_hashtag_post`·`brand_hashtag_post_matched_tags`) DROP도 다음 릴리스(expand-contract) | [설계](docs/superpowers/specs/archive/2026-08-27-hashtag-direct-collection-design.md) · [track](docs/tracks/MON-BT-브랜드-태그-모니터링.md) |
| 2026-08-27 | **알림을 주간 1건으로 통일 — 일일·즉시 레인 폐지, 발송을 monitoring→was로 이관** — 알림을 매주 월요일 09:00 KST 발송되는 지난주(월~일) 요약 다이제스트 1건(인앱+메일)으로 통합한다. 기존 4종 이벤트 레인(일일·즉시)을 폐지하고 섹션 3종(브랜드 새 게시물 발견·등록 게시물 광고 미표기 판정·기존 4종 요약)으로 재구성, 내용 있는 섹션만 노출한다. 이벤트 0건인 주는 upsert 대신 `DigestRepository#clearItems`로 그 (user, 주 시작일) 행의 items만 비운다(행·read_at·email_sent_at·email_attempts는 보존 — 최초 구현은 행 delete였는데, 킬 스위치 off·브랜드 연결 해제로 행이 지워졌다 복구되는 경로에서 발송 이력까지 함께 사라져 같은 주 메일이 중복 발송되는 결함이 있어 재리뷰로 교체). 발송 크론은 `monitoring.digest.weekly-cron`(월 09:00 정시)과 `weekly-catchup-cron`(요일 제한 없이 매일 09:10~23:50 10분 간격 재계산 따라잡기) 둘 다 was 소유이며, 메일 발송 주체를 monitoring의 5분 틱 디스패처에서 was(`RESEND_API_KEY`)로 이관하고 `MONITORING_ALARM_*` env·`alarm_reader` 롤은 폐지했다(운영 롤 회수는 배포 후 별도 작업). 알림 설정은 기존 4종 매트릭스에서 `weeklyEmail` boolean 토글 1개로 축소(FE 통지 필요 — 기존 옵트아웃은 하나라도 꺼져 있으면 off로 보수적 이관). `GET/PATCH /v1/notifications` 계약은 그대로 유지 | [설계](docs/superpowers/specs/archive/2026-08-27-weekly-notification-digest-design.md) |
| 2026-08-27 | **게시물 목록 서버 필터·패싯 + 인플루언서 집계 API — 협찬 판정 마커를 SQL로 이전(캡션 전송 제거)하되 소급성은 유지** — 브랜드 게시물 목록의 요청당 고정비(FE 실측 dev-api ~1.9초, 페이지 크기 무관)는 협찬 판정용 **캡션 원문 7.7MB(4,969행)를 매 요청 전송·JDBC 매핑**하는 데서 나온다. FE가 제안한 수집 시점 사전집계 버킷은 기각 — "협찬 판정은 저장하지 않는다(키워드 개선이 과거분에 즉시 소급)"는 설계 결정과 정면 충돌하고, 유저별 표시 창·자유 기간 범위 때문에 일 버킷이 필요하며 influencerCount(distinct)는 버킷 합산이 불가능하다. 대신 **마커 상수 4갈래(부분 문자열·라틴 해시태그 토큰 전체 일치·reklam 단어 접두·캡션 선두 접두 표기)에서 Postgres ARE 정규식 1개를 생성**(`BrandSponsorshipClassifier.postgresMarkerRegex()`)해 `lower(m.caption) ~ :markerRegex AS caption_marker` boolean만 받고, 판정 트리는 Java에 남긴다(`classify(Boolean, boolean)` 오버로드) — **마커 상수가 여전히 Java 한 곳이라 소급성이 그대로 유지**되는 것이 이 방식을 고른 이유다. ARE에 lookbehind/lookahead가 없어 소비형 등가 변환(`(?<![\p{L}\p{N}])`→`(^\|[^[:alnum:]])`, `l(?=\s)`→`l[공백]`), `\s`는 PG `[[:space:]]`가 로케일에 따라 유니코드 공백까지 넓어지므로 Java `\s`와 동치인 ASCII 6종 클래스(수직탭 `\013` 포함 — 빠뜨리면 SQL만 미탐)로 못박았다. 동치성은 Testcontainers 골든 코퍼스(`BrandSponsorshipSqlEquivalenceTest`)가 봉인 — 마커 상수를 고치면 그 테스트가 함께 검증한다. **perf119 실측(전송 포함 왕복, 중앙값)은 이득이 한 방향이 아니다**: 캡션 전송 제거분은 13.5ms→6.1ms(2.2배)지만 마커 평가가 WAS→PG로 옮겨오며 신 쿼리는 **13.5→107.2ms**가 됐다(구 경로가 WAS에서 치르던 Java 판정 87.1ms와 맞바꾼 등가 교환에 가깝다). **DB 측 전송분은 ~7.5ms에 불과해 이 하니스로는 ~1.9초 고정비의 본체(JDBC String 생성·GC·2코어 호스트 CPU 경합)를 재지 못한다** — 엔드포인트 개선 여부는 dev-api 배포 후 FE와 동일 조건 재측정이 판정 지점이다. 후속 후보: `lower(caption) ~` → `caption ~*` 로 107.2→58.7ms(1.8배, `lower()`가 101ms 중 ~51ms) — 케이스 폴딩이 `toLowerCase(Locale.ROOT)`와 유니코드 엣지에서 어긋날 수 있어 골든 코퍼스 보강 후에만. 함께: **`meta.facets`는 additive 신규 키**(기존 `meta.counts`·전량 모드 응답 형태 불변 — 기존 FE 무영향), 신규 서버 필터 5종(`contentType`·`follower`·`keyword`·`adRisk`·`authorUsername`)과 `GET /v1/brand-monitoring/influencers` 신설. **판정 규칙의 정본은 FE 화면 코드**로 두고 1:1 이식한다(`brand-influencers.ts`의 집계·정렬, `ad-disclosure.ts:78-106`의 `hasAdDisclosureIssue`) — 서버가 독자 해석을 넣으면 같은 화면에서 서버 패싯과 클라 표시가 갈라진다. 그 원칙에 따라 **follower 밴드는 요청서의 "6.21과 동일"이 아니라 실제 화면 코드 `BRAND_FOLLOWER_RANGES`의 5토큰**(`0-3k`/`3k-10k`/`10k-30k`/`30k-50k`/`50k+`, 하한 포함·상한 배타, followers null은 필터 시 제외)을 채택했다(FE에 통보 필요) | [설계](docs/superpowers/specs/2026-08-27-post-list-server-filter-facets-design.md) · [track](docs/tracks/MON-BT-브랜드-태그-모니터링.md) |
| 2026-08-27 | **홈/리빙 카테고리 추가 — 판정 3축화, F&B 병렬 복제 패턴 반복** — `influencer`에 `home_living_*` 병렬 컬럼 8개(enum은 기존 `CategoryClass` 재사용), 판정 프롬프트 1콜 3축 동시 판정(`MAX_TOKENS` 16384→24576), 기존 판정분 전체 홈/리빙 백필(선정 순서: 신규 → F&B 백필 → 홈/리빙 백필, 뷰티·F&B 기존 판정 불가침 — `fnbOnly` 마스크를 `backfillOnly`로 일반화), 수집·시드 편입은 `home-living.pipeline-enabled` 토글(기본 off). **INFLUENCER 경계는 제품 리뷰·공구형 + 집꾸미기·홈스타일링형**(순수 일상·가족형은 NONE — featuring 수집 CSV 실계정 예시로 확정, F&B의 "맛집 탐방형 포함"과 같은 철학). **정착 규칙(캡션 기반 판정 자동 재적용 금지·캡션 0→N 1회 업그레이드·MANUAL 보호)을 홈/리빙 축에 처음부터 적용** — F&B가 겪은 매 주기 판정 뒤집힘(08-26 순감 883) 예방. F&B 스펙의 "3개부터 범용 테이블 이관"은 이번에도 비채택 — 이관은 뷰티·F&B 전체 리팩토링이라 축 1개 추가 비용을 한참 넘고, 4번째 카테고리나 카테고리 서빙 개편 때 함께 간다. 대시보드는 ③-4 홈/리빙 그룹 신설 + ③-3 수집 모수를 3축 유니온(단독 3개 + 겹침 + 합계, 겹침=유니온−단독합)으로 개편. 서빙 무변경, `v_base_influencer` 노출만 추가 | [설계](docs/superpowers/specs/archive/2026-08-27-home-living-category-design.md) · 마이그레이션 `V20260827090717__influencer_home_living.sql` |
| 2026-08-27 | **성과 대시보드 목록 API 최적화 설계 — 2단 조립 복제 + 탭별 전용 엔드포인트 신설(PR ①~④ 전부 구현됨)** — FE 개선요청(평균 7.4초·p90 14.3초·20.8MB 전량 응답, 탭 3개가 전부 전량 대기)에 대해: 브랜드 목록 PR #602의 2단 조립을 대시보드에 복제하되 ref에 정렬·집계 필드(작성자·팔로워·최신 스냅샷 지표·상태)를 확장, 목록 페이지 슬라이스·인플루언서 집계·growth 버킷을 전부 경량 ref 위에서 계산. 레거시 계열은 현행 전량 조립 유지(유저당 ≤33행), 레거시 겹침 브랜드 코드만 풀 하이드레이트해 스냅샷 병합 의미론 보존, 나머지 브랜드 풀은 경량 프로젝션(scope=ALL·커버리지 클램프·노출 필터 승계 — 기존 indexForBrand는 ENRICHED_ONLY라 쿼리 신설). `/contents`에 sort 5종·offset/limit(생략 시 전량)·accountIds(쉼표)·authorUsername·snapshotMode=latest(옵트인)+previousDayValues, `/influencers`·`/growth` 신설(브랜드 모니터링 쪽 인플루언서 요청과는 스코프·참여율 분모가 달라 비통합), ETag는 08-13 기존 설계 재활용(그 설계의 "페이지네이션 기각"은 UI 개편으로 전제 소멸 — 본 설계가 대체). 요청 1(댓글 제외)은 08-12 기반영 확인 — FE 회신 예정. PR 4개 순차(목록→influencers→growth→ETag) | [설계](docs/superpowers/specs/archive/2026-08-27-perf-dashboard-list-api-optimization-design.md) |
| 2026-08-27 | **광고 판정 백필 1회 실행 상한 폐기** — 상한(총량 분산)은 expose 개통(08-19) 후엔 verdict NULL 잔량이 FE에 부분 판정·과소 위험 카운트로 서빙되는 해악이라 제거(당초 "LLM 호출 수로 계량 단위 교정"으로 착수했다가 사용자 논의로 폐기 확정). 버스트 방어=동시성(concurrency 4), 장애 방어=서킷브레이커 존치, 오판정 배포 대응=enabled 킬 스위치·expose env·리셋 재배포가 정본. 프로퍼티 backfill-max-per-run 삭제(기본 0=무제한이라 운영 동작 무변화) | `monitoring/src/main/java/com/celfit/monitoring/ad/AdDisclosureJudgeService.java` · [track](docs/tracks/MON-BT-브랜드-태그-모니터링.md) |
| 2026-08-27 | **완전 외국어 캡션은 FOREIGN_POST로 판정 제외 — 한국어 캡션+외국어 단독 표기는 종전대로 INSUFFICIENT 유지** — LLM 카테고리 FOREIGN(외국어 단독 표기)이 조합 단계에서 무조건 INSUFFICIENT+FOREIGN_LANGUAGE가 되는데, 실측상 FOREIGN 1,854건 중 92%(1,701건)는 캡션 전체가 외국어인 해외 게시물이라(예: 독일어 캡션+"Werbung") 한국 지침 적용 대상이 아니다. **법 적용은 게시자 국적이 아니라 겨냥한 소비자 시장을 따른다**는 원칙에 따라, 캡션에 한글이 전혀 없으면(음절 U+AC00–U+D7A3·자모 0자) LLM 호출 없이 신규 verdict `FOREIGN_POST`로 확정하는 Tier0 규칙을 추가한다(is_paid_partnership·공백 캡션 규칙보다 뒤, Tier1 사전보다 앞). 한국어 캡션+외국어 단독 표기(캡션에 한글이 1자라도 있음)는 이 규칙을 건너뛰어 종전 경로(LLM FOREIGN→INSUFFICIENT+FOREIGN_LANGUAGE)를 그대로 유지한다. `ad_verdict` CHECK 제약에 FOREIGN_POST 추가(`V20260827060558`, additive). **배포 시 운영 리셋 필요**: 기존 `verdict='INSUFFICIENT' AND ad_violations에 'FOREIGN_LANGUAGE' 포함` 행을 verdict NULL로 리셋해야 백필이 재판정한다(사전·프롬프트 변경은 자동 재판정 안 되는 기존 계약) — 리셋 SQL은 트랙 문서 참조 | `monitoring/src/main/java/com/celfit/monitoring/ad/AdDisclosureJudgeService.java` · [track](docs/tracks/MON-BT-브랜드-태그-모니터링.md) |
| 2026-08-27 | **6.21 발굴 목록 사전집계 + count 통합 — 캐시 미스 11.4s→수백 ms** — mainCategory 계열 필터의 4~11초(운영 요청 emj7j4s6 실측 11.4s)는 요청당 상관 서브쿼리 6,210회×2(findCards+countCards) + `account_beauty_ratio` 뷰·협찬 수 서브쿼리 풀집계×2가 원인. 파생 집계 3종을 matview로 사전계산(`account_beauty_ratio` 뷰→matview 전환 + `account_category_share`·`account_sponsored_counts` 신설 — V20260827045100, analytics 소유)하고, 입력을 쓰는 잡(MIRROR·ANALYZE·LATE_BACKFILL_ANALYZE·BATCH_COLLECT) 완료 시 `DerivedViewRefresher`가 REFRESH CONCURRENTLY. total은 `count(*) OVER ()`로 본 쿼리에 통합, countCards는 0행 폴백 전용. 운영 전수 대조(7개 대분류×전 계정 9,003쌍) 차이 0, 신 쿼리 본체 실측 ~85ms. 게이트·협찬수 반영 시점이 "조회 즉시"→"입력 변경 잡 완료 직후"가 됐지만 입력이 그 잡에서만 변해 관찰 가능한 차이 없음. 2026-07-30의 sp 핸들 푸시다운은 존재 이유 소멸로 제거 | [설계](docs/superpowers/specs/2026-08-27-discovery-precompute-design.md) |
| 2026-08-27 | **브랜드 게시물 목록 offset/limit 페이지네이션 + 2단 조립 — counts 사전 집계 테이블은 비채택** — FE 타임아웃 리포트(운영 marynmay_global, 30초 초과)의 원인은 "첫 페이지를 위해서도 전량 풀 조립"이던 구조(스냅샷은 게시물당 일 시계열 전량 — 463건 브랜드면 최대 ~17만 행, 댓글은 08-12 실측 조립 시간 57%). `GET /accounts/{id}/posts`를 경량 인덱스(`PostRef`: 풀 행+협찬 프로젝션, 스냅샷·댓글 없음) 위에서 counts·창·필터·정렬·슬라이스를 끝내고 응답에 실을 코드만 하이드레이트하는 2단 조립으로 전환. 목록은 `recentComments` 미포함(빈 배열·키 유지, 상세만 포함 — FE 요청 1), 페이지는 리더보드 계약 복제(limit 1..100)·생략 시 전량(하위 호환), `meta.limit`(수집 상한 의미 선점)은 불변이고 페이지 정보는 additive `meta.page={offset,limit}`로 분리. FE가 제안한 수집 시점 버킷 테이블(계정×일자×출처×협찬)은 **비채택** — 협찬 판정은 조회 시 계산이 정본(키워드 소급 설계), source·가시성이 조회자 종속(등록자 direct 승격·direct-only 노출·유저별 창), 취소 시 행 소멸 등 드리프트 원천이 많아 "counts=전량 계산 일치"를 증분 유지로 보장 못 한다. 풀 자체가 수집 상한 2,000으로 유계라 경량 인덱스 패스로 같은 목표(응답 시간이 게시물 수와 사실상 무관) 달성. 상세도 같은 인덱스 경로(단건만 조립) | [설계](docs/superpowers/specs/archive/2026-08-27-brand-posts-pagination-design.md) · `was/…/brandmonitoring/V1BrandPostsController.java`·`BrandPostAssembler.java` |
| 2026-08-27 | **F&B 판정 정착 규칙 + 비뷰티 재판정 쿨다운 — 정착된 판정을 모델 노이즈가 매 주기 덮던 루프 차단** — F&B 파이프라인 편입(08-25)으로 F&B 계정 ~6,500명이 "비뷰티인데 재방문마다 프로필이 갱신되는" 계정이 됐고, `findRejudgeTargets`가 전제하던 "비뷰티는 프로필이 거의 안 갱신된다"가 깨져 매 주기 전원이 재판정 큐에 재진입했다(08-26 재판정 런 5,024명에서 F&B INFLUENCER 이탈 899·편입 16, 순감 883 — 대시보드 ③-2 ~7,390→6,507, Loki 전수 대조 실측). 이탈 자체는 정당한 교정이었고 문제는 교정이 아니라 **정착 판정까지 반복해서 뒤집히는 구조**다. 두 축으로 안정화: ①`applyVerdicts`의 F&B 적용 가드 — 첫 판정(`fnb_class` NULL) 또는 업그레이드(기존 `fnb_caption_count=0` ∧ 이번 캡션>0)만 적용, 그 외 캡션 기반 판정은 자동 재적용 금지(이후 교정은 MANUAL만). **positive/negative 대칭 적용** — negative만 열어두면 노이즈 편입이 고착되는 상향 래칫이 된다. 업그레이드 대기 잔여는 캡션 0건 306명뿐이라 정착 후 ③-2는 고정적. ②`findRejudgeTargets`에 `cooldownBefore` 추가 — 계정당 재판정 월 1회(비뷰티→뷰티 뒤집힘 기회는 유지). 기본값은 yml `@ConfigurationProperties`(`BeautyProperties.rejudgeCooldownDays`, 30일)에 두고 `app_setting`의 `beauty.rejudge-cooldown-days`는 런타임 오버라이드만 — Flyway 시드 불요. beauty 크론 주기·뷰티 축 판정 로직·MANUAL 보호·대시보드 쿼리 불변, 스키마 변경 없음 | [설계](docs/superpowers/specs/archive/2026-08-27-fnb-rejudge-stabilization-design.md) · `crawler/…/service/BeautyJob.java` |
| 2026-08-25 | **만료 CDN 프로필 이미지 백필 — 어드민 수동 트리거로 재수집, 상시 스케줄 아님** — 아카이브 잡들(AuthorProfileImageArchiveJob·HashtagPostAuthorImageArchiveJob)은 CDN 서명이 살아있는 동안만 원본을 미러할 수 있는데, author_profile은 30일 stale+재등장 시에만 재조회돼 재등장하지 않는 게시자는 profile_pic_url이 영구 만료 상태로 남아 아카이브가 계속 skip한다(운영 실측: author_profile 만료분 7,302건, RELEVANT 해시태그 게시자 만료분 477명 — 그중 375명은 author_profile 행 자체 없음, 합집합 7,759명). `AuthorImageBackfillJob`이 Hiker 재조회로 만료 URL만 갱신(Phase A: author_profile by-id, Phase B: RELEVANT 해시태그 작성자 — author_profile의 미만료 URL을 먼저 재사용해 호출 절감, 없으면 by-username)하고 직후 같은 실행에서 기존 아카이브 잡을 그대로 호출해 다운로드까지 닫는다. `limit`은 두 phase가 공유하는 Hiker 호출 총량 상한. 트리거는 POST /api/author-image-backfill(`monitoring.image.backfill-trigger-enabled` 게이트, 기본 off — SweepController와 동형). 실패(계정 삭제·비공개·응답 셰이프 이상)는 건 단위 skip — 깨진 이미지보다 프론트 이니셜 placeholder가 낫다는 판단 | `monitoring/src/main/java/com/celfit/monitoring/image/AuthorImageBackfillJob.java` |
| 2026-08-25 | **삭제 감지 tagged 확장 — 열거 부재를 후보로, 확정은 검증 콜 404로만** — direct 한정 삭제 감지(같은 날 운영 반영)가 실사용 커버리지에서 빈약함이 즉시 드러났다(운영 실측: 꽁냥마켓 게시물 11건 전원 tagged-only). tagged는 열거 부재가 태그 해제·미커버 종료와 구분 불가라, **커버된 열거(coveredCutoff, ⑤상한 종료는 실제 커버 깊이가 하한)에서 사라진 tagged-only 행만** 단건 `fetchPost`로 확정한다: 404 → `unavailable_at`(direct와 동일 표식 → hidden), 생존 → `absence_checked_at`(7일 재검증 스로틀 — 태그 해제 게시물이 매일 과금 콜을 유발하지 않게, 재관측 시 해제라 재소실은 즉시 검증). 겹침 행은 direct 2단계가 이미 404를 잡아 후보 제외(중복 과금 방지), 상한 브랜드당 30콜/스윕(초과분 로그+이월). 정상 상태 0콜, 오탐 0. 계약 v2.16 | [설계](docs/superpowers/specs/2026-08-25-brand-tagged-deletion-verify-design.md) · [계약](docs/contracts/monitoring-was-contract.md) |
| 2026-08-25 | **cAdvisor 0.56.2 + containerd 소켓 마운트 — 컨테이너별 지표 무데이터 해소** — 운영 cAdvisor(v0.49.1)가 컨테이너 단위 시계열(`container_*{name=…}`)을 하나도 못 냈다. 원인은 도커 29 기본이 된 **containerd 스냅샷터 이미지 스토어**(서버 Storage Driver "overlayfs"): v0.49 도커 핸들러가 구 레이아웃 `image/overlayfs/layerdb/mounts/<id>/mount-id`를 읽으려다 실패해 컨테이너 핸들러 생성이 통째로 실패했고(로그 "failed to identify the read-write layer ID" 도배), raw 폴백도 없어 `id="/system.slice/docker.service"` 집계만 남았다. cadvisor#3643의 수정(0.54.0~)은 rw 레이어 경로를 containerd 스펙에서 직접 읽는 방식이라 **`/run/containerd/containerd.sock` 마운트가 필수**(없으면 도커 팩토리 등록 자체가 실패해 여전히 무데이터 — 버전업만으론 안 된다). 이미지 배포처도 gcr.io→ghcr.io 이관(gcr 최신 태그가 v0.52.1). `--docker_only` + `--store_container_labels=false`로 systemd 유닛 cgroup·compose 메타 라벨 시계열 억제. cgroupns:host·/rootfs 마운트 등 표준 레시피 보강은 **불필요 확인**(로컬 Docker Desktop — 동일 스냅샷터 구성 — 에서 기존 마운트+소켓만으로 대시보드 3개 지표 모두 name 라벨 검증). 부수 효과: 같은 원인이던 로컬 하니스 "컨테이너 지표 무데이터"(08-18 실측)도 해소돼 dev README 갱신 | [deploy/compose.yaml](deploy/compose.yaml) cadvisor · [dev 하니스](deploy/grafana/dev/README.md) |
| 2026-08-25 | **브랜드 direct 게시물 삭제 감지 — `unavailable_at` 가역 마킹으로 hidden 노출** — 야간 스윕 단건 콜의 404를 `brand_tagged_post.unavailable_at`에 영속화하고 was가 그 행을 `trackingStatus: "hidden"`으로 노출한다(FE "삭제·비공개" 칩은 계약 6.25로 이미 구현돼 있었음 — BE만 구멍이었다). 스펙 §8 "브랜드 파이프라인은 상태 전이를 하지 않는다"의 유일한 예외지만, 재관측(`touchCrawled` — 실관측에만 찍히는 시맨틱)이 해제하는 가역 마킹이라 종결 전이가 아니고 due 선정도 불변(IG 보관 후 재공개 자가 치유, 404 과금은 건당 $0.00069로 무시 가능). **tagged-only 행은 감지 대상 외** — 단건 콜이 없고, 열거 부재는 태그 해제·수집 상한 컷과 구분 불가(휴리스틱 오탐)·검증 콜은 과금이라 항상 tracking 유지. 성과 대시보드 합성 아이템도 status를 승계(하드코딩 tracking 제거). 계약 v2.15 — was가 컬럼을 무조건 SELECT하므로 monitoring → was 순서 배포 | [설계](docs/superpowers/specs/2026-08-25-brand-post-deletion-hidden-design.md) · [계약 v2.15](docs/contracts/monitoring-was-contract.md) |
| 2026-08-25 | **crawler 백업을 B2 직스트리밍으로 전환(`pg_dump\|zstd\|tee\|rclone rcat`) + B2 가드 SIGPIPE 레이스 수정** — 08-24 `hypenow-disk-high`(87%) 조사에서 두 문제 확인. ①`rclone listremotes \| grep -q '^b2:'` 가드는 b2:가 출력 첫 줄이라 grep -q 조기 종료 → rclone SIGPIPE(141) → pipefail 실패 판정으로 **~5% 확률 오판**(서버 100회 실측 5회 — 08-23 실제 발생: rclone이 출력 전에 죽고 `2>/dev/null`이 stderr마저 버려 로그 무흔적, 업로드 블록 통째 스킵 + 로컬 2개 유지가 디스크 알람까지 유발). 파이프 없는 명령 치환+문자열 매치로 교체, stderr 보존. ②덤프 19분→업로드 19분 직렬(로컬 11GiB 상주, 재읽기 iowait 24% — 08-24 sar 실측)을 tee 스트리밍 ~20분으로 합침 — **성공 시 로컬 crawler 0개**(-11GiB), 실패 시 로컬 전용 덤프 폴백(B2 장애 07-27~08-13 전력의 시점 스냅샷 안전망 유지). 동시화로 CPU가 겹쳐(덤프 구간 idle 13%) zstd·rclone은 `nice -n 19` — API 우선, 백업은 파이프 역압 감속. 반쪽 원격본은 실패 시 즉시 deletefile+cleanup(pg_dump 중간 사망 시 rcat이 잘린 스트림을 정상 종료로 닫는 함정). 목 하니스 5시나리오 검증 | [backup.sh](deploy/scripts/backup.sh) · [README §6](deploy/README.md) |
| 2026-08-24 | **F&B 카테고리 추가 — 판정 2축화, 수집은 토글 뒤** — 판정을 뷰티+F&B 2축으로 확장한다. `influencer`에 `fnb_*` 병렬 컬럼(뷰티 구조 복제 + 카테고리 중립 enum `CategoryClass`), LLM 1콜에서 두 축 동시 판정(콜 수 증가 없음), 기존 판정분은 **F&B 축만 백필**(뷰티 판정·MANUAL 보존). 수집·시드·비용 추정의 F&B 편입은 `app_setting` `fnb.pipeline-enabled`(기본 `false`)로 게이트하고, 서빙 모수(분석 뷰 01/02/20·was)는 뷰티 유지. **근거**: F&B(식품/음료 제품 시딩 + 요리·레시피) 타깃 확장. 복수 카테고리 허용(한 계정이 두 축 모두 가능)이라 boolean 축 병렬이 자연 모델 — 범용 `influencer_category` 테이블은 카테고리 2개 시점엔 조인 전환 비용이 과해 기각, 3개 이상이 되면 그때 이관. 기존 `BeautyClass` 저장값(`BEAUTY_SERVICE`·`NOT_BEAUTY`)은 운영 DB에 박혀 있어 통일 rename하지 않고 그대로 둔다 | [specs/2026-08-23-fnb-category-design.md](docs/superpowers/specs/archive/2026-08-23-fnb-category-design.md) · 트랙 [LL](docs/tracks/LL-fnb-카테고리.md) · 마이그레이션 `V20260824082708__influencer_fnb.sql` |
| 2026-08-24 | **caddy 액세스 로그를 Loki로 수집(`service="caddy-access"`) — 엣지 5xx 가시화** — blakshave 등록 "일시적 500" 조사에서 확인된 사각지대: 상태코드 전수 기록처는 caddy 액세스 로그뿐인데 파일로만 남아, 엣지에서 만들어지는 5xx(롤링 배포 순단 502 등)가 어떤 대시보드에도 안 잡혔다(홈 "API 5xx"는 앱 Prometheus 메트릭, ERROR 패널들은 Loki `level="ERROR"` 기반 — 이번 500 자체는 백엔드 무결로 판명, FE 계층 추정). alloy `loki.source.file`로 운영 `access.log`만 테일링(`tail_from_end`, 포지션은 신규 `alloy-data` 볼륨에 영속), status·uri는 라벨로 안 올리고 **5xx만 `level=ERROR` 승격**해 기존 ERROR 패널에 자동 편입 + 홈에 "엣지 5xx" 패널 추가. test-access.log·로테이션 산출물 미수집("운영만 계측"), 디스코드 알림 rule은 빈도 실측 후 후속 | [설계](docs/superpowers/specs/archive/2026-08-24-caddy-access-log-loki-design.md) · [config.alloy](deploy/alloy/config.alloy) · [리그 검증](deploy/alloy/test/README.md) |
| 2026-08-23 | **브랜드 폴더의 `[브랜드] 운영 건강`·`[브랜드] 수집 현황`을 `[브랜드] 운영`(`hypenow-brand`) 한 장으로 합본** — 출발은 "야간 일일 수집 row(신선도·오늘 성공/소요·게시물 갱신·티어별·브랜드별 처리)를 수집 현황으로 보내자"였는데, 검토 과정에서 두 가지가 드러났다. ①수집 현황의 건강·사용량 타일(오늘 신규 태그 게시물·enrich 잔여·해시태그 감지·적재 추이)은 **야간 스윕과 등록 백필 두 경로의 합산**이라(`brand_tagged_post`에 적재 경로 컬럼이 없다 — 적재 추이의 하루 1만 건대 스파이크(08-12·08-19)는 신규 등록 백필) "야간 전용 장"으로 부를 수 없고, ②남는 운영 건강(등록 응답 p95·런타임·외부 API)과 수집 현황(적재 결과)은 "과정 vs 결과" 축인데 둘 다 '건강'이라 불려 "오늘 괜찮나"를 묻는 사람이 어느 장을 열지 헷갈린다. 08-19 3장 분리의 동기는 광고 표기까지 한 장에 눌린 것이었으므로 광고 표기만 별도로 두고 나머지를 합쳐 **row 6개를 "오늘 결과 → 과정 → 규모·자원" 순**으로 배치했다: 야간 일일 수집 / 적재 결과(야간 스윕 + 등록 백필) / 등록·백필 처리 / 적재 추이·규모 / 런타임 / 사용량 — 뒤 3개는 기본 접힘(접힌 row는 쿼리를 안 내므로 첫 로드 부담은 합본 전과 같음). row 이름에서 '건강'을 빼고 합산 범위를 이름에 명시했다. 야간 전용으로 가르려면 적재 경로 컬럼 추가(스키마 변경)가 선행돼야 한다. 결과 체제는 **HypeNow 5탭 + 브랜드 폴더 2장(총 7장)**. `hypenow-brand-collection` 삭제에 따른 서버 잔존 파일 정리는 README §14-2-2 ⑤(main 배포 전 1회). 패널 ID는 전역 재번호(row 100~105·패널 1~26), 기존 ID 122 중복·런타임 row gridPos 겹침도 해소 | [대시보드](deploy/grafana/provisioning/dashboards/json-brand/hypenow-brand.json) · [deploy/README.md §14](deploy/README.md) · PR #541 |
| 2026-08-23 | **브랜드 크롤 정책 주기 판정에 12h 여유(`BrandCrawlPolicy.INTERVAL_SLACK`) — 스윕 슬롯 드리프트로 하루 밀리는 경계 레이스 제거** — 일일 스윕은 매일 KST 02:00 같은 순서로 브랜드를 순회하는데 `due()`가 경과를 주기(3/7/30일)와 초 단위로 정확히 비교해, 오늘 통과가 직전 touch보다 몇 분만 일러도 그 브랜드의 해당 티어 게시물이 전부 건너뛰어져 그날 하루 "미처리"로 보였다가 다음 날 수습됐다(08-23 운영 실측: 3일 티어 262건·7개 브랜드 전부 이 패턴 — 직전 touch 08-20 02:07~02:34, due가 오늘 스윕 통과 직후 도래). 여유 12h는 분 단위 드리프트를 흡수하면서 전날 스윕(경과 ≈ 주기 − 1일)은 여전히 건너뛰는 중간값이라 실질 주기가 줄지 않는다(PR #536이 범위 밖으로 남긴 수정). 대시보드 "미처리"(정확 주기 기준 SQL)는 손대지 않는다 — 여유로 잡힌 게시물은 오늘 touch돼 "오늘 처리"로 들어가므로 **미처리 > 0은 이제 수집 실패·미커버 종료만 뜻한다**. 원인 분리를 위해 브랜드 대시보드에 '미처리 브랜드별' 테이블 추가: `last_swept_on` 오늘 여부로 **브랜드 스윕 예외(미완료)** vs **스윕 성공인데 열거 미커버(③커서 미전진·④안전 상한 — 깊이 touch 생략)** 를 가른다(후자는 DB 흔적이 없어 Loki 로그로 확정). 같은 파일의 패널 id 122 중복(병행 PR 충돌)도 정리 | [정책](monitoring/src/main/java/com/celfit/monitoring/service/BrandCrawlPolicy.java) · [테스트](monitoring/src/test/java/com/celfit/monitoring/service/BrandCrawlPolicyTest.java) · [대시보드](deploy/grafana/provisioning/dashboards/json-brand/hypenow-brand.json) |
| 2026-08-23 | **'요청 추적' 대시보드 신설 — requestId 소비 UI** (MDC 요청 ID(PR #534)의 소비 측 후속) — 텍스트박스 변수 + was·monitoring 통합 로그 패널(시간 오름차순, `\|= \`[$request_id]\`` 대괄호 매칭) + 최근 에러 요청 테이블(ERROR 라인에서 requestId 정규식 추출·요청별 집계, 셀 클릭 → 변수 주입 재진입). requestId는 **라벨화 금지**(요청당 유니크 값 = 카디널리티 폭발) — 전 쿼리가 라인 본문 매칭. 추출 정규식은 correlation 슬롯 앵커(`\] \[(?P<request_id>[A-Za-z0-9_-]{1,64})\] \S+\s+: `)로 모듈명·스레드명 대괄호 오인 매칭 배제 — 임시 Loki에 실포맷 라인을 넣어 3케이스(두 모듈 추출·요청 밖 제외·스레드명 비매칭) 실검증. 로컬 하니스는 Loki 스텁이라 이 대시보드 전체가 무데이터인 게 정상(기존 관례) | [스펙](docs/superpowers/specs/archive/2026-08-23-request-trace-dashboard-design.md) · [JSON](deploy/grafana/provisioning/dashboards/json/hypenow-request-trace.json) |
| 2026-08-23 | **monitoring·was에 MDC 요청 ID 도입 — 로그를 요청 단위로 묶는다**(대시보드 진단 설계 후속, APM 없는 개별 요청 추적) — 등록처럼 다단계(수신→프로필 콜→저장→백필 제출) 요청이 느릴 때 Loki에서 그 요청의 줄들만 묶어 구간 간격을 읽기 위함. 각 모듈 `RequestIdFilter`(OncePerRequestFilter, HIGHEST_PRECEDENCE)가 요청당 8자 ID를 MDC `requestId`에 싣고 응답 후 정리, 유효한 `X-Request-Id` 헤더(영숫자·`-`·`_` 1~64자 — 로그 라인 개행 주입 방지)는 이어받는다. was→monitoring 내부 호출은 `RequestIdPropagatingInterceptor`(RestClient 2종 공통)가 MDC를 헤더로 전파해 두 모듈 로그가 같은 ID로 묶이고, 등록 실행기 풀 2종은 `MdcTaskDecorator`로 제출 시점 MDC를 워커에 복원(afterCommit 비동기 경로도 연결). **출력은 `logging.pattern.correlation`** — Boot 기본 콘솔 패턴의 correlation 슬롯(`[스레드명]` 뒤·로거명 앞)이라 타임스탬프·레벨 토큰 위치가 안 변해 운영 Alloy의 level 파싱·multiline 정규식([config.alloy](deploy/alloy/config.alloy))과 무수정 호환(로컬 bootRun 실측). `pattern.console` 직접 덮어쓰기는 금지 — 깨지면 `{level="ERROR"}` 대시보드·알람이 침묵. 그라파나 '등록 초기 응답' 패널의 라인 계약(`username=… durationMs=…`)은 메시지 본문이라 무영향. 모듈 간 Java 공유 금지(계약 모듈은 분석 전용)라 필터는 모듈별 별도 구현 | 필터: [monitoring](monitoring/src/main/java/com/celfit/monitoring/web/RequestIdFilter.java) · [was](was/src/main/java/com/celfit/was/logging/RequestIdFilter.java) · [전파 인터셉터](was/src/main/java/com/celfit/was/monitoring/RequestIdPropagatingInterceptor.java) |
| 2026-08-23 | **monitoring 외부 API 콜 타이머 계측 — `external_call_seconds` 단일 지표 + 태그(api·operation·outcome)** — 브랜드 등록·수집 경로의 "계층별 p95 뺄셈"(was p95 − monitoring 수신 p95 − 외부 콜 p95 = 내부 처리분) 마지막 빈 칸 해소. 기능별 지표 남발 대신 **지표 하나에 태그**(08-22 확정 방식): api=`hiker`\|`gemini`, operation은 경로 매핑 유한 집합(profile·tagged_feed·comments·hashtag_recent 등 9종+other / generateContent), outcome=`ok`\|`4xx`\|`5xx`\|`error`. 계측은 전송 seam(`HikerHttp`·`GeminiHttp`)의 **관찰 전용 데코레이터**(TimedHikerHttp·TimedGeminiHttp)로 — 재시도·타임아웃 제어 흐름 불변(과금 길목), 지표 기록 실패는 삼킨다. 체인 위치는 **최내곽**(전송 바로 바깥) — Recording·Counting의 DB 쓰기 시간이 외부 구간에 안 섞인다. 기록 단위는 재시도·백오프 포함 **논리 콜 1건**(재시도로 살아난 콜은 ok로 접힘). Hiker는 4xx도 과금(08-14 실측)이라 4xx를 별도 축으로 두고 `HikerFetchException`에 statusCode를 동봉해 4xx/5xx를 가른다(HTTP 교환 없는 IO·타임아웃·키 미설정은 error). Gemini는 상태코드 타입 신호가 쿼터 소진(429)뿐이라 4xx\|error만. percentiles-histogram을 켜 p95 산출, [브랜드] 운영 건강 런타임 row에 외부 콜 p95·실패율 패널 신설(인프라 대시보드와 중복 없음) | [TimedHikerHttp](monitoring/src/main/java/com/celfit/monitoring/hiker/TimedHikerHttp.java) · [TimedGeminiHttp](monitoring/src/main/java/com/celfit/monitoring/llm/TimedGeminiHttp.java) · [대시보드](deploy/grafana/provisioning/dashboards/json-brand/hypenow-brand.json) |
| 2026-08-22 | **monitoring 모듈 JVM 지표 수집 개통 — 모듈별 대시보드 대신 인프라 대시보드 확장** — 브랜드·경쟁사 스윕 실행 주체인 monitoring(mem_limit 768m·힙 512m, 08-12 OOM 전력)의 JVM 지표가 전혀 안 모이던 갭 해소. 계측(actuator+micrometer, was 패턴)·수집(prometheus 잡 `monitoring:9083`)·화면(인프라 대시보드 "컨테이너·JVM" row에 힙 사용률(모듈별)·old gen after-GC 신설 + 기존 3패널 job legend 확장). 관리 포트는 was(application-prod.yml 9081)와 달리 **compose env `MANAGEMENT_SERVER_PORT: 9083`** — monitoring은 prod 프로필 없이 env 주입이 관용구. 홈 "was JVM" 타일은 job 필터가 없어 두 모듈이 합산 오염되므로 `job="was"` 고정. 알람 룰·crawler/analytics 계측은 실측 후 후속 | [스펙](docs/superpowers/specs/2026-08-22-monitoring-jvm-metrics-design.md) |
| 2026-08-20 | **백업 IO 포화 완화 — 크론 15:00 UTC 이동 + rclone `--bwlimit 10M`** — 08-20 04:41 KST `hypenow-api-unreachable` 근본 완화. 매일 19:10 UTC 백업(crawler 10.4GiB + monitoring 0.9GiB 덤프 → B2 11.4GiB 업로드)이 2 vCPU 서버 iowait를 60~79%로 포화시켜 외부 health 프로브가 2~3분 실패(sar 실측 08-17·18에도 동일 창 반복). 조치 2건: ① 백업 크론 19:10→**15:00 UTC**(KST 00:00) — 구 슬롯은 야간 배치 열차(16~19시 크롤→19:25 스냅샷 refresh→19:30 미러→20:00 분석→02:40까지 배치 수집) 한복판이라 부하가 중첩됐다. 후보였던 "19:25 refresh 이동"은 기각 — refresh는 19:30 미러 "직전" 결합(캐시를 미러가 읽음)이라 옮기면 분석 크론 전체가 연쇄 조정되는 반면, 백업은 파이프라인 결합이 없는 유일한 잡. 15:00은 배치 전무 구간(02:40~16:00 UTC)의 끝자락 + KST 자정. ② rclone `--bwlimit 10M`(상수 `B2_BWLIMIT`) — 업로드가 실효 ~17MiB/s(08-19 실측 11.4GiB/11.4분)로 디스크를 읽는 게 포화 주범. 애초 후보였던 20~30M은 실효 속도보다 높아 무효과라 10M로 채번(업로드 ~20분, 16:00 크롤 전 완료). **ionice/nice는 기각** — 서버 sda IO 스케줄러가 `none`(실측)이라 IO 우선순위가 무시된다. 검증은 다음 백업 창(15:00~16:00 UTC)의 sar iowait로 | [backup.sh](deploy/scripts/backup.sh) · [setup-server.sh](deploy/setup-server.sh) · 크론 이동은 서버 crontab 직접 반영(스크립트는 CD 자동 동기화) |
| 2026-08-20 | **성과 대시보드 집계는 실수집 범위(covered_until 이후)만** — 사용자 결정("2,000건만 수집하고 성과 대시보드에도 2,000건만"). capped 브랜드의 컷 밖 tagged 기수집분(marynmay_global의 ~8천 행)을 성과 요약·비교 버킷 집계에서 제외한다. 구현은 `BrandPostAssembler.assembleBrandPosts`에 `capToCoverage` 파라미터 신설(행 단계 필터 — 컷 밖 수천 행의 메타·스냅샷·댓글 배치 비용도 함께 절감, scope처럼 편의 오버로드 없이 호출부가 매번 명시): 성과 대시보드만 true, **게시물 목록(컷 밖 기수집분 계속 서빙, v1 §3-3)·캠페인 존재/중복 판정("있는데 없다고 답하면 안 됨")은 false**. 기준은 coveredUntil의 KST 달력일·경계일 포함(covered 버킷 판정과 동일 규칙 — covered=true 버킷의 게시물이 집계에서 빠지는 불일치 없음), **direct 등록 행은 면제**(§7-3 — 컷 밖이어도 계속 실수집되는 살아있는 추적 대상이라, 걸러내면 사용자가 직접 등록한 게시물이 성과에서 사라진다). 결과: covered=false(빗금) 버킷은 direct·경계 버킷 커버측 분을 제외하면 빈 버킷으로 일관된다. | was `BrandPostAssembler.assembleBrandPosts`·`PerformanceContentAssembler.loadBrandPool` · [track](docs/tracks/MON-BT-브랜드-태그-모니터링.md) |
| 2026-08-20 | **성과 대시보드 covered는 실수집 깊이(covered_until)까지 반영한 4중 AND** — 종전 3중 AND(백필 완주 + 확장 중 아님 + 버킷이 collection_months 창 안)에 "버킷 먼 쪽 경계(from)가 `brand_account.covered_until`(수집 개수 상한 컷 시 monitoring이 영속화하는 실수집 깊이 — 수집 상한 v2 §7-1, `V20260819125244`) 이상"을 추가. 상한 컷 브랜드의 깊은 버킷이 "수집 완료·0건"으로 오표시되는 문제(상한 스펙의 알려진 여파 ①)의 해소다. 최초 구현(최고령 tagged `taken_at` 프록시)은 코드리뷰에서 기각 — 자연 완주 희소 브랜드를 대거 오표시(전 버킷 false 전락)하고 정작 재백필 컷 브랜드에선 무력(행 미삭제라 역대 최심 min)이라, monitoring의 확정값을 읽는 방식으로 재설계(추가 쿼리 0 — `findAccount`가 읽는 행에 컬럼 2개). covered=false의 의미가 "수집 창 밖" 하나에서 "창 밖 ∨ 컷으로 미도달" 둘로 늘었고 covered=false ∧ contentCount>0(부분 커버 버킷)이 정상 조합이 됐다 — FE 구분 표기는 계정 API의 `collectionCapped`·`coveredUntil`(계약 §10-1) 조합으로. **컬럼 의존이라 수집 상한 v2 브랜치 머지 후에만 머지·배포 가능(monitoring → was 순서).** | was `PerformanceComparisonAssembler.compare`·`PerformanceComparisonResponse.Bucket` javadoc(FE 계약) · [track](docs/tracks/MON-BT-브랜드-태그-모니터링.md) |
| 2026-08-20 | **수집 상한 v2 — 커버리지 영속화·확장 스킵·direct 면제·컷 클램프** — v1(아래 08-19 행)의 남은 구멍 4개를 닫았다. ① **커버리지 영속화**: `brand_account`에 `collection_capped`·`covered_until` 신설(`V20260819125244`), 백필 종료부가 "컷으로 끊겼나 / 어디까지 실제로 훑었나"를 남긴다 — 그전엔 상한 컷 사실이 INFO 로그에만 있어 "이 브랜드 데이터가 창 전체인가"를 아무도 답할 수 없었다. was `/brands` 응답에 `collectionCapped`·`coveredUntil` 2필드 노출. ② **확장 스킵**: **재백필의 컷이 기존 창 안에 떨어지는** 브랜드의 창 상향은 백필을 제출하지 않고 창·커버리지 마킹만(`raiseWindowCapped`) — 확장 구간에 한 건도 도달하지 못할 것이 확정이라 콜 전량이 낭비였다(구 경로의 `last_swept_on` 리셋도 타지 않음). 판정·폴백 입력은 **limit번째 최신 태그 행의 `taken_at`**(`nthNewestTagTakenAt`)이다 — 최종 리뷰에서 생애 누적 행 수 판정을 교체했다: 누적은 창 밖 과거분까지 세서 재백필이 컷되지 않을 브랜드(누적 2,400 / 창 안 900)를 capped로 오표기하고, 그 마킹이 ④ 클램프를 걸어 도달 가능했던 구간을 영구 동결시켰다(폴백 `now − 기존 창`도 "커버 하한"을 "도달 상한"으로 쓰는 타입 오류). ③ **direct 등록 게시물 상한 면제**: 사용자가 직접 등록한 게시물은 브랜드 창은 따르되 2,000 밖 — `touchCrawledDepth`·`trackedPosts`에 `direct_registered_at IS NULL` 가드, `directDuePosts`는 겹침 행까지 모수에 포함. v1의 "알려진 여파 ②(컷 밖 direct 게시물이 조용히 동결)"가 이걸로 해소됐다. ④ **capped 컷 클램프**: capped 브랜드의 일일 열거 컷을 `covered_until`로 클램프 — 컷 밖 태그 행의 due가 티어 주기마다 재장전돼 도달 불가능한 깊이를 여는 주기당 ~70콜 무익 딥 스윕이 영구 반복됐다. 동결을 touch 반복이 아닌 **범위 제외**로 구현. 알려진 예외(수용): 0~14일 겹침 행은 매일 티어의 due가 `last_crawled_at`을 보지 않아 1·2단계가 스윕당 1콜 중복(게시물당 최대 14일). **배포 순서는 monitoring(마이그레이션) → was, 롤백은 역순** — was가 `collection_capped`·`covered_until`을 무조건 SELECT하므로 순서가 뒤집히면 브랜드 계정 조회가 500이다. FE에는 계약 v2.14(§10)로 신규 2필드를 공유해야 한다. | monitoring `BrandCollectService`·`BrandRepository`(`updateCoverage`/`coverage`/`raiseWindowCapped`)·`TaggedPostRepository`·`BrandRegistrationService.expandIfRequested` · `db/migration/V20260819125244__brand_account_coverage.sql` · was `BrandReadRepository` · [스펙 §7](docs/superpowers/specs/2026-08-19-brand-collection-post-limit-design.md) · [계약 §10](docs/contracts/monitoring-was-contract.md) · [track](docs/tracks/MON-BT-브랜드-태그-모니터링.md) |
| 2026-08-19 | **브랜드 태그 수집 개수 상한(collection-post-limit 2000) 신설** — marynmay_global 등록 백필이 12개월 창 전량을 걷다 1만 건·$10.6에 도달(안전 밸브 ERROR). 수집량과 was 서빙량(POST_LIMIT 2,000)의 괴리가 곧 낭비 비용이라, 열거를 최신 2,000건에서 "의도된 자연 종료"로 끊는 전역 설정을 신설했다. 종료 시 목표 컷 전체를 touch해 컷 밖 due 재열거 루프를 차단하고 지표를 동결(계속 서빙)한다. 안전 밸브(`max-posts-per-sweep` 10,000·ERROR·커버 미처리)는 코드상 그대로 두지만 **기본 설정(2000 < 10000)에선 안쪽 컷이 항상 먼저 걸려 도달 불가(vestigial)**다 — 밸브가 다시 의미를 갖는 건 상한을 10,000 초과로 올리거나 0(무제한)으로 끈 구성뿐이고, 기본 구성에서 컷을 알리는 유일한 신호는 INFO 로그다(스펙 §4). 기간(`collection_months`)이 아닌 개수인 이유: 유입량이 브랜드마다 달라 기간으로는 개수를 표현할 수 없다(marynmay는 3개월도 ~3,400건). | monitoring `BrandCollectService`·`application.yml` `monitoring.brand.collection-post-limit` · [스펙](docs/superpowers/specs/2026-08-19-brand-collection-post-limit-design.md) |
| 2026-08-19 | **그라파나 도메인 폴더 분리 패턴 확립 — 브랜드 모니터링을 별도 폴더 + 대시보드 3장으로** — 08-18 6탭 개편(바로 아래 행)의 "브랜드 모니터링" 1탭이 스윕 건강·수집 현황·광고 표기를 한 장에 눌러 담게 되자, 탭을 늘리는 대신 **폴더를 축으로 쪼개는 패턴**을 택했다: `[브랜드] 운영 건강`(`hypenow-brand` — 기존 탭 이관) + `[브랜드] 수집 현황`(`hypenow-brand-collection` — 태그 게시물·해시태그 감지·enrich 잔여·백필 미완) + `[브랜드] 광고 표기`(`hypenow-brand-ad` — 판정 분포·규칙/LLM 경로·30일 추이·미표기 목록). 결과 체제는 **HypeNow 5탭 + 브랜드 모니터링 폴더 3장(총 8장)**. 구현 결정 3가지: ①제2 파일 프로바이더의 경로는 `json/`의 하위가 아니라 **형제 디렉토리 `json-brand/`** — 그라파나 file 프로바이더는 하위 디렉토리를 재귀 탐색하므로 `json/brand/`로 두면 두 프로바이더가 같은 파일을 이중 로드한다. ②**디렉토리명은 영문·폴더명은 yaml에서 한글**(`folder: 브랜드 모니터링`) — macOS↔linux 한글 NFC/NFD 정규화 차이로 경로가 어긋나는 것을 회피. ③GRANT는 최종 JSON rawSql 기계 추출로 재검산해 `brand_tagged_post`·`brand_hashtag_post`·`brand_post_meta` 3테이블 + `brand_account.collection_months`를 §14-2-2 ②에 증보(런북은 **서버 미적용** — main 배포 전 실행 필요, 그라파나는 staging에 없음). **부수 발견(운영 함정)**: CD의 프로비저닝 동기화는 `scp -r`이라 **추가·덮어쓰기만 하고 삭제하지 않는다** — 레포에서 대시보드 JSON을 지우거나 옮기면 서버엔 옛 파일이 남고, 이관 건은 같은 `uid`를 두 프로바이더가 동시에 프로비저닝해 폴더가 오락가락한다. 잔존 파일 수동 정리 규칙을 README §14 머리말에 명문화하고, 잔존 파일 제거(이관분 `json/hypenow-brand.json` + 08-18 개편이 지운 구 3장)는 §14-2-2 ④에 배포 전 1회 스텝으로 넣었다. 하니스 시드도 확장(`brand_post_meta` 8,000·판정 60%, enrich 분포, 빨강 재현 4종) | [설계](docs/superpowers/specs/archive/2026-08-18-grafana-brand-folder-design.md) · [계획](docs/superpowers/plans/archive/2026-08-18-grafana-brand-folder.md) · PR #501 · `deploy/grafana/provisioning/dashboards/dashboards.yaml`(프로바이더 2개)·`json-brand/` · [deploy/README.md §14·§14-2-2](deploy/README.md) |
| 2026-08-18 | **광고 판정 백필 상한 기본값 무제한 원복** — #499 방어선에서 재도입한 `backfill-max-per-run`(기본 1000)을 기본 **0(무제한)**으로 되돌림(사용자 결정 — #490의 "상한 제거" 결정이 정본). 429의 원인은 상한 유무가 아니라 무료 AI Studio 키 쿼터였고, Vertex 전환 + 연속 실패 서킷브레이커가 있어 무제한이 안전하다. 상한은 운영에서 일시적으로 누를 때만 env로 양수 지정. 서킷브레이커(`llm-failure-abort-threshold`)는 유지. | `monitoring/src/main/resources/application.yml` · `AdDisclosureConfig` · [track](docs/tracks/MON-BT-브랜드-태그-모니터링.md) |
| 2026-08-18 | **해시태그 스윕 전용 executor 분리(기본 1스레드)** — LLM 판정이 백필 보강 2-워커를 점유해 연속 등록 지연을 증폭한 08-18 운영 사고 후속. 신규 사용자가 브랜드 8개를 11분 사이 연속 등록 → 해시태그 스윕의 LLM 브랜드 관련성 판정(`BrandMentionJudge`, Gemini)이 쿼터 429로 콜당 ~6초 재시도 백오프를 태웠는데, 스윕이 등록 백필의 보강(enrich)과 같은 `brandEnrichExecutor`(2스레드)에서 돌아 두 워커를 포화시켜(429 warn 788줄 전부 brand-enrich-1/2 스레드 실측) 뒤 계정들의 백필이 10~13분 밀렸다. 429가 아니어도 LLM 판정은 콜당 수 초짜리 느린 외부 호출이라, 빠른 Hiker 콜 다수인 보강과 같은 큐에 섞이면 구조적으로 재발한다. `BrandBackfillConfig`에 `brandHashtagSweepExecutor` 신설(`monitoring.brand.hashtag-sweep-concurrency` 기본 1 — 스윕은 ready 폴링과 무관한 꼬리 작업이고 야간 크론이 백스톱하므로 직렬로 충분), `BrandRegistrationService.triggerHashtagSweep`이 이 큐로 제출하도록 변경. 전역 Hiker 동시 콜 예산 13→14(+1, 태그당 최대 4페이지 열거뿐이라 최소 증가) | monitoring `BrandBackfillConfig.brandHashtagSweepExecutor`·`BrandRegistrationService.triggerHashtagSweep` |
| 2026-08-18 | **빈 응답 지속 계정 30일 수명 정책 — 자체·Hiker 폴백 양쪽 모두 빈 응답인 상태가 마지막 성공 프로필 수집(`last_profiled_at`)으로부터 30일을 넘기면 404(계정 소멸)와 동일하게 소프트 딜리트(DELETED)** — 폴백 확장으로도 회수되지 않는 계정(비활성화·탈퇴 유예 추정, 08-18 실측 17개·최장 26일)의 무한 재시도를 끊는다. 별도 상태 신설·저빈도 재확인 프로브는 만들지 않는다(사용자 확정). 판정은 방문 시점에 **이번 방문에서 양쪽 빈 응답이 확인된 경우에만**(컴포지트가 `confirmedEmpty`로 보고 — Hiker 미확인 상태로는 삭제하지 않으며, **Hiker가 응답으로 빈 응답을 확인한 경우만** 해당: 요청 실패(5xx·타임아웃)는 `confirmedEmpty`가 아니라 카운터 유지 후 다음 방문 폴백 재시도 — 인프라 오류를 소멸 확인으로 오판해 소프트 삭제하는 것 방지, 리뷰 수리 08-18) + `last_profiled_at` 30일 경과 시 `NotFoundException`으로 기존 404 경로 합류. `last_profiled_at`이 null이면 기준점이 없어 삭제하지 않고 재시도 유지. 신규 컬럼·마이그레이션 없음(V1부터 있던 북키핑 재사용). 기간 내 복귀(재활성화)는 SELF/폴백 성공이 자연 회복 | `CollectJob.EMPTY_PROFILE_DELETE_AFTER=30d` · `ApifyResult.confirmedEmpty`/`Execution.confirmedEmpty` 채널 신설 |
| 2026-08-18 | **프로필 Hiker 폴백 트리거를 "HTTP 400"에서 "400 또는 연속 빈 응답"으로 확장** — IG가 일부 계정에 익명 web_profile_info에서 200 + 빈 응답(user 없음)을 주는 케이스가 있어(08-18 실측: 서빙 풀 11계정이 4~11일째 매일 방문 실패 반복, Hiker로는 수집 가능), 컴포지트(`SelfWithHikerFallbackProfileFetcher`)가 빈 응답도 폴백 대상으로 삼는다. 단 빈 응답은 400과 달리 진짜 소멸 계정(비활성화·탈퇴 유예)과 구분이 안 되므로 가드를 둔다: **연속 2회(인메모리 카운터) 도달 시에만 폴백**, 폴백 성공 시 카운터 유지(다음 빈 응답부터 즉시 폴백 — Hiker 수집 가능 확인됨), **폴백도 빈 응답이면 카운터 리셋으로 기존 재시도 경로 복귀**(소멸 계정 유료 콜을 임계값 주기당 1회로 제한), 폴백 404는 기존대로 notFound(소프트 삭제). **빈 응답 트랙은 COLLECT 잡 전용**(리뷰 수리 08-18): qualify는 대상 재선정(followers null)에 종결 장치가 없어 빈 응답 유료 폴백을 열면 숨겨진 DISCOVERED 계정이 무한 재과금된다(qualify에선 빈 응답이 기존대로 무료 스킵, 400 폴백은 두 잡 공통). 07-15 "유료 폴백으로 방문을 성공 처리하지 않는다(헛돈 방지)"와의 구분: 그 결정은 프로필 실패를 유료 *피드* 폴백으로 성공 위장하는 것의 금지였고, 이번 확장은 프로필 자체를 실데이터로 확보하는 회수라 방문 성공이 정당하다(400 폴백과 같은 성질) | [07-26 설계](docs/superpowers/specs/archive/2026-07-26-profile-400-hiker-fallback-design.md)의 트리거 확장 · `SelfProfileFetcher.collect(usernames, badRequestOut, emptyOut)` · `EMPTY_STREAK_FALLBACK_THRESHOLD=2` |
| 2026-08-18 | **공통 모듈 `common-llm` 신설 + monitoring Gemini 호출 Vertex 전환 + 백필 방어선** — monitoring의 `GeminiHttpTransport`(AI Studio 직결, `GEMINI_API_KEY`)가 무료 키 쿼터를 스테이징·운영이 공유하는 구조라, #490(백필 기동 즉시·상한 제거) 이후 스테이징에서 429 폭주(15분간 분당 83~146건 실측)가 났다. 근본 해소로 Vertex(`hypenow-llm-prod`, ADC — 프로젝트 전용 쿼터 DSQ라 공유 문제 자체가 없음) 전환을 결정. **경계 규칙 개정**(ARCHITECTURE.md §4-4) — 모듈 간 import 금지의 예외가 계약 모듈 `contract-analysis` 하나였는데, 이제 `common-llm`(LLM 전송 계층 전용: Vertex HTTP 호출·SA 토큰 발급·재시도/백오프·에러 매핑, Spring 의존 금지·프롬프트/판정 로직/도메인 스키마 반입 금지)이 두 번째 예외로 추가됐다. analytics의 검증된 `VertexHttpApi`·`VertexTokenProvider`를 이식(analytics 원본은 그대로 유지, **analytics의 common-llm 편입은 별도 후속 트랙** — 지금은 analytics·monitoring이 각자 독립된 Vertex 클라이언트를 갖는 과도기). monitoring 쪽은 `GeminiHttp` seam 뒤에 `VertexGeminiHttp`(AI Studio 경로→Vertex 경로 변환)를 추가하고 `LlmTransportConfig.useVertex`(SA 키 + `monitoring.llm.vertex-project` 둘 다 있어야 Vertex, analytics `LlmConfig.useVertex` 패턴 준용이되 monitoring엔 명시적 provider 토글이 없어 "프로젝트 설정 여부"로 대신 판정)로 그레이스풀 폴백한다. `BrandMentionJudge`·`AdDisclosureExtractorGemini`의 2번째 생성자 인자가 "apiKey 문자열"에서 "enabled 플래그"로 바뀜(Vertex 사용 시 GEMINI_API_KEY가 비어 있어도 활성이어야 하는 버그를 막기 위함). **백필 방어선**(`AdDisclosureJudgeService`) 2종 추가 — 연속 실패 서킷브레이커(`llm-failure-abort-threshold` 기본 10, 도달 시 진행 중 배치 나머지는 LLM 호출 자체를 스킵)와 1회 실행 상한 재도입(`backfill-max-per-run` 기본 1000, 0 이하면 무제한 — 08-18 상한 제거 개정 이전 계약으로 되돌아감이 아니라 안전 밸브 재장착). 기존 `GeminiHttpTransport`(AI Studio)는 SA 미설정 환경의 폴백 경로로 존치, 에러 본문 로그 절단 한도를 300자→1000자로 완화(쿼터 메트릭 이름이 진단에서 잘리지 않게). | [track](docs/tracks/MON-BT-브랜드-태그-모니터링.md) · `common-llm/src/main/java/com/celfit/common/llm/` · `monitoring/src/main/java/com/celfit/monitoring/llm/VertexGeminiHttp.java`·`config/LlmTransportConfig.java` · `monitoring/src/main/java/com/celfit/monitoring/ad/AdDisclosureJudgeService.java` |
| 2026-08-18 | **그라파나 대시보드 개편 — 홈 신호등 + 도메인 6탭 체제** — 분할 축을 데이터소스→여는 목적으로 전환: 홈(신호등 13타일, 무값 fail-loud·전 타일 클릭 착지) + 브랜드 모니터링(스윕 신선도·오늘 성공 X/Y·소요·브랜드별 처리 간격 — last_swept_at 유도 근사, 런 기록 테이블 없음) + 경쟁사 모니터링(sweep_run 축+등록·알림) + 탐색 + Hiker(비용·외부 의존 — brand/target_call_count 최초 개통) + 인프라(호스트·컨테이너·JVM·전 서비스 로그). 구 서비스현황·에러·API성능 3장 해체(사용자 리뷰 결정 — p95/p99·상위 SQL·WARN 패널 폐기). monitoring DB 데이터소스 신설(`hypenow-monitoring-pg`), node-exporter·cAdvisor 컨테이너 추가(oom_score_adj 500). 사용량은 stat+목록(밀도 미달 시계열 폐기), called_on 비교는 KST 정본. 로컬 목 하니스(`deploy/grafana/dev` — 스키마=레포 Flyway·시드=08-18 실측 밀도) 동봉. **이 PR은 틀까지** — 대시보드별 세부·임계 보정·GRANT 런북은 후속 PR(런북 전 staging 승격 시 monitoring 패널 No data 주의) | [설계](docs/superpowers/specs/archive/2026-08-18-grafana-dashboard-redesign-design.md) · [계획](docs/superpowers/plans/archive/2026-08-18-grafana-dashboard-redesign.md) |
| 2026-08-18 | **시딩 계정 관리 API 철회 — seededAuthor를 캠페인 데이터 도출로 교체(direct 통합 후 구조로 재조정)** — 08-18 오전에 신설한 시딩(협업) 계정 관리 표면(monitoring `BrandSeededAccountRepository`·`.../seeded-accounts` API 5종, was 프록시 5종 + `MonitoringCommandClient` 시딩 CRUD 5종)이 잘못된 신설이었다는 사용자 확정 — `seededAuthor` 판정은 브랜드가 신규 등록해야 하는 별도 목록이 아니라 **이미 존재하는 캠페인 관리 데이터**에서 나와야 한다. `seededAuthor` 산출 기준을 was 조회 시점 계산으로 교체(user 스코프 — 캠페인은 브랜드가 아니라 유저 단위), `BrandPostAssembler.resolveSeededUsernames`가 3원 합집합을 낸다: ①`app.monitoring_items`에서 `mode='account' AND campaign_id IS NOT NULL AND canceled_at IS NULL`인 행의 `input_value`(신규 `MonitoringItemRepository.findCampaignLinkedAccountHandles`) ②**같은 날 머지된 브랜드 direct 파이프라인 통합(바로 위 행)이 캠페인 연결 정본을 `app.brand_post_campaigns`로 옮겼으므로**, 신규 `BrandPostCampaignRepository.findShortCodesByUser`로 유저 스코프 캠페인 연결 shortcode(tagged·direct 공통)를 얻는다 ③이관 전(migrated_at IS NULL) 레거시 direct 등록만 과도기로 `BrandDirectPostRepository.findCampaignLinkedShortCodes`(app 스키마 내부 monitoring_items 조인)를 그대로 쓴다 — 이관(M2)이 진행되면 ②로 자연 흡수돼 비어간다. ②·③ 모두 shortcode → 게시자 username은 monitoring DB `brand_post_meta.username`에서 별도 조회(기존 `BrandReadRepository.findPostMeta` 재사용) 후 was 코드에서 조합(app·monitoring 물리적으로 다른 DB — 시스템 경계 원칙). `BrandPostAssembler.assembleBrandPosts`(direct 통합이 `assembleTagged`를 이 이름으로 재구성)에 `userId` 매개변수 추가 — `PerformanceContentAssembler.loadBrandPool`·`V2CampaignContentService.taggedPostUrls` 호출부도 함께 갱신. `BrandPostResponse.seededAuthor` 필드·응답 계약·노출 토글(`monitoring.brand.ad-disclosure.expose`)·adDisclosure 등 판정 파이프라인은 전부 불변. `brand_seeded_account` 테이블·마이그레이션은 이미 develop 머지·스테이징 적용 상태라 expand-contract상 이번엔 DROP하지 않고 미사용 상태로 남긴다(추후 contract 단계) | was `BrandPostAssembler`·`MonitoringItemRepository`·`BrandDirectPostRepository`·`BrandPostCampaignRepository`·`PerformanceContentAssembler`·`V2CampaignContentService` · monitoring `BrandController` · [계약 §9](docs/contracts/monitoring-was-contract.md) · [트랙 MON-BT](docs/tracks/MON-BT-브랜드-태그-모니터링.md) |
| 2026-08-18 | **브랜드 direct 게시물 파이프라인 통합 — 레거시 추적 파이프라인에서 브랜드 수집 파이프라인으로 합류(E1 monitoring·E2 was 구현 완료, 이관 미실행)** — 브랜드 직접 등록(direct) 게시물을 레거시 `app.monitoring_items`→monitoring `target`/`post_snapshot` 위임에서 떼어내 브랜드 수집 파이프라인(`brand_tagged_post`/`brand_post_*`)으로 합류시켰다. `brand_tagged_post`에 `source` 단일 enum을 두는 대신 **두 시각 컬럼**(`tag_detected_at`·`direct_registered_at`)을 추가하고 `source`는 `direct_registered_at IS NOT NULL` 파생값으로 둔다 — 한 게시물이 태그 발견분이면서 동시에 직접 등록분일 수 있고 PK가 `(brand_id, short_code)` 하나뿐이라, 단일 값으로 접으면 취소 시 태그 발견 사실을 잃기 때문이다. was는 레거시 `monitoring_registrations` 위임·`resolveLazyMappingBrand`·seq 인덱스 매칭을 걷어내고 전용 등록 테이블(`app.brand_post_registrations`+`entries`)과 전용 실행기로 재작성했고, `BrandPostAssembler`가 `brandPost()` 한 벌로 조립을 통합(`directPost`/`mergeByShortcode`/`promoteSponsorship` 삭제) — 겹침 게시물이 direct 셰이프로 영구 고정되던 비대칭과 창 밖 tagged 데드엔드 우회의 "의도된 대가"(둘 다 08-17 행)가 소멸했다. 캠페인 연결은 `app.brand_post_campaigns` N:M으로 옮겨 tagged에도 열었다(부착 API는 별도 트랙). **배포 순서는 monitoring→was 고정**(역순이면 was가 없는 컬럼을 조회해 브랜드 목록 전면 500, 08-13과 같은 의존). 이관은 링크 복제가 아니라 **재수집 방식**(레거시 이력 복사 후 재수집으로 채움) — 구현은 완료됐으나 **운영 실행은 미착수**, 배포 직후엔 과도기 폴백이 레거시 셰이프를 그대로 얹어 현행과 동일하게 보인다. **FE 통지 4건**: `trackingDays` 무시(검증 1~90은 유지) / `BrandPostResponse.trackingStatus` 항상 `"tracking"` / `Entry.monitoringItemId` 항상 null / 성과 대시보드 direct 콘텐츠 `item.id`가 숫자에서 `bt_<shortcode>`로 변경. **잔여**: M1(이관 대상 규모 확인)→승인→M2(이관 잡 실행)→M3(콜 증분 2주 실측)는 배포 후 별도 세션, C(다음 릴리스 — 레거시 폴백 조립 제거·`monitoring_item_id`/`migrated_at` DROP·`tag_detected_at` DEFAULT 제거) | [설계](docs/superpowers/specs/2026-08-18-brand-direct-pipeline-unification-design.md) · [계획(아카이브)](docs/superpowers/plans/archive/2026-08-18-brand-direct-pipeline-unification-plan.md) · 마이그레이션 `V20260818040742__brand_tagged_post_direct_source.sql`(monitoring)·`V20260818043332__brand_post_unification.sql`(was app) · monitoring `BrandDirectCollectService`·`BrandSweepJob`·`BrandController` · was `BrandDirectRegistrationExecutor`·`V1BrandDirectPostService`·`BrandPostAssembler` · 계약 v2.12([§8-5](docs/contracts/monitoring-was-contract.md)) |
| 2026-08-18 | **브랜드 등록 계정 게이트(markServing)를 첫 페이지 게시자 보강 직후로 단축 — 댓글·광고 판정 대기 제거** — 08-13 완결 배치 서빙이 게시물 게이트(`enriched_at`)는 08-17에 "게시자 보강 완료"로 좁혔지만, 계정 게이트는 옛 배선대로 첫 페이지 `enrich()` 전체(게시자+댓글+광고 판정) 반환을 기다려 두 게이트가 어긋나 있었다. `BrandCollectService.enrich`에 onVisible 훅(nullable, markEnriched와 같은 finally 지점 — ensureAuthors 하드 실패에도 발화)을 추가하고, `BrandRegistrationService.runBackfillSafely`가 첫 페이지에만 걸어 `markServing`을 그 지점에서 연다. 야간 스윕은 onVisible 없는 2-인자 `enrich` 위임 그대로라 무변. `touchSwept`(FE 폴링 종료 조건)는 무수정 — 모든 페이지 정산 뒤 그대로 | [트랙](docs/tracks/MON-BT-브랜드-태그-모니터링.md) · monitoring `BrandCollectService.enrich(BrandRow, List, Runnable)`(신설 오버로드)·`BrandRegistrationService.runBackfillSafely`·`runEnrichSafely` |
| 2026-08-18 | **광고 표기 판정 코드리뷰 반영 — 판정 킬 스위치·username 로케일 고정·시딩 판정 오기 정정·계약 문서화·배포 순서 명시** — 캡션 기반 광고 표기 판정(바로 아래 행) 최종 코드리뷰 지적 5건 처리. ①**판정 킬 스위치** `monitoring.brand.ad-disclosure.enabled`(기본 true) 신설 — `BrandCollectService.judgeAdDisclosuresSafely` 진입점에서 비활성 시 `adJudge` 호출 자체를 스킵(no-op, debug 로그)한다. was 노출 토글(`expose`)과 **독립적인 롤백 수단**이다 — LLM 쿼터 장애로 판정이 스윕 자체를 지연시킬 때 노출은 그대로 두고 판정만 끌 수 있다. `GEMINI_API_KEY` 제거로 죽이면 해시태그 판정까지 함께 죽으므로 이 토글이 더 좁은 롤백. ②`BrandController.normalizeUsername`의 `.toLowerCase()` → `.toLowerCase(Locale.ROOT)` — 소비 측 was(`BrandPostAssembler`)의 시딩 조인 정규화와 로케일을 일치시켜, 로케일 의존 대소문자 변환(터키어 "I"→"ı" 등)으로 조인이 깨지는 것을 방지. ③**시딩 계정 캡션 판정 오기 정정**(아래 08-18 행 및 트랙 MON-BT) — "시딩 계정은 판정을 거치지 않는다"는 최초 기재가 코드·스펙과 정반대였다. 실제로는 시딩 계정도 캡션 판정을 동일하게 거치고, `seededAuthor`는 판정과 무관하게 was 조회 시점 별도 조인으로 계산되는 필드다. ④계약 문서화(`docs/contracts/monitoring-was-contract.md` §9, v2.11) — `BrandPostResponse` 신규 4필드(adDisclosure/adViolations/adEvidence/seededAuthor, null=판정중, 노출 토글 기본 off) + seeded-accounts 엔드포인트 5종(monitoring `/api/brands/{username}/seeded-accounts*` + was 프록시 `/v1/brand-monitoring/accounts/{accountId}/seeded-accounts*`) 기재. ⑤배포 순서(monitoring → was, was가 새 컬럼을 항상 SELECT하므로 역순이면 브랜드 목록 조회 500) + 잔여 작업 목록을 트랙 MON-BT에 명시 | monitoring `BrandCollectService`·`BrandController`·`application.yml` · [계약 §9](docs/contracts/monitoring-was-contract.md) · [트랙 MON-BT](docs/tracks/MON-BT-브랜드-태그-모니터링.md) |
| 2026-08-18 | **캡션 기반 광고 표기 판정 도입 — 공정위예규 제499호 기반 4-Tier, 시딩 계정 확정 판정, 노출 토글 분리** — 브랜드 태그 게시물 캡션이 광고 표기 규정(예규 제499호 Ⅴ.6)을 지켰는지 게시물 단위로 자동 판정한다. 규칙 선처리(Tier0 메타·Tier1 고신뢰 사전 매칭) → LLM은 문구 추출만(Tier2, 판단 아님) → 코드가 환각 차단·위치 판정·최종 verdict를 결정(Tier3, 전부 LLM 없이 단위 테스트로 고정)하는 구조로, LLM에 verdict 자체를 맡기지 않는다. **시딩 계정**(`brand_seeded_account`, 브랜드가 직접 등록)의 게시물도 다른 게시물과 동일하게 캡션 판정을 거친다(2026-08-18 오기 정정 — 최초 기재는 "판정을 거치지 않고 시딩 표기로 확정 노출"이라 코드·스펙과 정반대였다) — `seededAuthor`는 판정과 무관하게 was 조회 시점에 별도 조인(시딩 목록 대조)으로 계산되는 boolean 필드일 뿐이다. "시딩 계정 + `NOT_DISCLOSED`" 조합일 때 위반이 확정됐다는 배지를 보여주는 것은 FE의 조합 로직이며, 그 배지 표시에도 캡션 판정(`NOT_DISCLOSED`)이 필수 전제로 들어간다. 노출 게이트(`enriched_at`)는 "게시자 보강 완료"로 좁아졌고, 댓글·광고 판정은 게이트 밖에서 프로그레시브로 채워진다(판정 실패·지연이 게시물 노출을 막지 않음). 판정 로직은 배포 즉시 가동하지만 **was 노출은 별개 토글**(`monitoring.brand.ad-disclosure.expose`, 기본 false)로 분리 — 기존 게시물 전량 재판정 + verdict 분포 드라이런 검토를 마친 뒤에만 켠다. 골드셋 정확도 평가(NOT_DISCLOSED 오탐률)는 별도 후속 | [설계](docs/superpowers/specs/archive/2026-08-17-brand-ad-disclosure-design.md) · [계획](docs/superpowers/plans/archive/2026-08-17-brand-ad-disclosure.md) · monitoring `AdDisclosurePatterns`·`AdPositionRule`·`AdVerdictCombiner`·`AdDisclosureExtractorGemini`·`AdDisclosureJudgeService`·`BrandSeededAccountRepository` · was `BrandPostAssembler`·`V1BrandAccountsController` · [트랙 MON-BT](docs/tracks/MON-BT-브랜드-태그-모니터링.md) |
| 2026-08-18 | **해시태그 발견 목록에서 tagged 겹침 제외 — 화면 정의와 정합, brandPostId는 direct 전용으로 축소** — `GET .../hashtag-posts`(발견 목록, "태그 안 된 게시물" 화면)에 이미 tagged로 측정 중인 게시물이 뜨는 것 자체가 화면 정의와 모순이라는 사용자 결정(08-17 스테이징 테스트 중 확인). 사진 태그(캡션에 안 보임)+해시태그 동시 게시물이 실제로 이 겹침을 만든다(캡션 멘션은 수집 시점 DIRECT_TAGGED 판정으로 이미 걸러진다). 조립 시 tagged 풀에 존재하는 shortcode 행을 응답에서 제외하되, **direct 매핑이 살아 있는 행은 tagged 여부와 무관하게 유지**(승격분 dim 잔존 계약 — direct가 우선). 결과적으로 `brandPostId`가 채워지는 경로는 direct 매핑뿐이 됐다 — "tagged로 채워지며 취소 시 400" 케이스는 소멸. 취소 API(`V1BrandDirectPostService.cancel`)의 tagged 400 분기(`TAGGED_POST_NOT_CANCELABLE`)는 방어적 분기로 그대로 유지 | `feat/hashtag-hide-tagged-overlap` · was `BrandHashtagPostAssembler` · 계약 v2.10 내 정정([§8-1](docs/contracts/monitoring-was-contract.md)) |
| 2026-08-17 | **하입 스코어 댓글 가중 ×3→×1.5 + 설정화** — 발굴 랭킹 1위 고착 진단: 참여 항 `comments×3`이 댓글≈좋아요인 품앗이 패턴 계정을 과대평가(1위 계정 피드 분자의 ~78%가 댓글 몫). 운영 시뮬레이션(w∈{3,2,1.5})에서 ×1.5만 실조회 기반 계정을 1위로 올려 채택. 가중은 `analytics.hype-comment-weight`(기본 1.5, 함수 COALESCE 단일 소스, 0=댓글 배제도 유효값)로 설정화 — 키에 3을 넣으면 순위 응급 롤백(척도 완전 복원은 뷰 원복 필요). 앵커 4종 재적합 동반(2026-08-17 운영 실측), refit 하니스는 단일 체인 v2로 개편(미러 점수 대신 신 가중 재계산) | [스펙](docs/superpowers/specs/archive/2026-08-17-hype-comment-weight-design.md) |
| 2026-08-17 | **브랜드 해시태그 FE 요청 일괄 — 태그 등록 즉시 스윕 · 제외 규칙 폐기 · direct 취소 API · brandPostId · 작성자 프로필 아카이브** — ①태그 `PUT/POST .../hashtag-tags`(및 브랜드 replay 재등록) 성공 시 비동기 해시태그 스윕 1회를 즉시 트리거(enrich executor 재사용, 실패는 야간 스윕 백스톱) — "등록 당시 조회해서 당일 게시물 추가" 합의 이행. DELETE는 트리거 없음. ②제외 문자열(hashtag-exclusions) 전면 폐기(08-17 FE 협의) — was 4종·monitoring 5종 API와 **조회 시점 즉시 필터(08-12 신설분)까지 제거**: 관리 API가 사라진 뒤 읽기 필터만 남기면 이미 시드된 기본 제외(root(username))가 유저가 볼 수도 지울 수도 없는 유령 필터로 영구 동작한다(구현 중 교정 — 최초 구현은 "범위 밖"으로 남겼었다). SELF 판정은 제외 문자열 substring 매칭 → 게시자·브랜드 username **정확 일치(대소문자 무시)**로 대체(계정명을 포함할 뿐인 스태프 부계정류는 더 이상 SELF가 아님 — 기존 저장 verdict는 불변, 신규 판정부터). 자동 시드 3종(브랜드명·계정명 루트·계정명) → **계정명 1종**(brandName 파라미터는 하위호환으로 받되 미사용). `brand_hashtag_exclusion` 테이블은 expand-contract상 존치(DROP은 다음 릴리스). ③`POST /v1/brand-monitoring/posts/{postId}/cancel`(postId=BrandPostResponse.id=shortcode) 신설 — direct 매핑의 레거시 아이템을 가능하면 함께 종결(이미 자연 종결이면 생략)하고 **매핑을 hard delete: GET .../posts에서 행이 빠지고(ended로 안 남음) 같은 URL 재등록이 새 등록으로 성립**(레거시 중복 판정 `findActiveByInput`은 이미 `canceled_at IS NULL` 필터라 무수정 — 등록→취소→재등록 왕복 통합 테스트로 실증). 원격 취소 실패는 삼키지 않고 전파 — 매핑만 지우면 레거시는 추적 중인데 브랜드 화면에서만 사라지는 불일치가 생긴다. tagged 행은 400 `TAGGED_POST_NOT_CANCELABLE`, 그 외 404. ④`BrandHashtagPostResponse.brandPostId` 신설 — direct 매핑 활성 또는 tagged 존재면 shortcode, 아니면 null(취소 시 null 복귀 — FE dim 해제와 정합). tagged 존재 판정은 12개월 표시 윈도우 **무제한** 경량 존재 조회라 FE 클라이언트 조인의 사각지대(12개월 전 게시물의 승격)도 해소. ⑤`brand_hashtag_post`에 author_image 3컬럼(`V20260817142317`) + `HashtagPostAuthorImageArchiveJob`(`monitor-hashtag-author/<username>.jpg`, RELEVANT만·작성자 단위 dedupe) — was는 아카이브 우선·원본 폴백으로 `authorProfilePicUrl` 서빙(08-12 KK 트랙이 남긴 마지막 빈칸 "해시태그 게시자 프로필만 아카이브 없음" 해소). **확인 사항**: 경쟁사(competitor) 계정도 해시태그 감지는 동일하게 돌지만 direct 등록이 403이라 발견 카드 "성과 측정 시작"은 own 전용(FE 회신 필요) | FE 요청서 2026-08-17 · 커밋 `be39cbd7`·`cc769f3b`·`3ec5f1f1`·`d2c656c8`·`b97960b9` · monitoring `BrandRegistrationService`·`BrandHashtagCollectService`·`BrandHashtagTags`·`HashtagPostAuthorImageArchiveJob` · was `V1BrandDirectPostService.cancel`·`BrandHashtagPostAssembler`·`BrandReadRepository`·`BrandDirectPostRepository` · 계약 v2.10([§8](docs/contracts/monitoring-was-contract.md)) |
| 2026-08-17 | **유저별 표시 기간은 링크 레벨 `brand_monitorings.collection_months` — 자산 max 위의 개인 창** — 08-12 "자산 레벨 max, 구독 레벨 관리 기각"을 부분 뒤집음: 여러 유저가 공유하는 브랜드(cclime 실사례)에서 3개월 신청 유저가 12개월치 전량을 받는 문제. 크롤 자산(유저 간 max, 축소 없음)은 불변이고, 링크에 신청값을 저장해 게시물 목록·counts·상세·계정 응답을 그 창으로 자른다(direct 게시물은 명시 등록이라 예외). 재등록 명시값은 그대로 반영(링크는 축소 허용 — 자산과 규칙이 다름), 생략은 불변. 기존 행 백필 12 + cclime 3개월 유저만 운영 수동 UPDATE(신청값이 영속화된 적 없어 복원 불가) | [설계](docs/superpowers/specs/archive/2026-08-17-brand-link-collection-months-design.md) · 마이그레이션 `V20260817074839__brand_monitorings_collection_months.sql`(app) · was `BrandLinkRow`·`BrandLinkRepository`·`V1BrandAccountService`·`BrandAccountAssembler`·`V1BrandPostsController`(Clock 주입) |
| 2026-08-17 | **analytics 이미지 아카이브에 영구 무효 URL 제외 추가(트랙 J) — 어드민 카드 상시 "실패" 뱃지의 원인 제거** — `ImageArchiveJob`이 4일 연속 매 실행 실패로 끝났는데(08-13~16 실측: 실패 26→23→19→14건), 실패 short code가 실행마다 동일했다. 실체는 영구 불능 URL 15건: **리터럴 `exception://` 5건**(Hiker 업스트림이 URL 자리에 넣는 센티널 — 07-31 monitoring 트랙 KK 결함②에서 실측·수정한 것과 같은 부류인데, 그때 가드는 monitoring 저장 경로에만 넣어 crawler→analytics 경로는 `v_contents`로 그대로 유출) + **`rsrc.php/null.jpg` 10건**(삭제·비공개 미디어에 IG가 주는 플레이스홀더 — 영구 HTTP 400). 후보 선정이 "성공해서 `image_assets`에 기록된 것만 제외"라 실패분은 매일 재후보가 되고, `oe` 만료 필터는 이 URL들에 `oe`가 없어 통과 — `failed>0`이면 `outcomeOf()`가 FAILED라 나머지 2,000여 건이 정상이어도 잡 전체가 실패로 표시됐다. **수정**: `permanentlyInvalid()`(비http(s) 스킴 ∨ `rsrc.php/null.jpg`로 끝남)를 만료 필터와 같은 지점에서 적용해 시도 전에 제외하고, 로그도 `만료 제외`와 나란히 `무효 제외 N건`으로 분리 — 만료와 마찬가지로 "실패"가 아니라 "제외"다(후보 선정이 매 실행 원본 뷰 기준이라 재크롤이 정상 URL을 주면 자연 복귀). **기각한 대안**: 뷰(00_base)나 crawler 저장 경로에서 거르기 — 원천 정화가 더 근본적이지만 raw 데이터 불변 원칙(크롤 결과는 받은 그대로 보존)과 충돌하고, 소비 지점 제외가 뷰 스키마·크롤러를 안 건드리는 최소 변경. **잔여**: 매 실행 동일 2건의 커넥트 타임아웃은 이 15건과 별개로 실체 미확정(확인 쿼리 미완) — 제외 대상 아님, 재발 시 추적 | `:analytics:test` 350개 통과(신규 1 — TDD red→green 확인) · 대상 15건 실측: `v_contents` 정상 146,855 / `null.jpg` 10 / `exception://` 5 · monitoring `CdnExpiry` 08-17 개선분(단일 패스 `Ranked`·개연성 하한·null 가드)의 역이식은 이 PR에 안 묶음 — 별건 후속 |
| 2026-08-17 | **monitoring 이미지 아카이브 6종에 CDN 만료(`oe`) 필터 + 만료 임박순 정렬 이식 — 배치 예산의 64%를 이미 죽은 URL에 태우던 결함** — analytics `ImageArchiveJob`은 07-14부터 인스타 CDN 서명 만료(`oe` 파라미터, hex unix 초)를 보고 만료분을 **시도 전에** 걸러내고 남은 대상을 만료 임박 순으로 처리했는데, monitoring의 동형 잡 6종에는 이 장치가 없어 후보를 **DB 임의 순서**로 순회하며 배치 상한(1,000)을 소모했다. 08-16 운영 실측: `BrandPostThumbnailArchiveJob`이 상한 1,000건 중 **723건을 HTTP 403(서명 만료)에 태우고** 아카이브는 277건만 전진(잔여 16,529건 이월). 실패분은 `image_object_path`가 null로 남아 매 스윕 다시 후보가 되므로 **백로그가 구조적으로 줄지 않는다** — 08-12에 고친 "스킵이 상한 창을 잠식하는" 결함(PR #421)과 같은 계열의 두 번째 사례다. 08-17 운영 DB 실측으로 규모 확정: 시도 대상 중 만료분이 `brand_post_meta` 11,121/17,263(64%)·`author_profile` 3,657/6,652(55%) — 나머지 4종은 캠페인 실사용 유입 전이라 0~1건. **핵심 판단**: 만료 URL은 재시도해도 영원히 403이라 "실패"가 아니라 **"제외"**다(재조회가 URL을 갱신하면 만료가 미래가 돼 자동 복귀). 그래서 상한을 소모하지 않고, 로그도 `실패`와 분리해 `만료 제외 N건`으로 따로 센다 — 안 나누면 매 스윕 수백 건 WARN이 정상 상태처럼 보여 진짜 실패가 묻힌다. **만료 미상(`oe` 없음·파싱 불가)은 만료로 취급하지 않는다** — 오탐은 살아있는 URL을 영구히 버리는 반면 미탐은 그 건이 한 번 실패할 뿐인 비대칭. 정렬에서도 미상은 맨 뒤(임박분에 양보). **기각한 대안 2개**: (1) analytics `ImageArchiveJob.expiryEpoch` 직접 재사용 — 모듈 간 import 금지(ARCHITECTURE §4-4), 계약 모듈은 분석 결과 record 전용이라 HTTP/URL 어댑터를 넣을 수 없다. `ImageDownloader`·`ImageStore` 복제와 같은 근거로 `com.celfit.monitoring.image.CdnExpiry`(~40줄)로 복제. (2) 만료 실패를 DB에 기록해 후보에서 빼기 — 스키마 변경(expand-contract 2릴리스)이 필요한데, `oe`는 URL 자체에 답이 있어 저장 없이 매번 판정 가능하다 | `:monitoring:test` 626개 통과(신규 24 — `CdnExpiryTest` 6 + 잡별 만료·정렬 계약 18) · **비공허 확인**: 이식 전 브랜드 잡 3종 테스트가 red였고(만료분이 다운로드됨·상한을 소모함·임박순 아님), 이식 후 green · 기존 픽스처의 자리표시자 `oe=abc`(hex 2748 = 1970년)가 만료로 잡혀 `신규_아카이브` 테스트 5개가 깨졌던 것은 절대값 리터럴 대신 `CdnUrls.farFutureOe()`(실행 시점 +10년)로 교정 — 만료 의미가 없던 자리표시자라 계약 변경 아님 · **08-17 자체 리뷰 반영 4건**: ①만료 시각 단일 계산(`Ranked` record — 정렬 키와 제외 판정이 같은 값, 이중 정규식 제거·IdentityHashMap 폐기) ②개연성 하한(2015년 미만 파싱값은 미상 강등 — `oe=abc` 1970년 실증) ③null 가드(후보 쿼리의 `LIKE 'http%'` 결합이 암묵적이라) ④정렬 계약 테스트 6/6 잡 전체로 확대(기존엔 1/6이라 5개 잡에서 정렬 호출을 지워도 green이었음) — ①②는 analytics 원본과의 의도적 차이(역이식 후속). **알려진 잔여**: `brand_hashtag_post`(insert-only)·`brand_post_meta` 180일 초과분·재등장 없는 `author_profile`은 재조회 갱신이 없어 "만료 제외"가 사실상 영구 유실 — 재목격 시 URL 갱신 경로는 후속 트랙(KK §CDN 만료 필터) · 만료분은 지금도 403으로 실패하던 건이라 **이미지 유실 위험 없음**(동작 차이는 "시도 안 함"뿐) · 08-14 GCS 컷오버 이후 적재는 정상 확인(버킷 182,477객체/19.61GB, `thumb/` 155,217 = `image_assets` thumbnail과 정확히 일치) — 이 결함은 업로드가 아니라 다운로드 단계 | 
| 2026-08-17 | **계정 카피 배치 전송 전환** — ACCOUNT_ANALYZE(KST 07:00)를 콘텐츠와 동형의 Vertex 배치 제출→수거로 전환. 새 토글 `analytics.account-analyze-transport`(기본 online — 콘텐츠 토글과 독립), 상태는 `account_batch_jobs`, 수거는 기존 BATCH_COLLECT 크론에 합승. 배경: 08-17 규명 — 쿨다운 7일+07시 정각 실행이 만드는 8일 주기 온라인 펄스(회당 ~2,700건·~$4.3, batch-limit 30000 운영 확정값), 전환 시 절감 ~월 $9~10. 8일 펄스 주기 자체의 보정은 스코프 제외(배치면 비용 반감·서빙 무영향) | 계획 [docs/superpowers/plans/archive/2026-08-17-account-analyze-batch-transport.md](docs/superpowers/plans/archive/2026-08-17-account-analyze-batch-transport.md) · `account_batch_jobs`·`AccountBatchLines`·`AccountBatchCollectJob`·`AnalyticsJobService`(BATCH_COLLECT 합승) |
| 2026-08-13 | **유료 프로필 페처의 요청 수도 전송 계층 실측으로 계상한다 — 성공 경로까지 확장** — 프로필 페처 4종(`hiker-mobile`·`hiker-webgql`·`datalikers`·`self-hiker`)이 전부 `requestCount` 없는 `ApifyResult` 생성자를 써서, **성공한 실행의 과금 요청이 통째로 0**으로 집계됐다(로컬 raw DB 4일치 실측: `profile-hiker-mobile` 1,252실행·13,084계정·집계 0 — 단가 0.0006 기준 $7.85로 같은 기간 비용 API 전체 보고액 $6.67보다 크다). `hiker-mobile`은 07-23 IG 400 버그 이후 **현재 운영 프로필 소스**라 매일 누적됐다. 페처마다 자기 카운터를 두는 원안은 기각 — 바로 위 결정이 같은 이유로 기각한 구조("새 유료 경로마다 저자가 잊지 않아야 성립")이고, 이 버그 자체가 그 실패 양상의 실례다. 대신 **①`CrawlExecutor` 성공 경로도 소스가 `requestCount`를 안 보고하면(null) 실측치로 채우고**(값을 보고하는 `ReelsJob`·`SimilarJob`·`HikerDiscoverFetcher`는 그대로 이겨 잡별 soft-404 규칙 불변, 이중 계상 없음), **②`PaidCallCounter.propagate()`로 워커 풀에 과금 스코프를 전파**(바로 위 결정이 명시한 한계 해소), **③`CountingDataLikersHttp` 신설**(DataLikers는 `HikerHttp`를 안 지나 기존 데코레이터 커버 밖). 결과적으로 페처 4종 중 3종은 코드 수정 없이 고쳐진다 — 인스턴스가 아니라 종류가 닫힌다 | `:crawler:test` 440개 통과(신규 5) · 팬아웃 관통 테스트는 `propagate` 제거 시 실패함을 확인(비공허) · 계정당 1요청은 `JobCostEstimator` 추정과 일치 · DataLikers 단가($0.0006)는 Hiker($0.001)와 다르지만 비용 API가 **전역 단가 하나**(`crawling.unit-price-usd`)를 전 소스에 곱하는 의도된 근사라 여기서 안 세면 오차가 오히려 커진다(공급자별 정산이 필요하면 job 단위인 집계 축부터) · **미해결**: Hiker는 400·403·404도 과금하는데(`https://hikerapi.com/faq` "What requests do you charge for?" — 같은 FAQ가 `/pricing` 하단에도 임베드. `/p/pricing`·`/docs`는 SPA라 안 읽힘) `CountingHikerHttp`는 예외로 빠져 안 센다. **함정**: 같은 답변의 "we only charge for successful requests"에서 Hiker가 말하는 "successful"은 2xx가 아니라 400·403·404를 포함한다 — 양쪽 모듈 주석의 "성공 응답 기준"은 인용이 아니라 **해석**이 틀린 것. 별도 PR로 분리(프로필 경로 영향은 DELETED 계정 5건으로 미미 · 규모는 `HikerDiscoverFetcher`·`SimilarJob`부터 재야 함 · `SimilarJob`은 soft-404를 이미 1로 자기 보고해 이중 계상 확인 필요 · monitoring 모듈도 같은 규칙) |
| 2026-08-13 | **전역 크롤링 비용은 콜 원본 축에서 합산하고, 크롤러 몫은 analytics 미러로 건너온다 — 두 소스는 대칭으로 열화한다** — 유저별 카드(08-12)의 전역판. ① 전사 합계를 유저별 값의 합으로 만들면 공유 브랜드가 유저마다 계상돼 실제 지출보다 커진다 → `brand_call_count`를 브랜드 축에서 직접 합산(유저별 카드들의 합 > 이 합계가 정상이며, 계약 문서에 명시). ② 크롤러 파이프라인 몫(`crawl_run.request_count`)은 was가 raw DB를 못 읽으므로 `v_crawl_call_daily` → `crawl_call_daily` 미러로 옮긴다 — crawler 내부 HTTP API는 3-tier "DB로만" 원칙을 깨는 데다 **스테이징에 test-crawler가 없어 dev-api에서 영구 부분 응답**이 된다. 대가인 신선도(04:30 미러)는 `sources[].latestCallOn`으로 드러낸다. ③ 유료 공급자는 Hiker 단일로 접어 공급자 축·신규 단가 PUT 없이 기존 키를 쓴다 — Apify(결과 건당 과금, `request_count IS NULL`)·무료 소스(0)는 `request_count > 0` 한 조건으로 빠지므로 액터 화이트리스트가 필요 없다. ④ **모니터링·크롤러 두 소스를 대칭으로 열화 처리한다**(구현 중 교정 — 최초안은 "미러 테이블이 was 기본 데이터소스에 있으니 `CRAWLER.available`은 실질적으로 항상 true"라고 적었으나, 이는 *데이터소스 다운*과 *테이블 부재*를 뭉갠 오판이었다). `crawl_call_daily`의 Flyway는 was가 아니라 **analytics 모듈 소관**(`db/migration/analysis`)이라, analytics가 마이그레이션을 적용하기 전에 was가 뜨거나 analytics를 롤백하면 데이터소스는 멀쩡한데 `relation "crawl_call_daily" does not exist`가 나 어드민 나머지는 정상인데 **이 엔드포인트만 500**이 된다 — 이 기능이 새로 들여온 크로스 모듈 배포 스큐가 정확히 이 모양이다. 조회 예외는 `CRAWLER.available=false` + WARN으로 접고, `computeIfAbsent`가 중간에 만든 부분 누적분은 되돌린다 | [설계](docs/superpowers/specs/archive/2026-08-13-admin-global-crawling-cost-design.md) · 신규 `GET /v1/admin/crawling-cost/summary` · 마이그레이션 `V20260813105711__crawl_call_daily.sql`(analysis) · 뷰 `analytics/views/30_crawl_cost.sql`(+ `v_base_crawl_run`) · was `AdminCrawlingCostSummaryService`·`CrawlCallDailyRepository` · 미러 record가 `LocalDate`를 갖는 첫 사례라 왕복을 `CrawlCallDailyMirrorTest`로 실증 · 스키마 마이그레이션 동반이라 **핫픽스(main 직행) 금지** |
| 2026-08-13 | **크롤러 과금 콜은 전송 계층에서 센다 — 실패 실행도 이미 산 요청 수를 남긴다** — `crawl_run.request_count`가 `CrawlExecutor`의 성공 경로(`finishOk`)에서만 기록돼, 예외로 끝난 실행은 이미 과금된 요청을 통째로 잃었다(페이지 4개를 사고 5번째에서 던진 DISCOVER = 0으로 집계). 전역 크롤링 비용 API가 이 컬럼을 크롤러 몫의 유일한 산지로 쓰는데, 실패는 장애 구간(07-24 Hiker IP 차단)에 몰려 **비용을 되묻는 바로 그 시점에 과소 보고가 가장 커지는** 구조였다. 대안이던 "예외에 카운트 싣기"·"부분 결과 반환"은 둘 다 새 유료 경로마다 저자가 잊지 않아야 성립해 유실이 조용히 재발한다 — 애초 버그와 실패 양상이 같아 기각. **`CountingHikerHttp` 데코레이터가 성공 응답마다 `PaidCallCounter`(실행 스코프 ThreadLocal)에 +1**하고 `CrawlExecutor`가 실패 경로에서 그 실측치를 기록한다. monitoring이 같은 비용 API의 다른 갈래를 이미 이 방식으로 세고 있어 정의가 맞는다(08-12 설계 §2). **성공 경로는 손대지 않았다** — soft-404를 '요청은 이미 샀다'며 1로 세는 잡별 규칙(ReelsJob·SimilarJob)을 이 변경이 뒤집지 않게, 실측치는 값이 아예 없던 실패 경로에서만 쓴다 | `PaidCallCounter`·`CountingHikerHttp` 신규 · `JdkHikerHttp`는 빈 해제 후 `CrawlerConfig`가 감싸 조립 · 성공 콜만 계수(타임아웃·5xx·404는 예외로 빠짐 — 부풀리면 오차 방향이 반대로 나빠진다) · 한계: ThreadLocal이라 워커 풀 팬아웃(HikerMobileProfileFetcher) 콜은 미계수 · 후속: 그 팬아웃 경로는 성공 시에도 `request_count`가 null이라 별도 과소 보고가 남아 있음 |
| 2026-08-13 | **브랜드 초기 수집은 완결 배치 서빙 — 서빙 판정이 "적재됨"에서 게시물 단위 "보강 정산"(`enriched_at`)으로 이동** (구현 완료) — FE 요청서(08-13)의 계약 변경 대응. 08-12 스트리밍이 만든 ready는 **열거만 끝난 상태**라 게시자·댓글 없는 반쯤 빈 카드가 먼저 내려갔다. 완결 단위 = 게시물 정산, 방출 단위 = 열거 페이지(21건) 배치, **첫 페이지 배치 완료 = `markServing`**(FE ready). 정산은 "필드가 다 찼다"가 아니라 **"보강 시도가 끝났다"**다 — 실측 404 2%·타임아웃 1%가 있는 이상 데이터 유무 기준은 영구 미노출을 만든다. 빈 값은 게시자 stale·댓글 워터마크가 다음 스윕에서 채운다. **08-12 결정 2건을 뒤집는다**: ①**서빙 창 30일 기준(`serving-window-days`) 폐기** — 첫 페이지 배치가 대체 ②**확장 중 상태 `collecting` → `ready`** — `expandWindow`가 `backfill_completed_at`을 리셋하게 되면서 was의 유도 분기(`last_swept_on null && backfill_completed_at 있음`)가 도달 불가가 돼 제거. status는 `collecting\|ready\|error` 3값 계약으로 고정되고 진행 판정은 `collectionCompletedAt == null`로 옮겨간다(FE 합의). **실행 중 확정된 셋**: ①등록 백필은 **비동기 파이프라인**(페이지를 enrich executor에 제출하고 열거는 계속 앞서 달림)이고 **`touchSwept`은 전 페이지 정산 후** — 이 값이 FE 폴링 종료 조건이라 미정산 페이지가 남은 채 찍으면 FE가 미완성 목록을 최종본으로 안다(열거 완주 ≠ 수집 완주). core 스레드의 `join()` 점유는 **유일한 브랜드 간 백프레셔**라 의도적 유지(`thenRun` 대안 기각) ②스윕 페이지 콜백의 보강 실패를 **격리** — `sweepCore`가 콜백 예외를 안 잡아 1페이지 보강 실패가 열거를 끊고 뒤 페이지가 적재조차 안 됐다(미적재분은 `trackedPosts`에 없어 다음 스윕 깊이 컷을 못 끌어내림 → 소급 태그 게시물 영구 미수집) ③**게이트는 표시 표면에만** — 스펙이 "세 표면이 조회 하나를 경유"라 단정했으나 실제 소비자는 4곳·성격 3종이었다(표시=적용, 존재·중복·집계 판정=해제). 리포지토리를 두 경로로 분리하고 `TaggedScope{ENRICHED_ONLY, ALL}`를 **필수 인자**로 둬 호출부가 의도를 명시한다 | [설계](docs/superpowers/specs/archive/2026-08-13-brand-initial-batch-serving-design.md)(§2·§3·§5는 실행으로 정정) · **실측 근거**(08-13 운영, 브랜드 5개·470여 콜): 첫 페이지 21건 완결 **~10초**(현행 ready 1분 30초보다 빠름 — 완결성과 속도가 상충하지 않는다) · 댓글 `/v2/media/comments` **p50 1.5초**(08-07 스펙의 "미실측 한계" 최초 해소, 표본 54건 전부 1콜 종료) · `/v2/user/by/id` **404율 2.0%에 재시도 1회로 2/2 복구** → "404 = 결정적 부재" 전제 반증(재시도는 전송 계층 아닌 보강 단계 `by/id`에만) · 5초 초과 1.0% · 워커 6/8/10 어느 레벨에서도 5초 초과 콜 미증가(꼬리는 동시성이 아니라 특정 콜에 산발적) · 마이그레이션 `V20260813115041__brand_tagged_post_enriched_at.sql`(monitoring) — **기존 25,759행 백필 필수**(누락 시 게이트 도입 순간 전 브랜드 목록 공백) · **배포 순서 monitoring → was**(역순이면 없는 컬럼 조회로 브랜드 목록 전면 500, 롤백도 같은 의존) · 동시성 enrich executor 1 → 2(설정화)·워커 6 → 10 · 커밋 `2d0d9b60`~`43b8a6a7` · monitoring `BrandCollectService`(`sweepCore` 페이지 콜백·`enrichSafely`)·`BrandRegistrationService`(`runBackfillSafely` 파이프라인)·`TaggedPostRepository.markEnriched`·`BrandBackfillConfig` · was `BrandReadRepository`(두 경로)·`BrandPostAssembler.TaggedScope`·`BrandAccountAssembler`(확장 분기 제거) |
| 2026-08-13 | **성과 대시보드 반복 요청은 조건부 요청(ETag/304)으로 끊는다 — 버전키는 데이터 유래 지문** (**PR ④로 구현됨 2026-08-28** — 표면 4종으로 확장·스테이징 §5 검증 후 운영) — 08-12 슬림 조립 이후 남은 비용은 "바뀌지도 않은 데이터의 재조립·재전송"이다(실측: 누적 1.6GB, 동일 응답 반복률 `/contents` 61%, 재요청 중앙 간격 2.0분). 조립 비용은 `/comparison`(응답 10KB인데 821ms)이 순수 측정치로 ~800ms. **버전키를 조립 전에 계산해 304로 조기 반환**하면 대역폭과 서버 시간이 함께 빠진다. 버전키는 `updated_at` 워터마크·별도 카운터 대신 **유저 소유 행의 가변 필드를 직접 해싱**한다 — `app.monitoring_items`에 `updated_at`이 없어 기간·캠페인 변경을 워터마크로 감지할 수 없고(제약 실측), 유저당 행이 작아(최대 33행) 지문 비용이 무시할 수준이라 정확성을 규율이 아닌 데이터에서 얻는다. `ShallowEtagHeaderFilter`는 기각(응답 전체 버퍼링 → 2코어 서버 힙 위험 + 조립을 그대로 다 함). **페이지네이션도 명시적 기각** — FE가 전량 1회 수신 후 클라이언트 필터라 왕복이 생기면 탐색 체감이 나빠진다 | [설계](docs/superpowers/specs/archive/2026-08-13-performance-dashboard-etag-design.md) · [계획(아카이브)](docs/superpowers/plans/archive/2026-08-28-perf-dashboard-etag.md) · 캐시 헤더 `no-store` → `private, no-cache`(구현 시 표면 4종 한정 — `/contents`·`/comparison`·`/influencers`·`/growth`. 설계 당시엔 2표면) · **CORS 304 헤더 누락 등 8개 항목은 스테이징(dev-api) 필수 확인**(설계 §5) · 후속: 조립 캐시, `/brand-monitoring/.../posts` 확장, FE 영속화 화이트리스트 버그(celfit-front `persist.ts:37`) |
| 2026-08-13 | **마이그레이션 가드 v4 — 신규 채번 질서 검사 + cd-test monitoring 헬스 게이트** — 08-12 KST 채번 사고(미래 번호 선점 → 뒤따르는 UTC 정상 채번 전부 Flyway out-of-order 거부, 운영 monitoring 크래시루프 2회) 재발 방지 2겹. ①예방: PR `migration-guard`가 신규 파일의 미래 채번(현재 UTC+1h 초과)·역전(자기 디렉토리 base 최대 이하)을 차단 — 의도된 미래 번호(핫픽스 #455 케이스)는 `-- allow-future-version: <사유>` 주석으로 통과. ②검출: cd-test가 배포 후 `test-monitoring` compose 헬스를 게이트해(운영 CD와 동일 관용구) 이 계열 문제가 스테이징 "success" 뒤에 숨지 않게 한다 | [deploy/README.md §5-1 v4](deploy/README.md) · `check-migration-safety.sh --ordering`(셀프테스트 40케이스) · `cd-test.yml` test-monitoring 헬스 스텝 |
| 2026-08-12 | **브랜드 수집 범위(collectionMonths)는 자산 레벨 max — 확장은 last_swept_on 클리어로 백스톱 상속** — 수집 창(1/3/6/12개월)은 공유 크롤 자산 `brand_account.collection_months` 하나로 관리하고 절대 줄이지 않는다(유저 간 max — 3개월 유저가 12개월치를 보는 쪽은 무해). 더 큰 값 재등록 = 기간 확장이며, 구현은 `last_swept_on` 클리어 + 백필 재제출이 전부다 — 실패해도 다음 새벽 스윕이 전체 창을 다시 여는 기존 백스톱을 그대로 상속한다. 확장 중 상태는 was 유도 규칙의 신설 분기(`last_swept_on null && backfill_completed_at 있음 → collecting`)로 표현하고 데이터는 계속 서빙한다. 부수 확정: 브랜드 스윕 크론은 서버 드리프트값 KST 02:00을 정본으로 수용(레포 compose 정렬 + nextScheduledAt 표기 기본값 2) | [설계](docs/superpowers/specs/archive/2026-08-12-brand-collection-months-design.md) · 마이그레이션 `V20260812220000__brand_collection_months.sql`(monitoring — `collection_months`·`collection_started_at`) · monitoring `BrandRepository`·`BrandRegistrationService`(확장 분기)·`BrandCollectService`(창 파라미터화)·`BrandSweepScheduler` · was `BrandCollectionMonths`·`BrandAccountAssembler`(확장 중 collecting 유도)·`V1BrandAccountsController` |
| 2026-08-12 | **비밀번호 재설정 API 3종 신설**(프론트 요청) — `/v1/auth/password-reset/send`(가입 이메일에만 발송, 미가입 시 404 `USER_NOT_FOUND` 열거 허용)·`/confirm`(6자리 코드를 소모하고 1회용 resetToken 발급)·`/password-reset`(토큰 소모 + 비밀번호 변경 + 전 세션 무효화). mail 발송 스택은 07-29 철거분(cc14c717)을 복원 — 철거 사유였던 "임의 주소 발송 남용 표면"은 재설정이 가입된 이메일에만 발송(미가입은 발송 없이 404)하므로 이번 기능엔 비성립. 404 열거 허용은 email-availability(6.24)가 이미 같은 정보(가입 여부)를 공개하고 있어 신규 노출이 아니라는 판단 + 쿨다운 60초·이메일당 시간당 5회·IP당 시간당 20회 레이트리밋(`RateLimiter`에 윈도우 길이 파라미터 신설, e8efd710)으로 열거 시도 자체를 제한. 토큰 설계는 2단계 — confirm 시점에 6자리 코드(TTL 5분, 오입력 5회 제한)를 소모하고 256비트 1회용 토큰(TTL 10분, DB엔 SHA-256 해시만 저장)으로 교환. 소비 경로는 DELETE..RETURNING 원자 조건부 쿼리(f89fb031) — 최초 구현이 SELECT 후 DELETE라 동시 요청이 토큰을 이중 소비할 수 있는 TOCTOU였고, 독립 리뷰에서 검출·수정. reset 성공 시 해당 유저 세션 전부 무효화(best-effort, 기존 6.13 관용구 재사용)·자동 로그인 없음. **배포 전 필수**: test·운영 서버 `.env`에 `RESEND_API_KEY` 등록 + Resend에서 hypenow.io 도메인 SPF/DKIM 인증 확인 — 미설정이어도 서버는 뜨지만 발송이 로깅 폴백으로 빠져 실제 메일이 안 나간다. **프론트 회신 필요**: 502 `EMAIL_SEND_FAILED`는 최초 요청서에 없던 추가 계약이라 프론트 확인 필요. 아카이브 분류는 `ArchiveInventoryTest.EXCLUDED` 등재로 해소(만료성 인증 자료 — `email_verifications` 동일 패턴, ec5c7651) | `MailConfig`·`RateLimiter`·`PasswordResetRepository`·`PasswordResetService`·`V20260812120000__password_resets.sql` · 계획 문서 `docs/superpowers/plans/archive/2026-08-12-password-reset.md` |
| 2026-08-12 | **성과 대시보드 목록·비교는 슬림 조립(댓글 미포함), 단건만 전체 조립** — FE 실측(응답 141배 축소에도 TTFB 고정 ~7초, 7KB 집계도 동일)의 원인은 요청당 전량 조립이었고, 운영 덤프 하니스 분해로 조립 시간의 절반 이상이 댓글 배치 조회 + 수만 행 매핑임을 확정(브랜드 7개 유저: 전체 1,140ms → 슬림 545ms, 로컬 기준 52% 절감). 목록·비교 표면은 댓글을 렌더하지 않으므로(FE 확인 — 성과 대시보드에는 댓글 UI 자체가 없음) `/contents`·`/comparison`은 `assembleSlim`(recentComments 항상 `[]`, commentsCollectedCount 0), `/contents/{id}` 단건만 종전 전체 조립. 스냅샷 유래 지표(commentsTotal·commentsHidden)와 나머지 계약은 무변경. HTTP gzip 압축(같은 날 선행 반영)과 별개 축 — 압축은 전송, 슬림은 조립 고정비 대응 | `PerformanceContentAssembler.assembleSlim`·`BrandPostAssembler.assembleTagged(account, withComments)` · 재측정 하니스 `PerfDiagnosisHarnessTest`(PERF_DIAG=1 게이트) · FE 요청서 2026-08-12 성과 대시보드 고정 지연 원인 규명 |
| 2026-08-12 | **로그 기반 에러 추적 대시보드 — Alloy multiline 병합 + `level` 라벨 + 단일 대시보드 Row 4단** — PLG 스택은 08-10에 올라갔지만 로그를 쓰는 대시보드가 0개였다. 용도를 "아침 훑기 + 장애 시 원인 추적" 둘로 한정하고, 두 동작이 같은 시간축에서 위→아래로 좁혀가는 하나의 흐름이라 화면을 나누지 않고 단일 대시보드(`hypenow-errors`) Row 4단(요약 → 유형 → 외부의존 → 드릴다운)으로 구성했다. 파이프라인은 **타깃을 JVM 4종/그 외 두 갈래로 분기** — multiline stage가 "firstline에 안 맞는 줄은 직전 엔트리에 이어붙인다"로 동작해서, 한 파이프라인에 태우면 caddy(JSON)·postgres 로그가 전부 하나의 거대 엔트리로 뭉친다. 새 라벨은 `level`(값 5개)뿐이고 분기 판정·서비스 그룹핑은 08-12 로그 누락 수정이 도입한 compose 서비스명 `service` 라벨을 재사용한다(계획 문서가 옮기라던 `deploy_prod` 네트워크 keep 규칙은 그 수정이 밝혀낸 누락 원인이라 되살리지 않음). 로거명·예외 클래스는 쿼리 시점 `regexp` 추출로 남겼다(라벨화 시 스트림 폭발). **설계를 바꾼 실측 세 건**: ① crawler·monitoring은 외부 의존 실패를 대부분 `log.warn`으로 남겨(monitoring 전체 `log.error` 2건) ERROR 전용 보드로는 크롤링 사고를 놓친다 → ERROR·WARN 두 축 병치, ② 로그가 평문 한국어 자유 문장이라 메시지 텍스트 그룹핑은 카디널리티가 터진다 → 축약 로거명·예외 클래스를 그룹핑 키로, ③ 예외 클래스 추출 정규식은 `\b` 경계 필수 — 없으면 로거명 `V1ExceptionAdvice`의 접두(`V1Exception`)가 오매칭돼 표가 무의미해진다(리그 실측). 범위 밖(후속): 로그 기반 알람 규칙(실제 분포를 본 뒤 임계값 결정), logback 구조화 로깅·MDC traceId. 검증은 `deploy/alloy/test/` 리그(운영 compose 서비스명·네트워크명을 흉내낸 로컬 스택)로 병합·라벨·비-JVM 회귀를 실측 | 스펙 [docs/superpowers/specs/2026-08-12-log-error-dashboard-design.md](docs/superpowers/specs/2026-08-12-log-error-dashboard-design.md) · [deploy/README.md §16](deploy/README.md) · `deploy/alloy/config.alloy`·`deploy/alloy/test/`·`deploy/grafana/provisioning/dashboards/json/hypenow-errors.json` |
| 2026-08-12 | **monitoring OOM 재발 방지 — 죽은 `rawJson` 필드 제거 + 백필 core 2병렬 + 힙 512m·ExitOnOOM** — 08-12 05:27 UTC 운영 monitoring(Xmx 384m)이 브랜드 등록 백필 중 `Java heap space`로 2건(lagom·mude) 실패(다음 스윕 백스톱으로 자동 복구 — 데이터 유실 없음). 근본 원인은 `PostInfo.rawJson`(+`ProfileInfo`·`ClipItem`)이 **응답 페이지 원문 전체**(TAGGED 평균 859KB·힙 ~1.7MB/페이지, 운영 raw.fetch_payload 실측)를 붙든 채 365일 백필 열거 전 페이지분이 posts 리스트에 누적되고, 그 리스트가 enrich 람다에 캡처돼 **무제한 단일 스레드 enrich 큐**에 브랜드 수만큼 상주한 것(tooq급 2,000건 브랜드 = ~96페이지 ≈ **~150MB/브랜드**, 5연속 등록으로 2~3개 상주 → 384m 초과. OOM이 HttpClient body 수신 중에 터져 `IOException: Java heap space` → `HikerFetchException`으로 표면화). 이 필드는 원형 적재가 RecordingHikerHttp(콜 단위 DB 저장)로 이관된 뒤 **소비처 0인 죽은 짐**이라 제거 — 브랜드당 상주 ~150MB → ~6MB. 상한 10,000 상향(#427)과 조합 시 이론 최악 ~800MB였어서 제거가 선행 조건. 병렬화 판단은 08-12 운영 서버 동시성 램프 실측(레벨당 30콜)이 근거: **동시 20까지 429·5xx 0건**(구 전제 "무저항 한계 8"은 차단이 아니라 레이턴시 특성), 동시 12부터 15~22초 꼬리 상시화 + request-timeout 15초 초과 시 재시도 과금 2배 → 안전 구간 전역 ~10. 백필 executor 1→**2스레드**(`monitoring.brand.backfill-concurrency:2`)로 연속 등록 대기 절반, 전역 최악 9(워커 6+스윕 1+core 2). 힙 예산 공식: **동시 in-flight 콜당 ~10MB**(body 1.7MB + Jackson 트리) — 브랜드 수가 아니라 동시 콜 수가 제약. compose는 Xmx 384→512m(mem_limit 768m 유지 — 오프힙 마진 256m) + `-XX:+ExitOnOutOfMemoryError`(OOM 후 반쯤 죽은 채 버티지 않고 재시작 복귀) | `PostInfo`·`ProfileInfo`·`HikerClient`(rawJson 제거)·`BrandBackfillConfig`(2스레드)·`application.yml`·`deploy/compose.yaml` — PR #430(스트리밍 적재) 위에 스택 |
| 2026-08-12 | **어드민 크롤링 비용에 캠페인·콘텐츠 모니터링 콜 계상 편입** — 비용 카드가 브랜드 태그 몫(brand_call_count)만 세고 있어 캠페인·콘텐츠 등록만 있는 유저는 $0으로 보였다(사용자 지적). 신설 `target_call_count(user_id, called_on, calls)`는 브랜드와 달리 **유저 키**다: target.user_id가 등록 필수값이라 monitoring이 콜 시점에 귀속을 끝낼 수 있고(`TargetCallContext` — 값이 유저 '집합'인 이유는 계정 열거 1콜이 그 계정의 캠페인 유저 전원을 서빙해서), was는 기간 계산 없이 유저 행만 합산한다. 스코프는 스윕(계정 공유 콜=전원, 단건·댓글=소유 유저)·등록(동기 수집+executor 백필 재전파)·share 해소(계약에 userId 옵션 필드 추가) 3경로. 과거 콜은 브랜드 소급(V20260812153000)과 동형으로 원형에서 1회 복원(V20260812161000, 운영 드라이런 실측: 유저 6명 2,286콜, 콜당 유저 중복 0) — MEDIA_INFO(share)만 원형에 유저 단서가 없어 소급 제외 | [설계](docs/superpowers/specs/archive/2026-08-12-admin-crawling-usage-design.md)(원 설계 — 범위만 확장) · monitoring `TargetCallContext`·`CountingHikerHttp`·`V20260812160000`·`V20260812161000` · was `AdminCrawlingUsageService.sumTargetCalls`·`MonitoringReadRepository.findDailyCallCounts` |
| 2026-08-12 | **브랜드 등록 백필 — 페이지 스트리밍 적재 + 서빙 창(30일) 조기 ready** — tooq.official 등록 실측 8분 24초(태그 열거 96콜 × p50 4.9초, 커서 체인이라 병렬화 불가)의 본체는 "365일 열거 완주 후 일괄 적재·ready" 구조였다. 열거를 페이지 단위 즉시 적재로 바꾸고, 서빙 창(`monitoring.brand.serving-window-days: 30`) 커버 시점에 `last_swept_at`만 당긴다(신설 `markServing` — `last_swept_on`·`backfill_completed_at`은 완주 touchSwept 유지: 조기에 last_swept_on을 찍으면 이후 실패 시 다음 스윕이 14일 컷만 돌아 30~365일 영구 공백). 선행 보강(서빙 시점까지 적재분의 게시자·댓글)도 같은 콜백에서 enrich 큐로 제출한다. FE 폴링 계약·스키마 변경 없음 — FE는 기존 폴링 + `backfillCompletedAt == null`로 "과거분 수집 중"을 판별한다. was 파급 1건: 성과 대시보드 `covered`는 `last_swept_at`이 더는 완주를 보장하지 않아 `backfill_completed_at`(최초 완주) 기준으로 정정 | [설계](docs/superpowers/specs/archive/2026-08-12-brand-backfill-streaming-serving-design.md) · `BrandRepository.markServing`·`BrandCollectService`(페이지 스트리밍 적재·서빙 커버 콜백)·`BrandRegistrationService` · 설정 `monitoring.brand.serving-window-days:30` |
| 2026-08-12 | **어드민 크롤링 비용 카드 API — 브랜드 Hiker 콜을 monitoring이 (brand_id, KST일) 단위로 집계하고 was가 연결 기간으로 유저 귀속** — 유저별 크롤링 비용 관측 수단 부재(프론트 요청서 08-12)를 해소하되 시스템 경계를 지킨다: 콜 카운트는 crawler/monitoring 영역이라 monitoring이 전송 데코레이터(`CountingHikerHttp` — RecordingHikerHttp와 같은 체인, "HTTP 교환 1번 = 1콜"이 구조적으로 성립하는 지점)로 `brand_call_count`에 콜당 +1 upsert하고, was는 읽기 전용 표면(was_reader)으로 SELECT + `app.brand_monitorings` 연결 기간(연결일~해제일, KST 양끝 포함)으로 Java 합산한다(크로스 DB 조인 불가·불필요). 브랜드 귀속은 ThreadLocal 스코프(`BrandCallContext`) — enrich 워커 팬아웃은 태스크 본문 runScoped 재전파, 등록 검증 1콜은 brand_id가 생기는 등록 성공 직후 사후 계상(실패 콜·캠페인 모니터링 콜 미집계). **성공 콜만 집계**(Hiker 과금 정합 — 실패까지 세면 비용 추정이 부풀어 오차 방향이 나쁨). **totalCalls는 과거 콜 소급 백필 포함**(같은 날 사용자 결정으로 최초안 "집계 시작 이후 누적"을 대체 — V20260812153000이 raw.fetch_payload에서 브랜드 귀속을 근사 복원: TAGGED 정확 / PROFILE은 brand_account 매칭 / 게시자·댓글은 태그 링크 최소 brand_id 대표, 해시태그 열거·캠페인 kind는 소급 제외). GET `/v1/admin/users/{id}/crawling-usage`는 **404를 절대 안 낸다**(프론트가 404=미배포로 해석 — 이력 없음·유저 부재·비숫자 id 전부 200+0). 전역 단가는 `app.app_setting['crawling.unit-price-usd']`(시드 0.0006 — Hiker 계약 단가 실측치(08-06 비용 산정), PUT `/v1/admin/crawling-cost/unit-price`로 즉시 조정, 감사는 앱 로그 — admin_audit_logs는 act-as 전용 스키마라 미사용) | [specs/2026-08-12-admin-crawling-usage-design.md](docs/superpowers/specs/archive/2026-08-12-admin-crawling-usage-design.md) · `CountingHikerHttp`·`BrandCallContext`·`brand_call_count`(V20260812100000) · was `AdminCrawlingCostController`·`AdminCrawlingUsageService`·V20260812100500 |
| 2026-08-12 | **브랜드 태그 열거 안전 상한 2,000 → 10,000 상향 + 도달 시 error 로그(커버 깊이 포함)** — tooq.official(id=34) 등록 백필이 상한 2,000에 걸려 2,016건/커버 깊이 171일에서 중단됐는데, `runBackfillSafely`가 상한 중단(정상 반환)에도 `touchSwept`를 찍어 365일 창의 172~365일 구간이 **조용히 영구 공백**이 됐다(이후 스윕 컷은 14일+due — 미수집 구간은 링크가 없어 due를 못 만든다). 실측 유입률 11.8건/일 → 365일 창 ~4,300건: tooq는 폭주가 아닌 정상 고물량인데 상한(cclime 847건 기준 캘리브레이션)에 닿았다 — "정상 경로에서 닿으면 안 되는 값"(정책 v1 §6) 전제가 값에서 이미 깨짐. 방치 시 09-11경 90~180일 티어 일제 due로 **일일 스윕도 매달 ~열흘씩 상한 루프**(~96콜/일 낭비 — 상한 중단은 touchCrawledDepth를 안 하므로 미열거 due가 다음 날 같은 깊이를 재개방). 재개형(이어받기) 백필은 기각 — 커서 체인이라 재개도 첫 페이지부터 재열거라 같은 상한이면 진전이 0이고, 진전을 만들려면 커버 깊이 영속화가 필요해 정책 v1 "저장 티어 상태 없음" 원칙을 깬다. 상한 도달 시맨틱 확정: touchSwept 유지(있는 만큼 즉시 서빙, 재열거 루프 방지) + error 로그(열거 건수·목표 컷·커버 깊이)로 운영 보정(상한 임시 상향 + `last_swept_on` 리셋 재백필) 유도. **tooq 보정은 배포 후 `UPDATE brand_account SET last_swept_on=NULL WHERE id=34` 1줄**(사용자 확인 후 실행) — 다음 야간 스윕이 365일 재백필(~205콜), was ready는 08-10부터 `last_swept_at` 기준이라 FE 무영향·전 경로 멱등. 고물량 유지비도 판단: tooq급 월 ~720콜($0.43)·최심 스윕일 ~103콜(~8.4분) 허용 — 매일 전량 대비 여전히 4배+ 절감 | [설계](docs/superpowers/specs/archive/2026-08-12-brand-sweep-cap-revision-design.md) · `BrandCollectService` · `application.yml` |
| 2026-08-12 | **이미지 스토리지를 OCI Object Storage → GCS(`hypenow-images`, asia-northeast3)로 이전** — 서빙용 이미지 아카이브(썸네일·프로필) 16.5GB가 OCI 무료 티어 20GiB 한도에 다가서서, 서빙 버킷만 GCS로 옮긴다. `ImageStore` 포트는 불변이고 모듈별(analytics·monitoring) `GcsImageStore` 구현을 추가해 `IMAGE_STORE=par\|gcs`(기본 par)로 전환한다 — 컷오버는 "잡 정지 → rclone 델타 복사 → front rewrite 전환 → 백엔드 `IMAGE_STORE=gcs`"의 순서 불변식(**서빙이 보는 버킷 ⊇ 쓰기 대상 버킷**)을 지킨다. **GCS 인증은 ADC가 아니라 전용 키 경로(`IMAGE_GCS_KEY`)로 분리** — ADC(`GOOGLE_APPLICATION_CREDENTIALS`)는 **JVM당 1개**뿐인데 analytics에선 이미 Vertex LLM이 점유 중이라, 덮으면 Vertex가 전면 403이 된다(Task 4 구현 중 발견). 공개 읽기 롤은 `legacyObjectReader`(objectViewer는 `storage.objects.list`를 포함해 148k 오브젝트 키가 익명 열거된다). OCI 버킷은 삭제하지 않고 동결 스냅샷 안전망으로 남긴다 | 스펙 [docs/superpowers/specs/2026-08-12-gcs-image-migration-design.md](docs/superpowers/specs/2026-08-12-gcs-image-migration-design.md) · 계획 [docs/superpowers/plans/2026-08-12-gcs-image-migration.md](docs/superpowers/plans/archive/2026-08-12-gcs-image-migration.md) · 컷오버 런북 [deploy/README.md](deploy/README.md) §5-2 · `GcsImageStore`(analytics·monitoring)·`deploy/scripts/post-container-metrics.py` |
| 2026-08-12 | **브랜드 모니터링 이미지 CDN 만료 대응 완결 — 게시물 썸네일 아카이브 신설 + was 서빙 계약 일괄 적용(트랙 KK 확장)** — 08-12 운영 실측: 아카이브 사본이 이미 있는 `author_profile`(1,323/3,997)·`brand_account`(17/19)조차 was가 `image_object_path`를 SELECT하지 않아 원본 서명 URL을 그대로 서빙했고(프론트가 `/img?u=` 프록시로 감싸도 서명 만료와 함께 깨짐 — 프록시는 사본이 아니다), `brand_post_meta`(5,796행)·`brand_hashtag_post`는 아카이브 컬럼 자체가 없어 썸네일이 만료되면 복구 수단이 없었다. ① `V20260812021500`으로 두 테이블에 3컬럼 additive + `BrandPostThumbnailArchiveJob`(`monitor-brand-post/<short_code>.jpg`)·`HashtagPostThumbnailArchiveJob`(`monitor-hashtag-post/<short_code>.jpg`, **RELEVANT만** — 서빙이 RELEVANT만 노출하고 판정은 저장 후 불변이라 비노출분 아카이브는 낭비. `(brand_id, short_code)` PK 중복은 `DISTINCT ON`으로 접고 UPDATE는 short_code 기준 전 브랜드 행) 신설, `BrandSweepJob` finally 배선. ② was `BrandReadRepository` 4개 쿼리(account·post_meta·authors×2·hashtag)에 `image_object_path` 추가, `BrandPostAssembler.resolveImageUrl`(아카이브 우선 + 원본 폴백, `TrackingItemAssembler` 동형)로 tagged 썸네일·게시자 프로필·브랜드 프로필·해시태그 썸네일(전용 API 분리 반영 — `BrandHashtagPostAssembler`) 서빙 일괄 전환 — 해시태그 게시자 프로필만 아카이브 없음(프로필 보강 자체가 스펙 §5 보류라 산지가 열거 관측값뿐). **신규 잡의 배치 상한은 다운로드 시도만 소모한다(스킵 공짜)** — 기존 잡들처럼 후보 리스트를 상한에서 먼저 자르면 "이미 아카이브됨" 행이 창을 잠식해 상한보다 큰 백로그(brand_post_meta 5,796 > 1,000)의 꼬리에 영영 도달하지 못한다(08-12 운영 실측: author_profile 백로그 2,675건이 상한 1,000에도 5일째 잔존 — 기존 3개 잡의 동일 결함 수정은 별도 후속). **긴급 보존(같은 날)**: 배포 대기 중에도 서명이 계속 만료되므로 서버 원오프 컨테이너로 8,473건(brand_post_meta 5,796 + author_profile 미아카이브 2,675 + brand_account 2)을 동일 키 스킴으로 버킷 선적재 + 매니페스트 기록(DB 무접촉) — 배포 후 매니페스트 기반 `image_object_path` 백필 예정 | [KK 트랙 §게시물 썸네일(브랜드)](docs/tracks/KK-모니터링-프로필-이미지.md) · `V20260812021500__brand_post_image_archive.sql` · `BrandPostThumbnailArchiveJob`·`HashtagPostThumbnailArchiveJob`(+Test) · was `BrandReadRepository`·`BrandPostAssembler`·`BrandAccountAssembler`·`BrandHashtagPostAssembler` · 서버 `~/archive-oneoff/`(스크립트·매니페스트) |
| 2026-08-12 | **브랜드 해시태그 발견 게시물은 전용 API로 분리(08-11 결정 정정) — 게시물 목록 병합 철회** — 08-11에 확정·구현했던 "was 피드에 `source:"hashtag"`로 병합"(위 08-11 행)을 FE 결정으로 뒤집었다. 해시태그 발견분은 보강(프로필·댓글·스냅샷) 파이프라인이 없는 별개 성격의 데이터라, tagged·direct와 같은 `BrandPostResponse` 계약(필터·정렬·`meta.counts`)에 끼워 맞추면 null 필드만 늘고 FE가 "해시태그 발견" 배지를 별도 UI로 그리려 해도 계약이 걸림돌이 됐다. **되돌림**: `BrandPostAssembler.assembleHashtag`/`hashtagPost`/`mergeHashtag` 제거, `BrandPostResponse.source`는 `"tagged"`/`"direct"` 2종으로 복귀, `meta.counts.hashtag` 소거. **신설**: `GET /v1/brand-monitoring/accounts/{accountId}/hashtag-posts` — 슬림 신규 record `BrandHashtagPostResponse`(실재 필드만: shortcode·postUrl·matchedTag·takenAt·caption·contentType·thumbnailUrl·author 4종·likes·comments·sponsorship·firstSeenAt, latestSnapshot 등 없는 필드는 필드 자체를 안 낸다), 조립은 신규 `BrandHashtagPostAssembler`(BrandPostAssembler와 완전 분리, `BrandReadRepository.findHashtagPosts`는 그대로 재사용). monitoring 쪽(신규 3테이블·스윕 파이프라인·제외 문자열 API)은 **무변경** — was 표면 계약만 바뀌었다. 계약 v2.8은 유지하되 같은 버전 내 "08-12 API 형태 정정"으로 갱신(재사용 근거는 monitoring 서브시스템·테이블·판정 로직이 전혀 안 바뀌었다는 점) | [계약 §8](docs/contracts/monitoring-was-contract.md#8-브랜드-태그-모니터링-확장--해시태그-감지-v28-2026-08-11-08-12-api-형태-정정) · [MON-BT 트랙](docs/tracks/MON-BT-브랜드-태그-모니터링.md) |
| 2026-08-12 | **클라이언트 이탈(연결 끊김)은 500이 아니다 — v1/v2 캐치올에서 걸러 DEBUG로 강등** — 성과 대시보드 목록(`/v1/performance-dashboard/contents`)에서 `HttpMessageNotWritableException: ... Connection reset by peer`가 `V1ExceptionAdvice` 캐치올에 걸려 ERROR + 전체 스택트레이스로 쌓이고 있었다. 실체는 Jackson이 응답을 소켓에 **스트리밍하던 중**(체인상 `data[389].item.post.caption` 지점) 상대가 이미 연결을 닫은 것 — 유저의 탭 닫기·새로고침, FE `AbortController` 취소, 프록시 read timeout이 원인이고 서버 결함이 아니다. 게다가 그 500 본문은 죽은 연결로 나가지 못해 **아무도 받지 않는 로그**였다. 판정은 자체 문자열 매칭이 아니라 스프링 `DisconnectedClientHelper.isClientDisconnectedException`에 위임한다 — 원인 체인의 타입(`ClientAbortException`·`EOFException` 등)과 최종 원인 메시지("connection reset by peer"·"broken pipe")를 함께 보고, 결정적으로 **`RestClientException`·`DataAccessException`을 제외 목록으로 둔다**: monitoring·DB 같은 **상류** 연결이 끊긴 건 메시지가 글자 그대로 같아도 우리 장애라 500으로 남아야 하는데, 문자열만 봤다면 이게 통째로 조용해졌을 자리다(회귀 테스트로 고정). 핸들러는 `null`을 반환해 응답에 아무것도 쓰지 않는다(죽은 연결에 envelope를 쓰면 write가 또 깨져 같은 예외가 한 번 더 올라올 뿐 — 실제로는 응답이 이미 200으로 커밋된 뒤라 상태코드도 못 바꾼다). **함께 검토했으나 채택하지 않음**: ① 목록 페이지네이션 — 이 에러는 응답 크기가 아니라 "클라가 끊었느냐"의 함수라 빈도만 낮출 뿐 없애지 못하고, abort 취소 패턴에선 요청 수가 늘어 **오히려 증가**한다. 게다가 FE 계약(모니터링 스펙 §316·4절 24번)이 정렬·검색·캠페인 집계를 전량 데이터에서 클라가 계산하도록 못박아 페이징하려면 그 전부를 서버로 옮겨야 한다 ② 속도 개선 목적의 페이징 — `PerformanceContentAssembler.assemble()`이 유저 전량을 조립한 뒤 자바 메모리에서 거르는 구조(필터가 SQL에 안 내려감)라 응답 건수를 줄여도 **서버 비용은 그대로**다. 실제 최적화는 필터 SQL 하향이 선행. monitoring 모듈의 동형 캐치올은 손대지 않는다 — 호출자가 브라우저가 아니라 was라 거기서의 연결 끊김은 "was가 타임아웃으로 포기함"이라는 진짜 신호다 | `V1ExceptionAdvice.handleUnexpected`·`V1ExceptionAdviceTest`(클라 이탈 / 상류 끊김 2건) |
| 2026-08-12 | **브랜드 구독 타입(own/competitor)은 관계 테이블에만 둔다** — FE 경쟁사 모니터링 요청(08-11)에 따라 `app.brand_monitorings.account_type`을 추가했다. 같은 인스타 계정이 유저마다 다른 타입일 수 있어(담당 브랜드 vs 경쟁사) 전역 `brand_account`에 두면 한 유저의 지정이 다른 유저 화면을 바꾼다. 수집은 브랜드당 전역 1회를 유지한다(크롤링 테이블 무변경). 기존 행은 `DEFAULT 'own'`이 백필해 별도 UPDATE가 없다. **상한은 타입별(own 6 / competitor 3)로 갈리고 초과는 409다** — 요청서는 400을 적었지만 POST가 이미 409 `BRAND_ACCOUNT_LIMIT_REACHED`를 내리고 FE가 그 코드로 분기 중이라, 같은 사건이 경로마다 다른 상태가 되는 것을 피했다. competitor만 코드(`COMPETITOR_ACCOUNT_LIMIT_REACHED`)를 신설했다. **캠페인 연결 방어도 요청 전체 400이 아니라 건별 실패**(`COMPETITOR_CONTENT_NOT_ALLOWED`) — 그 엔드포인트는 100건 배치의 부분 성공이 계약이라, 경쟁사 1건 때문에 정상 99건을 버리지 않는다. **PATCH는 `accountType`이 없거나 공백이면 400이고 `own`으로 접지 않는다** — PATCH에서 누락 필드가 파괴적 쓰기로 둔갑하면(빈 본문 한 번이 경쟁사 계정을 own으로 되돌린다) 안 된다. POST의 "생략 = own"은 타입 신설 이전 등록 호출을 살리기 위한 등록 경로 한정 하위호환 규칙이라 변경 경로로 옮겨오지 않는다. **08-12 후속(브랜드 해시태그 감지 병합)**: 등록 시 brandName 시드는 own 연결에만 전달하고 competitor는 null — 경쟁사 브랜드에 내 회사명 해시태그가 오염되는 것을 막는다(`V1BrandAccountService.register/brandNameOf`) | [트랙 PP](docs/tracks/PP-경쟁사-계정-타입.md), [설계](docs/superpowers/specs/archive/2026-08-12-competitor-account-type-design.md) |
| 2026-08-12 | **Alloy 로그 수집 필터를 네트워크 기준 → compose 서비스명 기준으로 — was·monitoring 로그가 한 번도 Loki에 안 들어가고 있었다** — 08-10 관측 스택 도입 이후 줄곧 **was와 monitoring 로그가 Loki에서 누락**돼 있었다(08-12 발견). 원인: relabel이 `__meta_docker_network_name == "deploy_prod"`로 keep하는데, `discovery.docker`는 **컨테이너당 타깃을 1개만** 만들면서 네트워크를 하나 고르고 그게 알파벳순 첫 번째다 — `deploy_monitoring-net` < `deploy_prod`라 이 둘만 조용히 탈락했다. 기존 설정 주석은 "컨테이너×네트워크로 타깃을 만들어 멀티 네트워크 컨테이너가 2개로 잡힌다"고 적었는데 **사실이 아니었고**, 그 잘못된 전제 위에 세운 keep 규칙이 정확히 반대 결과를 냈다. **로컬 도커에 운영 토폴로지를 재현해 실험으로 확정**(컨테이너 3개 → 원본 타깃 3개, 2개 네트워크 컨테이너가 monitoring-net을 배정받고 옛 규칙에서 탈락 → 새 규칙에서 통과). 운영 실측 대조도 일치(n=13, 반례 0: `deploy_prod`가 알파벳 첫 네트워크인 컨테이너 11개는 전부 수집, 누락은 monitoring-net을 가진 was·monitoring 둘뿐). 수정: test 스택 제외를 **compose 서비스명 접두어(`test-`) drop**으로 바꾼다 — 두 스택이 compose 프로젝트명(`deploy`)을 공유해 프로젝트 라벨로는 못 가른다. **`service` 라벨 신설**(compose 서비스명): 컨테이너 실명은 롤링 배포마다 `deploy-was-35`→`-36`으로 바뀌어 조회할 때마다 번호를 알아야 하고 라벨 카디널리티가 배포 횟수만큼 늘어난다 — `{service="was"}`가 안정 조회 축이다. `container` 라벨은 특정 배포분 지목용으로 유지(Grafana 대시보드가 이 라벨을 참조하지 않아 깨질 쿼리 없음 — 확인함). **파장**: 08-10 결정이 "배포 시 컨테이너 로그 유실도 Loki로 해결"이라고 적었지만 정작 was에는 적용되지 않아, 그동안 was 장애는 다음 배포에서 증거가 사라졌다 — 08-12 `Connection reset by peer` 조사가 이것 때문에 막혔다(35번 컨테이너 로그 소실, Loki엔 처음부터 없음) | `deploy/alloy/config.alloy` · 실험: 로컬 재현 후 Alloy `/api/v0/web/components` 로 입력·출력 타깃 대조 |
| 2026-08-11 | **브랜드 해시태그 감지 설계 확정·구현 — 해시태그-only 언급 게시물을 레이더에 추가** — 기존 유저태그 열거의 사각지대(끌리메 PoC 실측: 고객 UGC의 38~67%가 계정 태그 없이 해시태그만)를 `/v2/hashtag/medias/recent` 스윕으로 보완. 그룹형(브랜드 엔티티+계정 귀속 3규칙) 검토 후 **단순화 확정**: ① 태그 셋은 전자동 유도 3종 — #company_name(`user_type='brand'`만)·#계정명 루트(상용 접미사 제거)·#전체 계정명 — 유저 입력·시드 분석·관리 UI 없음 ② 발견 게시물은 hypenow 유저 종속 피드 하나(그룹 엔티티·귀속 규칙 폐기) ③ 스윕은 조기 종료 페이지네이션(페이지에 기존 게시물 등장 → 중단, 상한 4 — **구현 개정: "기존"은 이전 스윕 저장분만, 이번 실행 저장분은 제외** — 크로스 태그 백필 깊이 보존) ④ 필터 순서: 90일 창 → 자사 계열 제외(게시자 username에 제외 문자열 포함 — 기본값 루트 자동, 유저 관리 API·was 프록시, 실측 노이즈 76% 컷·오탐 0) → 중복 → 직접태그(usertags에 등록 계정) 제외(기존 스윕 영역 — 배지는 출처로 결정) → 멘션-only는 관련 확정(AI 스킵) → Gemini 판정(관련/무관/불확실 — 이름 충돌 방어, 컨텍스트에 brand biography, 모델 `gemini-3.1-flash-lite` 전 축 통일, 키 미설정 fail-closed→UNCERTAIN 비노출) ⑤ **관련만 노출**, 무관·불확실 비노출(저장은 유지 — dedup·재판정), **보강(프로필·댓글) 보류** — was 피드 `source:"hashtag"`·id `bh_`+shortcode·스냅샷 없음, 같은 shortcode는 direct/tagged 우선 ⑥ 시작은 등록 커밋 직후 백필 큐 편승(시드·백필 실패는 격리 — 등록 안 깨짐), 이후 일일 크론 합류 ~3콜/일/브랜드(월 ~$0.05). 검증: PoC+검증 19콜, 시뮬레이션 101건 → 노출 18·노이즈 0. 경쟁 리서치(Archive): 유저 등록 태그·비소급·best-effort가 업계 표준, 차별점은 유입 AI 분류·무연동·소급 백필. 구현: monitoring 신규 3테이블(`V20260811085943`)+스윕 파이프라인+제외 문자열 API, was 등록 brandName 전달+피드 병합+프록시(계약 v2.8) | [spec 2026-08-11](docs/superpowers/specs/archive/2026-08-11-brand-hashtag-detection-design.md) · [계획(아카이브)](docs/superpowers/plans/archive/2026-08-11-brand-hashtag-detection.md) · [MON-BT 트랙](docs/tracks/MON-BT-브랜드-태그-모니터링.md) · 원시 실측: 세션 스크래치패드 `poc-clime/` |
| 2026-08-11 | **이미지 아카이브 AAAA-only CDN 실패는 IPv6 개통으로 해결 — OCI VCN IPv6 + 전용 `v6egress` 도커 네트워크** — 배경: 인스타 지역 엣지 CDN(`instagram.f*.fna.fbcdn.net`)이 A 레코드 없이 AAAA만 반환해, IPv4 전용이던 서버에선 해당 썸네일 다운로드가 `Network is unreachable`로 전멸(08-11 실행: 10건 저장·74건 실패 — 74건 전부 fna 호스트, scontent 계열은 정상). 해결: ① OCI hypenow-vcn에 GUA `2603:c021:8021:6300::/56` 추가, 서브넷 /64·라우트 `::/0→IGW`·시큐리티(egress ::/0 + PMTUD용 ICMPv6 type2 ingress만 — **IPv6 인바운드 포트는 안 연다**)·VNIC IPv6 할당(`assign-ipv6`는 oci-cli 버그로 `network ipv6 create` 사용) ② 호스트 netplan `60-ipv6.yaml`(dhcp6+accept-ra) ③ compose에 `enable_ipv6` 네트워크 `v6egress` 신설, CDN 다운로드가 있는 analytics·monitoring만 추가 소속. **기존 prod 네트워크에 enable_ipv6를 켜지 않은 이유**: 네트워크 설정 변경은 재생성을 요구해 소속 컨테이너 전부(=전 스택) 다운이 필요 — 별도 네트워크면 두 컨테이너만 재생성(일반 배포 수준). 서브넷 미지정 ULA 자동할당+NAT66은 도커 27+ 기본. test 스택은 아카이브 no-op(PAR URL 빈 값)이라 미적용 | `deploy/compose.yaml` networks·analytics·monitoring |
| 2026-08-11 | **야간 콘텐츠 분석을 Vertex 배치 API 경로로 전환 가능하게 — app_setting 토글, 기본값은 기존 online 유지** — 배경: Vertex AI 배치 예측은 온라인 대비 50% 할인이고, 무료 크레딧이 9월 중순 소진 예상(소진 후 유료 과금 시작 시 월 비용 약 25만원 → 12.5만원 절감 예상). 범위는 콘텐츠 분석(ANALYZE·LATE_BACKFILL_ANALYZE)만 — 계정 카피 분석은 후속 과제. 구현: `analytics.analyze-transport`(online\|batch, 잡 실행 시점마다 매번 읽음 — 재기동 불필요) 신설, `content_batch_jobs` 상태 테이블(V20260811021726) 신설, `ContentAnalysisJob`이 transport=batch면 온라인 루프 대신 배치 제출(`submitBatch`)로 분기 — 후보 선정+3종 제외 게이트는 `resolveTargets()`로 추출해 온라인·배치 공유, JSONL 조립은 `GeminiBackfillRunner`(초기 백필 one-shot)의 requestLine/sidecarLine을 `GeminiBatchLines` 공용 유틸로 추출해 재사용(백필 러너 동작·기존 테스트 무변경 — 위임만). 배치 미지원 프로바이더(무료 gemini 폴백 등)면 경고 로그 후 온라인으로 자동 폴백해 잡이 죽지 않는다. 신규 `JobName.BATCH_COLLECT`(`ContentBatchCollectJob`) — pending 배치를 순회해 SUCCEEDED는 결과 저장+status=collected, FAILED류는 재시도 없이 status=failed(다음날 후보 diff가 자연 재대상하므로 불필요 판단), 실행 중은 no-op. 제출 경로 시작 시 pending 잔여를 먼저 수거해 중복 제출을 완화(이미 분석됨 diff + `ON CONFLICT DO NOTHING`이 이중 안전장치). `deploy/compose.yaml`에 `ANALYTICS_SCHEDULE_BATCH_COLLECT_CRON`(KST 05:10~11:40 30분 간격) 추가, transport 자체는 app_setting 수동 UPDATE로 전환(compose 미배선). 롤백은 `UPDATE app_setting SET value='online' WHERE key='analytics.analyze-transport'` 한 줄 | [spec 2026-08-11](docs/superpowers/specs/archive/2026-08-11-analyze-batch-transport.md) · `ContentAnalysisJob.submitBatch/resolveTargets`·`ContentBatchCollectJob`·`GeminiBatchLines`·`AnalyticsSettings.analyzeTransport` |
| 2026-08-11 | **brand_account 프로필 이미지도 아카이브 — author_profile 동형 이식(트랙 KK 확장)** — `brand_account.profile_pic_url`은 인스타 서명 CDN URL이라 며칠~2주면 만료되는데 아카이브 경로가 없어, 프론트가 이 컬럼을 직접 쓰면 등록 후 시간이 지난 브랜드부터 이미지가 깨진다(CLOSED 브랜드는 재조회도 멎어 영구). author_profile과 동형으로 3컬럼(`image_object_path`·`image_source_name`·`image_archived_at`, nullable additive — expand 단계) + `BrandProfileImageArchiveJob`(키 `monitor-brand/<ig_user_id>.jpg`, UPDATE는 id 기준)을 추가하고 `BrandSweepJob` finally에서 게시자 아카이브 직후 독립 실행(잡별 격리 — 한쪽 실패가 다른 쪽·스윕 결과에 무영향). 새 설계 없음 — 기존 관용구(`ImageStore`/`ImageDownloader`/`ParImageStore`, source_name 재다운로드 판정, 건 단위 격리, PAR 미설정 no-op) 그대로 재사용. was 서빙 계약(`image_object_path` 우선 + 원본 폴백)은 후속 | [KK 트랙 §브랜드 본인](docs/tracks/KK-모니터링-프로필-이미지.md) · `V20260811023454__brand_account_image_archive.sql` · `BrandProfileImageArchiveJob`(+Test)·`BrandSweepJob` |
| 2026-08-10 | **성능 측정은 self-hosted PLG + `pg_stat_statements` — 외부 APM 배제** — API·쿼리 성능 개선에 앞서 측정 스택을 먼저 도입한다: Prometheus(was 지표, 30초 스크레이프)·Loki+Alloy(전 컨테이너 로그)·기존 Grafana(시각화, 대시보드 "HypeNow API 성능")·postgres `pg_stat_statements`(SQL 누적 통계 + 슬로우 쿼리 로그 500ms). New Relic류 APM은 에이전트가 이 서버(2코어)에서 가장 무거운 선택지(힙 +100~300MB·CPU 1~3%)라 배제 — 이미 Grafana가 돌고 있어 시각화 층을 새로 얹지 않아도 된다는 점도 근거. 관측 컨테이너 3개는 전부 `mem_limit` + `oom_score_adj +500`(메모리 압박 시 서비스 컨테이너보다 먼저 희생) + 저장소 상한(Prometheus 30일·1GB, Loki 30일 compactor)으로 묶어 자원 상한이 구조적으로 보장된다. 호스트 포트는 셋 다 미노출 — 조회는 기존 Grafana SSH 터널로만. was 계측은 관리 포트 분리(9081, actuator 전용)라 공개 API 표면이 늘지 않는다. **배포 시 컨테이너 로그 유실 문제도 Loki 도입으로 함께 해결**(json-file은 컨테이너 재생성과 함께 사라졌다). 개통은 배포 1회 + 수동 2단계(analysis DB `CREATE EXTENSION`, `grafana_reader`에 `pg_monitor` GRANT — [deploy/README.md §15](deploy/README.md)) | 스펙 [docs/superpowers/specs/2026-08-10-query-performance-observability-design.md](docs/superpowers/specs/2026-08-10-query-performance-observability-design.md) · `deploy/compose.yaml`(postgres command·prometheus·loki·alloy)·`deploy/prometheus/prometheus.yml`·`deploy/loki/loki-config.yaml`·`deploy/alloy/config.alloy`·`deploy/grafana/provisioning/{datasources/observability.yaml,dashboards/json/hypenow-api-performance.json}` · was `management.server.port=9081`(prod 프로파일, 노출은 health·prometheus만) |
| 2026-08-10 | **성과 비교 집계 API — covered는 등록일이 아니라 수집 완료 기준** — `GET /v1/performance-dashboard/comparison`(브랜드 계정 × 5구간 집계) 신설. FE 원안은 "등록 전 구간은 빗금(covered=false)"이었으나, 백필이 등록 윈도우 365일 전체를 열거하므로(크롤링 정책 v1) 등록일 기준 판정은 실제 수집된 과거 구간을 가리는 오보가 된다. covered는 계정 단위 — lastSweptAt 존재(ready)면 5구간 전부 true. 집계는 목록 API와 같은 조립 전량 재사용(SQL 집계 기각 — 목록·막대 숫자 일치 우선). individual은 brandAccountId가 없어 비교에서 제외(source=individual이면 전 구간이 빈다). 합계는 non-null만 더하고 전무하면 null(0과 구분 — 피드 views). 참여율은 서버가 계산하지 않고 followersSum(분모)만 내린다 | 스펙 [docs/superpowers/specs/2026-08-10-performance-comparison-api-design.md](docs/superpowers/specs/2026-08-10-performance-comparison-api-design.md) · `PerformanceComparisonAssembler`·`PerformanceComparisonResponse`·`V1PerformanceDashboardController` |
| 2026-08-10 | **핫픽스는 `hotfix/*` → main 직행 + 역머지 자동화 — git 설정은 손대지 않는다** — 운영 장애를 고치는데 develop→staging→main 세 PR·두 배포를 거치는 건 너무 길다. 조사 결과 **막혀 있는 게 없었다**: ci.yml이 이미 main 대상 PR에서 필수 체크 3종을 돌리고, 룰셋 `protect-release-branches`에는 소스 브랜치 제한 규칙이 없으며(staging에서 와야 한다는 건 규약으로만 존재), main에는 머지 큐도 없다(큐는 `protect-develop`에만) — 즉 핫픽스는 큐 대기 없이 체크 통과 즉시 머지·배포된다. 그래서 룰셋·CI 트리거를 일절 변경하지 않고 **규약 문서화 + 역머지 자동화**만 한다. 진짜 위험은 역머지 누락이다 — main만 앞서면 다음 승격이 핫픽스를 되돌리거나 충돌하고 test 스택은 운영과 다른 코드를 검증하게 되는데, 급한 불을 끈 직후가 가장 잊기 쉬운 시점이라 자동화한다. `backmerge.yml`이 main push마다 main→develop·main→staging PR을 열고 auto-merge를 건다. **노이즈 방지 판정식은 `git log --no-merges <target>..main`** — 정상 승격은 새 비머지 커밋이 전부 target에 같은 SHA로 이미 있어 빈 목록(skip), 핫픽스만 걸린다. 이 판정은 **승격·역머지 PR이 merge commit일 때만 성립**한다(squash·rebase는 SHA를 새로 만든다). PR은 별도 역머지 브랜치 없이 `--head main`으로 직접 연다. **채택하지 않음**: ① main 직접 push(bypass actor) — CI 없이 운영 배포가 나가고, 급하게 짠 코드일수록 검증이 더 필요하다 ② 역머지 PR의 develop 큐 우회 — 룰셋 bypass는 PR 단위가 아니라 **행위자 단위**라 "핫픽스일 때만"을 표현할 수 없고 그 계정의 모든 develop 머지가 뚫린다(큐 도입 이유인 Flyway 번호 재검증이 무력화 — 07-30 #181 3차 재발). 역머지는 배포가 끝난 뒤라 큐 대기를 감수한다 | 스펙 [docs/superpowers/specs/2026-08-10-hotfix-direct-to-main-design.md](docs/superpowers/specs/2026-08-10-hotfix-direct-to-main-design.md) · `.github/workflows/backmerge.yml` · 시크릿 `BACKMERGE_TOKEN` 필요(`GITHUB_TOKEN`으로 만든 PR엔 워크플로가 안 붙어 필수 체크가 영영 안 달린다) · ⚠️ 레포가 `delete_branch_on_merge=true`라 head=main PR 머지 시 main 삭제를 막는 건 룰셋의 `deletion` 규칙뿐 |
| 2026-08-10 | **브랜드 계정 `collectionStatus` ready 판정을 `last_swept_on` → `last_swept_at`(완주 사실값) 기준으로 — collecting은 "정말 보여줄 게 없는 첫 수집"에만** — 08-09 정책 v1 소급 마이그레이션(`last_swept_on` NULL 리셋)과 08-10 새벽 Hiker 402(잔액 소진) 스윕 실패가 겹치자, 기존 수집분 87건이 있는 브랜드(cclime.beauty_kr 등 4건)가 FE 로딩 화면에 하루 종일 갇혔다. 게시물 API는 상태와 무관하게 기존 데이터를 서빙하므로 로딩으로 가리는 게 손해 — `last_swept_on`은 "이번 정책·가입 기준 완주"(스윕 스케줄러 마커)와 "사용자 노출 상태" 두 의미를 겸했는데, 전자 리셋에 후자가 딸려가는 결합을 끊었다. 파생 개정 2: ① 재가입(insertOrReactivate)도 잔존 데이터 기준 즉시 ready — "재가입은 collecting" 08-06 결정 폐기(데이터는 지워지지 않으므로 사실 부합) ② ready면 `backfill_error` 무시(재가입 백필 실패 등 — 데이터가 있는데 에러 화면이 오보, 다음 스윕이 백스톱). error는 완주 사실 없는 첫 수집 실패에만. FE 무변경(was 유도값만 바뀜) | `BrandAccountAssembler`(was) · 사고 전말: Hiker 402로 스윕 4건 실패, `BrandSweepJob`은 실패를 격리 로그만 남겨 backfill_error 미기록 → collecting 고착 |
| 2026-08-09 | **브랜드 크롤링 정책 v1(나이 기반 티어) 반영 — "매일 전량(90일 & 105개)"에서 게시물 나이 티어 주기로 전환** — 종전은 브랜드마다 매일 90일 & 105개 깊이를 통째로 다시 훑었다. 정책 v1은 지표 변화가 실제로 일어나는 구간에만 콜을 쓰도록 **게시물 나이 기반 티어 주기**로 바꾼다: 14일 이하 매일 / 14~30일 3일 / 30~90일 7일 / 90~180일 30일 / **180일 초과 영구 제외**(발견 시 스냅샷 1회로 종료). 동시에 등록 백필은 90일 → **365일**로 넓히고 **개수 상한(105)은 폐지**(폭주 방어용 안전 밸브 `max-posts-per-sweep:2000`만 남김 — 정상 경로에서 닿으면 안 되는 값). **판정은 저장 티어 상태 없이 `taken_at`·`last_crawled_at`·현재 시각만 보는 순수 함수**(`BrandCrawlPolicy`)다 — 티어 컬럼·상태 머신을 두지 않아 마이그레이션 직후 기존 행(`last_crawled_at` null = 무조건 due)이 첫 스윕에서 자연 수렴하고, 스윕이 하루 실패해도 다음 스윕이 밀린 깊이를 자동 커버한다(별도 보정 로직 없음). **실행은 게시물 단위가 아니라 "오늘의 열거 깊이"로 번역**한다 — 수집 경로가 `/v2/user/tag/medias` 열거 단일이라 "이 게시물만 다시 크롤"이 존재하지 않기 때문. 깊이 컷 = min(now−14일, 가장 오래된 due의 `taken_at`)이라 due가 없어도 신규 태그 발견용 14일 깊이는 매일 보장되고, 깊은 열거가 얕은 구간을 자동 포함해 정책의 "상위 크롤이 하위 포함·중복 제거"가 구조적으로 성립한다(스킵 로직 불필요). 열거에서 만난 게시물은 due 여부와 무관하게 전부 적재 — 페이지에 실려 온 지표는 콜 0 추가의 공짜 데이터. **정책 문서 중 채택하지 않은 항목(사용자 확정)**: 게시물당 단건 상세 콜 1회(08-06 라이브 실측 — 열거 대비 추가 지표 없음, 상세의 목적은 열거+게시자 프로필+댓글이 이미 충족), 6개월 이전 댓글 30일 보충(현행 게이트가 재등장 시 증가분 자동 수집), 첫 등록 부스트 크롤(v1.1로). 복권 3종(저장·공유·리포스트) 기회 적재·댓글 게시물당 45개(3콜) 상한은 **현행 유지 확정**. IG 요청량은 매일 전량 대비 오히려 줄고, 비용은 등록 1회 $0.2~0.6 + 월 유지 ~$0.05~0.07/계정 수준. **기존 등록 브랜드도 12개월 백필을 소급 적용**(사용자 확정) — 활성 브랜드의 `last_swept_on`을 마이그레이션으로 NULL 리셋해 다음 스윕이 기존 백필 경로 그대로 365일을 열게 한다(완주 전까지 하루 이내 "수집 준비 중" 표시는 수용). **`last_crawled_at`의 의미는 "이 게시물을 봤다"가 아니라 "이 깊이까지 커버했다"** — 자연 종료(페이지 전체가 컷 이전·커서 소진)한 열거는 컷 이후 링크 전체를 touch한다(안전 상한·커서 미전진 중단은 제외 — 자가 치유 유지). 안 그러면 삭제·태그 제거·비공개 전환으로 열거에서 사라진 링크의 due가 영구 true로 굳어 매 스윕이 그 `taken_at`까지 깊이를 다시 여는 요청량 누수가 된다 | 스펙 [docs/superpowers/specs/archive/2026-08-09-brand-crawl-policy-v1-design.md](docs/superpowers/specs/archive/2026-08-09-brand-crawl-policy-v1-design.md) · `BrandCrawlPolicy`·`BrandCollectService`·`TaggedPostRepository.trackedPosts/touchCrawled/touchCrawledDepth` · 마이그레이션 `V20260809120000__brand_tagged_post_last_crawled_at.sql`·`V20260809180000__brand_backfill_reset_policy_v1.sql` · 설정 `window-days`·`window-posts` → `registration-window-days:365`·`max-posts-per-sweep:2000` · 커밋 d18afdf9·8e5114ad·3749db01 |
| 2026-08-09 | **beauty_taxonomy 에스테틱 대분류 추가** — 게시물 단위 사각지대(디바이스·시술 후기가 is_beauty=true인데 어휘 밖이라 main_category=null) 해소. `esthetic`/'에스테틱'(main_order 7), 중분류 3(뷰티 디바이스 5·뷰티 툴 3·피부 시술·관리 6 — 경락 마사지 포함), 소분류 14 — 피부 축 한정(왁싱·반영구·네일·헤어·전문 화장품 제외, 사용자 확정). 어휘 단일 원천이라 additive 시드(V20260809063533) 하나로 프롬프트·sanitize·역유도·카테고리 믹스 뷰 자동 반영. 단 스펙의 "was 무변경"은 응답 경로만 본 오판으로 구현 중 정정 — was 요청 검증 allowlist 2곳(V1ContentQuery·V1InfluencerDiscoveryQuery `MAIN_CATEGORIES`)에 esthetic 추가(누락 시 `?mainCategory=esthetic` 400). '필링 시술'은 클렌징 '필링'과 라벨 충돌 회피 명명. 기존 분석분 소급은 ops SQL(`analytics/ops/requalify_esthetic_candidates.sql`) 동봉만 — 정상 행 삭제라 후보 뷰 교집합으로 한정(유실 방지), 실행은 운영 건수 확인 후 별도 결정. celfit-front 필터 어휘 동기화는 별도 저장소 후속(백엔드 선행 안전) | [specs/archive/2026-08-09-esthetic-taxonomy-design.md](docs/superpowers/specs/archive/2026-08-09-esthetic-taxonomy-design.md) |
| 2026-08-07 | **브랜드 보강(enrich) Hiker 콜을 제한 병렬화 — 공유 워커 풀 `brandEnrichWorkerPool`(고정 6스레드)** — 08-07 운영 실측(`deploy-monitoring-1` → HikerAPI 프로필 엔드포인트, 총 34콜·상이한 공개 계정)으로 **"동시 2 = 부하 완충" 전제가 반증**됐다: 순차 ×6 = 벽시계 11s(콜당 1.2~2.9s), 동시 4 = 웨이브당 2s, 동시 8 = 웨이브당 2s — 동시 8까지 콜별 레이턴시 열화·스로틀·429가 전무했다. 즉 종전 동시 2는 Hiker 보호 효과가 사실상 없었고 병목의 정체는 "콜당 ~1.5초 × 순차 실행"이었다. 변경: `BrandCollectService.enrich` 내부(`ensureAuthors` 게시자 프로필, `collectCommentsGated` 댓글)만 워커 풀에 제출 후 전체 대기 — 게이트·워터마크·게시자/게시물 단위 try/catch 격리(로그만, 나머지 계속)·backfill_error 규칙 전부 불변, 브랜드 단위 큐잉도 불변(executor 2개는 단일 스레드 유지). 동시성 6은 실측 무저항 한계 8에서 안전 마진을 둔 값(사용자 결정). **호출부별 풀이 아니라 공유 빈인 이유**: 스윕과 등록 백필이 겹쳐도 전역 동시 Hiker 콜이 워커 6 + 스윕 core 1 + 등록 core 1 = **최대 8**로 묶여 실측 한계와 동치로 유지된다(호출부별 풀이면 겹칠 때 12~14로 실측 범위 이탈). 효과: 보강 ~3분 → **~30초**, 등록→보강 완료 ~3.5분 → ~1분. 매일 스윕 총 시간도 같은 비율 단축 | 스펙 [docs/superpowers/specs/archive/2026-08-07-brand-enrich-parallel-design.md](docs/superpowers/specs/archive/2026-08-07-brand-enrich-parallel-design.md) · `BrandBackfillConfig.brandEnrichWorkerPool`·`BrandCollectService.ensureAuthors/collectCommentsGated` · 설정 키 `monitoring.brand.enrich-concurrency:6` |
| 2026-08-07 | **브랜드 등록 백필을 core/enrichment 2단계·전용 executor 2개로 분리 — ready(touchSwept)는 core 직후(단계식 ready)** — 운영 실측(cclime_official): 등록→ready 8.5분 중 ~5분이 앞 계정 백필 대기(단일 스레드 큐), ~3.4분이 자기 콜 체인 — 그런데 그중 게시자 프로필+댓글(전체 콜의 ~85%)은 목록 렌더에 필수가 아니다(was 조립기가 게시자는 열거 관측값 폴백, 댓글 수는 스냅샷 값 사용. 댓글 실패는 현행도 게시물 단위로 삼켜져 ready가 댓글 완전성을 보장한 적 없음). 분리: ① `BrandCollectService.sweep` = `sweepCore`(프로필 1콜+열거 ~5콜+적재 — 윈도우 게시물 반환) + `enrich`(게시자 stale·댓글 게이트 — core 결과 소비, 재열거 없음) ② 등록 백필은 core 완료 즉시 touchSwept(ready) 후 enrichment를 `brandEnrichExecutor`(신설, 단일 스레드)로 넘김 — 등록→ready ~30초, 연속 등록도 뒤 계정이 앞 계정 보강을 안 기다림(계정당 ~30초씩만 대기) ③ 보강 실패는 backfill_error 미기록(목록은 이미 서빙 중 — "초기 수집 실패"가 오보)·로그만, 게시자 stale 판정·댓글 comments_collected_count 워터마크로 다음 스윕이 자동 백스톱 ④ core 실패는 종전과 동일(backfill_error + 보강 미예약). 매일 스윕(BrandSweepJob)은 `sweep`(core+enrich 합본) 그대로 — 새벽 3시라 대기가 체감에 안 잡힘. 동시 Hiker 콜 최대 2(부하 완충 의도 유지) | `BrandCollectService.sweepCore/enrich`·`BrandRegistrationService.runBackfillSafely`·`BrandBackfillConfig` |
| 2026-08-07 | **그리드 숨김 릴스를 열거에 합류 — clips(릴스 탭) 전용 게시물을 medias 열거 결과로 승격** — 작성자가 "프로필에 공유"를 끈 릴스는 `/v2/user/medias`(프로필 그리드)에 영영 안 실리고 `/v2/user/clips`(릴스 탭)에만 실린다(운영 실측: rran.e_ 스카이밀크 캠페인 릴스 `DbdA0j4SDUd` — 캡션이 키워드 규칙에 완전 매칭인데 7일 연속 clips 원형에만 존재, medias 12건 응답에 단 한 번도 미포함 → 감지 0건). 종전 구조는 clips 응답을 medias 게시물의 조회수·복권 지표 머지에만 써서, 그리드 숨김 릴스(협찬 릴스에서 드물지 않은 설정)가 키워드 감지·스냅샷 수집 양쪽의 구조적 사각지대였다. 수정: `fetchClipPlays`가 media 노드 원형을 보존(`ClipItem`)하고, `fetchRecentPosts`가 medias에 없는 clips 코드를 `toPost`로 승격 합류(medias 파싱본이 항상 우선, viewsTrusted=true — clips는 재생수 인라인). 게시물 0건(medias 404) 계정도 clips 릴스가 있으면 합류한다. 부수 효과: 열거 창이 최대 +12건(릴스 탭 최근분) — 감지는 등록 시각 하한선(postedAfterRegistration)이 옛 릴스를 걸러 오탐 없음. 기감지 실패 캠페인(target 19, WATCHING·08-30까지)은 배포 후 다음 스윕에서 자연 감지된다 | `HikerClient.fetchRecentPosts`·`ClipItem` · 원인 조사: raw.fetch_payload id 55~882 (POSTS, subject 8419502146) |
| 2026-08-07 | **브랜드 계정 POST를 "계정명 등록(불변)" → "브랜드 연결(다계정, 한도 10)"로 개정 — IMMUTABLE·ALREADY_EXISTS 폐기, 멱등 반환** — FE 요청: 이미 다른 브랜드가 연결된 사용자의 추가 등록이 `BRAND_ACCOUNT_IMMUTABLE` 409로 막히던 것을, "기존 브랜드를 현재 사용자에게 연결"로 처리. 브랜드는 전역 1회 수집(monitoring `POST /api/brands`가 활성 브랜드 멱등 replay — Hiker 콜 0·백필 재시작 없음)이라 **monitoring 무변경**, was만 개정: ① 활성 연결 유니크를 `(user_id)` → `(user_id, brand_id)`로 교체(`V20260807140000`, 새 인덱스 생성 후 구 인덱스 DROP — 롤링 중 구코드는 앱 사전 409로 여전히 1개만 만들어 안전) ② 이미 연결된 같은 계정 재요청은 409 대신 기존 객체 202(멱등, monitoring 호출 생략) ③ `BRAND_ACCOUNT_LIMIT_REACHED`(한도 10, 강제 지점 `BrandLinkTransaction` — 한도는 유니크로 표현 불가라 users 행 FOR UPDATE 직렬화 위에서 카운트) ④ `users.instgram_account_name` 불변 계약 용도 소멸 — 읽기·쓰기 전부 제거, 컬럼 DROP은 후속 릴리스(expand-contract). 파생 개정: 목록·성과 대시보드 tagged·캠페인 v2 tagged는 연결 브랜드 전체 병합(연결 순, 같은 shortcode는 먼저 연결한 브랜드 우선), 게시물 상세는 브랜드 전체 탐색, 회원 탈퇴 훅은 연결 전부 해제 후 브랜드별 마지막 사용자 판정, share 지연 매핑은 같은 등록의 기존 매핑 → 단일 활성 연결 순 판정(불가면 보류) | [spec 2026-08-07](docs/superpowers/specs/archive/2026-08-07-brand-monitoring-was-api-design.md)(§5 등록·§5-4 불변은 이 행으로 대체) · `BrandLinkTransaction`·`V1BrandAccountService` |
| 2026-08-07 | **게시자 프로필 이미지 아카이브(트랙 KK 확장) — `author_profile.profile_pic_url`도 OCI 아카이브 대상에 편입** — 브랜드 태그 파이프라인의 CDN 만료(~4일) 방어는 두 갈래로 확정: `brand_post_meta.thumbnail_url`은 매일 스윕 upsert가 URL을 되살려 아카이브 불필요(08-06 결정 유지), `author_profile`은 30일 stale + 재등장 시에만 재조회라 upsert 방어가 성립하지 않으므로 `AuthorProfileImageArchiveJob`(ProfileImageArchiveJob 동형)으로 아카이브. 키는 `monitor-author/<ig_user_id>.jpg` — username 개명 시 고아 오브젝트 방지를 위해 PK 기준. 실행은 `BrandSweepJob.run()` finally(author_profile 갱신 주체가 브랜드 스윕이므로 — 캠페인 DailySweepJob 패턴 동형, 잡 전체 격리 + 건 단위 격리). was 서빙 연결은 MON-BT was API 쪽 후속 | [KK 트랙](docs/tracks/KK-모니터링-프로필-이미지.md) · `AuthorProfileImageArchiveJob` · `V20260807150500__author_profile_image_archive.sql` |
| 2026-08-07 | **캠페인 v2 콘텐츠 연결은 레거시 patch 재사용 대신 전용 슬림 경로(`CampaignItemLinker`) — 검증은 유지, 조립만 생략** — v2 콘텐츠 추가·제거가 아이템 1건 연결마다 `V1MonitoringItemUpdateService.patch`를 불렀는데, patch는 캠페인만 바꾸는 요청에도 응답 전량 재조립(스냅샷·댓글·프로필 배치 조회)까지 돌아 아이템당 ~9쿼리고 v2는 그 응답을 버리고 id만 쓴다(입력 상한 100건이 `@Transactional` 한 트랜잭션에 몰려 커넥션 점유 장기화·풀 고갈 위험 — Task 11 사후 리뷰 Important #2). 스펙(08-07 §8 구현)의 "레거시 검증 재사용" 원칙에서 의도적으로 일부 이탈하는 트레이드오프: patch의 **두 소유 검증(캠페인·아이템 `findByIdAndUser`)은 링커가 그대로 유지**하고(에러 메시지도 동일) 절감 대상은 조립뿐(아이템당 3쿼리 — link는 검증 2+UPDATE 1, unlink는 검증 1+UPDATE 1). 레거시 `/v1/monitoring/**` 는 0줄 변경 — 계속 patch를 쓴다 | `CampaignItemLinker`·`V2CampaignContentService.link/remove` |
| 2026-08-07 | **캠페인 v2 "기존 아이템" 판정 — 취소 아이템은 제외(재등록 경로), 자연 종결은 연결 유지** — Task 11 사후 리뷰(급행 머지로 생략됐던 것)가 찾은 판정 공백의 명시적 확정. 같은 shortcode의 아이템이 취소분뿐이면 기존엔 캠페인만 patch돼 "success인데 수집은 영영 없음"이 됐다. 확정 기준: **취소(canceled_at)는 사용자가 수집을 버린 행이라 '기존 아이템'이 아니다**(tagged에 있으면 재등록 위임, 없으면 NOT_FOUND — §6-4 직접 등록 activeItemId와 기준 정합). **자연 종결(ended)은 수집이 끝난 콘텐츠라 연결 유지**(캠페인은 "과거 콘텐츠 묶기"가 정당한 용도 — 재등록시키면 재수집 비용·collecting 상태만 남는다). 취소 여부는 레거시 응답으로 구분 불가(ended로 유도)라 아이템 행을 직접 보되, 종결 status 후보가 있을 때만 조회(평시 왕복 0). 동반 픽스 3: 제거는 소속 전건 해제(대표 1건만 끊으면 대시보드에 콘텐츠 잔존 — 레거시 아이템끼리는 중복 제거 안 됨)·202는 새 아이템 생성 시만·contentId trim. 레거시 위임 계약(campaignId null=해제, 문자열 campaignId 파싱)은 통합 테스트로 고정 — mock만으로는 레거시 리팩터가 v2를 조용히 깨는 구조였다. 잔여(별도 트랙): 링크 1건당 레거시 patch 전량 재조립(~9쿼리, 100건 상한이면 트랜잭션 1개에 수백 쿼리 — 슬림 경로 분리는 "레거시 검증 재사용" 원칙과 트레이드오프)·reasonCode 어휘 대소문자 이원화(v2 대문자 vs 레거시 entry 소문자 — FE 협의) | [MON-BT 트랙](docs/tracks/MON-BT-브랜드-태그-모니터링.md) · `V2CampaignContentService.withoutCanceled` · `V2CampaignContentIntegrationTest` |
| 2026-08-07 | **브랜드 모니터링 was API 구현 — 레거시 `/v1/monitoring/**` 완전 동결 + 전부 추가(접근안 A), FE 명세 대비 의도적 편차 5개** — FE 브랜드뷰 명세(브랜드 모니터링 v1 · 성과 대시보드 v1 · 캠페인 확장 v2)를 was 신규 패키지 3개(`v1/brandmonitoring`·`v1/perfdashboard`·`v2/monitoring`) + seam 확장(`BrandReadRepository` 읽기 배치 조회, `MonitoringCommandClient.registerBrand/deregisterBrand`)으로 착지 — 기존 `/v1/monitoring/**`·캠페인 파이프라인 0줄 변경. app 스키마는 `users.instgram_account_name`(불변) + `brand_monitorings`·`brand_direct_posts`, monitoring은 브랜드 3테이블 nullable 컬럼 + Hiker 파싱 확장만(API 콜 증가 0). **FE 공유 필요한 의도적 편차 5**: ① 등록은 monitoring 동기 검증 → was 커밋(FE §5.2 역순 — 오타 username 영구 저장 차단, 없는 계정은 폴링 error 대신 즉시 422) ② `lastDetectedAt`=`lastTrackedAt`=`last_swept_at` 동일값(08-06 감지/추적 구분 폐지 귀결) ③ ID는 숫자 문자열·shortcode(예시 접두사 `brand_`·`ig_`는 비계약) ④ `BRAND_ACCOUNT_LIMIT_REACHED`는 코드만 예약(1계정·불변 정책상 도달 불가) ⑤ 협찬 판정 근거 미저장 — 조회 시 계산(paid true→sponsored / 캡션 확정 키워드→sponsored / paid false→organic / null→unknown. 캡션이 저장돼 있어 키워드 개선이 과거분 즉시 소급). 그 외 확정: 직접 등록은 레거시 등록 파이프라인 위임 + `brand_direct_posts` 매핑, 캠페인 관계 1:1 유지(타 캠페인 소속은 entry duplicate), 대시보드 스냅샷 병합은 날짜·지표별 non-null 우선(둘 다면 브랜드 값)·대표 source individual>direct>tagged·tagged-only 합성 id `bt_`+shortcode. PR #354 급행 승격으로 운영 배포(전체 로컬 테스트·Task 11 리뷰 생략 — CI 사후 검증, 리뷰는 08-07 사후 실시). 후속 실측 핫픽스: `/v2/user/by/id` 파라미터는 `user_id`가 아니라 `id`(422 실측, 4ab01545) · 브랜드 스윕 크론(KST 03:00) 서버 override → 레포 compose 정합 | [spec 2026-08-07](docs/superpowers/specs/archive/2026-08-07-brand-monitoring-was-api-design.md) · [계획(아카이브)](docs/superpowers/plans/archive/2026-08-07-brand-monitoring-was-api.md) · [MON-BT 트랙](docs/tracks/MON-BT-브랜드-태그-모니터링.md) |
| 2026-08-06 | **릴스 수집 임시 액터 전환 토글(`reels.source`)** — Hiker 크레딧 절약 + 오결제 Apify 크레딧 소진 목적. ReelsJob 소스 분기: ACTOR면 계정당 `apify~instagram-reel-scraper` 런 1회(`reels.actor-results-limit` 기본 6 — 최신만 얕게), 결과를 `{"items":[...]}` 래퍼로 `raw_media_page(APIFY_ACTOR)` 저장, `v_base_reel_item` UNION 분기로 지표 연속(하이프 스코어 단절 없음). 액터는 username 기반이라 pk 미보유 계정도 수집, 0건 응답은 Hiker 404와 동일하게 수확 완료 마킹. 기본값 HIKER(코드 폴백) — 토글만으로 양방향 즉시 복귀, HIKER 경로·기존 테스트 무변경 | [spec 2026-08-06](docs/superpowers/specs/2026-08-06-reels-actor-collection-design.md) · `ReelsJob.visitActor`·`ReelsSourceSetting`·`v_base_reel_item` |
| 2026-08-06 | **브랜드 태그 모니터링 구현 스키마·주기 개정(설계 재논의) — 전면 브랜드 전용 테이블 + 매일 전량 105개 수집(감지/트래킹 구분 폐지)** — 최초 구현은 공용 테이블(post_snapshot 계열) 재사용 + 감지 매일 1콜/트래킹 3일이었으나, 사용자와의 설계 논의로 전면 개정. ① **전면 전용 스키마**: `brand_post_snapshot`·`brand_post_meta`·`brand_post_comment`·`brand_profile_snapshot` 신설(+기존 `brand_account`·`brand_tagged_post`·`author_profile`, 총 7테이블 — 지표·메타·댓글 키는 게시물 전역, 브랜드 스코프는 brand_tagged_post 링크로) — 캠페인 테이블을 브랜드 파이프라인이 한 줄도 안 건드린다. 근거 둘: 볼륨 비대칭(매일 전량 개정 후 연 ~7,700만 행 — 캠페인 서빙 테이블의 100~1,000배가 섞이면 vacuum·retention이 운명 공동체)과 **겹침 게시물 덮어쓰기 버그**(캠페인 추적+브랜드 태그 겹침은 시딩 정상 케이스인데, 02:00 캠페인이 재시도로 채운 복권 지표를 03:00 브랜드 꽝 세션의 전컬럼 upsert가 null로 되돌림 — 공유 테이블에선 구조적으로 재발). 공유의 잔여 이득(겹침 댓글 콜 절약)은 월 수십 원 수준으로 기각. fb 캐리포워드·역전파·0 캐리는 전용 테이블에 동형 이식, METRICS_HIDDEN 알람 결합도 소멸(브랜드 쓰기는 AlarmRecorder 미경유). 썸네일 CDN 만료(~4일)는 매일 스윕이 자체 방어 ② **매일 전량 수집**: 감지(1페이지)/트래킹(3일 5페이지) 구분을 폐지하고 매일 브랜드마다 프로필 1콜(followers·biography 최신값 + brand_profile_snapshot 추이 일 1행 — "등록 시 1회"에서 개정) + 105개 깊이 열거(~5콜), 윈도우 안 전 게시물이 매일 1행(기존 모듈과 같은 일 단위 시계열). 비용 2,000계정 월 ~$354 → **~$550~600** 수용(사용자 결정) ③ 백필 상태는 별도 컬럼 없이 `last_swept_on`(null=미완)으로 판별, 스윕은 브랜드 단위 격리만(당일 재시도·실행 대장 없음), 게시자 프로필은 최신 1행 캐시(이력 없음 — "태그 당시 팔로워" 서빙 배제), 윈도우 이탈 데이터는 영구 보존 — 전부 사용자 확정. 유지되는 결정: 열거 단일 경로(단건 배제)·105개 하드코딩 금지·페이지 단위 컷 중단·복권 3종 재시도 없이 부재=0·0 캐리 즉시 적용·댓글 게이트(comment_count>저장값, 기지 페이지 중단)·게시자 30일 stale·`/v2/user/by/id` 셰이프 by/username 동형 가정(라이브 미실측) | [MON-BT 트랙](docs/tracks/MON-BT-브랜드-태그-모니터링.md) · [최초 구현 계획(아카이브 — 스키마·주기는 이 행으로 대체)](docs/superpowers/plans/archive/2026-08-06-brand-tag-monitoring-impl.md) · 스펙·실측은 아래 08-06 행 |
| 2026-08-06 | **브랜드 태그 모니터링 수집 스펙 확정 — `/v2/user/tag/medias` 열거 단일 경로(단건 배제), 감지 매일·트래킹 3일 1회, 윈도우 90일 & 105개** — 회원가입 브랜드 계정(스케일 2,000 가정)에 태그된 게시물을 자동 모니터링(탈퇴까지). 라이브 실측 12콜로 결정: 태그 열거는 페이지당 21건이고 **릴스 조회수(`ig_play_count`)가 열거에 상시 실려**(프로필 열거와 다름) 조회수 보강·단건 콜이 불필요하며, 복권 3종(저장·리포스트)은 단건 당첨률이 열거보다 낮아 단건은 전면 배제. 수집: 조회수(릴스)·좋아요·댓글 수(열거) + 댓글 내용 최대 45개(3콜, **열거 `comment_count`가 저장값보다 클 때만** — 댓글 0이면 콜 자체 없음) + 게시자 프로필(신규 감지 시 1회 + **등장 시 30일 stale 갱신** — 월 1회 일괄 아님, 열거 user 객체엔 follower_count 부재). 복권 3종(저장·공유·리포스트)은 **DB 적재·FE 미노출**(열거가 주는 값, 콜 0 추가). 계정 추적은 탈퇴까지(휴면 완화 없음). 백필=트래킹 깊이=105개(5페이지) 정합으로 "백필분 전부 계속 갱신" 보장. 함정: 태그 열거 정렬은 태그 시점 순(taken_at 비단조 — 소급 태그 혼입)이라 감지·날짜 컷 모두 "페이지 전체가 기지/컷 이전일 때 중단". 비용(콜당 $0.0006): 브랜드당 등록 ~$0.10 일회·월 ~$0.18, 2,000계정 월 ~$354 — 지배 항은 게시자 프로필+댓글(~75%), 게시물 지표는 ~24% | [specs/2026-08-06-brand-tag-monitoring-schedule-design.md](docs/superpowers/specs/archive/2026-08-06-brand-tag-monitoring-schedule-design.md) · [findings §11](docs/superpowers/plans/2026-07-28-monitoring-hiker-findings.md) |
| 2026-08-05 | **0 캐리 — 구조적 키 부재 게시물(리포스트 0·공유 미상)은 매일 재시도하지 않고 0을 잇는다** — 0 간주는 재시도 소진 후에 하는데 스냅샷이 일 단위라, 키가 영영 안 오는 게시물이 다음날 또 null에서 시작해 상한(6회)까지 헛 재시도를 매일 반복했다(운영 실측 하루 ~100콜 — 스윕 본체와 맞먹는 낭비. 상한 6 자체는 당첨 즉시 중단이라 기대 ~2.2콜로 무릎점 유지). 판정: **양수 관측 이력 전무 ∧ 오늘 이전 마지막 행이 0으로 종료**(이중 근거, `SnapshotRepository.codesWith*ZeroCarry` 배치 IN절 1쿼리)면 재시도 진입 전에 해당 지표를 즉시 0 기록·재시도 제외(`CollectService.applyZeroCarry`). 리포스트·공유(숨김 제외) 동형 적용 — 사용자 결정. 자동 해제: 실제 값이 생기면 키가 오기 시작하고 양수 관측이 non-null 머지·이력 판정 양쪽을 이긴다. 등록 첫날은 전일 행이 없어 캐리 미적용(풀 재시도) — 자연스러운 안전장치 | `CollectService.applyZeroCarry`·`SnapshotRepository.codesWithRepostsZeroCarry/SharesZeroCarry` |
| 2026-08-05 | **공유수 영구 부재 = 게시자 숨김 11건 + 원인 미상 8건으로 규명 — 숨김은 null 유지(재시도 제외), 원인 미상은 소진 시 0 표기** — 공유 키를 영영 안 주는 추적 릴스 19건의 원인을 단건 응답 플래그로 판별: `share_count_disabled`(공유 횟수 숨기기 토글, 1건 — 78만 조회 `DaHSf2uB2Vj`) 또는 `like_and_view_counts_disabled`(좋아요 숨김이 공유 노출도 함께 끔 — IG 앱 문구 "좋아요 수 및 공유 횟수는 회원님만", 실측 lvcd=true 10건 전원 공유 영구 부재 vs 제공 31건 전원 false·전환 산증인 `DbSkrodp-WA`). 구현: `PostInfo.sharesHidden`(scd ∨ lvcd) 신설, 재시도 필요 판정을 `CollectService.needsMetricsRetry` 단일 기준으로 통합(스윕·등록·진입·종료 4곳) — 숨김이면 공유 항 제외(안 빼면 숨김 11건이 매일 상한까지 헛 재시도), 소진 0 간주도 제외(숨김은 비공개지 0이 아님 — 0 쓰면 78만 조회 릴스에 공유 0 오표시). 숨김 아닌 원인 미상 부재(8건 — 전부 조회 120~316 초소형·노출 정지 릴스)는 소진 시 0 표기(사용자 결정 — 공유는 reshare_count=0 관측 79건이 존재해 리포스트와 달리 "0=생략" 인코딩 근거는 없으나, 실해가 0~1 수준이라 실용 판단). FE 표시도 같은 PR에서 관통(**계약 v2.7** — `post_snapshot.shares_hidden` 컬럼(expand, `V20260805054500`, likes_hidden=true 백필 동봉) → was Snapshot 응답 `sharesHidden`. FE는 true면 `-` 대신 "비공개" 표시, FEED는 공유 미지원(null 강제)이라 플래그도 false로 접음 — 미지원≠비공개) | findings §2 결론 7 · [monitoring-was-contract.md v2.7](docs/contracts/monitoring-was-contract.md) · `PostInfo.sharesHidden`·`CollectService.needsMetricsRetry` |
| 2026-08-05 | **리포스트 키 부재 = 0 간주(재시도 병행) — `media_repost_count`는 값이 0이면 키가 생략되는 것으로 확정** — 근거: ① 운영 전 스냅샷에서 reposts=0 관측 0건(shares=0 82건·saves=0 61건과 대조) ② 잔여 미충족 게시물은 공유·저장을 매일 받으면서 리포스트만 10~12회 연속 부재(복권 29%면 확률 ~0) ③ 대조 실험 — 같은 분에 교차 호출 시 리포스트 111 게시물은 키 수신, 0 추정 게시물은 절대 미수신. 구현: 재시도(그대로 유지) 소진 시점에 **saves 관측됨 ∧ reposts 부재**면 0으로 기록(save·repost 키 동반 실림 566/596이 판정 근거), 전부 꽝인 날은 근거 부재로 0 안 씀(내일 이월) — null(미관측)/0(관측 해석) 구분 유지. 부수 확인: 세션 고착 가설은 raw 해시 검증으로 기각(10s 간격에도 회전) — 현행 10s×6 재시도 유효. **공유수를 게시물 단위로 영영 안 주는 케이스(잔여 19건)의 표시 정책은 별도 논의로 보류** | findings §2 결론 7 · `CollectService.assumeZeroRepostsIfOmitted` |
| 2026-08-05 | **열거 창 밖 추적 릴스는 단건 콜 복권 재시도로 3지표(저장·공유·리포스트)를 보강 — 08-04 "단건은 공유 확정 제공" 전제 반증에 따른 예외** — 08-04 개편 다음날 운영 확인: 추적 릴스 6건이 3지표 전부 null(5건은 clips 재시도가 "열거 창 밖" 즉시 포기, 1건은 6회 전패). 원인은 전제 붕괴 — 단건 콜 재실측(최근 14h, 200 응답 49콜)에서 `reshare_count` 59%·`save_count` 47%·`media_repost_count` 29%로, "단건은 좋아요·댓글·조회·공유 4지표 확정(25/25)"·"reshare 단건 87.5% 상시"는 당첨 세션 치우친 표본으로 판명(꽝 세션엔 `share_count_disabled` 불리언만 있고 값 키 부재). 창 밖(최근 12건×페이지 밖) 게시물은 clips가 영영 못 잡고 단건이 유일 공급원이므로, **창 밖 판정 시 clips 대신 단건 콜을 같은 상한(합산 6회)·같은 10s 간격으로 당첨까지 재콜**하고 3지표를 non-null 머지 — 단건 전환분 첫 재콜은 다음 시도부터(방금 정본 단건 콜 직후라 즉시 재콜은 Hiker 캐시로 같은 꽝 세션 되받음). **진입·종료 조건도 3지표 공통으로 확장(공유 포함)** — 종전 조건(저장·리포스트만)은 "공유는 단건 정본이 확정 제공" 전제의 산물이라, 부분 세션이 공유만 빠뜨린 날 재시도가 아예 발동하지 않아 공유수 단독 누락이 남았다(당첨 clips 열거는 공유를 사실상 상시 실어 추가 비용 거의 없음 — 스윕 `sweepAccount`·등록 `scheduleMetricsBackfill`·CollectService 진입 필터·양 경로 종료 조건 4곳 일괄). 부수: user.pk 부재(구형 셰이프) 계정도 이제 건너뛰지 않고 단건 복권만 돈다(스윕 `retryMetricsSafely`·등록 `scheduleMetricsBackfill` 게이트 제거). 08-04의 "단건 재시도 기각"은 창 안 게시물엔 유지(clips가 계정당 1콜로 우월). 예상 비용: 창 밖+당일 꽝 릴스만 게시물당 기대 +2~4콜. 추가 관측: 저장만 실리고 리포스트 안 실리는 부분 세션 반복 실측 — "한 콜 안 전부/전무"도 항상은 아니며 repost 키가 최약 | findings §2 결론 6 · `CollectService.retrySinglesOnce` |
| 2026-08-05 | **CI 가속 1단계: cache-warm 시딩을 `testClasses`→`test`로 확장 — 테스트 실행 결과를 빌드 캐시에 태운다** — PR CI의 실체는 `./gradlew test` 195~210s(잡별 실측: sql-harness 20s·가드 5~8s는 무시 수준)였고, `--profile`+테스트 XML 실측(런 30968497189)으로 분해하니 4 vCPU 포화 상태에서 analytics 189s·monitoring 164s·was 161s·crawler 96s(태스크 합 12m48s). 근본 원인은 develop push 시딩이 컴파일까지만 돌아 **테스트 결과가 캐시에 한 번도 안 들어가고, 모든 PR이 무관 모듈까지 4개 전부 테스트를 실행**한 것. test까지 시딩하면 PR·merge_group에서 안 바뀐 모듈이 FROM-CACHE로 스킵된다(develop push 런은 50s→약 4분이 되지만 아무것도 게이트하지 않아 대기자 없음). **동반 수정: was `build-info.properties`의 `build.time`이 매 빌드 바뀌어 test 입력 해시를 오염 → 캐시 영구 미스**(로컬 실증: clean 후 재실행에서 test 재실행) — `normalization.runtimeClasspath`로 캐시 지문에서만 `build.time`을 무시(런타임 캐시 prefix 용도는 그대로). 후속 후보(미착수): 풀 실행 케이스(승격 PR·merge_group)용 모듈별 매트릭스 분할 — required check 이름이 바뀌므로 룰셋 갱신 동반 필수 | `ci.yml` cache-warm 잡·`was/build.gradle` normalization · 실측 상세는 PR #345 |
| 2026-08-05 | **컨테이너 다운 감시 사각 해소 — SERVICES에 redis·grafana·ons-relay 추가 + relay 다운은 치명 토픽(이메일)** — 메모리 격리(08-04) 후속: mem_limit로 죽은 컨테이너는 자동 재기동에 기대는데 이 3종은 감시 목록 밖이라 재기동 실패가 조용히 묻혔다(redis는 fail-open이라 증상도 없음, ons-relay는 디스코드 알람 전체의 단일 경로). `hypenow-container-down`은 차원 필터 없는 스트림별 평가(`container_up[1m].max() < 1`)라 SERVICES 추가만으로 자동 커버. 단 "ons-relay 다운" 알람은 죽은 자신을 지나 디스코드로 갈 수 없어 전용 알람 `hypenow-ons-relay-down`(PT3M, CRITICAL)을 치명 토픽(EMAIL 백업)으로 신설. relay에 healthcheck 추가(python:alpine이라 bash /dev/tcp 대신 파이썬 소켓 — unhealthy를 스크립트가 다운으로 집계). 함정 수리: CD가 deploy/scripts에서 rollout.sh·backup.sh만 올려 metrics 스크립트는 레포↔서버 드리프트 구조였다 — cd.yml scp에 추가(backup.sh 07-30 드리프트와 동일 계열) | [deploy/README.md §9](deploy/README.md) |
| 2026-08-04 | **저장·리포스트는 세션 복권 — 스윕에서 clips 열거를 당첨까지 재시도(상한 6회), 피드는 제외** — 운영 원형(raw.fetch_payload) 전수 재실측으로 07-28 findings의 존재율 표를 정정: `save_count`·`media_repost_count`는 Hiker 세션의 ~30%(단건 15~23%·clips 열거 ~45%)에만 실리고, **한 콜 안에서는 전부 실리거나 전부 빠진다**(릴스 151콜 전수: 혼재 0건 — fb_* 키와 동일한 세션 게이트). `reshare_count`(공유)는 릴스에서 사실상 상시(열거 100%·단건 87.5%, PR #330 단건 정본화로 해결 완료). 그 결과 스냅샷 채움율이 릴스 saves 31%·reposts 29%에 그쳤다. **해법 선택**: 캐리포워드(fb_plays 방식)는 사용자 기각 — 대신 미관측 추적 릴스가 있으면 **clips 열거를 당첨까지 최대 6회 재콜**(당첨률 45% → 당일 확보율 ~98.5%, 기대 추가 콜 계정당 +1.2회 — 상한 4→6은 월 +$0.3에 구멍일 5%→1.5%라 무릎점). fb와 달리 스윕 재시도가 정당한 이유: 당첨 세션은 **전 릴스에** 키를 실어 "영영 안 오는 게시물"이 없고, 당첨 1회로 계정 릴스 전체가 채워진다. POST 등록만 있는 계정(운영 46개)은 열거 자체가 없었어서 **단건 응답 user.pk(신설 PostInfo.ownerUserId, 제로 콜)로 clips 열거를 새로 태운다** — 단건 재시도 기각(존재율 15~23%로 6회에도 74%·비용 2.5배). 재콜 간격 10s는 Hiker 응답 캐시(연속 콜 동일 응답 실측) 회피용. **피드는 전 경로 제외**(사용자 결정): 저장·공유는 전 세션·전 엔드포인트 키 부재(원형 181아이템 + 스크린샷 실물 게시물 라이브 8콜 = 0건 — IG가 앱 신버전 세션에만 내려주는 값), 리포스트(43%)는 취득 가능하나 was가 FEED reposts를 null 강제 중이라 노출은 별도 제품 결정으로 보류. 부수: clips 열거가 이미 받아오던 saves/shares/reposts를 파싱에서 버리고 있었음을 수정(ClipCounts 확장 — 재시도 없이도 채움율 개선). 등록 당일 공백은 **등록 응답 후 백그라운드 백필**로 해소 — 등록 동기 경로(was 10s 타임아웃 예산)에는 재시도를 못 품으므로, POST 등록 커밋 직후 전용 단일 스레드 executor(`metricsBackfillExecutor`, 데몬)가 같은 재시도 루프를 돌려 최대 ~1분 내 스냅샷을 채운다(동기 응답의 firstSnapshot은 여전히 null 가능 — FE 조회 표면은 스냅샷 SELECT라 새로고침이면 반영). ACCOUNT 등록은 추적 게시물이 아직 없어(WATCHING) 대상 아님 | HikerClient `ClipCounts`·CollectService `retryReelsMetrics`·DailySweepJob `retryMetricsSafely` · 실측 집계는 아래 findings §2-5 |
| 2026-08-04 | **monitoring 단건 게시물 조회를 `/v2/media/info/by/code`로 이전 — 미디어 데이터 동등성 실측 확인 후 파싱만 `media_or_ad` 언랩으로 교체** — 이전 전 운영 키로 실추적·외국 계정 14게시물(릴스 9·피드 5, 한국 1계정 + 서구권·일본 12계정) 짝 비교 + 결손 케이스 반복 콜 실측: 소비 필드(6지표·caption·user·썸네일·taken_at) 값 완전 동등, `save_count`/`fb_play_count`/`media_repost_count` 키 유무와 값 흔들림(reshare 1292↔1311)은 **같은 엔드포인트 안에서도 콜마다 재현되는 세션 편차**로 판명(같은 시도에서 양쪽이 동시에 키를 주고 안 줌 — 08-02·08-03 실측과 동일 양상, 기존 파싱 방어가 그대로 커버). 오류 응답도 400 ValidationError·404 MediaNotFound로 동일(전송 계층 상태코드 매핑 무변경). 차이는 응답 껍데기뿐: 구 `{items:[...]}` → 신 `{media_or_ad:{...}}`(share 해소 §2-6과 동일 셰이프). 변경 3점: ① fetchPost `media_or_ad` 언랩(200인데 media_or_ad 부재는 SubjectNotFound 강등) ② `RecordingHikerHttp.kindOf`에 신 경로 → `kind=POST` 분기(kind='POST' 필터 백필·포렌식 V20260803064353 계열이 새 행을 계속 보도록) ③ 테스트 픽스처 media-info-by-code*.json 전환. 뷰·analytics 무영향(raw JSON을 읽는 뷰 없음 — 전수 grep 확인). 유의: 이전 후 raw.fetch_payload의 POST 행은 media_or_ad 셰이프로 쌓이므로 향후 원형 재파싱 SQL은 두 셰이프를 COALESCE로 봐야 한다 | HikerClient `fetchPost` · 실측 원본 31건은 세션 스크래치패드 보관 |
| 2026-08-04 | **super 초대코드 — `is_super` 플래그, 소진 스탬프 미기록으로 무제한** — 코드 하나로 여러 명 가입 허용은 `signup_codes.is_super` 컬럼 하나로(uses 테이블 기각 — 가입자 추적은 signup_events가 이미 정본). claim의 원자 UPDATE에 `AND NOT is_super` 가드 — super 코드에 used_at이 찍히면 강등 시 소진으로 굳기 때문. PATCH `/admin/signup-codes/{code}`는 isSent·isSuper 부분 갱신으로 확장(둘 다 없으면 400). 적재 재제출로는 승격 불가(ON CONFLICT 스킵) | [2026-08-04-super-signup-code-design.md](docs/superpowers/specs/archive/2026-08-04-super-signup-code-design.md) |
| 2026-08-04 | **컨테이너 메모리 격리 — 전 서비스 mem_limit(느슨한 오버커밋) + oom_score_adj 희생 순서** — 12G 호스트에 운영 10+test 5 컨테이너 동거인데 cgroup 상한이 grafana뿐이라 한 서비스 폭주 시 호스트 OOM killer가 무고한 컨테이너(RSS 1위인 was, 최악엔 postgres)를 잡는 구조였다. 상한은 평소 실측·힙 상한 × 1.5~2의 느슨한 값(합계 ~15.7G > 물리 12G — 의도된 오버커밋: 단일 폭주만 차단, 평소 무영향. 엄격 분할 ≤12G는 새벽 배치 피크 오사살·상시 재튜닝 부담으로 기각). 동시 폭주는 새벽 창(크롤+미러 04:30+백업 04:10)에 구조적으로 겹치므로 oom_score_adj로 희생 순서 고정 — test +800 → 배치 crawler·analytics +500 → 기본 0 → was -300(자동 재기동되는 가용성 문제) → postgres×2·caddy·ons-relay -800(데이터·전면 다운·알람 실명. -1000은 극단 상황 커널 패닉이라 금지). postgres류 상한은 페이지 캐시가 cgroup 동반 과금이라 OOM이 아닌 캐시 축소(쿼리 지연)로 발현 — 미러 잡 느려지면 postgres-raw 상한만 상향. swap 4→8G 증설은 서버 수동 작업이라 별도 보류 | `deploy/compose.yaml` 상단 "메모리 격리" 주석 |
| 2026-08-03 | **METRICS_HIDDEN 판정 지표를 3종(조회수·좋아요·댓글)으로 축소 — 저장·공유·리포스트 제외** — 알림 설정 화면 문구("조회수·좋아요수·댓글수 중 일부가 비공개로 바뀌면")와 판정 대상을 1:1로 맞춘다. 화면이 약속하지 않은 지표의 알람은 사용자에게 설명 불가. BE(`AlarmRecorder.hiddenMetrics`)만 수정 — 이벤트 어휘·옵트아웃·was는 무관. 메일 조립의 saves/shares/reposts 라벨은 기적재 이벤트 렌더링용으로 유지 | `AlarmRecorder.hiddenMetrics()` |
| 2026-08-03 | **좋아요 숨김은 null 처리 + `likes_hidden` 플래그로 관통(monitoring↔was 계약 v2.6)** — 게시자가 `like_and_view_counts_disabled`를 켜면 like_count가 실측이 아니라 프리뷰 잔여값으로 잘려 온다(운영 raw.fetch_payload 실측: 서로 다른 두 게시물이 똑같이 3, `fb_like_count` 키 부재·`has_shared_to_fb=0`으로 FB 몫 가설 반증 — 캠페인 카드 "83→3 ▼80" 거짓 급감의 원인). 1차 수정(PR #316): 숨김이면 likes를 취득 불가(null)로 내려 기존 값→null 전이 감지(METRICS_HIDDEN) 경로에 태움 + 운영 오염 스냅샷 7행 수동 보정. 2차(이 트랙): null만으로는 FE가 "숨김"과 "그날 수집 실패(행 부재)"를 구분 못 해 `post_snapshot.likes_hidden`(expand-only, `V20260803125200`, likes IS NULL 백필 동봉)을 신설하고 PostInfo→SnapshotRepository→was MonitoringReadRepository→TrackingItemResponse.SnapshotResponse.`likesHidden`까지 관통 — FE는 true면 `-` 대신 "비공개 전환" 표시. 숨김은 좋아요만 잘린다(댓글·조회수는 실측 유지, ig_play_count 단조 증가 실측). 주의: 08-03 보정이 직전 스냅샷까지 null로 만들어 그 두 게시물은 METRICS_HIDDEN 전이가 소급 발화하지 않는다(신규 숨김 전환은 자동 발화) | [monitoring-was-contract.md v2.6](docs/contracts/monitoring-was-contract.md) |
| 2026-08-03 | **제품 공지는 `/v1/notices` 신설 — 기존 `/v1/notifications`(다이제스트) 미확장(트랙 OO)** — type enum·본문/링크 부재·하루 1건 전제·count 등 4필드가 충돌해 기존 계약을 건드리지 않는다. 예약 발행은 유저 목록 제외 + 어드민 목록 포함 | [docs/tracks/OO-업데이트-소식.md](docs/tracks/OO-업데이트-소식.md) |
| 2026-08-03 | **monitoring 조회수 시계열 정합 3종 — fb 유도(play−ig 우선)·첫 관측 역전파·원형 기반 백필** — 같은 날 합산 정책 결정의 후속. ① fb 키 없이 합산 play만 주는 세션(`DUrj0iGEn6G`)·fb 키 0인데 play>ig인 모순 세션이 실존해 FB 몫은 `play − ig` 유도를 fb 키보다 우선(정상 키일 땐 동치, 실측 검산). ② fb 첫 관측일에 이전 행이 IG 전용이면 성과 추이가 +fb를 "▲증가"로 오표시(실사례 221→305 ▲84) → 쓰기 시 첫 관측을 이전 미관측 행에 소급 UPDATE. ③ fb_plays 도입 전 적재분은 세션 혼합이라 조성 불명 → raw.fetch_payload에서 (code, KST 날짜)별 재추출해 일회성 마이그레이션 재계산(`V20260803064353`, 운영 드라이런 검증 후 확정. raw 없는 행은 불변). ②가 안전한 전제가 ③이다 — 백필 없이 역전파하면 옛 합산 행에 fb 이중 가산 | [findings §2 결론 4](docs/superpowers/plans/2026-07-28-monitoring-hiker-findings.md) |
| 2026-08-03 | **monitoring 저장 views는 화면(모바일 앱) 기준 합산값 — `views = IG 몫 + fb_plays(캐리포워드)`** — 08-02 결정(ig_play_count 정본)의 확장. 화면엔 FB 교차게시 몫이 합산 표시되는데(실측 `DbHZLrFM45b`: 화면 981 ≈ 합산 984, IG 몫만은 492) 합산 세션은 콜의 18.5~33.3%뿐이라 매 콜 취득 불가. 해법: `post_snapshot.fb_plays` 컬럼 신설(expand), fb가 안 실린 콜은 직전 관측 fb를 캐리포워드해 views 조립(FB 몫은 실측상 며칠 단위 정적이라 오차 미미·시계열 역행 없음). fb 관측 이력이 전무한 신규 릴스만 1회 재조회 — **등록 경로 한정, 스윕은 재시도 없음**(08-03 축소: 교차게시 안 한 릴스는 fb가 영영 안 잡힐 수 있어 스윕 재시도는 헛 콜 매일 반복 — 실측 단건 +65%. 역전파가 늦은 관측을 소급 정정하므로 게시물당 평생 최대 1콜이면 충분). 키 부재(세션이 FB 못 봄) vs 0(관측된 0)의 구분이 캐리포워드·재시도 판정 기준이라 PostInfo.fbPlays가 null/0을 구분 보존 | [findings §2 결론 4](docs/superpowers/plans/2026-07-28-monitoring-hiker-findings.md) |
| 2026-08-02 | **monitoring 릴스 조회수 정본은 `ig_play_count` — `play_count`는 폴백으로 강등** — Hiker v2가 콜마다 다른 IG 세션을 태우는데, FB 교차게시 데이터가 보이는 세션에서만 `fb_play_count`가 실리고 그때 `play_count`가 IG+FB 합산으로 커진다(세션은 Hiker 풀 소관이라 고정 불가). `play_count` 우선 파싱은 같은 릴스 조회수를 221→305→222로 역행시켰다(운영 `raw.fetch_payload` 실측 — 단건 `DX0U76Xy1D2`·클립 열거 `DXx7gtszvSV` 등). `ig_play_count`는 전 콜 존재·단조 증가라 이걸 우선한다(HikerClient `toPost`·`fetchClipPlays` 2곳). 의미 변화: 교차게시 릴스의 views가 IG 전용 수치로 소폭 낮아지는 대신 콜 간 비교 가능·단조가 보장된다. 부수 발견: 좋아요 급감(83→3)은 세션이 아니라 작성자의 `like_and_view_counts_disabled` 켬이 원인(별건) | [findings §2 결론 4](docs/superpowers/plans/2026-07-28-monitoring-hiker-findings.md) |
| 2026-08-02 | **어드민 캠페인 목록(트랙 MM 후속) — status는 어드민 표면에서만 서버 판정, registrationCount는 취소 포함 전량** — 프론트 변경요청서 4-2-6절 `GET /v1/admin/campaigns` 신설 + `GET /v1/admin/users` 목록 `companyName` 추가. **판단 3개**: ① 유저 표면 계약(모니터링 6.25)은 캠페인 상태를 "클라 파생"으로 못박았지만 어드민 요청서는 `status`(pending\|active\|ended\|no_date)를 응답 필드로 요구 — 어드민 표면만 서버가 유도하되 SQL이 아니라 순수 함수(`AdminCampaignStatus`, KST 오늘 기준, 경계일 오늘==start·오늘==end 둘 다 active)로 분리해 경계값을 단위 테스트. ② `registrationCount`는 canceled 필터 없이 캠페인 배정 추적 행 전량 — 기존 `CampaignRepository.countItems`(삭제 가드)·users 목록 `monitoringCount`와 같은 계약을 골라 어드민 화면 간 수치 모순을 차단. 집계는 campaign_id IN절 GROUP BY 배치(N+1 금지), userName은 registrations와 동형의 `findByIds` Java 결합. `seedingTarget`은 `seeding_count` 그대로(프론트 어휘 차이일 뿐, 유저 선언값·nullable 계약 불변). ③ `companyName`은 `AdminUserSummary`가 직접 들고 상세는 Summary에서 물려받도록 구조 변경 — 목록·상세가 같은 값을 공유해 두 표면 표류가 구조적으로 불가능. 부수: 통합 테스트 12번째 메서드부터 @BeforeEach 실로그인이 RateLimiter 분당 10회에 걸려 429 — 테스트 클래스 스코프로만 상한 완화(`was.rate-limit.per-minute=100`) | [docs/tracks/MM-어드민-백엔드-api.md](docs/tracks/MM-어드민-백엔드-api.md) |
| 2026-08-02 | **삭제 데이터는 soft delete 대신 아카이브 이관(트랙 NN)** — was의 삭제는 거의 전부 hard delete였고, 사용자 행동 시그널이 복구 불가로 사라지고 있었다. 전면 soft delete는 기각했다 — was 조회가 JdbcClient 생 SQL이라 `deleted_at IS NULL` 누락을 컴파일러가 못 잡고, 누락 시 삭제한 데이터가 사용자에게 다시 보인다. `users.email` UNIQUE 충돌과 CASCADE 무력화도 따라온다. 대신 삭제 직전 같은 트랜잭션으로 `archive.archived_rows`(단일 범용 jsonb 테이블)에 원본 행을 이관하고, 라이브 삭제·조회 경로는 그대로 뒀다. **was가 `app` 외 스키마에 쓰는 첫 사례**다 — 같은 DB여야 트랜잭션이 묶이고, 스키마를 분리해두면 축적량이 커졌을 때 옮기기 쉽다. `users`는 직접 식별 컬럼 7종을 뺀 가명화 형태로 이관한다(`id` 유지 — 자식 조인이 살아야 자산 가치가 있다). 누락 방지는 가드 테스트 2종(app 스키마 분류 완전성, CASCADE 재귀 도달성)이 CI에서 담당한다. fail-closed — 이관이 실패하면 삭제도 롤백된다. 나아가 이관 건수와 삭제 건수를 대조해 "0건 이관 + N건 삭제"라는 조용한 유실까지 막는다(fail-closed는 예외만 막기 때문) | [specs/2026-08-02-delete-archive-design.md](docs/superpowers/specs/2026-08-02-delete-archive-design.md) · [docs/tracks/NN-삭제-데이터-아카이브.md](docs/tracks/NN-삭제-데이터-아카이브.md) |
| 2026-08-01 | **어드민 백엔드 API(트랙 MM) — impersonation은 SecurityContext 스왑 필터 1개로, 어드민 health와 헬스보드는 단일 모집단 서비스로** — 어드민 프론트(admin.hypenow.io) 계약(2026-07-31 요청서)의 P0(role·X-Act-As-User)+P1(조회 3종)+P2(audit-logs) 구현. **핵심 판단 3개**: ① `X-Act-As-User`는 인가(AuthorizationFilter) **뒤에** 걸리는 필터가 요청 스코프 SecurityContext principal을 대상 유저 AppUserDetails로 교체하는 방식 — 인가는 어드민 본인으로 이미 끝난 뒤라 어드민 인증이 잠기지 않고, 이후 모든 컨트롤러(`/v1/me` 포함)가 무수정으로 대상 유저 기준 응답을 내 계약("모든 GET 적용, /me 예외 없음")이 자동 충족된다. 비어드민 세션의 헤더는 무시+WARN(운영 도메인 프록시엔 헤더 스트립이 없어 백엔드가 유일한 방어선 — 요청서 1-5a), 쓰기는 405+`Allow: GET, HEAD`(코드는 프론트 유니언 내 `FORBIDDEN` 유지), 대상 부재는 404(본인 폴백 금지 — 오인 방지), 반영 건은 `app.admin_audit_logs`(신설, 90일 보존 스케줄러) 기록. ② 유저 목록의 `health`와 헬스보드 rows는 **`AdminMonitoringHealthService` 하나가 같은 활성 모집단**(canceled 제외 + pending 미만료 or target WATCHING/TRACKING)에서 유도 — 두 화면이 모순되면 운영팀이 어느 쪽도 못 믿게 된다는 계약 요구를 코드 구조로 강제(정합 불변식 통합 테스트 동봉). 상태 5종 우선순위는 어드민 계약(error>hidden)을 따라 유저 표면 ItemStatus(hidden>error)와 의도적으로 다르다(주석 명시). 정체 임계 N=7일·M=48h. ③ `lastActiveAt`은 미기록 상태였어서 `app.users.last_active_at` 신설 + 인증 요청 5분 스로틀 갱신(사칭 중엔 어드민 본인 기준) — 소급 불가라 초기 전원 null. **회신 확정**: 세션 쿠키는 host-only(Domain 미설정)라 어드민 로그인이 운영 세션을 안 덮고(A1), userId는 bigserial 숫자라 프록트 패턴 무수정(A2). `role` 컬럼은 V11에 이미 있었고(Swagger Basic용) `/v1/me` 노출만 추가. 8절 통합 검색은 요청서 스스로 "확정 아님"이라 미착수 | [specs/2026-08-01-admin-backend-api-design.md](docs/superpowers/specs/2026-08-01-admin-backend-api-design.md) · [docs/tracks/MM-어드민-백엔드-api.md](docs/tracks/MM-어드민-백엔드-api.md) |
| 2026-07-31 | **`findCardsByHandles`의 협찬 수 서브쿼리에 핸들 푸시다운 — FROM 절을 두 경로로 분기** — 유사 카드 경로(`/v2/influencers/{handle}/similar` → `findCardsByHandles`)가 발굴 목록과 `FROM_JOINS` 상수를 그대로 공유하면서, 카드 10장을 위해 `sp`(협찬 게시물 수) 서브쿼리가 **계정 6.6천 개 전체를 집계**하고 있었다(test DB EXPLAIN ANALYZE 실측: 전체 158ms 중 121ms = 77%가 이 노드 — `account_content_series` 77,943행 Seq Scan ⋈ `content_analyses` 48,072행 Seq Scan → HashAggregate 5,815행). 원인은 상수 공유 자체 — 발굴 목록은 모수 전체를 필터링해야 하므로 전체 집계가 맞고, 핸들 지정 조회만 좁히면 된다. **결정**: `FROM_JOINS`를 `FROM_JOINS_TEMPLATE`(`sp` 서브쿼리 안쪽에 `%s` 슬롯 1개)로 바꾸고 `FROM_JOINS`=`formatted("")`, `FROM_JOINS_BY_HANDLES`=`formatted("WHERE s.account_handle IN (:handles)")`로 분기 — **문자열 복제 대신 템플릿 1개**로 둔 이유는 두 경로가 나머지 조인(ip·cp·br)을 계속 공유해야 표류하지 않기 때문이고, 발굴 목록 경로의 SQL은 `%s`→`""`라 **기존과 바이트 동일**(소스 diff로 유일 차이가 `%s` 삽입뿐임을 확인)이라 회귀 여지가 없다. 안전성 논거: `sp`는 `su.handle`에 LEFT JOIN되고 `su.handle = a.handle`이므로 바깥이 `a.handle IN (:handles)`로 고정하는 경로에서는 나머지 계정의 집계 행이 애초에 조인되지 않는다 — 결과 동일·스캔만 축소. `:handles`가 서브쿼리 안팎에 두 번 나오지만 JdbcClient 명명 파라미터는 이름 기준이라 바인딩 1개로 양쪽에 들어간다. **실데이터 검증(test DB)**: ① 무작위 10핸들 표본 30회 + 협찬 상위 20계정 + 협찬 0건 20계정 + 존재하지 않는 핸들 혼합에 대해 카드 전체 행을 `EXCEPT ALL` 양방향 대조 — 차이 0, ② 전 계정 `sponsored_count` 전수 대조 6,584/6,584 일치, ③ 발굴 목록 경로는 SQL 무변경. **효과**: `findCardsByHandles` 117ms → **1.9ms**(3회 중앙값, 계획이 Seq Scan 2개 → `idx_account_content_series_handle` Bitmap Index Scan + `content_analyses_pkey` Nested Loop로 전환). 회귀 테스트 2종 추가(부분집합·단건에서도 협찬 수가 전체 집계와 동일 / 핸들 지정 경로가 ip·cp·sp 보강 필드를 그대로 채움) — 기존엔 `findCardsByHandles` 커버리지가 0이었다. 2026-07-30 HikariCP 풀 고갈 장애(PR #268, `findSimilarHandles` 1611ms→753ms) 조사에서 함께 나온 잔여 항목 | [V1InfluencerDiscoveryRepository.java](was/src/main/java/com/celfit/was/v1/influencer/V1InfluencerDiscoveryRepository.java) |
| 2026-07-31 | **test 도메인 액세스 로그 신설 — Caddy 로거는 사이트 블록 단위라 "다른 블록 로그에 남는다"가 성립하지 않는다** — 07-30 test HikariCP 풀 고갈(PR #268) 조사에서 사건 시각 요청을 재구성하려다 발견: `caddy.d/test-api.caddy`에 `log` 지시어가 아예 없었고, 운영 `access.log`(6.8MB, 사건 시각 포함) 전수 집계에서 `host=dev-api.hypenow.io` **0건**이었다 — 로테이션 유실이 아니라 처음부터 미기록. `caddy adapt` 실측으로 구조 확인: 어댑터가 사이트별 로거를 만들고 `srv0.logs.logger_names`로 host→logger를 매핑하므로(`api.hypenow.io→log0`, `dev-api→log1`), `log`가 없는 블록은 액세스 로그 자체가 생성되지 않는다. **결정**: 운영 블록과 같은 JSON+`format filter` 형식(자격증명 헤더 4종 삭제)으로 test 블록에 추가하되 **출력 파일은 분리**(`/var/log/caddy/test-access.log`) — 한 파일에 섞으면 운영 로그를 볼 때마다 host로 걸러야 하고 운영의 롤링 창(약 25일)이 test 트래픽에 잠식된다. **공용 스니펫으로 묶지 않는다**: 본 Caddyfile은 main 배포로만, `caddy.d/*`는 test CD로 따로 서버에 도달하므로 스니펫 참조는 "정의가 아직 없는 서버"에서 reload를 깨뜨린다(caddy.d 분리 원칙의 필연적 대가로 중복 수용). **디스크**: 로테이션은 Caddy 내장 `roll`(logrotate 불필요, 롤 파일 gzip)이고 캡은 운영(50MiB×5)보다 작은 **10MiB×5(<60MB)** — 운영 실측 약 10MB/일(6.9MB/16.4h)에 test는 CD 헬스체크·수동 검증뿐이라 훨씬 적어 수십 일치가 남는다. 두 파일 합쳐 최악 310MB로 96G(63% 사용)·디스크 85% 알람에 무영향. compose 변경 불필요(caddy의 `./logs/caddy:/var/log/caddy` 바인드를 운영·test가 공유). 부수 조치로 `deploy/logs/`를 `.gitignore`에 추가(클라이언트 IP 포함 파일의 실수 커밋 차단). **검증**: `caddy validate` 통과 + 로컬 컨테이너 실동작 — 두 host로 요청을 넣어 각각 `access.log`/`test-access.log`에만 기록되고 `Cookie`·`X-Xsrf-Token`이 필터로 제거됨을 확인(평문 유출 0건). **잔여 관찰**: 액세스 로그가 아닌 **에러 로그**(기본 로거→stderr→docker json-file)는 `format filter`가 적용되지 않아 502 등에서 `X-Xsrf-Token`이 평문으로 남는다(Cookie·Authorization은 Caddy가 기본 REDACTED). 운영·test 공통의 기존 동작이며 세션 쿠키 자체는 가려지므로 이번 변경 범위 밖으로 둠 | 본 행 + [deploy/README.md §9-1](deploy/README.md) |
| 2026-07-31 | **Caddy 에러 로그의 자격증명 헤더도 가린다 — 기본 로거 전면 변경이 아니라 `http.log.error` 네임스페이스만 분리해서** — PR #271 작업 중 발견: 사이트 블록 `log`의 `format filter`는 **액세스 로그에만** 걸리고, 업스트림 dial 실패(502) 등에서 나오는 `http.log.error.*` 항목은 기본 로거를 타고 stderr → docker json-file로 나가 필터를 안 받는다. `Cookie`·`Authorization`은 caddy가 기본 `REDACTED` 처리하지만 커스텀 헤더는 평문으로 남는다. **조사 중 더 심한 결함을 함께 발견**: 필터 목록에 `X-Api-Token`(`/crawler/api/manual-discoveries`의 인증 헤더 — `ManualDiscoveryController`가 `@RequestHeader("X-Api-Token")`로 검증)이 아예 없어 **에러 로그뿐 아니라 액세스 로그에도, 502뿐 아니라 성공한 전 요청에** 평문으로 남고 있었다(로컬 실측 재현). 세션과 무관한 단독 정적 시크릿이라 이것만으로 등록 API 호출이 가능해, 원 신고건(`X-Xsrf-Token`)보다 심각도가 높다. **결정**: ①현행 유지 기각 — 액세스 로그 주석이 이미 "자격증명 헤더는 로그에 절대 남기지 않는다"를 불변식으로 선언해 놓고 한 경로에서만 성립하는 상태였고, `X-Api-Token` 건은 심각도 자체가 방치 불가. ③was의 CSRF 헤더명 변경 기각 — `X-XSRF-TOKEN`은 Spring 기본값이자 프론트가 의존하는 관례고, 헤더명을 바꿔도 다음 커스텀 헤더에서 같은 문제가 재발하며 `X-Api-Token`은 해결되지 않는다(원인이 아니라 증상을 옮기는 수정). ②채택하되 **기본 로거에 직접 필터를 거는 방식은 기각** — 실측상 `log { format filter ... }`는 기본 로거를 쓰는 caddy 자체 로그(기동·tls·admin)까지 출력을 JSON→console(ANSI 색상 포함)로 전면 변경한다. 대신 `log errors { include http.log.error; format filter { wrap json ... } }`로 에러 네임스페이스만 분리해 필터를 걸어 **기존 stderr JSON 형식을 그대로 유지**. `log default { exclude http.log.error }`는 실측상 caddy adapt가 자동 추가해 산출 JSON이 동일(v2.11.4)하지만, 이 필터의 유일한 실패 모드가 "기본 로거에 안 걸러진 사본이 함께 남는 것"이라 버전 의존을 없애려 명시했다. 액세스 로그 필터에도 `X-Api-Token` 추가(총 5키). 전역 옵션이라 `caddy.d/*`의 test 도메인도 같은 기본 로거를 타 한 곳으로 운영·test가 함께 덮인다 — `test-api.caddy`는 무수정(PR #271과 파일 충돌 없음). 검증: `caddy validate` 통과 + `caddy adapt` 로거 구조 확인 + `caddy:2-alpine` 컨테이너에 3경로 502 유도 실주입 → 시크릿 5종(`X-Xsrf-Token`·`X-Api-Token`·세션쿠키·XSRF쿠키·Bearer)이 stderr·access.log 양쪽 모두 평문 0건, 에러 라인 JSON 1줄·ANSI 0·중복 0, 기동 로그 JSON 유지. **잔여(미수정, 별건)**: `/internal/ons-relay/<토큰>`은 토큰이 **URL 경로**에 있어 헤더 필터 대상이 아니며 두 로그의 `uri` 필드에 평문으로 남는다(실측 확인). 헤더 방식으로 옮기거나 `request>uri` regexp 필터가 필요 — OCI ONS 구독 설정 변경을 수반해 이번 범위에서 제외 | [deploy/Caddyfile](deploy/Caddyfile) |
| 2026-07-31 | **모니터링 프로필 이미지 결함 2건 해소(트랙 KK) — monitoring 자체 아카이브 잡 신설 + 업스트림 스킴 검증** — 07-31 모니터링 QA 실측(§6-4)으로 결함 2건을 확인. **결함①**: `TargetRepository.findActive()`가 `WATCHING`·`TRACKING`만 조회해 CANCELED/EXPIRED 종료 target은 스윕에서 영구 제외되고, 그 순간부터 `profile_meta.profile_image_url`이 다시는 갱신되지 않아 인스타 CDN 서명 만료(~4일) 후 영구히 깨진다 — 등록 목록은 종료돼도 삭제되지 않아 유저가 계속 열어본다. **기존 자산(트랙 J `ImageArchiveJob`) 재사용을 기각한 정량 근거**: 그 잡의 프로필 후보군은 `v_accounts`(크롤러가 뷰티 인플루언서로 판정·분석 중인 계정)뿐인데 monitoring은 유저가 임의 IG 계정을 등록·추적하는 기능이라 두 집합이 겹칠 구조적 이유가 없다. test `profile_meta` 19건 실측: `image_assets(kind='profile')` 겹침 0/19(테이블 자체 0행 — 아카이브 잡 미실행), analysis `accounts` 미러 겹침 10/19(53%). 운영 monitoring DB는 target 0건(실사용 유입 전), 운영 `image_assets`는 profile 7,116 / thumbnail 70,535건으로 정상 가동 중. **표본(19건)이 작아 53%는 일반화 불가하지만, "원리적으로 전수 커버가 불가능하다"는 결론은 표본 크기와 무관** — 후보군 확장으로는 결함①을 해소할 수 없다는 판단의 근거. **핵심 설계 판단**: 아카이브 대상을 `target`이 아니라 `profile_meta` 전체로 잡는다 — `profile_meta`는 username PK로 target 상태와 무관하게 영구 존속하므로 `findActive()`를 전혀 건드리지 않고도 종료 항목이 자동으로 커버된다. **기각한 대안 3개**: (1) analytics `ImageArchiveJob`이 monitoring DB를 읽도록 확장 — 시스템 경계 위반(analytics는 raw 읽기 + 분석 결과 쓰기 전용) + 후보군 문제 그대로 잔존. (2) 종료 항목도 주기적으로 Hiker 재조회 — 종료 캠페인이 누적될수록 Hiker 콜이 무한 증가하고 여전히 CDN 만료에 종속. (3) 만료 추정 시 was가 null 서빙(플레이스홀더) — 구현은 간단하지만 이미지가 복원되지 않아 종료된 캠페인 프로필 사진이 영구히 안 보임. **클래스 복제**: `ImageDownloader`/`ImageStore`/`ParImageStore`(~90줄, 순수 JDK)를 `com.celfit.monitoring.image`로 복제 — 계약 모듈 `contract-analysis`는 분석 결과 record·enum 전용이라 HTTP 어댑터를 넣을 수 없고 모듈 간 import는 §4-4가 금지하므로, 90줄 중복이 새 공유 모듈 신설보다 원칙에 맞다고 판단. **키 프리픽스** `monitor-profile/<username>.jpg`로 트랙 J의 `profile/<handle>.jpg`와 분리 — 같은 OCI 버킷 안에서 핸들이 겹쳐도 두 잡이 서로 덮어쓰지 않게 한다. 실행은 별도 크론이 아니라 `DailySweepJob` 종료 직후(finally, 스윕 결과와 격리), PAR 미설정 시 no-op, 건 단위 catch-log-continue. **결함②**(스킴 검증, 이 세션 이전에 별도 커밋·PR #277로 develop 머지 완료): Hiker 업스트림이 `"exception://"` 같은 무효 스킴을 `profile_pic_url`로 보낸 사례를 실측 확인(`cherish__sy`) — 저장·서빙 양쪽에서 http(s)만 통과시키도록 검증하고, 계정 모드 `upsert`의 `profile_image_url`을 POST 모드 `upsertOwnerFromPost`가 이미 쓰던 `COALESCE(EXCLUDED, 기존값)` 보존 시맨틱으로 통일 — 업스트림이 일시적으로 쓰레기를 줘도 마지막 유효 이미지가 남게 했다. 기존 오염행은 마이그레이션으로 NULL 정정 | [specs/2026-07-31-monitoring-profile-image-design.md](docs/superpowers/specs/2026-07-31-monitoring-profile-image-design.md) |
| 2026-07-31 | **계정 하입 스코어 고정 분모 도입(트랙 Z 후속) — 수집 누락이어도 감점한다** — test 스택 실측(`ynp.ny` 2건 7위·`sunyvvin` 1건 8위·`zero_lyrical` 1건 12위)으로 최근창 점수산출 콘텐츠가 1~2건뿐인 계정이 상위권에 섞이는 결함(`docs/hype-score.md` §7 "계정 점수 표본 하한 없음")을 확인. 원인 실측: 창이 12로 꽉 찬 계정 6,350개 중 2,633개(41%)가 점수산출 콘텐츠 <12건이고, 결손의 99.9%가 `likes`/`comments_count` NULL(크롤러 수집 누락). **결정**: 원인이 수집 누락이어도 감점한다 — 점수산출 불가 게시물은 인플루언서 상세 화면(최근 12개 카드)에 애초에 뜨지 않으므로 유저 입장에서 "1개만 올린 계정"과 구분되지 않는다. 화면·점수 정합성을 원인 규명보다 우선한 결정. **구현**: `v_account_summaries.avg_hype_precise_raw`를 `avg(콘텐츠 출력매핑 점수)`(분모=창에 실제로 든 콘텐츠 수)에서 `sum(...) / analytics.recent-window`(분모=창 크기 고정, 리터럴 대신 기존 설정 키 재사용)로 교체 — `sum()`이 NULL을 무시해 점수산출 불가 콘텐츠는 분자에 0 기여로 자연 감점되고, 창 전체 NULL이면 `sum()`도 NULL이라 "창 전체 점수 불가 → NULL" 계약은 유지된다. 분모가 바뀌어 raw 분포가 하향 이동해 계정 소수 앵커 재적합(`1.4856/23.6566/56.3961/77.0479` → `1.2417/19.4383/52.2401/74.0179`). `avg_hype_score`(bigint)·`avg_hype_raw`(구, 값·의미 불변 동결·다음 릴리스 DROP 대상)는 무변경. 회귀 테스트 3종 추가(`10_account_score_rescale.test.sql`) — 핵심 회귀는 개별 콘텐츠 점수가 동일한 1건 vs 12건 계정을 만들어 구 코드(avg)로는 완전히 동점(raw 66.3988... 동일 → 91.0525 동일)이었을 것을 테스트 추가 전 수동 확인 후, 신 코드에서 뚜렷한 격차(18.2545 vs 91.0525, 약 4.99배)를 단언. 마이그레이션 없음(새 컬럼 없음, 함수 COALESCE 기본값·뷰 SQL만 교체) | [specs/2026-07-31-account-score-fixed-denominator-design.md](docs/superpowers/specs/2026-07-31-account-score-fixed-denominator-design.md) |
| 2026-07-30 | **staging→main 승격(PR #247) — BEHIND 교착이 반대 방향에서 재현 + CD 대기 취소 함정 재확인** — PR #247 머지(main `e95ef742`)로 모니터링 v3(#223)·뷰티 판정 품질(#216)·미러 성능(#221)·하입 정렬키(#222)·저장 정렬 결정성(#219)·랜딩 나노밴드(#181)·캠페인 seedingCount(#241) 외를 운영 승격. **BEHIND 재발**: 같은 날 앞선 "staging 룰셋 분리" 결정(아래 §, `protect-staging` strict 해제)은 develop→staging 방향만 풀었을 뿐 `protect-release-branches`(develop·main)의 strict는 그대로라, **승격 방향(staging→main, PR #247)에서 같은 형태의 BEHIND가 재현**됐다 — 구조가 동일하다: 승격마다 대상 브랜치에 새 머지 커밋이 하나 생기고, 그 커밋이 소스 브랜치엔 없어 다음 PR을 strict가 막는다. 해소는 같은 패턴(#227)을 main 쪽에 재적용한 백머지 PR #249(`origin/main`을 staging에 `--no-ff` 머지, **내용 변화 0**) — **승격 1회당 반복될 구조**라 재발 시마다 같은 백머지로 대응하면 되지만, 반복 비용을 인지하고 있을 가치가 있어 기록. **CD 대기 취소 함정 재확인**: 머지 직후 자동 트리거된 CD가 `concurrency: deploy-server`(`cancel-in-progress: false`) 그룹에서 **대기 중 취소**됐다 — `cancel-in-progress: false`는 "실행 중인 잡을 안 죽인다"는 뜻이지 "취소되지 않는다"는 보장이 아니다: GitHub Actions는 그룹당 **대기 슬롯을 1개만** 유지해, 뒤에 새 실행이 큐잉되면 앞서 대기 중이던 실행이 조용히 취소된다(deploy/README.md §12 "주의(함정 3건)"에 이미 07-20 사례로 기록된 것과 같은 기전 — 오늘은 그 메커니즘의 `cancel-in-progress: false` 세부를 실측으로 재확인). 수동 `gh run rerun`으로 재실행해 이미지 4종 빌드·push + 서버 배포 전부 success(`deploy-was-4` 롤링 교체). 운영 Flyway 적용도 함께 확인: analysis **V49**(account summaries avg hype raw)·crawler **V22**(beauty judgment evidence) 둘 다 success | [deploy/README.md §5-1·§12](deploy/README.md) |
| 2026-07-30 | **POST 등록 캠페인의 `profile_meta`를 단건 응답 owner 파싱으로 채우기로 결정(트랙 II)** — POST(게시물 URL) 모드 등록 계정은 계정 갈래(`saveAccount`→`profileMeta.upsert`)를 영구히 안 타서 `profile_meta` 행이 아예 안 생겨 was 서빙에서 `profileImageUrl`이 항상 null이었다. 단건 응답(`/v2/media/by/code`)의 `user.full_name`·`user.profile_pic_url`이 이미 응답에 실려 오는데 `HikerClient.toPost`가 버리고 있던 것을 확인 — **Hiker 콜을 추가하지 않고 파싱만 넓혀 해결**(제로 콜 원칙). 스윕이 매일 이 경로(`SnapshotWriter.savePost`)를 타므로 인스타 CDN 서명 만료(~4일)도 부수 효과로 자동 갱신된다. `lastUploadedAt`은 **의도적으로 제외** — 단건 응답에 없는 필드라 채우려면 게시물 열거(+2콜)가 필요해 제로 콜 원칙과 충돌, POST 전용 계정에서 계속 null로 남는다. `followers`도 같은 이유(프로필 콜 +1)로 처음엔 제외했으나 **07-31 사용자 결정으로 뒤집어 수집하기로 변경** — 단 매일이 아니라 `profile_snapshot` 행이 아직 없는 계정만 프로필 1콜로 **최초 1회**만 채운다(was가 서빙하는 followers는 시계열이 아니라 최신 단일값이라 매일 갱신의 실익이 없다 — 계정당 평생 약 1콜, 기존 등록분도 다음 스윕에 한 번씩 채워진다). 이 조회는 **best-effort**라 실패해도 캠페인 hidden 전이에 영향이 없다 — POST 등록분의 생존 판정은 추적 게시물 단건 콜 하나라는 기존 계약을 유지해, 프로필 조회 실패가 멀쩡한 캠페인을 죽이는 새 고장 경로를 만들지 않는다 | [docs/tracks/II-POST-등록-프로필-메타.md](docs/tracks/II-POST-등록-프로필-메타.md) |
| 2026-07-30 | **콘텐츠 출력 매핑 + 하입 스코어 소수점 노출 (트랙 Z 재재후속) — 콘텐츠도 척도 상단이 비었던 문제를 계정과 다른 방식(정렬 키 분리 아니라 표시값 자체를 소수화)으로 해소** — 랭킹 경로(`is_beauty AND (metric_timeliness='timely' OR NULL)`) 실측(n=5,683)에서 `contents.hype_score` 분포가 p05=5·p50=23·p90=44·p99=60.8·**max=76**임을 확인 — 1등이 76점에 그쳐 0~100 척도의 77~100 구간이 미사용. 원인은 §9-6과 같은 구조: 콘텐츠 Q 앵커가 **전체 서빙 코퍼스**(n=117,600)에 맞춰졌는데 실제 노출은 그보다 훨씬 좁은 랭킹 경로 모수뿐이라, 좁혀 보면 낮은 쪽에 몰린다. **결정 1(출력 매핑)**: 타입 무관 단일 출력 매핑 `analytics.hype_score_output(raw)` 신설(같은 4점 구간선형, 앵커 `5/23/44/60.8` — 랭킹 경로 실측값 그대로) — 타입을 다시 안 나누는 이유는 타입 정규화를 Q 앵커가 이미 끝냈기 때문(재분기하면 v3가 확보한 타입 중립이 도로 깨진다). **결정 2(소수점 노출)**: 정수 반올림을 그대로 출력 매핑에 통과시켰다면 §9-6과 같은 동점(랭킹 경로 n=5,683이 계정 모수 n=6,505와 비슷한 규모라 콘텐츠도 같은 함정)이 재발했을 것 — 그래서 표시·정렬을 소수 4자리(반올림 아니라 절사)로 바꿔 동점 자체를 없앴다(계정처럼 "정렬 키만 분리"하는 우회보다 단순). `analytics.hype_score()`를 `hype_score_raw`(반올림 전 연속값, 신설)와 `round(...)::bigint`로 분리해 **값·의미 100% 항등**을 유지(리팩터). `contents.hype_score_precise`·`account_summaries.avg_hype_score_precise`(둘 다 numeric, `round(...,4)`, Flyway `V20260730122340__hype_score_precise_columns.sql` — ADD COLUMN만, 맨 끝 추가) 신설, contract-analysis `Content.hypeScorePrecise`·`AccountSummary.avgHypeScorePrecise` 동봉. **결정 3(계정 앵커 재분리)**: 콘텐츠 출력 매핑이 붙으면 계정 raw 평균(`avg_hype_precise_raw`, 창 콘텐츠의 `hype_score_output(hype_score_raw(...))` 평균)의 분포가 완전히 달라져 앵커 재적합이 필요했다 — `hype_account_score`(구)의 기본값을 직접 바꾸는 대신(그러면 `avg_hype_score`(bigint)가 정의상 같이 바뀌어 "값·의미 불변" 계약 위반) 완전히 새 함수 `analytics.hype_account_score_precise`(앵커 `1.4856/23.6566/56.3961/77.0479`, 새 키 `hype-anchor-acct-precise-*`)를 신설해 분리 — 계약 위반 없이 재적합하려면 함수 자체를 나눠야 한다는 게 이번 세션의 핵심 판단(문구만 보면 "기존 함수 앵커 재적합"이 자연스러워 보이나 하드 제약과 충돌해 기각). was는 `/v1/contents`·`/v1/influencers?sort=hype`의 표시·정렬 전부를 `hype_score_precise`·`avg_hype_score_precise`로 전환(`ContentCard.hypeScore`·`InfluencerCard.hypeScore` 타입도 Long/Integer→BigDecimal). AccountAdCanon `PROMPT_IRRELEVANT_COLUMNS`에 `avg_hype_score_precise` 추가(카피 프롬프트 무관 컬럼 자동 유출 재발 방지, email·avg_hype_raw와 동일 패턴). SQL 하니스에 출력 매핑 단조성·앵커점·클램프·NULL 규칙·구신 컬럼 일관성·동점 제거 핵심 회귀(`02_hype_output_mapping.test.sql`, `02_serving.test.sql`, `10_account_score_rescale.test.sql` 확장) 추가. **다음 릴리스 드롭 대상**: `contents.hype_score`·`account_summaries.avg_hype_score`·`avg_hype_raw`(값·의미 불변으로 유지했으나 참조 코드가 끊기면 불필요) | [specs/2026-07-30-hype-score-v3-decay-after-mapping-design.md §10](docs/superpowers/specs/2026-07-30-hype-score-v3-decay-after-mapping-design.md) · [docs/tracks/Z-하입-스코어-v3.md](docs/tracks/Z-하입-스코어-v3.md) |
| 2026-07-30 | **모니터링 감지 이중 추적 배제 — hidden·error 상태도 배제 대상에 포함(트랙 FF)** — 프론트 계약 §6.25가 요구하는 "같은 유저의 이중 추적 방지"를 감지 경로(`firstDetection`)에 구현하면서, "활성"의 범위에 `tracked_hidden_at`(hidden)·`fetch_failing`(error) 신호가 세팅된 target도 포함하기로 확정(사용자 결정). **근거**: v2.2부터 hidden·error는 `status`(WATCHING/TRACKING)를 바꾸지 않는 신호일 뿐이라 그 행은 여전히 "이 유저가 이 게시물을 추적 중"이라는 사실 자체는 유효하다 — 비공개 전환으로 hidden된 행이 재공개 시 수집 성공으로 자동 복귀하는데, 그 복귀 이전 창에서 감지가 같은 게시물을 새 target으로 잡으면 복귀 순간 이중 추적이 된다. 배제 쿼리(`TargetRepository.findTrackedShortCodesByUser`)는 `status IN ('WATCHING','TRACKING')`만 걸고 `tracked_hidden_at`·`fetch_failing`은 걸지 않는 것으로 이 결정을 반영(추가 조건 자체가 없어 자연히 대칭). 알려진 한계로 남긴 것: was의 pending 행(등록 접수됐으나 monitoring target 미생성)은 monitoring이 was DB에 접근하지 않아 볼 수 없고, 등록 접수 직후 수 분 창에서는 감지가 같은 게시물을 잡을 수 있다(수용된 한계) — [monitoring-was-contract.md](docs/contracts/monitoring-was-contract.md) v2.3 |
| 2026-07-30 | **게시물 캡션 보존 (트랙 HH) — 파싱 부재였지 유실이 아니었다** — 착수 전제("게시물 캡션 원문이 DB에 없다")를 실측으로 뒤집었다: 캡션은 raw jsonb(`raw_media_page`·`raw_profile`)에 그대로 있었고, 없던 것은 도달 경로였다 — `MediaItemExtractor`가 `unwrapMedia()`로 미디어를 정규화하면서도 캡션 필드를 파싱하지 않아 값을 만들어보지도 못하고 버리고 있었다. 조사 초기엔 `raw_post_detail`(0행)과 `raw_discovery_post`(`origin='DISCOVERY'`만 커버)를 조인해 "캡션이 DB에 없다"고 오판했었다 — 재발 방지로 §3에 "캡션을 찾을 때" 항목을 신설. **결정**: 추출기를 정본 파서로 삼아 세 소스(릴스 jsonb·피드 jsonb·SELF_GQL 내장 타임라인)에서 캡션을 뽑아 신설 `content_caption`(`content_caption` 마이그레이션, content_id PK·최신 1건·source provenance, 실측 96MB)에 보존하고 라이브 수집(`CollectJob`·`ReelsJob`)에 배선, 이미 적재된 원형은 `CAPTION_BACKFILL` 원샷 잡(인스타 재호출 0회·라이브와 동일 파서·`app_setting` 워터마크로 재개 가능)으로 소급 적재. `content`에 컬럼을 직접 붙이지 않은 이유는 148,559행 UPDATE의 블로트·TOAST 팽창을 피하기 위함. 압축·보존기간 제한을 두지 않은 이유는 pglz 실측 이득이 7%뿐이라 관리 비용 대비 실익이 없어서. 운영 실측(07-30)으로 **어떤 analytics 뷰도 읽지 않는 현역 소스 `HIKER_V1_MEDIAS`**(9,691행·1GB·07-18~현재)를 발견 — 여기 갇힌 FEED 캡션 약 6,240건(표본 40건 중 33건=82.5%)을 재크롤 비용 없이 건졌다. 참조 코드가 없는 `raw_post_detail`(0행)은 같은 PR의 `drop_raw_post_detail` 마이그레이션으로 제거(두 마이그레이션 모두 머지 전 UTC 타임스탬프 채번(07-30)으로 재번호 — `content_caption`→`V20260730122500`, `drop_raw_post_detail`→`V20260730122600`, 순서 보존). **범위 밖으로 명시한 것**: 이 트랙은 저장 측면만 해소한다 — analytics 뷰는 여전히 jsonb를 직접 읽고 `HIKER_V1_MEDIAS`를 무시하므로 복원된 ~6,240건이 뷰·랭킹·LLM 판정에 반영되려면 analytics 트랙이 별도로 필요하다. **트랙 문자 이력**: A~Z 소진(Z=하입 스코어 v3, AA=발굴 표면 뷰티 비율 게이트가 같은 날 선점) 후 애초 AB로 이어받으려 했으나 첫 `origin/develop` 병합 시점 그쪽이 같은 날 AB~EE를 이미 채워놔 FF로 재번호(07-30 1차 develop 병합) — 이후 §5 표 분할(PR #238) 반영을 위한 2차 `origin/develop` 병합 시점엔 열린 PR #235가 FF·PR #243이 GG를 각자 브랜치에서 이미 주장 중이라 HH로 다시 재번호했다(07-30 2차 develop 병합) | 트랙 HH — [specs/2026-07-30-caption-preservation-design.md](docs/superpowers/specs/2026-07-30-caption-preservation-design.md) |
| 2026-07-30 | **모니터링 다이제스트 자정 경계 유실 — 회고 창 재계산(A안) 채택, 완료 가드 원안 기각 (트랙 GG)** — `DigestJob`이 항상 "지금의 KST 날짜"만 집계 대상으로 삼아, 본틱 09:00+따라잡기 09:10~23:50(매 10분) 사이 D일 23:50 이후~자정 전 도착한 `alarm_event`가 D일 마지막 틱엔 이미 늦고 D+1일 모든 틱은 D+1만 보므로 **영구 유실**되던 버그(PR #223 "알려진 후속" 항목)를 해소. **핵심 논거**: `occurred_at DEFAULT now()`라 D일에 속하는 이벤트 집합은 D일 자정에 확정되고 그 뒤로는 절대 늘지 않는다 — 따라서 D+1의 아무 틱에서든 D일을 한 번만 다시 집계하면 논리적으로 완결이고, 회고 1일이면 충분하다(기본값 2일의 여분은 was 다운타임 대비). **채택**: 매 실행이 최근 N일(`monitoring.digest.lookback-days`, 기본 2)의 이벤트를 범위 조회해 `occurred_at` KST 날짜별로 묶고 각 (유저, 날짜)마다 기존 멱등 upsert를 그대로 반복 — `sweep_run`은 전혀 참조하지 않는다. **기각**: ① 코드 주석이 제안했던 원안 "오늘 02:00 이후 성공한 sweep_run이 없으면 스킵" — 회고 경로 자체를 안 만들어 유실 기전을 못 막고, `sweep_run` 기록 실패·수동 스윕 시 다이제스트가 조용히 안 나가는 fail-closed 실패 모드를 새로 들인다(이번 유실 버그를 고치려다 새 유실 경로를 만드는 셈이라 기각). ② 따라잡기 틱 24시간 확장 — 회고 경로가 없으면 자정 직전 틱과 자정 사이 간극은 틱을 아무리 촘촘히 박아도 단독으로는 안 없어진다. ③ B안(09:00 본틱에서만 어제 1회 보정) — 쓰기량은 최소지만 그 실행 1회를 놓치면 어제분이 영구 유실된다. **계약 영향 없음** — `alarm_event`·`sweep_run` 스키마, monitoring↔was 계약(v2.3) 무변경. 프론트 계약 §6.32(`date`=이벤트 발생 KST 날짜)·§6.25(이틀치 안 섞임)는 날짜별 upsert로 계속 지켜지나, §6.25 "9시 이후 늦어도 그날 발송"의 실제 동작이 "23:50 이후 도착분은 다음 날 09:00에 그 이벤트 날짜(어제) 다이제스트로 반영"으로 미묘하게 달라져 문서 수정이 아닌 프론트 통지 대상으로 남긴다 — [specs/archive/2026-07-30-digest-lookback-window-design.md](docs/superpowers/specs/archive/2026-07-30-digest-lookback-window-design.md) |
| 2026-07-30 | **머지 큐 도입 후 CI 중복 정리 — develop push는 검증이 아니라 Gradle 캐시 시딩 전용으로 축소** — 머지 큐(#239)를 켠 뒤 변경 1건이 test 서버에 닿기까지 CI가 6번 돌았다(PR 2회[base 이동 재실행 포함]·merge_group·develop push·승격 PR·staging push). 이 중 **develop push 런은 merge_group이 방금 검증한 바로 그 머지 커밋을 다시 도는 순수 중복**이다(룰셋이 직접 push를 막으므로 develop에 들어오는 커밋은 예외 없이 큐를 거친다). **그런데 통째로 끌 수는 없다**: `gradle/actions/setup-gradle`은 기본 브랜치(develop) push에서만 캐시를 쓰고 PR·merge_group은 읽기만 한다 — 머지 큐 임시 브랜치(`gh-readonly-queue/...`)에서 쓴 캐시는 그 ref 스코프라 재사용되지 않으므로, push 트리거를 없애면 시딩이 끊겨 **모든 PR CI가 캐시 미스로 느려진다**(실측 1m16s→3m). **결정**: develop push에서 `test`·`sql-harness`를 건너뛰고 `testClasses`까지만 도는 `cache-warm` 잡 하나만 남긴다 — 캐시 시딩은 유지하면서 중복 검증만 걷어낸다(develop push당 약 2분+러너 1대 절감). `testClasses`인 이유는 PR이 실제로 아끼는 것이 의존성·컴파일 산출물이고 테스트 실행 결과는 코드가 다르면 어차피 재사용되지 않기 때문. **staging push는 그대로 전체를 돈다** — `cd-test.yml`이 `workflow_run(CI, staging)`을 배포 게이트로 쓰므로 여기서 줄이면 배포 전 검증이 사라진다. 승격 PR(head=develop, base=staging)의 CI도 내용상 중복이지만 `protect-staging` 룰셋의 required check라 제거 불가 | [.github/workflows/ci.yml](.github/workflows/ci.yml) |
| 2026-07-30 | **§5 작업 트랙 표 → 트랙당 파일 분할(`docs/tracks/`)** — 여러 세션이 동시에 다른 브랜치에서 작업하면서 최근 머지 큐 충돌이 거의 전부 §5의 단일 표에 각자 새 행을 추가하다 생긴 both-add 충돌이었다. 표라는 "단일 슬롯"을 없애 트랙 1개=파일 1개(`docs/tracks/<트랙문자>-<슬러그>.md`)로 분할(48개 트랙: 상세 분석 작업 트랙 A~EE 39개·API 스펙 정렬 트랙 P1~P4 4개·클로즈베타 전환 트랙 CB1~CB3 3개·모니터링 트랙 MON·MON-P2 2개 — 분할 전후 트랙 개수·상태 문자열·의존·본문 전부 스크립트로 자동 대조해 유실 없음 확인). 새 트랙은 다음 미사용 문자로 파일을 새로 만들면 되고, 파일 존재 여부 자체가 문자 선점 대장이라 별도 인덱스 파일을 두지 않았다(인덱스를 두면 그것 자체가 또 다른 단일 슬롯 충돌점이 된다 — `docs/tracks/` 디렉토리 목록이 곧 인덱스). CLAUDE.md "세션 시작"도 §5 표 대신 `docs/tracks/`를 갱신하도록 문구 수정, "컨벤션"에 세션 위생 규칙 2개(작업 종료 시 draft라도 PR 즉시 오픈 — 열린 PR이 영역 점유 신호·자기 worktree/머지된 브랜치 정리) 추가 | `docs/tracks/` |
| 2026-07-30 | **캠페인 `seedingCount` 필드 추가(프론트 요청 A-1) — 트랙 EE 계약(6.25/6.31)의 소규모 확장, 새 트랙 미신설** | `app.monitoring_campaigns.seeding_count integer NULL`(`V20260730114930`, ADD COLUMN만이라 파괴적 변경 없음 — 같은 날 도입된 UTC 타임스탬프 채번 컨벤션 적용, 아래 가드 v3.2 행 참조) 추가 + POST/PATCH `seedingCount?`(0 이상 정수, PATCH는 기존 필드와 동일한 `containsKey` null 의미론) + `CampaignResponse.seedingCount`(nullable 키 항상 포함). **핵심 판단**: 이 필드는 등록된 추적 행 수(`monitoring_items`)와 무관한 **유저 선언 입력값**이다 — "시딩은 했지만 계정을 등록하지 않은 사람"은 추적 행 자체가 없어 행 카운트로는 절대 구할 수 없다. 그래서 6.25의 "서버가 하지 않는 것"(캠페인 집계는 클라 파생) 규칙과 충돌하지 않는다고 문서에 명시(집계값이 아니라 저장·반환만 하는 입력 필드) — 나중에 "서버가 집계 안 한다면서 왜 이 숫자는 받냐"는 혼동을 막기 위함. 등록 행 수보다 작은 값을 막지 않는다(40명 입력 후 43명 추가 발송이 정상 시나리오). **새 트랙을 만들지 않은 이유**: 기존 트랙 EE(was v3 표면)가 이미 구현한 캠페인 CRUD API의 필드 1개 확장이며 새 엔드포인트·새 모듈·구조 변경이 없는 순수 애드온이라 트랙 표(§5)에 별도 행을 만들 정도의 단위가 아니라고 판단 | [docs/contracts/monitoring-frontend-api-spec.md](docs/contracts/monitoring-frontend-api-spec.md) 6.25·6.31 |
| 2026-07-30 | **마이그레이션 가드 v3.2 — 신규 Flyway 마이그레이션을 UTC 타임스탬프로 채번해 번호 경합을 구조적으로 제거** — v3·v3.1(같은 날, 아래 행)은 병행 세션의 정수 연번 경합을 "잡는" 검사였지만 근본 원인은 그대로였다(V18→V19, V22→V23, PR #181까지 3연속 재발 — 여러 Claude 세션이 동시에 각자 브랜치에서 다음 번호를 집는 구조 자체가 계속 경합을 만든다). **결정**: 신규 마이그레이션은 `V<YYYYMMDDHHMMSS>__<설명>.sql`(UTC, CLAUDE.md 컨벤션 절)로 채번 — 다음 빈 정수를 아무도 "집을" 필요가 없어져 경합 자체가 사라진다. **기존 `V1`~`V49`(4개 디렉토리 전부) 파일은 절대 rename하지 않는다** — `schema_history`에 버전·체크섬이 기록돼 있어 rename하면 운영 DB 마이그레이션이 깨진다(제약 위반 시 운영 DB 파손급 사고). **정렬 전제 실측**: `V42 < V20260730112500`이 성립함을 이 저장소가 실제로 쓰는 `flyway-core` 12.4.0의 `MigrationVersion` API로 직접 확인했다(`compareTo` 음수 반환, `V07`==`V7` 선행 0 정규화도 유지) — Flyway가 버전을 정수(BigInteger)로 비교하는 한 자릿수 차이는 순서에 영향이 없다. **가드 스크립트(`check-migration-safety.sh`)는 무수정으로 호환** — `normalize_version`의 정규식과 `10#$p` 산술 둘 다 자릿수 제한이 없어 14자리 타임스탬프를 그대로 파싱한다(v3의 정수 전용 설계가 아니었다는 뜻). 셀프테스트에 정수·타임스탬프 공존/동일초 충돌 케이스 4건을 추가해 31케이스로 회귀를 막았다. `ci.yml` sql-harness의 crawler 마이그레이션 적용 순서(`sort -t V -k 2 -n`)도 순수 숫자 정렬이라 무수정으로 호환 확인 | [deploy/README.md §5-1](deploy/README.md) |
| 2026-07-30 | **staging 룰셋 분리 — 승격 PR BEHIND 교착 해소를 위해 staging만 strict 해제** — 같은 날 도입한 `strict_required_status_checks_policy=true`(develop·staging·main 일괄)가 **승격 PR(head=`develop`, base=`staging`)을 영구히 `BEHIND`로 만들어 머지를 막는 교착**을 낳았다. 원인은 staging에만 있는 과거 승격 머지 커밋(#180~#226, 15개)이다 — 내용은 전부 develop에 있지만(그 커밋들 자체가 develop을 staging에 넣은 머지 커밋) 커밋이 develop에 없어 strict가 "head가 base tip을 담아야 한다"에서 걸린다. 빠져나갈 길이 없었다: PR의 **Update branch**는 develop 직접 push라 `pull_request` 규칙에 막히고(`422 Changes must be made through a pull request`), 반대 방향 PR(head=`staging`)도 staging이 develop 최신을 안 담아 똑같이 `BEHIND`. **해소는 2단계** — ① 백머지 PR #227(develop에 `origin/staging`을 `--no-ff` 머지, **내용 변화 0**)로 당장의 교착을 풀고, ② 재발을 막으려 룰셋을 쪼갰다: `protect-release-branches`(id 18823767)의 조건에서 `refs/heads/staging`을 빼 develop·main만 남기고(strict 유지), **`protect-staging`(id 20038027) 신설 — 규칙 4종(`deletion`·`non_fast_forward`·`pull_request`·`required_status_checks` 3체크)은 동일하고 `strict_required_status_checks_policy=false`만 다르다.** ①만으로는 부족한 이유: 승격할 때마다 staging에 머지 커밋이 1개씩 새로 생기므로 승격 1회당 백머지 PR 1회(CI 한 사이클)가 영구히 붙는다. **staging에서 strict를 빼도 안전한 근거**: staging에 올라가는 내용은 develop에서 이미 strict CI를 통과한 것과 동일하고, strict를 도입한 동기였던 #181류(base 이동 후 PR CI 재실행 부재로 Flyway 번호 경합 통과)는 **develop 대상 PR에서 발생하는 실패 모드**라 develop·main의 strict만으로 막힌다. 대안이었던 "staging을 required_status_checks에서 통째로 제외"는 기각 — `cd-test.yml`이 `workflow_run: workflows:[CI] branches:[staging]`으로 걸려 있어 배포 트리거가 CI 성공에 의존한다(체크 자체는 남겨야 한다. CI 트리거는 `push: [develop, staging]`이라 룰셋과 무관하게 계속 돈다) — [deploy/README.md §5-1](deploy/README.md) |
| 2026-07-30 | **발굴 목록 계정 정렬 키를 반올림 전 raw로 분리 (트랙 Z 재후속) — 표시값과 정렬 키를 나눠 상위권 순위 불변을 보장** — 계정 하입 스코어 척도 재교정(§9-4) 배포 후 발굴 목록 상위권 순서가 뒤섞이는 걸 확인. 원인: 매핑 최상단 구간 `97 + 3*(raw-a99)/(a99-a90)`이 raw 44.86~58.92(상위 1% 전체, 폭 14)를 정수 97~100 **4개 값**으로 압축(test 스택 실측: ≥97점 54개 계정에 서로 다른 점수값 4개, ≥90점 176개에 11개, 전체 6,505개에 101개) — `ORDER BY avg_hype_score DESC, handle`이 이 동점을 handle로 깨면서 상위권 정렬이 사실상 알파벳순에 지배됐다(상위 50 집합은 46/50 겹치지만 순서가 여러 자리 뒤바뀜). 단조성(순위 불변)은 반올림 **전** raw에서는 여전히 성립 — 기존 하니스 5개 단언이 전부 단조성(`<=`)만 봐서 동점(`=`)을 못 잡았다. **결정**: 표시 점수는 정수 `avg_hype_score` 그대로 두고 **정렬만 반올림 전 raw 평균**(`v_account_summaries.avg_hype_raw`, 신설)을 쓰게 분리 — 표시가 동점이어도 순서는 정확하고, 계정이 늘어도 재발하지 않는다. `avg_hype_raw`는 `10_account_detail.sql` base CTE에서 한 번만 계산해 `avg_hype_score = hype_account_score(avg_hype_raw)`와 신설 컬럼 둘 다에서 재사용(매핑 함수를 두 식으로 따로 부르면 표류할 수 있어 방지). Flyway V49·contract-analysis `AccountSummary.avgHypeRaw` 동봉 — **셋 다 맨 끝(email 뒤)에 추가**했다(`CREATE OR REPLACE VIEW`는 기존 컬럼 사이에 새 컬럼을 끼워 넣을 수 없고 끝에 추가하는 것만 허용 — avg_hype_score·email도 그 시점엔 항상 끝에 추가돼 온 선례와 일치). was `V1InfluencerDiscoveryRepository.orderBy()`의 `sort=hype`를 `su.avg_hype_score`→`su.avg_hype_raw`로 교체, API 응답엔 avg_hype_raw 비노출(순수 내부 정렬 키). SQL 하니스에 raw 평균이 다르지만(likes 실측으로 역산해 34·36점→raw 35.0 vs 35·36점→raw 35.5) `hype_account_score`가 똑같이 85로 반올림되는 두 계정을 시딩해 `avg_hype_raw DESC` 정렬이 raw 순서를 보존하는지 단언(핸들 알파벳순은 반대). was 레포지토리 테스트도 동점 표시값+다른 raw 픽스처로 재현 — 구코드(`avg_hype_score DESC, handle ASC`)로 되돌리면 두 테스트 모두 실패함을 직접 확인. 콘텐츠 점수는 서빙 코퍼스가 110,488건이라 상위 1%가 1,100건 이상으로 정수 100개 구간에 충분히 퍼져 같은 문제가 없다 — [specs/2026-07-30-hype-score-v3-decay-after-mapping-design.md §9-6](docs/superpowers/specs/2026-07-30-hype-score-v3-decay-after-mapping-design.md) |
| 2026-07-30 | **마이그레이션 가드 v3.1 — #181 원인 정정 + ci.yml 인라인 버전 검사를 스크립트로 통합** — v3(같은 날, 아래 행) 도입 시 "버전 중복 검사가 없어서 #181이 났다"고 서술했으나 부정확했다: `ci.yml` `test` 잡에는 07-21(V35·V36 재발) 이후 붙은 인라인 검사(`ls \| grep -oE '^V[0-9]+' \| sort \| uniq -d`)가 이미 있었고, 실측 결과 **그 검사도 로직상 #181을 잡을 수 있었다**(PR CI가 `refs/pull/N/merge`를 체크아웃해 머지 트리에 V43 2개가 보였을 것). 실제 원인은 검사 부재가 아니라 **PR CI 재실행 부재** — #181 CI는 07-29 08:25Z 실행, develop이 V43-trait을 얻은 건 07-30 04:40Z, 그 사이 재실행 없이 머지됐다. **이 통합은 그 레이스를 고치지 못한다** — 고치는 건 브랜치 보호 룰셋이고 **같은 날 적용 완료**: `protect-release-branches`(develop·staging·main)에 `required_status_checks` 추가 + `strict_required_status_checks_policy=true`(머지 전 브랜치 최신화 요구), 요구 체크 3종(`Gradle 전체 테스트`·`마이그레이션 롤링 호환 가드`·`분석 뷰 SQL 하니스`). 이제 base가 움직인 PR은 `BEHIND`로 머지가 막히고 최신화 시 CI가 재실행돼 이 검사가 최신 base 기준으로 다시 돈다 — #181류를 실제로 막는 지점은 여기다. 부작용: 머지 시점 "Update branch" 필요, PR CI가 `cancel-in-progress`라 취소분은 재실행 필요 — 이 통합의 실제 이득은 검사 로직이 셀프테스트로 보호되는 단일 구현이 된 것, 인라인 검사가 빠뜨렸던 monitoring 디렉토리 포함, 선행 0 정규화(인라인엔 없었음), 그리고 base 대조 모드(PR 전용 `migration-guard`)까지 갖춘 것뿐이다. `test` 잡은 push 이벤트에서도 돌아 base_ref가 없고 checkout도 얕은 클론(`fetch-depth` 미지정)이라, 그 경로엔 git 비의존 트리 단독 검사 `check-migration-safety.sh --versions-tree`를 신설해 `check_versions`(base 쪽을 빈 목록으로 넘겨 "HEAD 트리 내부 중복" 로직 재사용, 새 로직 아님) 위에 얹었다. 셀프테스트 27케이스(기존 22 + `--versions-tree` 5) | [deploy/README.md §5-1](deploy/README.md) |
| 2026-07-30 | **마이그레이션 가드 v3 — Flyway 버전 번호 중복 검사 (base ref 대조)** — PR #181이 `V43__landing_stats_nano_band.sql`을 들고 있는 사이 develop이 `V43__trait_taxonomy_makeup_review.sql`을 선점, 머지되면 같은 버전 2개로 Flyway 기동이 거부되는 사고가 3번째 재발(V18·V43). PR 브랜치 자기 트리만 봐서는 못 잡는다 — 그 브랜치엔 V43이 1개뿐이라 충돌은 base와 합쳐질 때만 드러나므로, `check-migration-safety.sh`에 base ref와 HEAD의 트리 스냅샷을 직접 대조하는 검사를 추가(`git ls-tree`, diff 아님 — diff 기반이면 PR이 안 건드린 기존 파일과의 충돌을 놓친다). **스코프가 기존 파괴적 DDL 검사(analysis DB만)와 다르다** — 버전 중복은 어느 Flyway 인스턴스든 기동 자체를 거부시키는 별개의 실패 모드라 was·analytics·crawler·monitoring 4개 디렉토리 전부를 대상으로 한다(각각 독립 버전 공간이라 디렉토리별 독립 검사, was V1과 analytics V1은 정상). 버전 비교는 Flyway와 동일하게 숫자 기준(선행 0 정규화). 집합 단위 검사라 `--versions <base-목록> <head-목록>` seam을 신설해 git 없이 순수 로직만 테스트(`check-migration-safety.test.sh`, PR #181 재현 포함 22케이스). **한계**: base ref 스냅샷은 CI 실행 시점 기준이라, base가 바뀐 직후 곧바로 머지되는 극단적 레이스는 여전히 못 잡는다(브랜치 보호 "머지 전 최신 상태 요구" 설정이 근본 해법, 코드 범위 밖) | [deploy/README.md §5-1](deploy/README.md) |
| 2026-07-30 | **was v3 표면 트랙 구현 완료(트랙 EE, PR 대기)** — monitoring v3 프론트 계약(6.25~6.33)을 was `/v1` API 9종 + 크론 2종(다이제스트 생성·따라잡기) + 탈퇴 해지 루프로 구현. 핵심 결정: ①승인 큐를 monitoring 자동 전환으로 흡수(FE에는 승인 없는 자동 수집처럼 보임) ②프론트 상태 6종은 영속화하지 않고 monitoring 원시 상태에서 **조회 시 유도** ③다이제스트는 `alarm_event`를 유저·날짜 단위로 **멱등 재계산**(워터마크 완전 폐지 — 트랙 DD의 id 대장을 단일 원천으로 소비) ④이메일 발송은 monitoring 소유로 확정, was 발송 경로 제거 ⑤app 스키마 V13 매핑을 **V16**으로 재구성(추적·알림 실사용 테이블만 남기는 재구축) ⑥어휘 경계 매핑 — 소문자 프론트 상태 어휘 ↔ 대문자 monitoring 상태 어휘 변환은 was 경계(`MonitoringEventTypes`)에서만 수행. 3-트랙 병합(P2·알람·P1) 후 갭 파악·해소 전 과정은 [plans/2026-07-30-monitoring-v3-merge-gaps.md](docs/superpowers/plans/archive/2026-07-30-monitoring-v3-merge-gaps.md) 참조 | [plans/2026-07-30-monitoring-v3-was.md](docs/superpowers/plans/archive/2026-07-30-monitoring-v3-was.md) · [contracts/monitoring-was-contract.md](docs/contracts/monitoring-was-contract.md) v2.2 |
| 2026-07-30 | **monitoring v3 P2 표면 4종 — 계약 v1.1 (트랙 MON-P2, PR #195)** — 댓글 수집은 `/v2/media/comments` 실측으로 확정: media pk가 shortcode의 base64url 디코딩으로 산술 유도돼(검증: `DbV7LgZsKG8`→`3951324523536622012`) 별도 저장 불필요, 작성자 본인 답글은 응답 동봉 `preview_child_comments[].is_created_by_media_owner`(∨ username 일치 — coauthor 게시물 대비)로 판정해 답글 콜 0. 상한은 확장 요구의 20건 대신 **15건**(Hiker 페이지 단위 = 1콜, 프론트 표시 8건 커버) — 수집 모수가 IG 랭킹 정렬(최신순 아님)임을 계약에 명시하고 최신순 정렬은 was 응답 단계로. share 해소는 등록 API 통합 제안 대신 **분리된 전처리 API**(`POST /api/share/resolve`, Hiker `/v2/media/info/by/url` — 200/404/400이 계약 어휘와 1:1) — 병행 알람 트랙이 재작성 중인 등록·승인 파일 4종 무변경으로 충돌 회피(실제 share 토큰 실측은 샘플 확보 불가로 잔여 — 계약 §2-6 명시). `profile_meta`는 스냅샷과 달리 이력 없는 최신 1행 upsert(`SnapshotWriter.saveAccount` 트랜잭션 동승, 빈 열거가 last_uploaded_at을 안 덮게 COALESCE). 신규 표면 was_reader SELECT는 V2 default privileges로 자동(MigrationTest 실검증). 부수: 테스트 클래스 순서 의존(was_reader 역할 생성 시점) 제거 | [contracts/monitoring-was-contract.md](docs/contracts/monitoring-was-contract.md) v1.1 · [plans/2026-07-28-monitoring-hiker-findings.md](docs/superpowers/plans/2026-07-28-monitoring-hiker-findings.md) §10 |
| 2026-07-30 | **성과 요약 통계 왜곡 가드 — 운영 배포 완료, 4라운드 staging 실측 결론 (트랙 Y 마무리)** — main `5aa340cc` CD 성공으로 뷰 적용·미러까지 완료. 4라운드에 걸친 test 실측이 확정한 핵심 원칙: **금지는 지시가 아니라 입력 제거로 강제해야 지켜진다** — 입력에서 아예 제거한 always-strip 컬럼·`TOO_LONG`은 4라운드 전부 누출 0건인 반면, "지시만" 걸었던 항목은 3라운드 연속 위반(`LONG_SPAN`이 프롬프트 지시만으로 방향성 단정을 새어 보냄 → 폐지). 측정된 개선: 창 90일 초과 위반 3/10→1/10(값 제거 확대 후), 성과 요약 수치 인용 20%→0/30(`LlmGuard` 스코프 분리 후). 그리고 **핵심 재정의가 30배 큰 모수를 찾아냈다** — 표본 부족을 계정 단위로 재면 0.9%(65개)뿐이지만 지표(조회수) 단위로 재면 27.9%(1,965건)로, 초기 감으로 잡았던 문제 크기가 실제보다 30배 작게 보이고 있었다. 재생성 7,033건은 `copy_version` 게이트 단독 대상, 새벽 정기 배치(`AccountAnalysisJob`, KST 07:00) 실측 확인 완료 — 오늘 밤 자동 전량 처리 예정. 정정 한 건: 스펙 §7의 "뷰는 수동 런북 적용" 전제는 틀렸다 — main 배포 CD가 07-20부터 뷰를 자동 적용한다(가드 자체는 CD 실패·수동 미러 선행·test 경로에서는 여전히 유효, 스펙 §7에 07-30 정정 서브섹션 추가) | 트랙 Y — [specs/2026-07-30-perf-summary-statistical-guards-design.md](docs/superpowers/specs/2026-07-30-perf-summary-statistical-guards-design.md) §7 |
| 2026-07-30 | **`content_metric_snapshots` 미러 중단 + 레거시 `/api` 2개 제거 — 미러 12분 30초 → 약 5~6분** — 미러 잡 단계별 실측에서 이 테이블(약 70만 행)이 6~7분, 즉 절반 이상을 차지했다. 부수 효과로 미러가 길면 다른 세션의 develop→staging 승격이 test-analytics 컨테이너를 재생성해 미러 잡을 죽이는 충돌 창도 길어진다(07-30 2회 발생). **소비자 전수 조사**: 미러가 있어야만 동작하는 소비처는 was 레거시 `GET /api/contents`(D3·H)·`GET /api/posts/{shortCode}?endDate=`(D) 둘뿐이었고(was는 analysis DB만 읽는 §4-4 원칙상 대체 소스가 없다), analytics 어드민 4곳은 repoint 가능했다. monitoring 모듈은 무관 — 데이터소스가 `monitoring` DB 하나뿐이고 자체 `profile_snapshot`·`post_snapshot`(`captured_on date`, 미러의 `captured_at timestamptz`와 계약도 다름)에 HikerAPI 응답을 직접 upsert한다. **소비자 부재 근거 3중**(07-27 `/api/influencers/{handle}` 제거 때는 로그 실측이 불가했으므로 그 선례보다 근거가 강하다): ①운영 caddy 액세스 로그 3,723건 중 `/api/*` 0건(단 로그 커버리지 7시간 — 이것만으론 약한 근거) ②celfit-front 전 브랜치(main·develop·feat 2개) grep 0건 + `src/lib/api/config.ts`가 `API_PREFIX="/v1"` 하드코딩이라 `/api` 경로 생성 자체가 구조적으로 불가(유일한 `app/api/**`는 Next 자체 리스트업 라우트) ③`/api/**`는 SecurityConfig 화이트리스트에 없어 `anyRequest().authenticated()` 뒤 — 인증 클라이언트는 celfit-front뿐. **`/ui` 신선도 배지 repoint의 핵심 판단**: 배지는 07-20 크롤 정지를 잡아낸 관측 신호라 민감도 손실이 허용되지 않는데, 대체 후보 `contents.metric_captured_at`이 "+3일 핀 기준이라 지연되어 보인다"는 우려가 있었다. 실측으로 기각 — 운영에서 두 값이 **마이크로초까지 동일**(`2026-07-29 19:02:28.890229+00`). 이유는 `v_pinned_metrics`(00_base.sql)의 +3일 핀이 우선순위 ①로 **성숙한 콘텐츠에만** 적용되고 3일 미경과분은 우선순위 ②("완비된 것 중 최신")로 떨어져 최신 크롤 시각을 그대로 반영하기 때문(MAX 동률 3건 중 2건이 1.7일 경과 게시물). 더 결정적으로 **핀 값은 스냅샷 `captured_at`의 부분집합이라 `MAX(pinned) ≤ MAX(snapshot)`이 항상 성립** — 대체 지표는 현행보다 신선하게 보고하는 것이 원리적으로 불가능하므로 크롤 정지를 놓칠 수 없고, 최악의 경우 오탐 방향으로만 틀린다. 민감도는 현행 이상. 쿼리도 더 빠르다(67ms vs 174ms — 5초 폴링마다 캐시 없이 호출되는 자리라 중요). 이 조사 과정에서 §4의 "미성숙 폴백은 구크롤러 잔재 5건·소멸 예정" 진술이 사실과 반대임을 확인해 함께 정정(실측 lag 3일 미만 8,729건 상시 존재). **repoint 내역**: 신선도 배지 → `max(metric_captured_at) FROM contents` / `check/coverage.sql` ASSERT → `contents WHERE metric_captured_at IS NOT NULL` / `/ui/coverage` "지표 스냅샷" 타일 → **제거**(없어진 미러를 세는 타일이므로 재정의가 아니라 삭제 — 사용자 결정) / `ops/reconcile_content_analyses.sql`은 short_code별 날짜창 내 개별 스냅샷 존재 여부를 보는 이력 조회라 핀 값 1개로 대체 불가 → 로직 보존하고 재실행 시 raw 뷰로 고쳐야 한다는 경고 주석만 첨부(1회성). **테이블 처리는 TRUNCATE + `COMMENT ON TABLE`**(V48) — 갱신이 끊긴 70만 행을 남기면 미래 세션·쿼리가 07-30에 얼어붙은 데이터를 최신으로 착각하는 footgun이 되고 디스크도 회수 안 된다(오라클 디스크 63% 상황). `DROP TABLE`은 was 롤링 창에서 구 코드가 없는 테이블을 조회할 위험이 있어 expand-contract 원칙대로 **다음 릴리스로 분리**(migration-guard도 차단). TRUNCATE는 가드의 명시적 블라인드스팟(`check-migration-safety.sh:24`)이라 CI를 통과하므로 리뷰 확인 사항. 뷰 `v_content_metric_snapshots`는 raw에 존속 — 이력 조회 재료이자 "향후 추이 그래프"(specs 2026-07-12 §52-55)의 데이터 근거이며, raw 스냅샷 누적은 계속된다. 회귀: was 429 · analytics 306 · crawler 357 · monitoring 78 = **1,170 GREEN** | [MirrorConfig.java](analytics/src/main/java/com/celfit/analytics/mirror/MirrorConfig.java) · V48__content_metric_snapshots_mirror_stop.sql · 07-27 선례 `642c1aed`가 "잔여 관찰"로 예고한 두 건 해소 |
| 2026-07-30 | **인플루언서 이메일 — LLM 대신 biography 정규식 파싱 (트랙 BB)** — 발굴 목록 `email` 필드가 P4(07-28) 이래 "크롤러 미수집(V31)" 스텁으로 항상 null이던 것을 해소. 운영 실측(계정 7,033 중 biography 보유 6,808·정규식 `[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}` 매치 2,553=37.5%, 샘플 30건 육안 확인 오탐 0건, 도메인 분포 gmail·naver 위주로 깨끗)으로 **LLM 없이 정규식만 채택** — 난독화 후보가 최대 0.75% 추정이라 회수율 이득이 비용을 정당화하지 못함. POSIX `substring(... from regex)`는 leftmost match만 반환해 "이메일 2개 이상이면 첫 번째만"이 정규식 엔진 특성상 자연히 성립(별도 로직 불필요), 후행 문장부호(`abc@gmail.com.`)도 그리디 매칭 백트래킹으로 자연 제외. `v_account_summaries.email`(뷰 SELECT 끝) + analysis Flyway V46(`account_summaries.email` ADD COLUMN, expand만) + `AccountSummary.email`(record 끝, MirrorJob·FlywaySchemaTest 위치 대조 통과) + was `V1InfluencerDiscoveryRepository`(`findCards`/`findCardsByHandles` SELECT·`CardRow.email`, `contactOpen` 필터를 죽은 `AND false` → `AND su.email IS NOT NULL`로 교체) + `InfluencerCard.email` 배선. 뷰티 필터는 `v_recent_content`(QUALIFIED ∧ beauty ∧ ¬beauty_company)가 이미 적용 중이라 무변경(확인만). 범위 밖: `/v1/influencers/{id}` 상세(`InfluencerProfileResponse`, 스펙 6.4)의 email은 이번 트랙에서 미배선 — 발굴 목록(6.21) 카드만 대상 | 트랙 BB — [specs/archive/2026-07-30-influencer-email-from-bio-design.md](docs/superpowers/specs/archive/2026-07-30-influencer-email-from-bio-design.md) |
| 2026-07-30 | **계정 뷰티 판정 품질 — 판정 캡션은 실측 폴백, 프로필 리스냅샷은 폐기 (트랙 AB)** — 서빙 중 뷰티 인플루언서 7,095개 중 게시물 뷰티 비율 0%인 886개, 스팟체크 20개 중 17개(85%)가 오판(육아·다이어트·여행·피트니스 계정이 뷰티로 분류)임을 실측으로 확인하고 네 가지를 결정했다. ①**판정 캡션은 `raw_media_page`(HIKER_V2_CLIPS) 실측을 폴백으로 쓴다.** 프로필 리스냅샷(SELF_GQL 재수집)은 미판정 계정까지 커버하는 유일한 경로였지만 계정당 1콜 × 약 2만 콜이라 비용 대비 이득이 맞지 않아 **폐기했다(재도입 금지)** — 대가로 미판정 계정(전체 34%)은 이 트랙으로 해결되지 않는다. ②**캡션 재판정 경로(`findCaptionRejudgeTargets`)만 `beauty=true`를 대상으로 삼는다.** false positive 교정이 목적이며, 기존 `findRejudgeTargets`(`beauty=false`)는 그대로 둔다 — 무한 재대상은 판정 시각 갱신으로 닫힌다. ③**인스타그램 `category_name`은 금지 근거가 아니라 우선순위가 낮은 근거로 다룬다.** 프롬프트에서 비뷰티로 기울이면 게시물 수집(`findCollectTargets`)이 `beauty=true`만 대상이라 게시물이 안 쌓여 되돌릴 길이 없다 — 대신 `basis=CATEGORY_ONLY`로 저확신을 가시화한다. ④**근거 빈약 계정은 판정을 보류하거나 비뷰티로 확정하지 않는다.** 둘 다 되돌릴 수 없는 일방향 결정이고, 통과시키되 근거를 기록하는 쪽만 자기교정 경로(②의 캡션 재판정)가 살아 있다. 서빙 노출 자체는 이 트랙이 아니라 PR #204(analytics/was 층 게시물 실측 비율 게이트)가 막는다. **후속으로 해소한 것**: 야간 `beauty` 크론이 `jobService.trigger(JobName.BEAUTY, TriggerType.SCHEDULED)` 2-인자 오버로드를 쓰면 3번째 `requalify` 인자가 `false`로 고정돼 그대로 `BeautyJob.run(trigger, rejudge=false)`로 전달돼 캡션 재판정 경로가 스케줄로는 안 돌고 어드민 `/ui`의 수동 트리거에서만 열리는 문제가 있었다 — `ScheduleRunner.beauty()`가 3-인자 오버로드를 `rejudge=true`로 호출하도록 배선해 야간 크론에서도 자동 자기교정이 돌게 했다(다른 크론은 변경 없음) | 트랙 AB — [specs/2026-07-30-beauty-judgment-quality-design.md](docs/superpowers/specs/2026-07-30-beauty-judgment-quality-design.md) |
| 2026-07-30 | **계정 하입 스코어 척도 재교정 (트랙 Z 후속) — 순위는 유지, 척도만 콘텐츠와 같은 매핑으로** — v3(감쇠를 매핑 뒤로)를 test 스택에 반영해 실측하니 콘텐츠 타입 편향은 목표대로 해소됐지만(≥70점 피드/릴스 비 1.91→0.86), **계정 점수(`avg_hype_score`) 척도가 무너져 있었다**: 콘텐츠 `hype_score` 최댓값 84(90점 이상 0건, n=110,488)인데 계정 평균 최댓값은 59(`beauty_linyas2`), 0점 계정 비율 20.72%(1,348/6,505) — 0~100 척도인데 상위 40점 구간이 사실상 도달 불가. 원인: 계정 점수는 최근창(최대 12개) 콘텐츠 `hype_score`의 단순 평균인데, 콘텐츠 함수가 이미 매핑 뒤 감쇠(`0.5^(경과일/14)`)를 적용한 값을 평균 내다 보니 창이 벌어질수록(게시가 뜸할수록) 뒤쪽 게시물이 깎여 평균 자체가 눌린다 — 실측 최상위 계정(창 스팬 1.8~16.9일)조차 뒤쪽 게시물이 `0.5^(16.9/14)≈0.43`으로 깎여 59에 그친다. **사용자 결정**: 게시 빈도가 계정 점수를 좌우하는 것은 의도된 동작(hypenow 제품 판단) — 순서(순위)는 그대로 두고 척도만 재교정한다. 콘텐츠 점수 산식·반감기·앵커는 건드리지 않는다. **구현**: 콘텐츠 함수와 동일한 4점 구간선형 매핑 관용구로 `analytics.hype_account_score(raw numeric)` 신설(`10_account_detail.sql`) — 창 평균(반올림 전, 이중 반올림 제거)을 입력받아 `[0,100]`으로 재매핑, `raw IS NULL`은 NULL 유지·`raw=0`은 자연히 0. 앵커는 계정 raw 평균의 **0점 제외 모수**(반올림 시 0이 되는 `raw<0.5` 포함 제외 — 안 그러면 p05=0이라 매핑 첫 구간이 0/0=NULL) 분위수로 콘텐츠와 별도 적합: test 스택 실측 정밀값 p05=1.0833·p50=12.8333·p90=31.2000·p99=44.8600(n=5,157, 반올림 전 최댓값 58.9167). 재적합 재현 쿼리는 `analytics/check/hype-anchor-refit.sql`에 계정 섹션으로 동봉하되, **운영이 아직 v2.1일 수 있어 설치된 `analytics.hype_score()`를 호출하지 않고 v3 산식을 인라인으로 재현**(콘텐츠 앵커 섹션과 다른 점 — 롤아웃 전에도 정확한 값이 나오게 하기 위함). 계정 점수는 이제 "절대 원점수"가 아니라 **플랫폼 내 상대 순위**를 뜻하게 되며, 콘텐츠 앵커와 마찬가지로 코퍼스가 자라면 표류하므로 정기 재적합이 필요하다 — [specs/2026-07-30-hype-score-v3-decay-after-mapping-design.md §9](docs/superpowers/specs/2026-07-30-hype-score-v3-decay-after-mapping-design.md) |
| 2026-07-30 | **`LONG_SPAN` 폐지 — 성장세 유효성을 `OK`/`UNAVAILABLE` 2상태로 (트랙 Y 3차 실측)** — test 스테이징 3차 실측(30개 표본)에서 `TrendValidity.LONG_SPAN`(창 90~365일, `trend_*` 값을 프롬프트 입력에 남긴 채 "장기간에 걸친 변화처럼 완만하게 표현하라"는 지시로만 통제하던 구간)이 새어나오는 걸 확인: `0205s.y`(창 207일)가 "뚜렷한 우상향 추세를 보입니다", `02_10.13`(157일)이 완화 없이 "상승하는 추세입니다", `119irl`(201일)이 "상승세를 보이고 있으나"로 방향성을 단정. 반면 값을 아예 제거한 `TOO_LONG`(창 365일 초과)·always-strip 7컬럼은 1·2차 실측에 이어 이번에도 누출 0건 — "입력에서 제거한 것만 지켜진다"는 §3-3-1 원칙이 세 번째로 재확인됐다. **결정**: `LONG_SPAN`을 폐지하고 90일 초과 전 구간을 `TOO_LONG`과 동일하게 `trend_direction`·`trend_change_pct`·`trend_older_avg`·`trend_newer_avg` 4컬럼 제거로 통합 — 두 상태가 완전히 같은 동작을 하게 됐으므로 `PerfConfidence.TrendValidity`도 `OK`/`UNAVAILABLE` 2상태로 단순화(임계값은 90일 그대로). **트레이드오프(의도된 수용)**: 창 90일 초과 34.6%(§2 실측) 계정에서 성장세 문장이 통째로 사라진다 — 부정확한 추세 서술보다 없는 게 낫다는 판단. **알려진 한계로 남긴 것**(같은 실측에서 결함 확인, 값 제거를 적용할 수 없어 미해결): `WEAK` 헤지 누락(`0o0.soni`는 헤지 자체 없음, `000bk`는 좋아요 서술에만 누락 — 값을 지우면 `INSUFFICIENT`와 구별 안 돼 등급이 무의미해짐)과 dominance 프레이밍 미반영(`00.young__da`, top1 점유율 85%인데 "계정 성장을 견인"으로 서술 — `median_views`·게시물 원값이 근거로 필요해 제거 불가). 향후 선택지는 코드가 결정론적 문장을 조립하는 방식으로 옮기는 것 | 트랙 Y — [specs/2026-07-30-perf-summary-statistical-guards-design.md](docs/superpowers/specs/2026-07-30-perf-summary-statistical-guards-design.md) §3-2·§3-2-1 |
| 2026-07-30 | **발굴 표면 뷰티 비율 게이트 (트랙 AA) — 계정 판정 오판을 게시물 실측 비율로 발굴 표면에서만 걸러낸다** — 계정 단위 뷰티 판정(crawler `influencer.beauty_class`)이 bio 곁가지 키워드·인스타 자기신고 `category_name` 매칭에 낚여 육아·다이어트·여행·피트니스 계정을 뷰티 인플루언서로 잘못 서빙하는 사례를 실측 확인: 서빙 게이트 통과 7,095개 중 뷰티 게시물 비율 0% 계정 886개를 스팟체크 20개(bio·캡션 원문 판독) 했더니 오판 17개(85%). 부가 원인 하나는 `HIKER_MOBILE`/`DATALIKERS` 소스 프로필 payload에 게시물이 없어 캡션 0건으로 판정된 경우. 계정 판정 로직 자체는 crawler 트랙(팀원 영역)이라 손대지 않고, 발굴 표면에서 게시물 실측 기반 게이트로 대신 걸렀다 — `account_beauty_ratio` 뷰(analysis Flyway V45, analytics 소유 — 계정별 창 내 분석 완료·뷰티 판정 원시 카운트만 담고 임계값 정책은 없음) + was 조회 시점 게이트(분석 8건 미만은 표본 부족으로 보류·통과, 8건 이상이면 뷰티 비율 20% 미만 제외 — 20%는 기존 카테고리 게이트와 동일 기준 재사용). 적용 표면은 발굴 목록 `GET /v1/influencers`와 유사 인플루언서 후보 단계(`V2InfluencerReportRepository.findSimilarHandles`)뿐 — 랭킹 `/v1/contents`·상세 직접 조회·저장 목록은 `account_summaries`를 조인하지 않는 경로라 의도적으로 미적용. **기각한 대안**: bio 전문 어휘 화이트리스트로 구제하는 2중 조건 — 실측 구제 규모가 1,912개 중 47개(2.5%)뿐이고 구제 샘플 15개 중 10개가 잘못된 구제였다. 지배적 실패 패턴이 직업·전공 bio("화장품 연구원"·"코스메틱 디자이너"·"cosmetics industry"·"향장학과")로, 뷰티 업계 종사자 ≠ 뷰티 콘텐츠 제작자인데 `ILIKE` 매칭으로는 문맥을 구분할 수 없다. 오제거는 뷰 층 조건이라 자기치유적(계정이 뷰티 콘텐츠를 올리면 다음 미러에서 자동 복귀)이라 단순 조건 쪽이 교환비상 유리하다고 판단. **영향**: 발굴 목록 모수 7,033 → 5,177(제외 1,856, −26%) — 프론트 통지 필요. **함정 2건**: ①분모(`analyzed_count`)는 `account_content_series` = 최근 12개 윈도(`analytics.recent-window`)일 뿐 계정의 전체 분석 게시물이 아니다 ②Postgres는 `OR` 단축 평가를 보장하지 않아, 창 전체가 `is_beauty IS NULL`(캡션·썸네일 둘 다 없어 판정 불가)인 계정은 `account_beauty_ratio`에 행은 있되 `analyzed_count=0`이 되어 division by zero로 발굴 목록 전체가 500이 될 수 있다(운영에 그런 계정은 현재 0건이나 `is_beauty NULL` 게시물 자체는 584건 존재해 시간 문제) → `NULLIF(br.analyzed_count, 0)` 방어를 후속 커밋으로 동봉, 기존 카테고리 게이트가 이미 쓰던 관용구와 일치시켰다. **배포 순서**: analytics(V45 뷰 생성) → was — 역순이면 `account_beauty_ratio`가 없는 채로 was가 조인해 발굴 목록 전면 장애 | [V45__account_beauty_ratio_view.sql](analytics/src/main/resources/db/migration/analysis/V45__account_beauty_ratio_view.sql) · [V1InfluencerDiscoveryRepository.java](was/src/main/java/com/celfit/was/v1/influencer/V1InfluencerDiscoveryRepository.java) · [V2InfluencerReportRepository.java](was/src/main/java/com/celfit/was/v2/influencer/V2InfluencerReportRepository.java) |
| 2026-07-30 | **`LlmGuard` 계정/콘텐츠 스코프 분리 — 근거 수치 인용 지시는 콘텐츠 경로만** — 트랙 Y 검증 중 test 스테이징에서 계정 `0_tsuki2`의 `perf_summary`가 "평균 좋아요 수는 1,605개 수준"으로 나온 걸 확인. 원인은 `GeminiAccountSynthesizer.INSTRUCTIONS_TEMPLATE`의 perfSummary 절("구체 수치를 문장에 그대로 인용하지 마라" — 계정 카피는 캐시돼 며칠 서빙되므로 낡는 수치를 박으면 안 됨)과, 콘텐츠·계정 카피가 공유하던 `LlmGuard`의 "핵심 주장에는 근거 수치를 함께 인용하라"가 정면 충돌한 것 — LLM이 후자를 따랐다. 콘텐츠 해석 문구(특정 게시물 1건의 확정 사실이라 수치가 안 낡음)에는 이 지시가 타당해 전역 삭제 대신 스코프만 분리: `LlmGuard.RULES`(콘텐츠 전용, 기존 그대로)/`ACCOUNT_RULES`(계정 카피 전용, 인용 지시 제외) 신설, `GeminiAccountSynthesizer.instructions()`가 `ACCOUNT_RULES`를 쓰도록 한 곳만 변경(`AnthropicAccountSynthesizer`는 이 메서드를 그대로 호출해 프로바이더 양쪽이 함께 고쳐짐 — 07-21 프롬프트 복제 사고와 같은 함정을 피함). 트랙 Y의 "금지는 입력 제거로 강제" 원칙은 이 건엔 적용 불가(avg_likes 등은 perfSummary 근거로 필요해 입력에서 뺄 수 없음) — 지시 정리가 유일한 강제 수단이라 `AccountAnalysisWriter.hasNumericCitation()`으로 저장 직전 숫자 포함 여부를 관측(차단은 안 함, WARN만) — [specs/2026-07-30-perf-summary-statistical-guards-design.md](docs/superpowers/specs/2026-07-30-perf-summary-statistical-guards-design.md) §8 |
| 2026-07-30 | **뷰티 판정 기준을 "서술 언어"로 명문화 + 재판정 범위 누락 사고 (트랙 CC)** — 운영 실측으로 두 문제가 겹친 것을 확인했다. ①**재판정 범위 누락**: v3 프롬프트(`FOREIGN_INFLUENCER` 도입, 트랙 P)는 07-28 05:00 UTC 컷오버됐지만, 그 전 판정을 지우는 `reset-influencer-judgments-v3.sql`이 **운영에서 실행되지 않았다** — 현재 서빙 중인 `INFLUENCER` 7,128건의 92.7%(6,605건)가 FOREIGN_INFLUENCER 선택지 자체가 없던 4분류 시절 산출물이고, `findRejudgeTargets`가 `beauty=false`만 대상이라 INFLUENCER는 구조적으로 자가 치유가 안 된다. ②**프롬프트 결함**: v3 이후로 새로 판정된 계정 중에서도 한글 0+외국어 문자 4자 이상인 표본 33건 전수 확인 결과 33/33이 일본 뷰티 인플루언서인데 INFLUENCER로 판정돼 있었다 — "한국어 콘텐츠 중심"이라는 표현을 LLM이 "한국 관련 콘텐츠"로 읽어, 한국 화장품을 일본어로 리뷰하는 계정을 한국어 콘텐츠로 오인한 것. 결정: 판정 기준을 **서술 언어**(bio·캡션을 어떤 언어로 쓰는가)와 **다루는 제품·주제의 국적**(한국 브랜드인가)을 프롬프트에서 명시적으로 분리 — 한국 브랜드를 리뷰해도, 한국에 거주해도 글을 외국어로 쓰면 FOREIGN_INFLUENCER임을 예시(`韓国コスメ`를 일본어로 리뷰하는 일본 계정)와 함께 명문화하고, 히라가나·가타카나·한자·태국어·키릴 등 문장형 외국어 신호와 카오모지(장식 문자, 신호 아님)를 구분하는 규칙을 추가했다. 재판정은 `reset-influencer-judgments-v4.sql`을 `beauty.batch-limit` 슬라이스 단위로 반복 실행(전량을 한 번에 비우면 재판정 완료 전까지 서빙 게이트에서 통째로 이탈하므로) — 오래된 판정부터(`beauty_judged_at nulls first`) 소진 | 트랙 CC — [plans/2026-07-30-beauty-foreign-influencer-rejudge.md](docs/superpowers/plans/2026-07-30-beauty-foreign-influencer-rejudge.md) |
| 2026-07-30 | **하입 스코어 v3 — 감쇠를 앵커 매핑 뒤로 (트랙 Z)** — 발굴 목록에서 피드 콘텐츠가 릴스보다 과대 노출된다는 관찰을 실측으로 확인: 피드 ≥70점 비율 7.61% vs 릴스 3.98%, 상위 50 계정의 피드비율 중앙값 0.70(전체 계정 중앙값 0.18) — 발굴 상위가 피드로 편중. 원인 규명: v2.1은 앵커를 감쇠 **후** `qf`(=Q×decay)에 적합시켜, 캘리브레이션이 코퍼스의 연령 구성에 오염됐다 — 피드가 릴스보다 옛날 꼬리가 두꺼워 감쇠가 더 세게 눌렀고, 그게 타입별 점수 분포 차이의 구조적 원인이었다. 채택하지 않은 안: 기준량은 그대로 두고 피드 앵커만 재적합하는 안을 검토했으나, 적합 시점과 서빙 시점의 코퍼스 연령 구성이 달라지면 실서빙 모수에서 반대 방향 과잉교정으로 뒤집히는 게 확인돼 기각. 결정: 감쇠를 앵커 매핑 **뒤로** 옮겨 `점수 = clamp(map_Q(Q), 0, 100) × 0.5^(경과일/halflife)`로 재정의(클램프는 감쇠 **전** — base가 [0,100]이어야 "품질 백분위 × 신선도"가 성립), 앵커 8개는 **감쇠 전 Q 기준·전체 서빙 코퍼스**로 재적합(운영 실측이 스펙값과 n까지 정확히 일치 — feed 0.0447/0.6135/1.6320/3.0144·n=35,657, reels 0.1373/1.3798/4.5716/10.3883·n=81,943). 범위 외로 남긴 것: 역-U형 잔차(피드비율 1~33% 구간에서 점수 정점 — 재적합 시뮬레이션 전후에도 잔존, 팔로워 정규화 추정이나 정량화 미실행)·계정 평균 점수의 표본 하한(상위 50 중 8%가 산출 콘텐츠 1~2건뿐 — 트랙 Y 성격)·`followers`가 게시 시점이 아닌 현재값을 쓰는 구조적 결함 | 트랙 Z — [specs/2026-07-30-hype-score-v3-decay-after-mapping-design.md](docs/superpowers/specs/2026-07-30-hype-score-v3-decay-after-mapping-design.md) |
| 2026-07-30 | **성과 요약의 통계 왜곡 가드 — 표본은 지표 단위로 세고, 판정 근거를 평균→중앙값으로 옮긴다** — 운영 실측(계정 7,033) 결과 초기 가설 2건이 폐기됐다: `views > 0` 필터의 상향 편향은 릴스 43,433건 중 `views=0`이 **1건**(영향 계정 1개)뿐이라 실익 없고, `likes = -1` 센티널은 `00_base.sql`의 `NULLIF`에서 이미 NULL 처리돼 혼입이 없다. 대신 두 사실이 설계를 결정했다 — ①**표본 부족은 계정 단위가 아니라 지표 단위**(`analyzed_count` 12+가 95.4%인데 혼합 계정 66.8%의 릴스 비중 p10=0.10이라 조회수 평균의 실질 모수가 1인 계정이 실존. `analyzed_count`는 지금도 화면에 "샘플 12건"으로 나간다) ②**한 건 지배는 예외가 아니라 기본값**(top1 조회수 점유율 p50 0.466 — 플래그로 경고하면 30% 계정에서 떠서 경고가 죽으므로, 판정 입력 자체를 중앙값으로 교체하는 게 맞다). 성장세 왜곡도 건수(절반당 1건 1.4%)가 아니라 **시간 축**(창 길이 p90 417일·max 9년)이 원인이라 `window_span_days` 기준으로 잡았다. 임계값은 뷰가 아니라 Java 상수에 둔다 — 운영 뷰 적용이 수동 런북이라 SQL에 굳히면 임계값 조정마다 운영 DDL이 붙는다. 재생성은 `copy_version` 게이트로: 계정 카피엔 `Synthesis.VERSION`류 게이트가 없어 새 게시물을 올리지 않는 계정의 문구가 영구 고정됐다(대안인 `perf_summary` 일괄 NULL은 파괴적 UPDATE라 기각 — 재생성 실패 시 빈 문구 노출). 파생 함정 하나를 코드로 닫았다: 뷰(수동)와 미러 컬럼(Flyway)의 배포 경로가 달라 미러가 뷰보다 먼저 돌면 전 계정이 최대 억제 등급을 받아 저품질 카피가 생성되고 그게 최신 버전으로 찍혀 영구 고정되므로, 9컬럼 전부 NULL(= `count(*) FILTER`가 0을 반환하는 정상 계정과 구분 가능)이면 생성을 건너뛰고 WARN을 남긴다 | 트랙 Y — [specs/2026-07-30-perf-summary-statistical-guards-design.md](docs/superpowers/specs/2026-07-30-perf-summary-statistical-guards-design.md) |
| 2026-07-30 | **CD 소요 시간 단축 3종 — amd64 드롭·Spring Boot layered jar·분석 뷰 적용 스킵+lock_timeout** — ①**amd64 이미지 빌드 드롭**: 운영 서버(오라클 A1)가 arm64 단일이고 로컬 colima도 arm64라 amd64 소비처가 전혀 없음(실측: 실행 중 이미지 11개 전부 arm64) — `cd.yml`·`deploy/scripts/deploy.sh`의 `docker buildx build --platform`을 `linux/arm64,linux/amd64`→`linux/arm64`로 축소. `setup-qemu-action`은 유지 — 러너가 amd64라 arm64 크로스 빌드에 여전히 필요(제거 대상 아님, 목적 재서술만). ②**Spring Boot layered jar**: `java -Djarmode=tools -jar <bootjar> extract --layers --launcher`로 dependencies(변경 적음)/spring-boot-loader/snapshot-dependencies(빈 디렉토리)/application(변경 잦음) 4레이어 추출 후 Dockerfile 4종(was·analytics·crawler·monitoring)을 단일 `COPY *.jar app.jar`에서 레이어 COPY 4단으로 교체, ENTRYPOINT는 `-jar app.jar`→`org.springframework.boot.loader.launch.JarLauncher`(exec·JAVA_OPTS 전개 유지 — graceful drain 무영향). 코드만 바뀐 재배포의 전송량은 application 레이어뿐 — 실측 dependencies/application: was 47M/1.5M · analytics 59M/1.0M · crawler 82M/1.3M · monitoring 22M/280K(loader 604K는 공통·불변). CD·cd-test.yml `bootJar 빌드` 스텝 뒤에 레이어 추출 스텝 추가(plain jar 배제 후 정확히 1개 검증, 아니면 빌드 실패) — 로컬 colima(arm64 네이티브) 실측: 4개 서비스 전부 `docker build` 통과, JarLauncher가 클래스패스 해석·Spring 정상 부팅(DB 연결 실패는 무DB 환경이라 정상), 애플리케이션 코드만 바꿔 재빌드 시 dependencies/spring-boot-loader/snapshot-dependencies 레이어 CACHED·application만 재실행 확인. 빈 snapshot-dependencies/ COPY도 buildkit이 문제없이 처리. ③**분석 뷰 적용 스킵+lock_timeout**: (a) `analytics/views/*.sql` 해시를 서버 `~/deploy/views.sha256`(cd-test는 `views-test.sha256`)에 기록해 해시 동일+analytics(_dev) 스키마 뷰 존재 프로브 통과 시 적용 스킵(적용 성공 후에만 해시 기록 — 실패 시 기록하면 다음 배포가 잘못 스킵), (b) `SET lock_timeout = '15s'` + `--single-transaction`으로 DDL이 락 큐에 무한 대기해 뒤따르는 리더까지 줄 세우는 걸 차단(실측: 89초 락 대기의 정체는 `00_base.sql` 앞쪽 `CREATE OR REPLACE VIEW` 9개 — 연산 비용 있는 CREATE TABLE/INDEX는 그 뒤라 이미 즉시 끝났음). 기존 5회·15초 재시도 루프 유지, 재시도 로그 문구에 lock_timeout 초과 가능성 명시. cd-test.yml도 같은 스킵 기법 적용(치환 전 원본 해시 비교라 analytics_dev 치환과 상호 무관해 복잡해지지 않음), 단 원래 없던 재시도 루프는 신설하지 않음(운영 CD만 재시도 대상으로 명시된 요청 범위) | .github/workflows/cd.yml · cd-test.yml · deploy/scripts/deploy.sh · was·analytics·crawler·monitoring/Dockerfile |
| 2026-07-30 | **첫 실전 was 롤링 배포 성공 + rollout.sh 이미지 검증 오탐 픽스** — staging→main 승격(#185·#188)으로 트랙 X 첫 실전. 1차 시도는 rollout.sh의 기준 이미지 추출(`docker compose config --images <svc> | head -1`)이 **의존 서비스 이미지까지 반환**하는 compose 동작(운영 실측: was 요청에 analytics·postgres×2·was 4줄)에 걸려 analytics 이미지를 기준 삼아 정상 신 컨테이너를 오탐 중단 — 무중단 실패 모드는 설계대로 동작(구 was 서빙 지속, 1초 폴링 순단 0). config JSON에서 서비스명 키로 정확 추출하도록 수정(PR #186) 후 재배포 성공: 신 기동→전환·구 제거 24초, 운영 1초 폴링 548샘플 중 비정상 1건(그마저 caddy 컨테이너 재생성 순간). 동반 ops: monitoring 운영 개통(README §13 — monitoring·was_reader 계정+사설 DB 생성, `.env` 2키. 첫 CD가 env 게이트에서 의도대로 차단된 뒤 개통). 재확인된 함정: 운영 CD가 cd-test와 concurrency 경합으로 cancelled(README §12 — 재실행으로 해소), 로컬 백그라운드 폴링 10분 제한(장시간 관측은 서버 nohup으로) | [deploy/README.md §5-1](deploy/README.md) |
| 2026-07-30 | 모니터링 알람: 알람 소유를 was → monitoring으로 이동. 워터마크 대신 **id 기반 대장**(`alarm_event`)으로 순서·유실 문제를 없애고, 발송 실패는 행 단위 FAILED로 다음 틱 재시도. 승인 플로우는 제거(감지 즉시 자동 추적, 첫 1건 = 게시 시각 최신) — 캠페인:추적 게시물 1:1은 유지. 일시 오류(5xx·IO)는 알람이 아니라 재시도 대상(콜 백오프 + 스윕 말미 라운드). 수신자 해석은 analysis DB app 스키마 **두 객체만** 읽는 별도 읽기 전용 롤(역방향 계약 신설) | [specs/2026-07-30-monitoring-alarm-module-design.md](docs/superpowers/specs/archive/2026-07-30-monitoring-alarm-module-design.md) · [contracts/monitoring-was-contract.md](docs/contracts/monitoring-was-contract.md) |
| 2026-07-29 | **마이그레이션 가드 v2 — DROP COLUMN ↔ 보정 UPDATE 짝 검사 (트랙 X 확장)** — 컬럼 이행(add→전환→drop)에서 "백필 계획 누락" 감지가 리뷰에만 의존하던 갭을 좁힘. 계획 여부(의도)는 정적 검사 불가가 원리적 한계지만, ①백필 통째 누락은 신 컬럼 전 행 NULL = 기능이 비어 보여 staging 관문에서 걸리고 ②진짜 조용한 롤링 창 유실(수십 초분)은 contract 시점 보정으로 기계 강제 가능 — DROP COLUMN이 있는 마이그레이션은 같은 파일에 그 컬럼 참조 UPDATE(멱등 보정)가 없으면 CI 실패, 미러 소유 등 보정 불요는 `-- no-backfill: <사유>` 태그(allow-destructive와 독립 검사). dual-write는 현 트래픽에서 불요 판단 — 창 유실을 초 단위로 못 참게 되면 도입. 리뷰 잔여는 "실시간 쓰기 컬럼인가" 판단뿐이며 app 스키마는 이행 자체를 피하는 게 규칙. 셀프테스트 15케이스 | [deploy/README.md §5-1](deploy/README.md) |
| 2026-07-29 | **랜딩 통계 모수 하한 3,000 → 500 확장(나노 포함)** — 랜딩 "보유 마이크로 인플루언서 풀"(3,741)과 실제 보유 뷰티 인플루언서(6,931)의 체감 불일치 해소: 뷰티 QUALIFIED의 44%(3,074명)가 팔로워 3천 미만이라 정의상 제외되고 있었음(데이터 유실 아님 — 전원 프로필 보유·NULL 0건 실측). 하한을 crawler `qualify.min-followers`(500)와 정합시켜 모수 = 자격 구간 전체(500~5만, ~6,922명)로. 분포에 500-3k 구간 추가(디스커버리 follower enum과 동일 문자열) — expand 단계로 `followers500to3k`를 미러 테이블 끝에 ADD COLUMN(V47), record·뷰 컬럼도 마지막이 정본(§4-3 순서 대조). was는 다음 미러까지 NULL 창을 COALESCE 방어. **프론트 계약 변화**: followerDistribution이 3→4구간(선두 "500-3k"), 랜딩 카피 "3천~5만"은 프론트 저장소에서 별도 수정 필요 | [analytics/views/20_landing_stats.sql](analytics/views/20_landing_stats.sql) |
| 2026-07-29 | **was 무중단 롤링 배포 + expand-contract CI 가드 (트랙 X)** — 배포마다 was 다운타임 1~3분(analytics healthy 대기 결합 + 2g pre-touch 기동)을 해소. 롤링 대상은 **was만**(무상태 전제 충족: 세션 JDBC·캐시 외부 redis — analytics·crawler는 내부 서비스라 재기동 유지). 방식은 docker-rollout 패턴의 자체 스크립트(`rollout.sh`): 잔재 검사(정지분 포함 — 정지 잔재를 `--scale`이 재기동해 구버전이 신으로 둔갑하는 무증상 롤백 차단, 리뷰 C1 실측) → `--scale was=2 --no-recreate`로 신 컨테이너 추가 → 이미지 일치 검증 → healthy 대기(TCP 8081, 최대 180s) → 스모크(`/v1/stats` — TCP 리슨이 못 잡는 "기동됐는데 쿼리 깨짐" 방어) → 구 `stop -t 40`(graceful drain)·제거. 실패 모드는 **무중단 실패**(신만 제거, 구가 계속 서빙, CD만 실패 — 단 신 analytics+구 was 스큐 잔존, README §5-1). 교대 순간 신규 연결 실패는 Caddy `lb_try_duration 10s` 재시도가 전 메서드 흡수(잔여: 유휴 keep-alive의 비-GET 수 ms 창 — 의도적 수용). 순서 규약: rollout 전 `up -d --wait analytics`가 analysis Flyway 완료를 보장(was depends_on은 `--no-deps`로 우회되므로 07-20 "새 분석 컬럼 500" 가드를 CD 순서가 대신). 부수 해소: CD에 caddy reload 스텝 추가(기존엔 운영 Caddyfile 변경이 scp만 되고 미반영). 동반 규율: **expand-contract** — 롤링 중 신구 코드가 같은 DB 공존이라 DROP·RENAME·타입 변경·SET NOT NULL은 참조 코드가 끊긴 다음 릴리스로 분리, CI `migration-guard`(PR 전용, 셀프테스트 동반)가 차단하고 의도된 contract는 `-- allow-destructive: <사유>` 주석으로 통과. 로컬 검증: 더미 compose로 교대 성공·실패 생존·후속 up -d 무재생성 3경로 실측 | [deploy/README.md §5-1](deploy/README.md) |
| 2026-07-29 | **monitoring 모듈 구현 완료 (트랙 MON — 개통 ops·머지 대기)** — 12태스크 완주: 사설 monitoring DB(raw/public 2스키마, Flyway V1·V2) · 등록/승인/거절/연장/해지 명령 API 5종 · KST 02:00 일일 스윕(키워드 감지→후보 적재, 등록 시각 이후 게시물만) · 조회 표면(테이블 4 + 뷰 2 `v_target_overview`·`v_target_timeseries`) · test/운영 compose 배선. Hiker는 실측으로 **v2 계열 3종 확정**(v1은 6지표 중 좋아요·댓글만 줌 — 릴스 재생수 보강 포함 계정당 3콜), 열거는 기본 1페이지. 계약 문서 v1.0으로 동결(뷰 컬럼 실명·에러 어휘). 미결(후속): raw 원형 보존 기간, 종결 캠페인 데이터 청소. 회귀: `./gradlew test` 전 모듈 GREEN(1,015 tests / 0 fail — monitoring 77 신규) | [specs/2026-07-28-monitoring-module-design.md](docs/superpowers/specs/2026-07-28-monitoring-module-design.md) · [contracts/monitoring-was-contract.md](docs/contracts/monitoring-was-contract.md) · [plans/archive/2026-07-28-monitoring-module.md](docs/superpowers/plans/archive/2026-07-28-monitoring-module.md) · 개통 ops [deploy/README §13](deploy/README.md) |
| 2026-07-29 | **모니터링 감지 대상을 등록 시각 이후 게시물로 한정** — 캠페인 등록 전의 옛 키워드 게시물이 첫 스윕에서 후보로 뜨는 노이즈를 차단(PO 확정). 스윕 키워드 매칭에 게시 시각(taken_at) ≥ target.registered_at 조건, taken_at 미상은 보수적 제외(놓친 건 다음 스윕·멱등 후보 생성이 회복, 잘못 올린 후보는 사람이 지워야 하므로 비용 비대칭). 감지 제외 ≠ 관측 제외 — 스냅샷은 열거 전량 적재 유지 | [specs/2026-07-28-monitoring-module-design.md](docs/superpowers/specs/2026-07-28-monitoring-module-design.md) §5 · 계약 §3 |
| 2026-07-29 | **모니터링 명령 API 인증을 토큰 → 전용 도커 네트워크로 대체 + 스테이징 스택 편입** — ①정적 토큰 폐기: `monitoring-net`(was·monitoring만 소속) + 호스트 포트 미노출로 접근 통제를 구조화 — 미소속 컨테이너는 DNS 해석부터 실패라 dev 오배선이 connection error로 즉사(fail-closed), 공유 비밀 관리 소멸. 토큰의 잔여 방어 시나리오(was 자체 침해)는 토큰도 같이 뚫려 이득 0. 이 배포의 첫 네트워크 레벨 경계(기존 dev/운영 격리는 전부 DB 계정 권한 — 평면 네트워크는 postgres-raw 공유 설계상 유지). 부수: 운영 호스트 8083은 test-analytics(구 dev-analytics) 터널이 점유 중이라 포트 충돌도 회피. ②스테이징 스택에 `test-monitoring` 신설(`:staging`, test-postgres의 monitoring DB, `test-monitoring-net`, 스윕 크론 off — K "스케줄 전부 off" 원칙) — test-was의 모니터링 기능 실환경 검증 경로 확보. (최초 구현은 트랙 W 전 명명이라 dev-monitoring·`:develop`이었고, 07-29 develop 병합 시 트랙 W 매핑대로 리네임했다) | [specs/2026-07-28-monitoring-module-design.md](docs/superpowers/specs/2026-07-28-monitoring-module-design.md) 결정 11·12 |
| 2026-07-29 | **가입 시도 추적은 DB 이벤트(`app.signup_events`, V14) — 요청 1건당 1행 + 내부 progress 기록** — 가입 중도 이탈 추적 수단이 없던 갭(4xx 무로깅 설계·Caddy 액세스 로그 부재·발송 실패 무흔적) 해소. 4xx 무로깅(V1ExceptionAdvice)은 유지하고 POST /v1/auth/signup의 모든 시도를 email·outcome(ok/에러코드)·ip·detail(jsonb)로 영속화 — detail.progress가 내부 단계(rate_limit→signup_code→validation→register→auto_login→session) 중 어디까지 갔는지 담아 "유저는 생겼는데 응답 실패" 같은 부분 실패도 구분. 기록은 fire-and-forget(gate_events 선례), 비밀번호는 detail에 절대 불포함 | 본 행 + PR #170 |
| 2026-07-29 | **staging 승격 브랜치 + test 스택 리네임 + prod/test 네트워크 분리 (트랙 W)** — develop 머지 = dev 배포 자동 결합(원치 않는 배포·실행 중 잡 중단)과 dev 명칭 3중 의미(브랜치·스택·도메인), 평면 네트워크(dev→운영 방벽이 자격증명뿐)를 한 번에 해소. ①승격 흐름 develop→staging→main(A안 — test 서버에서 검증된 커밋만 운영 승격, cd-test는 CI@staging `workflow_run` 게이트 유지) ②dev 계열 전부 test 통일(B안), 단 도메인·`DEV_*` env·`analytics_dev`·볼륨 `dev-pg-data`는 구명 유지(DNS·계정·데이터 이관 비용 대비 이득 없음) ③compose.yaml에 브리지 prod/test 선언(전 서비스 소속 명시 — 커스텀 네트워크 체제라 누락 = 고립), caddy·postgres-raw만 양쪽. cd-test up에 `--remove-orphans`(전체 파일 세트 기준이라 안전 — 구 dev-* 자동 정리) + 구 명칭 파일 서버 삭제(같은 도메인 이중 선언 caddy 오류 방지). monitoring 스펙(07-28 결정 11·12)의 `monitoring-net`은 이 대분리 위 소네트워크로 충돌 없음 — 배선 시 dev-monitoring→test-monitoring·`:develop`→`:staging` 매핑(07-29 monitoring 브랜치에서 적용 완료) | [specs/2026-07-29-staging-branch-test-stack-design.md](docs/superpowers/specs/archive/2026-07-29-staging-branch-test-stack-design.md) |
| 2026-07-29 | **이메일 소유권 인증 제거(핫픽스)** — 프론트가 가입 위저드에서 이메일 인증 스텝을 전면 제거함에 따라 백엔드도 인증 스택 전체를 철거: 가입 게이트(`requireVerified`/`consume`), `/v1/auth/email-verification/send·confirm` 엔드포인트, `EmailVerificationService`/`Repository`, mail 패키지(Resend 발송기·MailConfig)와 `was.mail.*` 설정. 프론트 미호출 상태에서 send 엔드포인트만 살아 있으면 임의 주소 메일 발송 남용 표면이라 게이트만 빼지 않고 전부 제거. 가입 관문은 1회용 가입 코드(V8)만 유지. V7 마이그레이션·`email_verifications` 테이블은 적용된 Flyway 이력이라 잔존(무해) — 드롭은 필요 시 후속 마이그레이션으로. 서버 `RESEND_API_KEY` env는 무참조라 정리 불요(재도입 대비 보존 가능) | [specs/2026-07-18-email-verification-design.md](docs/superpowers/specs/archive/2026-07-18-email-verification-design.md) 🗄 |
| 2026-07-29 | **계정 하입 스코어 = 최근 12창 콘텐츠 hype_score 단순 평균(신선도 감쇠 포함, 콘텐츠 함수 재사용)** — 발굴 목록(6.21) 카드에 계정 레벨 점수가 없어 콘텐츠 단건 `hype_score()`(v2.1, `analytics/views/02_serving.sql`)를 계정별 최근 12개 창에 그대로 얹어 단순 평균했다. 산식 위치는 B안(`v_account_summaries` 확장) — 미러 3곳(analytics 뷰·계약 record·analysis Flyway V42)을 동시 변경하는 대신 새 파생 뷰를 신설하지 않아 접점을 하나로 유지. 감쇠를 포함한 채로 평균 내면 최근 활동이 뜸한 계정이 불리해지는 트레이드오프가 있으나, 운영 실측(계정 6,656건)으로 분포를 먼저 확인한 뒤 원안 확정 여부를 재검토하기로 함. 유사 카드(6.23, R)는 발굴 카드와 SELECT·DTO를 공유하므로 `avg_hype_score`가 자동 포함됐다(의도된 부수효과, 별도 배선 불필요) | [specs/archive/2026-07-29-influencer-avg-hype-score-design.md](docs/superpowers/specs/archive/2026-07-29-influencer-avg-hype-score-design.md) |
| 2026-07-29 | **trait 어휘 통제 착수 — 데이터 주도 172개 어휘 + 1:N 분해 매핑** — 2단계 보류 트리거("카테고리·태그라인 변경 반영 분석 데이터 확보")가 카피 백필 완결(07-29 01:33Z, 6,656계정 100%)로 충족됐고, 새 프롬프트 산출에서도 난수화 지속(고유 4,242/25,818, 싱글톤 62.6% — "솔직 리뷰/솔직한 후기/솔직한 리뷰"가 전부 별개 값)이 실측돼 착수. 반려됐던 44개 수동 초안(표현력 하락) 대신 **운영 최다 빈도 표기를 그대로 캐노니컬로 채택한 172개·13축**(무쌍 메이크업·수부지·여쿨 등 니치 보존). 복합 trait는 원자 태그 최대 2개로 분해 매핑(1:N — 어휘를 작게 유지하면서 조합 표현력·Jaccard 교집합 확보). 이행은 재합성 없이(Synthesis.VERSION 불변) LLM 배치 매핑, `trait_canon_log` 감사·dry-run 선행. 매핑 후 유사도 분포 재실측→컷 0.30 재점검 잔여 | [specs/2026-07-29-trait-vocabulary-control-design.md](docs/superpowers/specs/2026-07-29-trait-vocabulary-control-design.md) |
| 2026-07-29 | **유사 인플루언서(6.23) 휴면 계정 제외 — 쿼리 파생 필터(저장 플래그·스케줄러 없음)** — 휴면 = 최근 업로드(`account_summaries.last_posted_at`)가 3개월 밖 또는 미확인(NULL). 미러가 매일 갱신하는 값에서 즉시 파생 가능해 영속화 없이 `findSimilarHandles`의 후보 조인 조건 하나로 구현(휴면 정의 정본은 이 조건). 기준 계정 자신은 휴면이어도 목록 반환. 랭킹 대시보드 등 다른 표면은 무필터 유지(휴면 표시 필드도 불필요 확인) — 향후 다른 표면에서 숨길 땐 같은 조건 재사용 | 본 행 + [V2InfluencerReportRepository.java](was/src/main/java/com/celfit/was/v2/influencer/V2InfluencerReportRepository.java) |
| 2026-07-29 | **Redis 캐싱 도입 — 순수 캐시 전용, TTL 백스톱만, 전면 fail-open** — 세션은 "강제 로그아웃 금지" 정책상 이관하지 않고 Spring Session JDBC 유지, 미러 완료 즉시 무효화 연동 없이 TTL만으로 stale 허용(미러가 새벽이라 최악 6h), Redis 장애는 캐시 미스로 DB 직행(SPOF 아님) | [specs/2026-07-28-redis-caching-design.md](docs/superpowers/specs/2026-07-28-redis-caching-design.md) |
| 2026-07-29 | **캐시 키는 정규화 쿼리의 단사(canonical) 인코딩 SHA-256** — record `toString()` 기반이었다가, 리터럴 문자열 "null" 파라미터가 필터 생략과 동일 해시로 충돌하는 캐시 오염을 리뷰에서 발견(비로그인 GET 1발로 재현)해 교체 | 위와 동일 |
| 2026-07-29 | **캐시 값 스키마 세대 = 빌드 시각 epoch을 키 prefix에 반영**(`buildInfo()`) — DTO 필드 변경 배포 시 구 페이로드를 신 코드가 무증상 오독하는 사고를 차단, 배포 직후는 콜드 캐시로 수용 | 위와 동일 |
| 2026-07-29 | **캐시 쓰기는 `immediateWrites` 동기화** — Spring Data Redis 4.1 기본 비동기 쓰기는 put 실패가 로그 없이 소실돼(errorHandler 도달 불가) 본 절 fail-open 로그 계약을 위반 — 무효화 없이 TTL 백스톱만 쓰는 현 설계의 정합성 전제. 향후 `@CacheEvict` 무효화 도입 시 재검토 | 위와 동일 |
| 2026-07-28 | **모니터링 모듈 설계 확정 (트랙 MON — 구현 미착수)** — 시딩 캠페인 모니터링(was가 계정 URL+키워드 또는 게시물 URL 등록 → 프로필 감시 중 키워드 게시물을 후보로 감지 → **FE 사용자 승인 시** 그 게시물로 추적 전환 → 일별 지표 추이 적재. 감지는 후보 축적일 뿐 자동 전환 아님 — 오감지 필터링은 사용자 몫)을 **신규 4번째 모듈 `monitoring`**으로. 근거: 수집 결과가 수집 대상을 바꾸는 상태 기계라 단방향 파이프라인과 도메인이 다르고, 수집이 Hiker-only(유료 안정 HTTP)라 crawler 페처 기계 재사용 유인이 없음(crawler 편입·analytics 편입 기각). 결정 넷: ①**명령(등록·승인·거절·연장·해지)은 내부 HTTP API — "층 사이는 DB로만" 원칙의 명시적 예외 — 조회는 was가 monitoring DB `public` 조회 표면을 읽기 전용 SELECT**(등록 요청-응답 안에서 계정 검증+첫 수집 동기 완결·상태 기계 명령 시맨틱·쓰기 캡슐화가 API의 근거. 단순 조회까지 API를 태우면 가용성 결합·구현 비용만 늘어 조회는 DB로 — analytics→was 읽기 계약 패턴 동일, raw 스키마는 was 무권한으로 원형 비노출 권한 강제. 기존 crawler↔analytics↔was 경계는 DB-only 불변). 수용 비용: 명령 경로 한정의 가용성 결합. ②**monitoring 소유 단일 사설 DB + 내부 2스키마(raw/public — 원형만 스키마 격리)** — was가 DB로 읽지 않으므로 원형·서빙 분리 강제 근거(읽는 주체 분리)가 소멸, 원형·가공 동거는 crawler DB 전례와 동일. 두 DB 분리안은 실질 격리 이득 0에 운영 비용만, state/serving 추가 분리는 상태도 API로 서빙되는 같은 소비자라 경계 근거 없음 — 모두 기각. ③배치는 기존 `postgres` 인스턴스에 데이터베이스 신설(계정 권한 fail-closed, 크로스 DB 쿼리 불가로 격리 완성 — 전용 컨테이너는 메모리 제약상 기각), 임의 계정이 `influencer`/`content`/서빙 모수에 불침투. ④**target은 캠페인(등록) 단위, 스냅샷은 관측 대상(계정·게시물) 단위** — 같은 인플루언서를 키워드 다른 캠페인 여럿이 동시 감시 가능(감지 귀속은 target_id로 명확), 수집·적재는 계정당 1회 공유, 등록 멱등은 was 생성 registration_key. 감시·추적 일 1회 배치(KST 02:00)+등록 시 동기 첫 수집, 기간은 was(FE)가 `expires_at` 지정·monitoring이 만료 판단. 감지 이메일 알람 발송은 was(09:00 크론 — 수신자 매핑·on/off·발송 채널 전부 app 스키마 소유, 신규 후보는 was가 읽기 전용 SELECT로 조회) | [specs/2026-07-28-monitoring-module-design.md](docs/superpowers/specs/2026-07-28-monitoring-module-design.md) |
| 2026-07-28 | **모니터링 seam(was)** — monitoring용 DataSource·JdbcClient·RestClient는 빈 비노출(자동구성 back-off 회피 — 도메인 빈 3개만 노출), 등록은 멱등키 선저장 2단계+전송실패 1회 재시도, 조회는 계약 베이스 테이블 4개만. 내부망 전용이라 명령 API 토큰 인증 제거. 계약 스냅샷 docs/contracts/ 반입 | [specs/2026-07-28-monitoring-was-seam-design.md](docs/superpowers/specs/2026-07-28-monitoring-was-seam-design.md) |
| 2026-07-28 | **인플루언서 상세 최근 12개는 게시물 뷰티 판정 무관 — 성과 모수와 화면 1:1 정렬** — 6.4 `findRecentCards`가 확정 비뷰티(is_beauty=false)를 제외해, 성과 지표(account_summaries·시계열 — `v_recent_content`는 계정 단위 뷰티 인플루언서 필터만)의 모수 "실제 최신 12개"와 화면 카드가 어긋났고, v2 리포트 차트 contentIds(프론트가 recentContents와 short_code 조인)도 비뷰티 막대가 조인 실패하는 구조였다. 필터를 제거해 인플루언서 뷰는 뷰티 무관 최신 12개를 그대로 노출·성과도 동일 모수. 비뷰티 게시물 제외는 랭킹(6.1 `is_beauty=true`)만의 정책으로 유지 | [V1InfluencerRepository.java](was/src/main/java/com/celfit/was/v1/influencer/V1InfluencerRepository.java) |
| 2026-07-28 | **미분석 "왜" 가시화 — 상태 분해 정본 스크립트 + 대시보드 원인 라벨 승격** — "제때창(3일) 놓쳐서 분석 안 함"과 "분석 대상인데 대기"가 구분 안 되던 문제(세션 보고·대시보드 해석 양쪽 재발). 원인: 미분석 콘텐츠의 timely 여부는 분석 완료 전엔 영속화되지 않아(`v_analysis_candidates.timely`가 유일한 판정 지점) 즉석 카운트 쿼리가 트랙을 뭉갬 + /ui 라벨이 결과 분류("상세 트랙"·"영구 제외")만 있고 원인(제때창 놓침)이 없었음. 조치: ①`analytics/check/pending.sh` — 미분석 상태 12분해(미성숙/랭킹·상세 트랙별 기분석·대기·댓글 게이트·미러 갭/영구 제외/캡션 없음) + /ui 항등식 대조 + 마킹·뷰 불일치(소급 런북 잔여 감지) 크로스 DB 리포트, 잔여 카운트는 이 정본만 사용(CLAUDE.md 함정 등재) ②/ui 콘텐츠 보드 — 트랙 라벨에 원인 명시("제때창(3일) 안 크롤 성공"/"제때창 놓침"), 미분석 칩에 "분석 대기", 자격 밖(미성숙·영구 제외)을 각주에서 "분석 안 함 — 실패·지연 아님" 행으로 승격 | [analytics/check/pending.sh](analytics/check/pending.sh) |
| 2026-07-28 | **유사 인플루언서 유사도 v2 1단계 — v1→v2 표면 이식 + 최대 9→10명 변경** — traits 자유 서술의 Jaccard가 난수화(고유값 5,847/28,387, 67%가 1회 등장)되어 혼합 점수(Jaccard 0.6 + 카테고리 믹스 히스토그램 교집합 0.4, 집합 기반 SQL — 운영 규모 실측 85ms)·컷 0.30(운영 dry-run 실측)·handle 타이브레이크로 교체하는 작업 도중, `/v1/influencers/{id}/similar`가 #149 최종본(f8da9d4d)에서 삭제되고 `/v2/influencers/{id}/similar`(7c67a411, 6.21 카드 재사용)로 신설돼 알고리즘을 `V2InfluencerReportRepository.findSimilarHandles`로 이식했다. 응답이 카드 목록이라 유사도 %·점수 비노출은 표면 형태로 자연 충족. 겸사겸사 스펙 6.23의 서버 고정 9명을 **10명으로 변경**(사용자 확정, 코드 주석·문서 동기화). 어휘 통제(2단계)는 보류 — 카테고리·태그라인 변경 반영 분석 후 재결정 | [specs/2026-07-28-similar-influencer-similarity-v2-design.md](docs/superpowers/specs/2026-07-28-similar-influencer-similarity-v2-design.md) |
| 2026-07-28 | **6.22 리포트 v2는 `/v2` 병행 신설** — v1(6.5)은 프론트가 라이브 소비 중이라 원형 보존, 프론트 전환 완료 후 별도 PR로 v1 폐기 | [plans/2026-07-28-influencer-report-v2-spec-alignment.md](docs/superpowers/plans/archive/2026-07-28-influencer-report-v2-spec-alignment.md) |
| 2026-07-28 | **유효 팔로워는 실반응 산식 단일 원천**(`EffectiveFollowers` 유틸) — 발굴 목록(6.21)의 구 피어 중앙값 CTE 폐기, 6.21·6.22 동일 값 보장(스펙 7절 17번) | 위와 동일 |
| 2026-07-28 | **브랜드 hover 엔드포인트(`/v1/brands/{brand}/influencers`) 삭제** — 6.22 `ads.brands` 인라인(`otherInfluencers`·`contentIds`)으로 흡수 | 위와 동일 |
| 2026-07-28 | **피어 퍼센타일(topPct)은 v2 계약에서 제외** — V39 `account_peer_stats` 뷰는 유사 후보 풀·LLM 내부 재료로만 유지 | 위와 동일 |
| 2026-07-28 | **dev 스테이징 개통 + E2E 검증 완료 (태스크 K 종결)** — 체크리스트 실행(DNS A 레코드·서버 .env `DEV_*` 4종·`~/deploy/caddy.d` 선생성) → PR #147(develop)·#150(release) 머지 → 운영 CD가 caddy 재생성(caddy.d 마운트·import 발효)으로 `dev-api.hypenow.io` 즉시 개통(운영 postgres-raw는 무접촉 — compose.dev.yaml 분리 검증됨). E2E: 가입 코드 시드(SQL 파이프, `DEV-*` 5건) → 이메일 인증(로깅 폴백 — 코드는 dev-was 로그) → 가입 201 → `/v1/me` 200 → `analytics_dev.refresh_snapshot_cache()`(63만 행, 4분) → 미러 수동 실행(11분 — accounts 6,720·contents 124,310·스냅샷 583,422) → **`/v1/stats`·`/v1/influencers/{handle}` 실데이터 200 확인**. 확인된 동작 특성 2건: ①`/v1/contents`는 dev에서 빈 목록이 정상(LLM 분석 미실행 → `content_analyses` 0 — "분석 완료만" 조건) ②develop 머지 시 cd-dev 재배포가 **실행 중인 dev 잡을 중단**시킴(첫 미러가 이렇게 유실 — 재실행으로 해소, 스테이징 수용 트레이드오프·README §12 주의에 명시) | [deploy/README.md §12](deploy/README.md) · [specs/2026-07-26-dev-staging-environment-design.md](docs/superpowers/specs/archive/2026-07-26-dev-staging-environment-design.md) |
| 2026-07-28 | **발굴 목록 API `GET /v1/influencers`(스펙 6.21) — 전부 was 조회 레이어로 구현** — "지금 뜨는 인플루언서" 명함 카드 그리드의 서버 필터·정렬·페이지네이션. 신규 뷰·마이그레이션 없이 was JdbcClient 쿼리(6.1 리더보드 관용구: fromJoins/where 공유 빌더+count 분리, beauty_taxonomy 중분류 확장, `/img/` COALESCE 이미지)로 구현하고, 페이지 100건에만 보강 3쿼리(카테고리 비중·협업 브랜드·최근 썸네일 4개). 결정: ①**유효 팔로워** = 리포트 개편(07-27) 확정 산식 `followers × min(1, 계정 ER/피어 중앙값 ER)`(피어=최빈 대분류×팔로워 버킷, n<3 전체 중앙값 폴백)을 **인라인 CTE로 동일 계산** — 미머지 redesign 브랜치(V39 뷰)에 의존하지 않되 정의 동일(→ 07-28 v2 정렬에서 실반응 산식 `EffectiveFollowers`로 대체됨 — 아래 07-28 두 번째 행 참조) ②**광고 정본 = `content_analyses.ad_type='sponsored'`**(협찬 수·브랜드·썸네일 배지 일관) ③**categoryShares 분모 = 창 내 뷰티·대분류 보유 게시물**, mainCategory 20% 임계값도 같은 분모·같은 round로 판정(표시=필터 일치) ④**email 항상 null**(크롤러 미수집 — V31 확정), `contact=open`은 0건: 수집은 crawler 트랙 갭 ⑤로그인 월 예외는 `GET /v1/influencers` 정확 매치만(상세 잠김 유지) | [plans/2026-07-28-influencer-discovery-list.md](docs/superpowers/plans/archive/2026-07-28-influencer-discovery-list.md) |
| 2026-07-28 | **뷰티 판정 v3 — 한국어 콘텐츠 필터(`FOREIGN_INFLUENCER` 5분류)** — 서비스 목적(한국 시장 뷰티 시딩·협찬)에 맞춰 INFLUENCER 정의에 "한국어 콘텐츠 중심" 조건을 추가하고, 외국인 뷰티 개인 크리에이터를 신설 `FOREIGN_INFLUENCER`로 분리(V21 — 제약 5분류 재생성). 기준은 국적이 아닌 **한국어 콘텐츠**(LLM이 프로필 텍스트만으로 판정 가능·마케팅 목적 부합), 필터는 개인에만(COMPANY는 해외 브랜드도 컨택 타깃이라 언어 무관). BEAUTY_SERVICE 선례대로 beauty=false 파생 — 시드·수집·서빙 자동 제외, 세그먼트 보존(글로벌 확장 시 재활용). 프롬프트 v3 경계 규칙: 캡션이 최우선 신호(영어 bio+한국어 캡션→한국어 콘텐츠), 혼용은 주 오디언스 기준, 캡션 미수집 시 이름·bio로. 하류(analytics 뷰·was)는 파생 boolean만 읽어 무변경, rejudge 선정(비뷰티∧재료 갱신)에도 자동 포함. 어드민 FOREIGN 타일·외국인 필터·배지 확장. 머지 후 일회성 운영: `deploy/scripts/reset-influencer-judgments-v3.sql`(CLAUDE 판정 INFLUENCER만 초기화, MANUAL 보존)→어드민 BEAUTY 잡 재판정(batch-limit 단위 수 회) | [specs/2026-07-28-beauty-korean-filter-design.md](docs/superpowers/specs/archive/2026-07-28-beauty-korean-filter-design.md) |
| 2026-07-28 | **timely 판정을 캘린더일(KST)로 단일화 — `ContentAnalysisJob` 간격식 제거 + 기존 마킹 양방향 소급** — timely(제때 크롤) 판정 기준이 두 곳에서 갈라져 있었다: 후보 뷰(04)는 07-20에 캘린더일 기준(캡처 캘린더일 ∈ [업로드일+pin, +pin+slack))으로 재정정된 정본인데, Java 잡은 간격식(`metric_captured_at ∈ [posted_at+3일, +4일)`)을 "판정식 07-20 보존"으로 유지. 크롤이 KST 새벽 01~03시 1회라 두 식은 구조적으로 갈린다 — 뷰는 D+3일 크롤 1회면 timely, 잡은 낮 업로드 기준 D+4 새벽 크롤에도 잡혀야 간격 [3,4)에 든다(핀 규칙 v_pinned_metrics의 성숙도 같은 간격식). **07-28 운영 실측**: 대시보드(뷰 기준) timely 미분석 1,262건인데 잡 기준 잔여 0 — "새벽 배치가 돌았는데 안 줄어든" 표류. 잔여 분해: 간격 [3,4) 0건 / 간격 2.x일 925건(성숙 스냅샷 없음 — 계정은 크롤됐으나 게시물이 수집 범위에서 빠짐 745 + 계정째 미크롤 180) / 4일+ 337건. 이 누수(일 ~350–770건)는 late_backfill 마킹→**랭킹(timely만) 영구 제외**. 결정(PO): ①캘린더일이 정본 — 잡의 후보 선정을 raw 후보 뷰 직접 소비(`WHERE timely = ?` 서로소 분할)로 교체, 간격식·성숙 게이트·rn≤12 Java 중복 구현 삭제(수식은 뷰 한 곳). '이미 분석됨'·댓글 게이트·미러 존재(라이브 뷰-미러 간극 가드)는 Java 셋 대조 — 뷰 04 주석의 원 설계 회귀. 기각: 미러에 timely 컬럼 운반(접점 4곳+경계 오염), Java 수식 복제(이원화 재발 구조). ②**소급은 양방향 전체 정합** — 기존 마킹 전수 대조 실측(캘린더-timely인데 late_backfill 2,444 / 캘린더-not-timely인데 timely 4,144+뷰이탈 861 / 레거시 NULL·immature 중 timely 369) 기준 랭킹 모수 순감소 ~-2.5천(뷰티 필터 전)을 캘린더 정의에 맞춘 정직한 결과로 수용. 소급 런북(dry-run→승인→COMMIT)은 plans 문서 §Task 4 — 배포 후 별도 실행. 게이트 회귀는 SQL 하니스 04로 이관(recent-window=0 킬스위치 케이스 신규 추가). 회귀: `:analytics:test` GREEN·SQL 하니스 ALL GREEN | [specs/2026-07-28-timely-calendar-alignment-design.md](docs/superpowers/specs/2026-07-28-timely-calendar-alignment-design.md) · [plans/archive/2026-07-28-timely-calendar-alignment.md](docs/superpowers/plans/archive/2026-07-28-timely-calendar-alignment.md) |
| 2026-07-28 | **늦크롤 백필 분석 크론 점화 — 운영 compose에 `ANALYTICS_SCHEDULE_LATE_BACKFILL_ANALYZE_CRON: "0 0 21 * * *"`(KST 06:00) 추가** — 07-23 timely/late_backfill 분리 배포 때 "cron 실값은 운영 env에서 별도 설정"으로 스코프 아웃했던 항목이 그대로 누락돼 `runLateBackfill()`이 자동으로는 한 번도 안 돌았고(수동 트리거 전용), 신규 계정 온보딩마다 백카탈로그가 계단식으로 재적체됨(07-27 실측: 신규 계정 812개 유입으로 백필 잔여 569→9,041). 시각은 timely 분석(KST 05:00)과 계정 카피(07:00) 사이 06:00 — timely가 예산 독립이라 경합 없음. 커버리지 자체(계정별 최근 12개 경계)는 불변 — 12개 밖 옛 게시물 확장은 리포트 요구 생길 때 별도 결정 | 본 행 + [content-analysis-timely-backfill-split 스펙](docs/superpowers/specs/archive/2026-07-23-content-analysis-timely-backfill-split-design.md) |
| 2026-07-27 | **피어 퍼센타일·중앙값 ER은 analysis DB 파생 뷰(V39 `account_peer_stats`)** — 인플루언서 리포트에 "상위 N%" 상대 위치를 보여주려면 피어 집단(주 카테고리×팔로워 버킷)의 분포가 필요한데, 버킷 경계는 `app_setting` 런타임 설정으로 빼지 않고 뷰 SQL에 하드코딩 — 조회 시점마다 계산되는 파생값이라 크롤러 시드처럼 배포 불변성이 요구되는 기준값이 아니고, 버킷 자체를 바꾸는 일이 드물어 설정화 비용이 안 맞았다. 광고 판정은 새 축을 만들지 않고 기존 정본 `ad_type` 그대로 재사용(중복 판정 로직 신설 금지 원칙 유지) | [plans/2026-07-27-influencer-report-redesign-backend.md](docs/superpowers/plans/archive/2026-07-27-influencer-report-redesign-backend.md) |
| 2026-07-27 | **`account_analyses` 요약 3분할(V40) — 구 카피 컬럼은 삭제 대신 보존·미기록** — 카피 7종을 perf/content/ad 3개 요약 문자열로 재편하면서, 기존 7종 컬럼을 DROP하지 않고 신규 잡이 더 이상 쓰지 않는 채로 남겨뒀다(과거 이력 행 보존 + 롤백 여유). 카피 7종→5종 개편과 맞물려 `perf_summary IS NULL`을 자연 재대상 조건으로 삼아 별도 백필 스크립트 없이 기존 잡 사이클이 낡은 행을 스스로 갱신하게 함 — synthesis-partial-refresh 결정(콘텐츠 해석 문구 부분 갱신)과 같은 패턴의 재적용 | [plans/2026-07-27-influencer-report-redesign-backend.md](docs/superpowers/plans/archive/2026-07-27-influencer-report-redesign-backend.md) |
| 2026-07-27 | **ad_headline·성장세·유효 팔로워·유사도는 LLM 제거하고 was 알고리즘 산출로 전환 — `account_analyses` 기록은 `AccountAnalysisWriter` 단일 원천화** — 이 4개 값은 이미 확보된 결정 지표(팔로워 시계열·피어 중앙값 ER·traits)에서 결정론적으로 계산 가능해, LLM 카피에 맡기던 걸 걷어내고 was 응답 조립 시점 알고리즘으로 옮겨 비용·비결정성을 없앴다. 부수적으로 analytics 쪽 `account_analyses` 쓰기 경로가 여러 잡에 흩어져 있던 걸 `AccountAnalysisWriter` 한 곳으로 추출·단일화(CLAUDE.md의 "analytics만 분석 결과 쓰기" 경계는 그대로, 그 안에서 쓰기 지점만 정리) | [plans/2026-07-27-influencer-report-redesign-backend.md](docs/superpowers/plans/archive/2026-07-27-influencer-report-redesign-backend.md) |
| 2026-07-26 | **프로필 조회 400 → Hiker 폴백(`SELF_HIKER_FALLBACK`) — 계정별 폴백으로 SELF 무료·피드 열거 회복** — `web_profile_info`가 특정 계정에서 **IP 무관 HTTP 400**을 내는 인스타 버그(비즈니스 카테고리 관련 추정)가 07-23 기준 ~29%까지 번져 `profile.source`를 `HIKER_MOBILE`로 전면 전환해 뒀는데, 그 대가가 두 개였다: ①**피드 열거 상실**(게시물 열거는 SELF 프로필 원형 `edge_owner_to_timeline_media`에서만 가능 — HIKER 프로필엔 게시물이 없어 유료 `HIKER_V1_MEDIAS` igUserId 폴백 의존) ②정상 계정 ~71%까지 유료 Hiker 호출. 계정별로 400일 때만 폴백하면 정상은 무료 SELF(피드 포함), 버그 계정만 Hiker로 갈린다. 결정 셋: **①폴백 트리거는 400만** — IP 무관·결정적이라 재시도 무의미해 즉시 폴백, 404(계정 소멸)·블록(429/401/403) 소진·기타 실패는 기존 동작 그대로. **②`SelfProfileFetcher` 내장·셀렉터 오케스트레이션이 아닌 별도 컴포지트 `ProfileFetcher`**(`SelfWithHikerFallbackProfileFetcher`, 라벨 `profile-self-hiker`) — 기존 셀렉터·`app_setting` 런타임 토글 컨벤션 그대로 SELF ↔ SELF_HIKER_FALLBACK ↔ HIKER_MOBILE 전환이 되고, 컴포지트가 자기 라벨로 `executor.execute()`를 한 번만 감싼 뒤 두 페처의 collect를 직접 호출(패키지 가시성 개방)해 **crawl_run은 1건**으로 유지. `rawSource()`는 `SELF_GQL`(혼합 배치 기본 소스). **③혼합 배치의 아이템별 소스는 `ProfileExtractor.detect` 셰이프 감지**(루트 `data`→SELF_GQL, `user`/`pk`→HIKER_MOBILE, 그 외 기본값) — 페처 인터페이스 개편은 변경 범위 과다, 페이로드 마커 주입은 "응답 원형 그대로" 컨벤션 위반이라 배제. 소비처 3곳(`CollectJob.refreshProfile`·`QualifyJob.applyChunk`·`ProfileSupplementer.apply` — `DEFICIENT`에 새 소스 추가)의 배치 고정 source를 아이템별 감지로 교체해 `raw_profile.source`가 행 단위로 정확해진다. **이번 작업은 옵션 추가까지** — 실제 전환은 `profile.source` 수동 UPDATE 또는 어드민 `/ui/settings` 라디오로 사용자가 결정(피드 열거 제약상 소스 변경은 사용자 확인 필수), 롤백도 같은 경로. 회귀: `:crawler:test` 357 GREEN(신규 테스트 포함) · `:analytics:test` 226 GREEN. `:was:test`는 375 중 1 실패 — `InfluencerDetailControllerTest`가 시스템 시계를 쓰면서 픽스처 `lastPostedAt`을 `2026-07-12`로 고정해 isActive 14일 경계가 오늘(07-26) 넘긴 **본 작업과 무관한 기존 시각 의존 결함**(브랜치 diff에 was 무접촉) | [specs/2026-07-26-profile-400-hiker-fallback-design.md](docs/superpowers/specs/archive/2026-07-26-profile-400-hiker-fallback-design.md) · [plans/archive/2026-07-26-profile-400-hiker-fallback.md](docs/superpowers/plans/archive/2026-07-26-profile-400-hiker-fallback.md) |
| 2026-07-26 | **dev 스테이징 환경 설계 확정 (태스크 K)** — develop→main 머지 전 실환경 확인처가 없던 것을 해소. 배치는 **운영 인스턴스 동거**(OCI 무료 A1 한도가 06-15부로 2 OCPU/12GB로 반토막 — 운영이 전량 사용 중이라 신규 인스턴스 불가, 실측 여유 메모리 7GB+에 dev 스택 ~3GB + mem_limit 상한). raw는 **운영 postgres-raw 공유 + `analytics_dev` 스키마 격리** — raw가 10GB·하루 ~1GB 성장이라 사본 복원안은 지속 불가, 뷰(조회 공식)만 두 벌 설치하고 데이터는 한 벌. dev DB 계정은 crawler 테이블 읽기 전용 + `analytics_dev`만 소유(치환 누락도 권한 오류로 끝나는 fail-closed). Java는 `analytics.raw-schema` 프로퍼티(기본 `analytics`) 신설로 하드코딩 30곳 치환 — 운영 동작 불변, dev만 env 오버라이드. 승격 = develop→main 머지 그 자체(코드·설정 수정 0 — 같은 아티팩트, 다른 배포 설정). dev CD는 develop 푸시 자동(`cd-dev.yml`, arm64 단일 빌드, 뷰 `analytics.`→`analytics_dev.` 따옴표 제외 치환 + 잔존 참조 검증), 접근은 `dev-api.hypenow.io`(로그인 월 + dev 전용 가입 코드), dev 스케줄 전부 off(LLM 이중 지출·야간 raw 부하 경합 방지). 기각: E2.1.Micro(1GB — JVM 스테이징 부적합), 서버 분리(부하 실측상 불필요 + 무료 불가), raw 읽기 전용 공유+운영 뷰 사용(뷰 변경 검증 불가). 부수 발견: **운영 디스크 런웨이 4~5주**(여유 42GB, 하루 ~1.5GB 소모) — 별도 세션에서 대응 확정. (구현 07-28: 스키마 주입은 문자열 30곳 치환 대신 **조회 SQL 무접두어화 + raw DataSource `connection-init-sql`의 search_path**(`analytics.raw-schema`, env `ANALYTICS_RAW_SCHEMA`)로, dev 라우팅은 develop의 운영 Caddyfile 변경이 main 배포 전에 발효되는 뒷문을 막으려 **`caddy.d/dev-api.caddy` 분리**로 확정 — 본 Caddyfile은 glob import 한 줄. 트리거는 develop 푸시 직결이 아니라 CI 성공 후(`workflow_run`), 뷰 적용·잔존 참조 검사는 `ON_ERROR_STOP` fail-closed. 개통 순서 제약 2건은 [deploy/README.md §12](deploy/README.md) 체크리스트에 — `~/deploy/caddy.d` 선생성(root 소유 방지)과 dev 라우팅 발효를 위한 운영 배포 1회 선행. 최종 리뷰 반영 3건: dev 서비스 정의를 `compose.dev.yaml`로 분리(develop의 운영 서비스 정의 변경이 dev 배포의 depends_on 경유로 운영 postgres-raw를 재생성하는 뒷문 차단 — caddy.d와 같은 원리), 치환 스크립트의 `CREATE SCHEMA`는 치환 아닌 **삭제**(dev 계정에 DB CREATE 권한이 없어 IF NOT EXISTS도 권한 오류 — 스키마는 prepare 스크립트가 생성), 두 CD를 concurrency `deploy-server`로 직렬화(같은 `~/deploy` 경합)) | [specs/2026-07-26-dev-staging-environment-design.md](docs/superpowers/specs/archive/2026-07-26-dev-staging-environment-design.md) |
| 2026-07-23 | **`ContentAnalysisJob` 동시 처리(병렬화) — LIMIT 폐지가 드러낸 순차 처리 병목 해소** — timely/late_backfill 분리(같은 날 앞선 결정, LIMIT 완전 제거)로 예산 배분 문제는 없앴지만, `runQuery()`가 여전히 완전 순차라 대량 백필 적체(운영 실측 26,167건)가 7~14시간 걸리는 문제가 남아 있었다. Vertex는 RPM 페이싱이 없어(DSQ) 병렬화 여유가 있다는 사전 조사(LLM HTTP 클라이언트·`VertexTokenProvider`·`GeminiHttpApi.pace()` 전부 이미 스레드 세이프, DB 커넥션도 LLM 콜 동안 안 붙잡음) 후, `Executors.newFixedThreadPool(analytics.analyze-concurrency, 기본 8)` + `invokeAll`로 전환. 쿼타 소진(429)은 `AtomicBoolean` 플래그로 조정 — 이미 진행 중인 호출은 완료시키고 미착수 대상만 스킵. 구현 중 실제 데이터 레이스 발견·수정: 테스트 픽스처의 공유 `ArrayList`(`insightCalls`/`thumbnailArgs`)가 병렬 기본값(8) 아래서 동기화 없이 여러 스레드에 쓰여 간헐적 실패(5회 재실행 중 4회 실패) — `Collections.synchronizedList` + 순서 의존 테스트만 `concurrency=1` 고정으로 해소, 재현 검증(반복 실행 5/5 그린, 두 리뷰어 교차 확인). 회귀: `:analytics:test` 226 GREEN(Testcontainers 실행 포함, 3회 반복 무변동) | [specs/2026-07-23-content-analysis-concurrency-design.md](docs/superpowers/specs/archive/2026-07-23-content-analysis-concurrency-design.md) · [plans/2026-07-23-content-analysis-concurrency.md](docs/superpowers/plans/archive/2026-07-23-content-analysis-concurrency.md) |
| 2026-07-23 | **가입 코드 어드민 API 인증을 토큰 체인으로 통일** — 07-22 설계는 조회 GET·발송 PATCH를 ADMIN Basic 전제로 뒀지만, 어드민 FE는 세션·Basic 없이 `CODES_API_KEY` 헤더 방식만 사용해 `GET /admin/signups`가 401. @Order(0) 토큰 체인 매처를 `/admin/signup-codes`·`/admin/signup-codes/*`·`/admin/signups`로 확장해 적재·조회·발송 표시 모두 Bearer 키 인증으로 통일. 트레이드오프 수용: 해당 경로는 Basic 불가(Swagger try-it-out 안 됨), 정적 키 하나로 가입자 이메일 열람 가능(노출 표면 확대 인지) | SecurityConfig `signupCodeIngestFilterChain` |
| 2026-07-23 | **`ContentAnalysisJob` timely/late_backfill 후보 선정 분리 — 예산 독립화 + LIMIT 폐지** — 늦크롤 백필 후보가 몰리면(신규 계정 대량 유입 등) 매일 갱신돼야 할 timely 분석이 밀리는 문제. `base`/`ranked` CTE는 공유하되 바깥 WHERE를 `WHERE timely`(신규 `run()`)와 `WHERE NOT timely AND 윈도우`(신규 `runLateBackfill()`)로 상호 배타 분리 — 두 잡이 동시에 같은 short_code를 집을 수 없어 `content_analyses` INSERT 경합도 원천 차단. LIMIT은 양쪽 다 완전 제거(실질 상한은 기존 LLM 429 쿼타 이월 로직 재사용) — 신규 app_setting·마이그레이션 불필요. `JobName.LATE_BACKFILL_ANALYZE` 신설로 어드민·스케줄(`analytics.schedule.late-backfill-analyze-cron`) 독립 배선. 클래스는 진입점만 분리(별도 클래스 안 만듦 — 기준선 로딩 `loadBaselines()` 공유). 회귀: `:analytics:test` 212 GREEN(Testcontainers 실행 포함) | [specs/2026-07-23-content-analysis-timely-backfill-split-design.md](docs/superpowers/specs/archive/2026-07-23-content-analysis-timely-backfill-split-design.md) · [plans/2026-07-23-content-analysis-timely-backfill-split.md](docs/superpowers/plans/archive/2026-07-23-content-analysis-timely-backfill-split.md) |
| 2026-07-22 | **Swagger 표면 확장 + 가입 코드 발송 추적** — Swagger 표면을 `/v1/**`에서 `/v1/**, /admin/**`로 확장, 어드민 FE가 Swagger에서 어드민 API 시그니처 확인(접근은 기존 ADMIN Basic 게이트) / `app.signup_codes`에 `is_sent` 칼럼 추가(발송 여부, 소진과 별개 축), 양방향 PATCH 엔드포인트 제공 | `PATCH /admin/signup-codes/{code}` · [specs/2026-07-22-signup-codes-is-sent-design.md](docs/superpowers/specs/archive/2026-07-22-signup-codes-is-sent-design.md) |
| 2026-07-22 | **수동 발굴 등록 API + 크롬 익스텐션** — 크롤링 두 경로(DiscoverJob·SimilarJob)뿐이던 인플루언서 유입에 수동 경로 추가: crawler `POST /api/manual-discoveries`(신규만 DISCOVERED 생성, 기존 계정은 상태만 응답 — 반복 클릭의 discovery 이력 스팸 방지, 출처 keyword `수동:크롬`), 사전 공유 토큰 `X-Api-Token` 인증(`MANUAL_DISCOVERY_TOKEN`, 미설정 시 503 fail-closed). Caddy는 이 경로만 `/crawler/api/manual-discoveries`로 외부 공개 — 어드민 `/ui`·`/admin`은 계속 SSH 터널 전용. 수동 등록도 기존 qualify→beauty 파이프라인 동일 적용(팔로워 필터 면제 없음). 크롬 익스텐션(MV3 팝업 — 현재 탭 URL에서 username 추출, DOM 무접촉)은 레포 밖 별도 저장소 `hypenow-extension` | [specs/2026-07-22-manual-discovery-extension-design.md](docs/superpowers/specs/2026-07-22-manual-discovery-extension-design.md) |
| 2026-07-21 | **OCI 모니터링 알람 도입 + 보안 표면 점검** — 알람 5개 → ONS 토픽 `hypenow-alerts`(디스코드 웹훅을 Slack 프로토콜 구독으로 연결, 웹훅 URL 뒤 `/slack`): 인스턴스 CPU·메모리 85%·인스턴스 다운(oci_computeagent/infrastructure_health) + 컨테이너 다운·디스크 85%(커스텀 `hypenow_custom` — 서버 크론 1분 `post-container-metrics.py`, 인스턴스 프린시펄 인증이라 서버에 키 없음). 점검 결론: 방화벽은 시큐리티 리스트(22/80/443만 개방, 앱 포트 미노출)로 충분해 유료 방화벽 불채택. 레이트리밋은 `forward-headers-strategy: framework`가 XFF→remoteAddr를 복원해 **이미 IP별 정상 동작**(스푸핑은 Caddy 2.5+가 외부 XFF 폐기로 방어) — 취약 의심은 오진이었고 설정 한 줄에 걸린 보증이라 회귀 가드 테스트만 추가([PR #110](https://github.com/subtle-madness/hypenow-backend/pull/110)). 부트 볼륨 100GB는 전량 파티션 반영·사용 30% 확인(볼륨 증설 불필요) | deploy/scripts/post-container-metrics.py · deploy/README.md §10 |
| 2026-07-21 | **해석 문구 부분 갱신 도입 — 사실 추출과 해석 문구의 수명 분리** — `content_analyses`가 성격이 다른 둘을 한 불변 행에 묶고 있었다: **사실 추출**(브랜드·카테고리·`ad_type`·제품 — 캡션/썸네일에만 의존, 안 낡음)과 **해석 문구**(`ai_content_summary`·`contents_pattern`·`ai_comment_insight` + 기준선 스냅샷 — 기준선 수치를 인용해 정의가 바뀌면 낡음). 07-21 하루에 기준선 참여율 분모(조회수→팔로워, #97)와 `contentsPattern` 의미(#95)가 바뀌어 해석 문구가 전량 낡았는데, 통합 1콜+불변 행 구조라 **멀쩡한 사실까지 버리고 재추출**해야 했다(썸네일 다운로드·분류표 72행 재전송 = 비싼 쪽). 게다가 미러가 서빙 자격 집합만 담아 전량 재분석으로는 **후보에서 빠진 6,155건을 손댈 수 없었다**(지우면 영구 구멍). 해결: `synthesis_version`(**V38**)으로 해석 문구 계약 버전을 행에 기록하고, `ContentSynthesisRefreshJob`이 낡은 행만 골라 **문구+기준선 스냅샷만 UPDATE**(사실 컬럼 무접촉). 프롬프트는 파트 B만 떼어낸 `GeminiContentSynthesizer` — 분류표·썸네일이 빠져 훨씬 싸고, 문구 정의는 통합 콜과 `SYNTHESIS_RULES` 상수를 **공유**해 복제 드리프트를 차단한다. 기준선 스냅샷을 함께 갱신하는 이유는 동결의 의미("LLM이 본 것 = LLM이 말한 것")를 유지하기 위함이고, `metric_timeliness`는 수집 시점 사실이라 제외. **앵커를 못 구하는 행(미러·시계열 어디에도 없음)은 보존**한다 — 앵커 없이 재생성하면 "표본 부족" 문구가 되어 기존보다 나빠지기 때문(건수는 로그로 노출). 후보 자격과 무관하게 갱신되므로 전량 재분석이 손댈 수 없던 옛 콘텐츠까지 커버한다. **참여율 단위 정합(같은 PR)**: 저장·뷰는 비율(0.0812)인데 화면(was `engagementRateValue`)은 퍼센트(8.12)라 프롬프트가 단위 표기 없이 비율을 넘겨 LLM이 단위를 **추측**하고 있었다 — 운영 실측 137건은 변환해 맞혔지만 **22건은 원값을 그대로 인용**("평균 엔게이지먼트율 0.0023" vs 화면 0.23%). 프롬프트 키를 `recent12_avg_engagement_rate_pct`로 바꾸고 화면과 **같은 단위·같은 반올림**(×100, 2자리 HALF_UP)으로 실으며, 지시문에도 "_pct는 이미 퍼센트"를 명시했다. **DB 컬럼은 비율 그대로 유지** — was 소비처(postdetail·postdemo·V1 리포트·admin 템플릿)가 많아 개명 파급이 크고, 프롬프트 단위만 맞추면 문제가 해소되기 때문. 부수로 네 곳(통합 잡·갱신 잡·백필/버스트 러너)에 복제돼 있던 기준선 맵 조립을 `PromptBaseline`로 통합했다 — 07-21에 뷰만 고쳐지고 프롬프트가 남은 것도 이 복제 탓이다. was 산식을 테스트가 대조해 두 구현을 묶는다. 검증: `./gradlew test` 900 GREEN | `ContentSynthesisRefreshJob`·`PromptBaseline`·`V38__content_synthesis_version.sql` |
| 2026-07-21 | **adHeadline을 "광고 비교 한 줄"에서 "광고 활동 한 줄"로 재정의 — 4분기 상황값·객관 진술만** — 헤드라인 생성 게이트가 `hasAdComparison`(organic·협찬 평균이 **둘 다** 있는가) 불리언이라, 협찬이 아예 없는 계정과 측정 가능분이 전량 협찬인 계정이 **영구 NULL**이었다(운영 실측 **207계정/22%** — NO_ADS 91·ALL_ADS 116). PO 판단(07-21): 협찬이 없다는 사실도, 전량 협찬이라는 사실도 마케터에겐 정보다. `AdSituation` 4분기(COMPARABLE/NO_ADS/ALL_ADS/INSUFFICIENT)로 바꾸고 **근거가 아예 없는 INSUFFICIENT(37계정)만 NULL** 유지 → 헤드라인 보유 688→**895계정(96%)**. 프롬프트는 케이스별 진술 지침 + **평가·권유 금지 명시**("좋다·나쁘다·유리하다·적합하다" 금지 — 수치와 사실만, 판단은 읽는 사람 몫. 기존 LlmGuard의 조언 금지 규칙을 광고 문구까지 확장). 부수 정리: `AnthropicAccountSynthesizer`가 프롬프트 전문을 **복제**하고 있어 이번 재정의에서 옛 문구로 남을 뻔했고(컴파일로 발각), `GeminiAccountSynthesizer.instructions()/userText()` 공유로 통일했다 — 07-21 `hasAdComparison` 복제 사고와 같은 유형. 버스트 사이드카 키도 `has_ad_comparison`(불리언)→`ad_situation`(enum명), 옛 파일은 INSUFFICIENT로 보수 처리. 검증: `./gradlew test` 865 GREEN, 상황 분기 로직이 운영 실측 분포와 정확히 일치(688/116/91/37) | `AdSituation`(신규)·`AccountAdCanon.situation()`·`GeminiAccountSynthesizer` |
| 2026-07-21 | **서빙 이미지 아카이브(태스크 J) 구현** — image_assets(V37, analysis DB, PK (kind,key) — MirrorConfig 미등록)·ImageArchiveJob(썸네일 1회·프로필 파일명 변경 감지·건 단위 실패 격리)·스케줄 04:50 KST(미러 04:30→아카이브 04:50→분석 05:00)·was 서빙 /v1+레거시 /api 전부 `COALESCE('/img/'||object_path, 원본)` 폴백 치환(응답 계약 무변경)·PAR 어댑터 @Lazy(미설정 시 그 잡만 실패). 운영 개통(버킷 ObjectReadWithoutList·쓰기 PAR·서버 env)은 수동 ops 대기 | [PR #98](https://github.com/subtle-madness/hypenow-backend/pull/98) |
| 2026-07-21 | **서빙 이미지 아카이브 설계 확정 (태스크 J — 구현 미착수)** — 인스타 CDN URL ~4일 만료로 깨지는 서빙 이미지 3종(프로필·릴스 썸네일·게시글 썸네일)을 OCI `hypenow-images`(도쿄) 버킷에 영속화. ①소유=analytics 별도 잡(raw 읽기→다운로드→업로드→analysis DB `image_assets` 기록 — 경계 규칙 부합·크롤러 무변경) ②치환=was 조회 COALESCE(`/img/`+객체경로 상대 서빙, 미아카이브분은 신선 CDN 폴백 — 뷰·미러 무변경) ③갱신=썸네일 shortCode당 1회(기록된 건 다운로드 생략 — 12개 윈도우 중복 재저장 없음)·프로필은 URL 파일명(미디어 ID) 변경 시만 같은 키 덮어쓰기, `image_assets`는 미러 목록 제외 누적(content_analyses 전례) ④접근=퍼블릭 읽기 버킷+Vercel rewrite(`/img/:path*`→OCI, 프론트 배포 완료) 엣지 캐시, 캐시 헤더는 업로드 시 부착(썸네일 immutable·프로필 1일). 결정적 객체 키(`thumb/{shortCode}`·`profile/{handle}`)로 계정·콘텐츠당 1객체 고정 — 2만 계정에도 축적 없음. 이미 만료된 과거분은 회복 불가(현상 유지), 크롤 지속 대상은 다음 잡에서 자동 회복 | [specs/archive/2026-07-21-image-archive-design.md](docs/superpowers/specs/archive/2026-07-21-image-archive-design.md) |
| 2026-07-21 | **분석 대시보드 v3 — 퍼널 폐기, 2축(계정·콘텐츠) + 크로스 DB 대조(G1·G2)** — v2 퍼널 `수집→후보→분석→미러`는 각 칸의 모수·시점이 달라(스냅샷 vs 누적, 미러는 독립 경로) 숫자가 역전되는 깔때기 아님이 운영 실측으로 확인됨(누적 분석 16,827 > 현재 후보, 계정 카피 1,517/모수 723 같은 >100%). 재설계 축: ①**G1 크로스 DB 잔여 대조** — `PipelineStatsService` 무거운 집계(TTL 30분)에 analysis `content_analyses` short_code 셋 로드(HashSet, `ContentAnalysisJob` 기존 패턴) → raw `v_analysis_candidates`와 Java 대조를 편입, 트랙별(timely=랭킹/윈도우=상세) × (기분석/미분석) 4분할과 "진짜 잔여"(후보 ∩ 미분석 — 아무도 계산 안 하던 그 숫자)를 산출. 계정 카피도 동일(raw `v_accounts` 핸들 ∩ analysis 카피 핸들). raw 쪽 스캔은 REPEATABLE READ 한 트랜잭션(후보·풀·서빙·계정이 같은 스냅샷 — 구 "한 statement" 제약의 승격). ②**G2 커버리지 재정의** — 분모·분자 모두 "현재 서빙 모수"(raw `v_serving_content` ∩ 분석 보유), 누적 16,827은 모수 리비전 혼재라 각주 강등. ③건강 스트립 "마지막 미러" 라벨을 **"크롤 지표 신선도"로 개명**(실체 = max(captured_at) — 07-20 크롤 정지를 잡은 신호인데 이름이 미러 잡 시각으로 오독됨). 오늘 예정/완주일 계산도 진짜 잔여 기준으로 교체. 운영 대조 실측으로 항등식 검증(후보 9,308 = 기분석 8,987 + 미분석 321 — 뷰티 재판정 진행 중이라 절대값은 문서 §1 시점과 다름, 구조 성립). 잔여 갭 G3(판정 히스토리 — crawler 영역)·G4(미러 잡 시각 영속)·G5(크롤 지표 정지 — 운영)·G6(리비전 마킹)은 미착수 | [plans/2026-07-21-analytics-dashboard-v3-data-model.md](docs/superpowers/plans/archive/2026-07-21-analytics-dashboard-v3-data-model.md) |
| 2026-07-21 | **인스타 유료 파트너십 태그를 콘텐츠 분석 프롬프트에 확정 사실로 주입 (재분석 선행 조건)** — 광고 판정 정본은 캡션 분류(`content_analyses.ad_type`)인데, **인스타 공식 태그(`ad_marked`)가 프롬프트에 아예 안 실려** LLM이 태그가 붙은 게시물을 organic으로 뒤집는 사례가 **운영 87건**(gemini 59·opus 21·haiku 7 — 모델 비례 분포 = 모델 문제 아닌 프롬프트 결손). 반대 방향(태그 없음 ∧ sponsored) 7,021건은 정상(캡션 고지 광고 — ad_headline 버그의 근거). 태그는 인스타가 보증하는 hard evidence라 **증거력이 단방향**: '있음'이면 반드시 sponsored, '없음'은 organic의 근거가 못 된다(피드는 태그 기능 자체가 없고 릴스도 캡션 고지가 흔함) — 지시문에 이 비대칭을 명시. 렌더링은 값이 아니라 **콘텐츠 타입**으로 분기한다(미러가 피드에 false를 채우므로 값만 보면 "없음"→"광고 아님" 오독). 배관: `v_contents`에 `p.paid_partnership AS ad_marked` 투영(이미 조인된 `v_pinned_metrics`라 조인·플랜 불변) → 미러 `contents.ad_marked`(**V36**, V35는 다른 브랜치 선점) → 일상 잡. `v_analysis_candidates`는 같은 컬럼을 통과만 시켜 백필·버스트 러너도 공유. **미러 경유가 불가피한 이유**: `ContentAnalysisJob`은 analysis DB만 읽는데 `account_content_series.sponsored`는 30,358건 중 18,124건(60%)만 덮어 대체 불가. 검증: `./gradlew test` 843 GREEN, SQL 하니스 ALL GREEN, **구 뷰 위에 새 뷰 얹는 CD 경로 실측 통과**(CREATE OR REPLACE는 끝에 컬럼 추가만 가능 — 제거는 불가). 카테고리 앵커(`category_top_percentile` 등 전 행 NULL)는 `03`에서 하드코딩 NULL 상수이고 크로스 DB·닭달걀이라 **이번 범위에서 제외**(별도 설계 필요) | `02_serving.sql`·`04_analysis_candidates.sql`·`V36__contents_ad_marked.sql`·`GeminiContentAnalyzer` |
| 2026-07-21 | **카테고리 맥락 스텁 2건 복구 — 크로스 DB 제약을 미러가 아닌 "각 층에서 라이브 계산"으로 우회** — V8에서 crawler `main_group`이 사라지며 카테고리 관련 산출물이 상수 스텁으로 남았고(운영 실측: `content_analyses.category_*` 16,827건 중 3건만 채워짐, 미러 `account_category_stats` 0행), 대체재인 V30 캡션 분류(`content_analyses.main_category`)가 끝내 연결되지 않았다. **연결이 안 된 진짜 이유는 DB 경계** — `analytics.*` 뷰는 raw DB에 있고 `main_category`는 analysis DB에 있어 뷰에서 조인할 수 없다. 미러(raw 뷰 → analysis 테이블) 경로로는 구조적으로 채울 수 없는 값이었다. 해소 방식 두 갈래: ①**계정 카테고리 믹스**는 미러를 포기하고 **analysis DB 안의 파생 뷰**(V35 — 테이블 DROP 후 동명 뷰)로 재정의. 입력(`account_content_series`⋈`content_analyses`⋈`beauty_taxonomy`)이 전부 analysis DB라 raw가 필요 없고, 이름·컬럼이 같아 소비자 3곳(`AccountAnalysisJob`·`ClaudeBurstRunner`·was `InfluencerDetailRepository`)은 무접촉. 라벨은 `beauty_taxonomy.main_label`, 모수는 최근 N개 윈도우(§4-1). 운영 실측 3,244행/1,405계정/9,941건 — 계정 종합 LLM 프롬프트의 "카테고리 믹스: `[]`"가 채워져 traits 품질이 개선된다. ②**콘텐츠 상세 카테고리 맥락 3필드**는 was 조회 시점 **라이브 계산**(`V1ContentReportRepository.findCategoryContext`). `content_analyses.category_*` 3컬럼은 **영구 NULL로 확정**했는데, `main_category`가 그 행을 만든 통합 1콜 LLM의 산출물이라 저장 시점엔 아직 카테고리를 모르고(닭-달걀) 테이블이 불변(INSERT-only)이라 나중에 채울 수도 없기 때문 — 조회수 baseline·rank를 라이브로 옮긴 A2(07-19)와 같은 처방이며 **과거 16,827건 전량에 소급**된다(백필 불필요). NULL 규칙: 표본은 `is_beauty` ∧ 같은 대분류 ∧ **조회수 NOT NULL**(피드는 views가 항상 NULL이라 모수는 사실상 릴스) ∧ 시점 편향 없는 분(`timely` 또는 레거시 NULL — 랭킹 `/v1/contents`와 같은 필터. 늦크롤 백필을 섞으면 평균이 부풀려진다: cleansing 전체 평균 75,543 vs timely 18,499). 백분위는 이 게시물이 표본과 같은 잣대일 때만 계산하고(피드·늦크롤 백필이면 null) 평균·표본 수는 그대로 노출. 회귀: `:was:test` 359 / `:analytics:test` 172 GREEN + SQL 하니스 ALL GREEN | `V35__account_category_stats_view.sql`·`V1ContentReportRepository`·`V1ContentReportAssembler` |
| 2026-07-21 | **계정 카피 광고 판정을 캡션 분류(`content_analyses.ad_type`) 정본으로 통일 — `ad_headline` 영구 NULL 해소** — 화면(was)은 07-17(`e203c3c`)에 광고 지표를 `ad_type='sponsored'` 정본으로 옮겼는데 **카피 잡의 `hasAdComparison` 판정만 옛 소스**(`account_summaries.organic_avg`/`ad_avg` = `ad_marked` = 인스타 유료파트너십 태그, **릴스 전용**)에 남아, 캡션으로만 협찬을 고지한 계정은 프롬프트가 "광고 비교 데이터: 없음" → LLM 빈 문자열 → 저장 시 무조건 NULL이 됐다(운영 실측 `ad_headline` NULL **1,304/1,517 계정(86%)**, 옛 게이트 통과는 311개뿐). 수정: 판정·수치를 **`AccountAdCanon` 단일 원천**으로 추출 — `AccountAnalysisJob`·`ClaudeBurstRunner`에 복제돼 있던 로직(한쪽만 고쳐지는 재발 원인)을 공유 함수로 합쳤다. 게이트는 was `Assembler.comparison`과 **동일 조건**(기준지표>0인 organic·ad 두 그룹이 모두 있을 때만) — 한쪽만 있으면 화면에도 비교가 안 떠 설명 대상 없는 헤드라인이 되므로. 프롬프트에 싣는 수치도 같은 정본으로 치환(`organic_avg`·`ad_avg`·`ad_drop_pct`·`sponsored_count`·비교 건수·`last_ad_posted_at` + 게시물별 `sponsored`) — 헤드라인이 설명하는 숫자와 화면 숫자가 갈리지 않게. 정본 테이블은 analysis DB에 있어 잡의 analysis JdbcTemplate으로 읽는다(raw의 `analytics.*` 뷰에선 DB가 달라 조인 불가). 운영 시뮬 실측: 게이트 통과 311→**1,045계정**(신규 해금 771, 이탈 37 = 측정 가능한 게시물이 전부 협찬이라 비교 자체가 불성립 — 화면도 동일하게 비교 없음). 미러 뷰(`10_account_detail`)의 `ad_marked` 집계 컬럼은 **무변경**(다른 소비처 보존, 카피 잡만 정본으로 치환). **이 수정은 다음 잡 실행분부터 적용** — 기존 NULL 1,304건은 재분석 필요(본 PR 범위 밖) (07-30 정정: 07-27 개편으로 쓰기 경로가 제거돼 재분석으로는 채울 수 없다 — 작업 소멸) | `AccountAdCanon`(신규)·`AccountAnalysisJob`·`ClaudeBurstRunner` |
| 2026-07-21 | **수동 배포 이중 가드 (07-20 배포 겹침 장애 후속)** — CD 도입 당일 develop 체크아웃 상태의 수동 deploy.sh가 CD의 main 배포를 20분 만에 덮어 was(develop)·analytics(main) 버전 불일치 → was의 `is_beauty` 필터가 미적용 마이그레이션(V34) 컬럼을 참조해 랭킹 API 전면 500(main 롤백으로 복구). 재발 방지 2겹: (1) deploy.sh에 HEAD≠origin/main 거부 가드(`--force`로만 우회 — 긴급 경로 보존), (2) CLAUDE.md 컨벤션에 "배포는 develop→main 머지로만" 명문화(클로드 세션 차단). GHCR write PAT 회수(완전 차단)는 CD 신뢰 축적 후 별도 판단, main branch protection은 무료 플랜 private 레포라 불가 | deploy/scripts/deploy.sh |
| 2026-07-21 | **뷰티-무대분류 콘텐츠 무한 재대상 루프 종결 (self-heal §3-3 개정)** — `ContentAnalysisJob`이 `isBeauty=true`이나 복구 후에도 `main_category=null`인 콘텐츠를 "행 미기록→NOT EXISTS 재대상"(self-heal)으로 두던 것이, 분석이 **temperature 0 결정론**이라 재실행해도 동일 결과 → 매 실행마다 같은 콘텐츠를 다시 집어 LLM 호출만 태우고 영영 미완(운영 로그 `뷰티 콘텐츠인데 대분류 미분류 — 재대상` 반복, 실측 스턱 다수). 수정: 이 케이스를 **`is_beauty=false`로 종결 저장**(`ContentAttributes.asNonBeauty()` — isBeauty만 false, 나머지 속성 보존)해 루프를 끊는다. 불변식 'main_category null ⇒ 서빙 비뷰티'는 그대로 보존(is_beauty=false라 랭킹·인플루언서 상세에서 제외) → **was 서빙 무변경**. 진짜 일시 실패(빈 종합·파싱 오류)의 재대상 폴백은 유지. 백필 러너(`GeminiBackfillRunner`)는 별개 경로라 미접촉(스트리밍 개조 #89 진행 중). 회귀: `ContentAnalysisJobTest`를 종결저장 케이스로 전환, `:analytics:test` GREEN | `ContentAnalysisJob`·`ContentAttributes.asNonBeauty()` |
| 2026-07-21 | **랭킹 `/v1/contents` 시점 노출 정책 확정 — late_backfill 제외, 인플루언서 상세만 노출** (07-19 `metric_timeliness` 결정의 "서빙 노출 정책 미결" 해소) — 최근12 백필 재도입(트랙 M)으로 늦크롤 백필분(`late_backfill`)이 대량 유입되는데, 이들은 고정 지표를 +3일이 아닌 **늦은 시점에 캡처**해 누적 좋아요·조회수가 상향 편향된다. PO 결정(07-21): **랭킹에는 시점 편향 없는 분만 노출**하고 백필분은 인플루언서 상세에서만 보이게. 구현은 was 서빙 계층 한 곳 — `V1ContentRepository.buildWhere`(findCards·countCards 공용)에 `an.metric_timeliness = 'timely' OR IS NULL` 추가. **NULL 처리**: V33 이전 미분류 레거시(백필 편향과 무관)는 비회귀로 유지, `late_backfill`·`immature`만 제외. 인플루언서 상세 `recentContents`(`findRecentCards` LEFT JOIN)·저장 목록·per-content 리포트는 **무변경**(사용자가 특정 계정·콘텐츠를 직접 여는 맥락이라 노출 유지). 레거시 `/api/contents`(ContentListRepository, 구 최신 스냅샷 패러다임·정리 대기)는 범위 밖. 커버리지 확장 본체(#1·#2·#5)는 트랙 M이 이미 구현·머지 → 본 세션은 이 정책 해소만. 회귀: `V1ContentRepositoryTest` late_backfill 제외·timely/레거시NULL 노출 케이스 추가, `:was:test` GREEN | [specs/2026-07-21-ranking-timeliness-exposure-policy-design.md](docs/superpowers/specs/archive/2026-07-21-ranking-timeliness-exposure-policy-design.md) |
| 2026-07-20 | **뷰티 판정 v2 — 4분류(beauty_class)로 시술·서비스 분리 + judge 기본 claude-api(구독) 전환** — 판정 목적(뷰티 제품 시딩·협찬 대상 발굴)에 맞게 피부과·성형외과·에스테틱·헤어샵 등 시술·서비스(업체+그 영역 개인)를 신설 BEAUTY_SERVICE로 분리(INFLUENCER/COMPANY/BEAUTY_SERVICE/NOT_BEAUTY, `influencer.beauty_class` — 마이그레이션은 develop V16·V17 선점으로 **V18**). boolean(beauty/beauty_company)은 `BeautyClass` 파생 유지 — BEAUTY_SERVICE→beauty=false라 SIMILAR 시드·수집·분석 뷰·was 무변경 자동 제외. 프롬프트 전면 개정(목적·경계 규칙 명시), `crawler.beauty.judge` 기본 gemini→claude-api(ANTHROPIC_AUTH_TOKEN 구독 우선, matchIfMissing 포함 이동 — gemini/claude-cli는 롤백). 어드민 명단·오버라이드·대시보드 4분류 확장. 머지 후 일회성 운영: `deploy/scripts/reset-beauty-judgments.sql`(MANUAL 포함 전체 초기화)→서버 어드민 재판정 | [specs/2026-07-20-beauty-class-v2-design.md](docs/superpowers/specs/2026-07-20-beauty-class-v2-design.md) |
| 2026-07-20 | **Vertex 기본 승격 + SA키 없으면 gemini 그레이스풀 폴백 (전환 후속)** — "웬만하면 vertex로"를 안전하게: `LlmConfig.useVertex(provider,credsPresent)` — `provider=vertex`라도 `GOOGLE_APPLICATION_CREDENTIALS`가 없으면(크레딧 소진·무설정 로컬·CI) 무료 gemini로 폴백(로그 경고), 코드 상수 `DEFAULT_LLM_PROVIDER`는 gemini 유지(시드 없는 순수 환경 안전망). baseline 승격은 **V17 신규 마이그레이션**으로 — V16이 이미 운영 적용돼 체크섬 고정이라 인플레이스 수정 불가. V17은 `UPDATE ... WHERE value='gemini'`(조건부): 운영은 이미 런타임 vertex라 불일치→보존, 신규/재생성 환경만 gemini→vertex 승격. anthropic 롤백 폴백 모델도 opus→haiku(#85 머지분에 포함). 로컬은 SA 키 배치 전까지 폴백으로 gemini. (feat/vertex-migration은 머지됨 — 이 후속은 develop 기준 새 브랜치) | `LlmConfig`·`V17__analytics_llm_provider_vertex.sql`·analytics/README |
| 2026-07-20 | **분석 후보 스코프를 "최근창 ∩ 날짜자격"→"날짜자격 전체"로 확장 (계정 평균 앵커)** — 프론트 `/v1/contents`는 업로드 날짜 범위로 조회하는데 분석 후보가 최근창(`analytics.recent-window`=12) ∩ 날짜자격이라, **다작 계정**(며칠 새 12개 초과 게시)의 "날짜 자격 있으나 최근 12 밖" 성숙분이 분석에서 누락 → 날짜조회에서 빠졌다(운영 실측 138건, elire_85·sjiette81 등 계정당 5~8개). 후보 선정 3곳(`ClaudeBurstRunner.exportContents`·`GeminiBackfillRunner.submit`·`ContentAnalysisJob.run`)의 `candidates ⨝ baseline`(short_code INNER)을 LEFT로 완화. **A-rich**: 계정 평균(recent12_avg_*·recent_reels_avg_views 등)은 윈도우 밖 게시물에도 계산 가능하므로 앵커로 붙이고(aiContentSummary "계정 평균 대비" 근거 유지), `rank_in_recent_reels`만 윈도우 밖에선 null(피드 게시물이 이미 rank null인 기존 경로 재사용). 뷰 리팩터: 계정 집계를 `v_analysis_account_baseline`(account_handle 키)로 분리, `v_analysis_baseline`은 이를 조인해 출력 불변(기존 소비자 무접촉). `contents` 미러는 이미 뷰티 모수(v_contents→v_serving_content)라 비뷰티/DISCOVERY 누출 없음. 트레이드오프: 분석 볼륨 ≈6%↑(다작 계정 위주, 스팸성 가능). B안(recent-window 상향)은 baseline 의미가 바뀌어 비채택. 하니스(03)·Java 3종 테스트 GREEN. **#85(Vertex 백필) 위에 재통합(reconcile/79-on-85)** — 같은 07-20 스코프 확장 둘(‘최근창 밖 날짜자격’=본 건 / ‘늦크롤 최근창 백필’=#85)이 `ContentAnalysisJob.run`·`GeminiBackfillRunner`의 자격 쿼리를 각자 재작성해 충돌. 하나의 eligible 쿼리(**timely(창 무관) OR 최근 N 윈도우** + 창 닫힘 게이트)로 통합하고 eligible 전량 iterate + withBaseline→accountBaseline→EMPTY 폴백으로 합침, 러너 SELECT는 `ab.*`(계정평균)+`c.timely`(V33 마킹) 병존. **운영 배포·138건 재-export 미실행(사용자 승인 대기)** (07-30 정정: 138건은 별도 재-export 없이 #85 배치에 흡수 완료 — main 도달 릴리스 #90) | feat 브랜치 (03 뷰 리팩터 + 러너/잡 LEFT JOIN), 관련 PR #71(날짜자격)·#75(sargable)·#77(스냅샷 캐시) |
| 2026-07-20 | **Vertex AI 전환 + 최근 12개 백필 재도입** — LLM 인프라를 AI Studio 무료 티어 → Vertex AI(SA OAuth, global 엔드포인트, DSQ 쿼터)로 완전 전환(`analytics.llm-provider=vertex`, 동기+배치 GCS 양쪽). 동기는 `VertexTokenProvider`(SA JSON→cloud-platform 액세스 토큰)+`VertexHttpApi`(순수 REST, 기존 `GeminiHttpApi` 요청 바디 재사용 — 프롬프트·스키마·파서 무접촉, RPM 페이싱 제거(DSQ)·429/5xx 지수 백오프는 유지), 배치는 GCS JSONL 업로드→배치잡 생성→출력 prefix 목록 조회·파싱(상관관계 재설계 — `key` 필드가 없어 에코된 요청 텍스트 첫 줄에서 short_code 복원). 전환 동기는 $300 무료 체험 크레딧이 AI Studio Gemini API엔 적용되지 않고(2026-03 정책 변경) Vertex 퍼블리셔 Gemini 모델에만 적용돼 Vertex가 유일 경로라는 점. **crawler 뷰티 판정(GeminiBeautyJudge)은 무접촉**(무료 AI Studio 키 그대로 유지). 병행: 04 뷰·`ContentAnalysisJob` 분석 자격에 "제때 크롤 EXISTS **OR** 계정별 최근 N개 윈도우 포함"으로 OR을 넓혀 07-19 "백필 MVP 제외" 결정을 번복 — 늦크롤분도 최근 창 안이면 대상에 포함하고 `metric_timeliness`를 가드 충족 여부(timely 불리언)로 직접 timely/late_backfill 분기(하드코딩 폐지, 일상 잡·백필 러너 공통). 초기 백필 물량(~1.4만)은 Vertex 배치로 일회 소화(50% 할인, 추정 $5~10 — 크레딧 내). 검증 유보(구현 중 스모크 확정 대상): 배치 `model` 필드 짧은/풀 경로, GCS 버킷 리전 제약, 429 실동작. 운영 적용 절차는 [런북](docs/runbooks/2026-07-20-vertex-backfill-runbook.md) | [specs/2026-07-20-vertex-migration-recent12-backfill-design.md](docs/superpowers/specs/2026-07-20-vertex-migration-recent12-backfill-design.md) |
| 2026-07-20 | **콘텐츠 단위 뷰티 판별 + main_category NULL 정합** — 운영 811건 중 342건(42%) main_category NULL의 두 원인((a)비뷰티 미표현 (b)sanitize 드랍)을 통합 처리. ①통합 1콜 속성 프롬프트·스키마에 `isBeauty` 추가(별도 콜 없음)·`content_analyses.is_beauty`(V34, main_category 있는 기존 행은 true 백필). ②sanitize가 어휘 밖 대분류를 드랍 대신 sub_categories·detectedProductCategories 유효 라벨로 **역유도 복구**(`BeautyTaxonomy.deriveMain`, 최다 득표·동점은 분류표 순 tie-break, 재질의 없음). ③실패 시맨틱: 뷰티인데 복구 후에도 미분류면 **행 미기록→NOT EXISTS 재대상**(self-heal), 비뷰티는 is_beauty=false 저장해 루프 이탈. ④서빙: 비뷰티는 계약 확장 없이 API에서 제외 — 랭킹 `is_beauty=true`, recentContents `IS DISTINCT FROM false`(미분석 유지), 카테고리 믹스는 기존 main_category NOT NULL로 자동 제외(생산자 불변식이 지탱 — sanitize가 비뷰티면 main_category를 null로 확정해 "main 있음⇒뷰티"를 전역 보장, 소비처 필터 무증설). ⑤기존 342건은 `ops/reprocess_uncategorized_content_analyses.sql`(is_beauty NULL∧main NULL 삭제→재자격, 멱등). 04 후보 뷰는 raw 계층이라 무변경(SQL 하니스 회귀 없음). 트레이드오프: sub 없이 isBeauty=true만 주는 극소수는 매 실행 재시도(무료 쿼터 무해, 시도 상한은 후속). 자매 태스크(커버리지 최신12 확장)는 이 머지 후 별도 세션 | [specs/2026-07-20-content-beauty-flag-and-category-fix-design.md](docs/superpowers/specs/2026-07-20-content-beauty-flag-and-category-fix-design.md) |
| 2026-07-20 | **crawler DB 오프사이트 백업 신설 + run_item 중복 잔재 삭제** — 07-19 크롤링 주체가 서버로 넘어가며 서버 raw가 유일 원본이 됐는데 backup.sh는 analysis만 백업(07-19 미결 해소). backup.sh에 crawler 일일 pg_dump 추가 — 서버 3일 + Drive `hypenow-backups/crawler/` 최신 3개 롤링(무료 15GB 제약, 증설 시 상향. analysis 30d 프루닝은 `--max-depth 1`로 crawler/ 비간섭), pull-backup.sh도 2종 pull. 백업 크기 절감 선행 작업: PR #48(COLLECT·REELS run_item 이중 저장 제거)이 새 쓰기만 멈추고 기존 중복은 남겨둔 것을 확인 — 타입 테이블 원형 보존 검증(불일치 0건) 후 pre-dedup 안전 덤프를 서버 `~/backups/`에 떠두고 12,292건(~1.8GB) DELETE + VACUUM FULL. crawler DB 5.1GB→3.3GB | deploy/scripts/backup.sh |
| 2026-07-20 | **CD 파이프라인 신설 — main 푸시 = 배포** — 배포가 맥 수동 `deploy/scripts/deploy.sh`뿐이던 것을 GitHub Actions `cd.yml`로 자동화. 트리거는 **main 푸시**(develop→main 머지가 곧 배포 — develop CI 통과가 전제), 대상은 was·analytics·**crawler** 3종(기존 deploy.sh 기본값은 was+analytics). 이미지는 매트릭스 병렬로 bootJar→buildx 멀티아치(arm64+amd64)→ghcr push(`GITHUB_TOKEN`, `:latest`+`:sha-<short>` — 태깅·롤백 절차는 deploy.sh와 동일 유지). 서버 배포는 SSH(`DEPLOY_SSH_KEY`·`DEPLOY_HOST` 시크릿)로 compose pull·up. **분석 뷰도 CD에 포함** — 전부 `CREATE OR REPLACE`(멱등)이고 CI sql-harness가 프레시 DB 적용을 사전 검증하므로 매 배포마다 raw DB에 재적용(유일하게 남아 있던 수동 배포 단계 제거). crawler Flyway 기동 경합 대비 재시도 5회, 마지막에 `/health` 확인. deploy.sh는 수동·긴급 경로로 잔존 | [.github/workflows/cd.yml](.github/workflows/cd.yml) |
| 2026-07-20 | **콘텐츠 리포트 표시 참여율을 팔로워 기준(피처링식)으로 전환 (v2.1 후속)** — hype_score 랭킹은 팔로워 정규화로 바꿨는데 프론트(api.hypenow.io) 콘텐츠 AI 리포트의 표시 참여율만 `÷조회수`라, 상위(고조회수·바이럴) 게시물이 0.6% 같은 낮은 %로 보이는 불일치 발생(예: `_so_eunii_` 72만뷰 상위 게시물 0.58%). 수정: `V1ContentReportAssembler`에서 게시물별 `value`와 계정평균 `baseline`을 모두 `(좋아요+댓글)/팔로워×100`(피처링식)로 통일 — `V1ContentReportRepository`가 `account_summaries.followers`를 조인(`s.handle=c.account_handle`), was 라이브 계산이라 **분석 뷰·content_analyses·백필 불필요**. 효과: `_so_eunii_` 0.58%→64.28%, baseline도 팔로워 기준(8.36%). 부수 정합화: 기존엔 value가 퍼센트(0.58)인데 baseline은 비율(0.0373)이라 단위가 어긋나 있던 것도 해소. 피드는 팔로워 기준이라 값이 생김(÷조회수의 NULL 규칙 벗어남). 범위 밖(불변): 어드민 `dashboard.html`·`postdetail`·`account_summaries.avg_er_pct`의 ÷조회수 참여율. was 재배포 필요 | v2.1 후속(참여율 표시 기준 통일) |
| 2026-07-20 | **hype_score v2.1 — 릴스 참여 축 조회수→팔로워 정규화 (저조회수 뭉침 해소)** — v2에서 릴스 engage 분모가 **조회수**라 조회수가 극소인 릴스가 참여율 폭발로 과대 점수를 냄(예: 1뷰·1좋아요·팔로워 2.1만이 99점). 도달:참여 1:1이라 폭발한 참여가 낮은 도달을 이김. 조회수가 릴스에서 도달·참여 **양쪽에 이중** 사용된 게 근본 원인. 수정: 릴스 engage 분모를 피드와 동일하게 `(followers+1000)`로 교체(피처링식 `(좋아요+댓글)/팔로워`) → 조회수는 **도달 축에만** 남김. 분모 변경으로 참여 스케일 이동 → **e0 0.02→0.01**(팔로워당 참여율 중앙값≈0.0094 반영), **릴스 앵커 재적합**(운영 `v_analysis_candidates` 릴스 n=1717 새 qf 백분위 = 0.0736/0.7379/2.6312/5.5619; 기존 앵커가 같은 집합 old qf 백분위와 일치함을 확인해 산출 기준 동일 검증). 피드 로직·화면 참여율 지표(`03` engagement_rate=÷views)·NULL 규칙 불변. 검증(운영 전수 18,814 릴스 시뮬): 상위 뭉침 ≥95 119→76건, 저조회수 극단 교정(1뷰 99→1점), 조회수 집계 깨진 고참여는 팔로워 신호로 보존(좋아요 4894/팔로워 7887→74점). app_setting 오버라이드 없어 코드 기본값이 단일 소스 | [specs/2026-07-20-reels-hype-engage-follower-normalization-design.md](docs/superpowers/specs/archive/2026-07-20-reels-hype-engage-follower-normalization-design.md) |
| 2026-07-20 | **스냅샷 flatten 물질화 캐시 (`analytics.content_snapshot_cache`)** — `v_base_content_snapshot`(raw JSON을 매 쿼리 jsonb_array_elements로 펼침)이 데이터 증가(스냅샷 14.7만)로 병목이 돼, 미러 20분·후보 쿼리(v_analysis_candidates) 40분+·분석 잡이 느려짐. flatten 결과를 담는 **캐시 테이블 + 인덱스(content_id,captured_at)** 신설, 소비 뷰(v_base_detail·v_pinned_metrics·v_content_metric_snapshots·v_analysis_candidates) 4곳을 이 캐시를 읽게 repoint → flatten 1회 계산으로 수 분→초. **갱신은 `analytics.refresh_snapshot_cache()`**(TRUNCATE+INSERT from live view) — 야간 잡 직전 **서버 cron**(Java·이미지 무변경) + 필요 시 수동. 신선도=refresh 주기(모든 소비처가 배치/야간이라 수용). MV 대신 테이블+함수: CREATE OR REPLACE·CREATE TABLE IF NOT EXISTS라 idempotent 적용·CASCADE 없음, 하니스는 시드 후 refresh 호출(본문 raw 삽입 테스트는 삽입 후 refresh — prod 동작과 동일). 배포는 apply+refresh를 한 트랜잭션(원자적, 빈 캐시 노출 0) | feat/snapshot-matview 브랜치 |
| 2026-07-20 | **가입 코드 일괄 적재(`POST /admin/signup-codes`)** — 정적 토큰(CODES_API_KEY) @Order(0) 체인(사람용 Basic과 분리), channel 접두사 유도(빈 접두사 400), ON CONFLICT 스킵 후 {inserted,skipped}. | [specs/2026-07-20-admin-signup-codes-ingest-design.md](docs/superpowers/specs/archive/2026-07-20-admin-signup-codes-ingest-design.md) |
| 2026-07-20 | **hype_score v2 — 연속 절대식·타입별 앵커 재설계 (상위 뭉침 해소)** — v1(하드캡 재보정)이 중앙값은 올렸으나 `min(x,1)` 포화로 서로 다른 지표의 콘텐츠 20건이 91점에 뭉치고(최상위 변별이 신선도만 남음), 피드는 참여율 3.5% 초과 시 전부 포화(10.9%=4.0%=91점), 바이럴 릴스(조회 153배)는 66점에 억눌림. v2: **하드캡 제거** — 연속·무제한 로그 축(reach=`ln(1+views/(fol+1000))`, engage=`ln(1+참여율/e0)`) → 합성 Q(도달:참여 **1:1** — 참여 편중은 "안 퍼진 고참여"(도달 0.1배·참여 49%)를 98점으로 띄우는 함정 실측 확인) → 신선도 곱 → **타입별 4점 앵커**(p05→10·p50→45·p90→80·p99→97, 운영 07-20 산출·동결)로 0~100 구간 선형 매핑 — 피드·릴스 동일 출력 분포(동일 스케일 비교). 함수는 6-인자 복귀, 상수는 **함수가 app_setting 직접 조회(STABLE)** — e0/f0/가중치/반감기/앵커 8종 전부 런타임 튜닝. 검증(운영 797건): 중앙값 45·95점 이상 12건(뭉침 해소)·바이럴 66→99·피드 참여율순 분리(17.6%→96 > 10.1%→89). 절대식이라 모집단 이동 시 앵커 재보정 필요(스펙 §6). comments×3 어뷰징 엣지는 보류 | [specs/2026-07-20-hype-score-v2-redesign-design.md](docs/superpowers/specs/archive/2026-07-20-hype-score-v2-redesign-design.md) |
| 2026-07-20 | **hype_score 스케일 재보정 + 튜닝 상수 4종 app_setting화 (평균 점수 과소 대응)** — 운영 분석 집합(`contents⋈content_analyses` 1,041건) 실측 결과 중앙값 19·평균 21로 과소. 축 분해(릴스 743건 중앙값): reach 0.112·engage 0.132·fresh 0.522 — **병목은 reach·engage, 신선도 아님**(콘텐츠 중앙 나이 6.6일이라 이미 신선). 원인은 만점 기준이 모집단 현실 대비 과도(조회 30배·참여 12%/10% vs 실측 0.47배·1.6%). 재보정: reach 30배→**3배**, engage 12%→**4%**, feed 10%→**3.5%**, 신선도 반감기 7일→**14일**. 운영 데이터 전수 재계산으로 중앙값 21→40 확인(옵션 C; D=2배/3%/3% 중앙값 46은 상위 포화 더 큼). 4종 모두 `app_setting` 키로 분리(`hype-fresh-halflife-days`·`hype-reach-target-mult`·`hype-engage-target`·`hype-feed-engage-target`) — 기본값은 함수 내 COALESCE 단일 소스, 재배포 없이 튜닝. 트레이드오프: 목표 하향으로 상당수 콘텐츠가 reach·engage 만점 포화 → 최상위 변별이 신선도 위주로 축소. 인자 추가로 구 함수 오버로드 잔존 → 뷰 재정의 후 파일 끝에서 구 시그니처(6·7인자) DROP. 반영은 뷰 적용+재미러(was 무변경) — 운영 반영 완료(분석 집합 중앙값 19→39) | [PR #70](https://github.com/subtle-madness/hypenow-backend/pull/70) |
| 2026-07-20 | **제때 크롤 가드를 시간 간격→캘린더 날짜 기준으로 (04 뷰)** — 가드 창을 `captured_at ∈ [posted+3, posted+3+slack)`(시간 간격) 에서 **`캡처 캘린더일(KST) ∈ [업로드일+3, +3+slack)`**(날짜)로 전환. 이유: 화면에 뜨는 건 업로드 '날짜'인데 시간 간격이면 저녁 업로드는 72~96h 창이 다음날로 걸쳐 ①같은 날짜인데 자격 시점이 흔들리고(운영에서 07-16이 "창 아직 열림"으로 83만 보이다 오늘 크롤로 ~600으로 뒤늦게 참) ②한 크롤일이 두 업로드일을 물어 date-bleed. 날짜기준이면 업로드일 D ↔ 크롤일 D+3 1:1, 확정적. 성숙 가드는 "제때창이 **완전히 지난 날**"(D+pin+slack ≤ 오늘 KST)로 통합 — 구 `analyze-maturity-days` 키 흡수(04에서 미참조). **slack 코드 기본값 2→1**(운영 app_setting=1과 정합, app_setting 지워져도 회귀 방지). 사용자 결정(07-20): "크롤-업로드 diff는 정확히 3일, 화면 날짜와 딱 맞아야". 트레이드오프: D+3 당일 크롤 못 잡히면 누락(slack로 완충). `test/04_*`에 경계 픽스처(창 어제 닫힘=포함/오늘=제외)·비-usable·slack=2 케이스 추가, 전체 하니스 GREEN. 배포 후 잔재는 `ops/reconcile_content_analyses.sql`(날짜기준)로 정리 | feat/analysis-date-based-window 브랜치 |
| 2026-07-19 | **관리자 가입 코드 조회(`GET /admin/signups`)** — 기존 ADMIN Basic 체인(스웨거 전용 `swaggerFilterChain`)을 `adminBasicFilterChain`으로 일반화해 `/admin/**` 매처를 추가 편입(새 인증 메커니즘 신설 없음). `app.signup_codes LEFT JOIN app.users`로 소진(누가 언제 썼는지)·미소진 코드를 한 번에 반환, 미소진·탈퇴(FK ON DELETE SET NULL) 행은 email/userId/usedAt이 명시적 null(`SignupUsageRow`에 `@JsonInclude` 미부착) | [specs/2026-07-19-admin-signup-codes-design.md](docs/superpowers/specs/archive/2026-07-19-admin-signup-codes-design.md) |
| 2026-07-20 | **저장 2종 메모 편집 전용 PUT 추가 (프론트 요청)** — 기존 `POST /v1/saved-{contents,influencers}`가 저장+메모수정 upsert를 겸했으나, 프론트가 "이미 저장한 항목의 메모만 고치는" 전용 엔드포인트를 요청. `PUT /v1/saved-contents/{contentId}`·`PUT /v1/saved-influencers/{influencerId}`(body `{memo}`) 신설. **편집 전용 시맨틱**: 저장 이력이 없으면 404(POST와 달리 새 저장을 만들지 않음 — 프론트 실수로 미저장 항목에 PUT해도 조용히 저장되지 않게)로, `updateContentMemo`/`updateInfluencerMemo`가 `UPDATE ... WHERE user_id AND id RETURNING`으로 0건이면 empty→404. memo 정규화(공백→null)·응답 카드/프로필 조합은 POST와 동일. 인플루언서는 목록과 같은 정책으로 프로필 미러 부재 시에도 handle-only로 200(저장이 정본), 콘텐츠는 카드 부재 시 404. status는 불변, 인플루언서만 updated_at 갱신. 스펙 6.6~6.11엔 없던 추가분 | 프론트 요청(세션) |
| 2026-07-19 | **분석 후보 자격을 "핀 기반"→"창 내 usable 스냅샷 EXISTS"로 재설계 (결정론화)** — 구 04 가드는 `v_contents.metric_captured_at`(고정 핀) ∈ [posted+pin, posted+pin+slack) 로 판정했는데, 핀 선택 로직이 시점·정책에 따라 옮겨다녀(특히 릴스 usable 우선순위 PR #58) 같은 데이터라도 자격이 뒤집혔다. 운영 실측: `content_analyses` 2,130건 중 옛 기준 잔재가 다수(핀 기준 298 / 새 EXISTS 기준 570) — 게시일 2023~2025 백필 포함. 가드를 `EXISTS(창 안 usable 스냅샷)`으로 바꿔 (업로드시각, 스냅샷 캡처시각)만의 함수로 결정론화 — 핀 로직·잡 실행 시점과 무관. 부수 효과: 창 안 스냅샷이 비-usable(뷰 비공개 릴스 등)뿐이면 제외(구 가드는 핀되면 포함 — 회귀 아닌 의도된 강화). `test/04_*`에 dummy_pd(핀은 창 안·usable은 창 밖) 회귀 케이스 추가, 전체 하니스 GREEN. 배포 후 기존 잔재는 `ops/reconcile_content_analyses.sql`로 재조정 삭제(EXISTS 정합 570건, self-healing — 재자격 시 재분석) | [PR #67](https://github.com/subtle-madness/hypenow-backend/pull/67) |
| 2026-07-19 | **인플루언서 상세 "최근 12개"를 분석분만→실제 최신으로** — `GET /v1/influencers/{id}`의 `recentContents`(`findRecentCards`)가 `contents ⨝ content_analyses` **INNER JOIN**이라, 분석 스코프(숙성 3일·백필 제외 → 사실상 특정일 이후분만)가 그대로 전이돼 "최근 12개"인데 최신 게시물이 안 뜨는 문제. 목록 조회(6.1)는 카드가 분석 산출이라 분석분만 노출(INNER, 유지)이지만, 인플루언서 상세는 "그 계정의 실제 최신 12개"가 자연스러운 기대라 **LEFT JOIN으로 전환** — 미분석 게시물도 노출(분석 필드 main_category·ad_type·brands 등은 null/빈배열, 지표·썸네일은 채움). 어셈블러는 이미 null-safe(빈배열 반환). 계약 변경: recentContents는 더 이상 "분석 완료분만"이 아니라 최신 12개. was 조립 계층만(미러·record·SQL뷰 무변경) | findRecentCards LEFT JOIN + V1InfluencerRepositoryTest |
| 2026-07-19 | **콘텐츠 리포트 comparison.views 차트 라이브 분리 + recentReels 식별자** — 최근창 usable-핀·광고 ad_type 통일(같은 날 다른 브랜치)이 릴스 조회수 NULL과 인플루언서 광고 소스는 고쳤지만, 콘텐츠 AI 리포트(`GET /v1/contents/{id}/ai-report`)의 `comparison.views`는 여전히 baseline·multiple·rankInRecent·recentCount를 분석 시점 `content_analyses`에 **고정**한 값을 서빙해 라이브 `value`와 드리프트가 남았다. 이 4개 파생값을 was에서 라이브 recentReels 리스트로 재계산(비NULL 릴스 평균·경쟁 랭크, §4-2 응답 조립)해 `views` 블록 내부 정합을 보장 — **AI 서술이 인용한 수치는 `content_analyses`에 그대로 고정**(서술 고정·차트 라이브, 사용자 결정). `content_analyses.recent_reels_*` 컬럼은 삭제하지 않고 서술/디버그용으로 잔존. 추가로 `recentReels[]`에 `contentId`·`isCurrent` 필드 신설 — 프론트가 "마지막 항목=현재 콘텐츠" 위치 계약 없이 강조 막대를 그림(프론트 요청 3). 미러·record·SQL 무변경(was 조립 계층만) | [plans/2026-07-19-ai-report-data-consistency.md](docs/superpowers/plans/archive/2026-07-19-ai-report-data-consistency.md) |
| 2026-07-19 | **커버리지 대시보드를 was → analytics 어드민으로 이전** — was `/coverage`는 로그인 월 뒤에 있지만 운영에서 api.hypenow.io 전체가 was로 프록시되고 users에 role 구분이 없어 일반 가입자도 열람 가능한 노출 문제. 내부 화면의 정위치는 127.0.0.1 바인딩(SSH 터널 전용) analytics 어드민 — `/ui/coverage` 신설(매트릭스 28행은 07-18 /v1 실소비 필드 정의 유지 + 수집 모수 타일 신설: raw 서빙 뷰 `v_accounts`·`v_serving_content`를 그대로 읽어 미러 분모로 표시 — 모수 필터는 02_serving.sql이 정본, Java에서 중복하지 않음), was coverage 패키지·템플릿 제거. **원칙: 커버리지 등 내부 검증 화면은 analytics 어드민 소속, was는 고객 서비스 표면만.** 매트릭스 정의 쌍은 analytics 내부로 이동(CoverageRepository ↔ check/coverage.sql) | [specs/2026-07-14-was-coverage-page-design.md](docs/superpowers/specs/archive/2026-07-14-was-coverage-page-design.md)(🗄) |
| 2026-07-19 | **분석 백필 MVP 제외 — 판정은 스냅샷 시점 가드** — 데일리 LLM 분석 후보를 "제때(+3일 근방) 크롤돼 고정 지표가 성립한 분"으로 한정(크롤 시작 07-16 이전 업로드분 ≈ 백필은 분석 안 함). 최초 구현(posted_at 14일 창)은 늦크롤분(07-05~12 업로드)을 오포함해 재설계 — `metric_captured_at ∈ [posted_at+pin(3), posted_at+pin+slack(2))` 가드를 04 뷰·분석 잡 양쪽에 적용(새 키 `analytics.analyze-timely-slack-days`). 늦크롤 백필(+3일 지표 없음)과 미성숙 폴백 지표(분석되면 영구 고정되는 누수)를 함께 차단, 분석 밀림은 나이 무관 재대상(SLA는 크롤링에만) | [PR #49](https://github.com/subtle-madness/hypenow-backend/pull/49) |
| 2026-07-19 | **usable 핀을 최근창 경로로 확장 + 광고 지표를 ad_type 정본으로 (프론트 리포트 stale 신고 후속)** — (1) PR #58의 usable 우선순위 핀이 `v_contents`(랭킹)에만 적용돼, 최근창 경로(`v_recent_content`→recentReels·`v_analysis_baseline`·`account_summaries`)는 여전히 `v_base_detail`(최신 스냅샷 승)을 써 최신 빈 타임라인 스냅샷이 릴스 조회수를 NULL로 밀어냈다(운영 리포트 recentReels·baseline 조회수 결측). 핀 로직을 공유 뷰 `v_pinned_metrics`(00_base)로 추출해 v_contents·v_recent_content가 공유 — 지표는 pin(성숙∧완비 우선)·메타는 v_base_detail(최신) 분리 유지. 실데이터에서 v_recent_content.views가 v_contents와 일치 복구, 하니스 회귀 `test/01b_recent_window_pin.test.sql`. (2) 인플루언서 리포트 광고 블록(strip·sponsoredCount·comparison·lastAdNote)이 `ad_marked`(인스타 유료파트너십 태그·릴스만)만 잡아 캡션 고지 광고를 놓쳐 brands(ad_type 기반)와 어긋났다 → was `findSeries`가 `content_analyses.ad_type='sponsored'` 조인으로 sponsored 유도, `toAds`가 광고 블록 전체를 series 기준 재계산(분석 결과끼리 조인 §4-4). **LLM 재호출 불필요**(재미러+was 배포로 반영). 동결 baseline(기분석분)은 INSERT 전용 잡이라 자가 치유 안 됨 — 백필/재분석은 별도 판단 보류 | feat/analytics-recent-window-pin 브랜치 |
| 2026-07-19 | **뷰티 판정 Claude API(SDK) 어댑터 — 클라우드에서 구독 과금** — 크롤러 클라우드 합류로 `claude-cli`(로컬 Claude Code CLI 자식 프로세스) 사용 불가 → `crawler.beauty.judge=claude-api`로 선택되는 `ClaudeApiBeautyJudge` 추가(기본은 gemini 유지). 프롬프트·파서는 팀 단일 원천(ClaudeCliBeautyJudge) 재사용, 모델 `claude-haiku-4-5`(`crawler.beauty.claude-model`). 과금은 자격증명이 가른다: `claude setup-token`으로 발급한 구독 장기 토큰을 `ANTHROPIC_AUTH_TOKEN`에 — authToken 우선·oauth 베타 헤더(analytics LlmClientFactory 패턴을 crawler에 복제, §4-4 모듈 격리). 서버에 `ANTHROPIC_API_KEY`를 두지 않아야 API 과금 유출이 없다 | [specs/archive/2026-07-19-claude-api-beauty-judge-design.md](docs/superpowers/specs/archive/2026-07-19-claude-api-beauty-judge-design.md) |
| 2026-07-19 | **Swagger 운영 노출 + admin 게이트(07-17 미노출 결정 대체)** — users.role(USER/ADMIN, V11) 최소 권한 체계 도입, 스웨거 경로 전용 @Order(1) 체인(HTTP Basic 팝업·STATELESS·hasRole ADMIN, 매처는 /v3/api-docs.yaml·/swagger-ui.html 포함). AppUserDetails.role은 transient — principal 직렬화 형상 불변, 단 세션엔 토큰 authorities 스냅샷이 남아 STATELESS 근거는 "무권한"이 아닌 신선도(매 요청 Basic 재인증이 현재 DB role 반영). 운영 반영엔 admin 계정 승격(수동 SQL `UPDATE app.users SET role='ADMIN'`) 필요, prod 브루트포스 완화(caddy rate limit)는 후속 | [specs/2026-07-19-swagger-admin-gate-design.md](docs/superpowers/specs/archive/2026-07-19-swagger-admin-gate-design.md) |
| 2026-07-19 | **릴스 지표 핀에 usable 우선순위 도입 (hype_score 결측 대량 복구)** — `v_contents` 지표 고정(pin)이 성숙 스냅샷 중 '가장 이른 것'만 골라, view 비공개(0→NULL)인 SELF_GQL 내장 타임라인 스냅샷이 clips보다 먼저 수집되면 그걸 핀했다. 릴스 hype_score는 views 없이 못 구하므로 옆에 지표 완비 clips 스냅샷이 있어도 점수가 NULL이 됨(운영 릴스 결측 1,720건 중 1,567건이 이 경로). 핀 우선순위에 `usable`(릴스=views·likes·comments, 피드=likes·comments 완비)을 추가: 성숙∧완비 → 완비 최신 → 성숙 최이른 → 최신. 완비 스냅샷이 없으면 구 폴백 그대로라 원천 결측(피드 좋아요 비공개 등)은 NULL 유지·회귀 없음. 서버 실데이터 BEGIN/ROLLBACK 검증: 릴스 결측 1,720→153, 피드 3,567→3,558(부수 9), 전체 5,287→3,711. `test/02b_reels_pin.test.sql`로 회귀 고정 | [PR #58](https://github.com/subtle-madness/hypenow-backend/pull/58) |
| 2026-07-19 | **클로즈베타 전환(초대코드 가입 전용)** — 단일 공용 코드를 배치 1회용 코드(V8 `signup_codes`)로 **즉시 전환**(병행 없음 — 검증 로직 두 벌 유지가 더 위험, 기존 코드를 넣으면 1회용으로 의미가 바뀜). 소진 정본은 `used_at`(used_by는 ON DELETE SET NULL — 탈퇴해도 소진 유지), 선점은 가입 INSERT와 **한 트랜잭션의 조건부 UPDATE**(동시 가입 레이스 1명만 통과, 409 실패 시 미소진 롤백). 관문 UX용 사전 검증 `POST /v1/auth/signup-code/verify`는 소진하지 않음(TOCTOU는 가입 재검증이 흡수). 가입 필드 경량화(V9 — 필수는 이메일·비밀번호·이름·userType·companyName·약관 3종, 값 있으면 어휘 검사 유지). 도입문의 V10 `inquiries`는 **uuid PK**(공개 응답 순번 노출 회피), 레이트리밋은 시간 윈도우 확장 없이 분당 2회로 갈음. ⚠️ 배포 순서: 코드 배치 적재 → 프론트 env 코드 교체·호환 모드 기본값 제거 통지 | [PR #53](https://github.com/subtle-madness/hypenow-backend/pull/53)·[#54](https://github.com/subtle-madness/hypenow-backend/pull/54)·[#56](https://github.com/subtle-madness/hypenow-backend/pull/56) (스택, 순서대로 머지) |
| 2026-07-19 | **가드 이전 기분석분은 삭제 대신 마킹 (`metric_timeliness` V33)** — 가드(PR #49) 도입 전 분석된 431건 중 374건이 현 기준 후보 밖(늦크롤 백필 345·미성숙 폴백 29)으로 실측됨. 데이터 보존을 위해 삭제하지 않고 `content_analyses.metric_timeliness`(timely/late_backfill/immature)로 마킹: 데일리 잡은 timely 스탬프(후보 뷰 가드가 보장), 유료 Batch 백필은 late_backfill, 기존 행은 운영 1회성 UPDATE(raw 대조)로 분류. 대시보드 커버리지·잔여 계산 분자를 timely로 정정(전체 431 기준은 과대). **서빙(랭킹) 노출 정책은 미결** — 랭킹은 "분석 완료만" 노출이라 백필 기분석분의 지표 비교 편향(늦은 시점 상향)이 남아 있음 | feat/analysis-timeliness-marking 브랜치 |
| 2026-07-18 | **SELF 차단 대비 유료 피드 우회 경로 — HIKER_MOBILE 프로필 + /v1/user/medias/chunk 보충** — IG가 익명 웹 요청을 광범위 차단하면(SELF 401·Hiker gql 계열 전면 500 동시 발생, 07-18 실측) 프로필·피드 모두 모바일 API 계열로 우회 가능. CollectJob이 내장 타임라인 없는 프로필 소스에서 chunk 1페이지를 보충(원형 raw_media_page 저장, 실패 시 방문 실패·재시도 — 게시물 없는 '방문 완료' 방지 가드). Hiker web_profile_info는 타임라인을 안 줌(실측 — WebGQL 소스만으론 피드 불가). 평시 기본은 무료 SELF 유지, 전환은 `profile.source` 런타임 설정 | feat/beauty-captions 브랜치 |
| 2026-07-18 | **재방문 대상 선정을 달력일(KST) 기준으로 전환** — "마지막 방문 후 24h(N일) 경과" 대신 경계 = 오늘(KST) 자정 − (N−1)일(`RevisitCutoff`, 기본 N=1이면 오늘 안 돈 계정 전부·자정 자동 리셋). Clock 빈도 UTC→Asia/Seoul(존은 날 경계 계산에만 쓰임 — 저장은 전부 Instant라 데이터 영향 없음, 데일리 대시보드 "오늘" 타일도 KST 자정으로 정렬). collect/reels 선정·JobCostEstimator·StatusService due 수치가 같은 경계 공유. 설정 키 `collect.revisit-interval-days`는 유지, 의미만 달력 기준으로 | [specs/2026-07-18-kst-daily-targets-design.md](docs/superpowers/specs/archive/2026-07-18-kst-daily-targets-design.md) |
| 2026-07-18 | **재스냅샷(RESNAPSHOT) 기능 제거** — qualify가 SELF(캡션 포함 재료)로 첫 판정을 하게 되면서 캡션 백필 루프의 존재 이유가 소멸(SELF 4스레드 병렬화로 속도 문제 해소). 마지막 드레인(2,261명 재수집 → rejudge)으로 백로그를 소진한 뒤 잡·선정 쿼리(`findResnapshotTargets`)·설정 키(`resnapshot.batch-limit`)·UI 트리거·다이어그램 루프·`ProfileExtractor.isPrivate`를 제거. 드레인 실측 구제 +74명(~3.4%)으로 07-16 실험치(~30%) 대비 크게 낮아 잔존 가치 없음도 확인. `JobName.RESNAPSHOT`은 crawl_run 실이력 렌더 때문에 판독 전용 레거시로 보존(AGGREGATE 전례). rejudge는 범용(재료 갱신 시 재판정)이라 유지. 단, HIKER 등 캡션 없는 소스로 대량 qualify 웨이브를 다시 돌리려면 이 기능이 필요하다 — git 이력(9ce462d)에서 복원 | feat/beauty-captions 브랜치 |
| 2026-07-19 | **COLLECT·REELS의 raw_run_item 이중 저장 제거** — 응답 payload가 타입 raw 테이블(raw_profile·raw_media_page)에 1:1 무가공 저장되는 잡은 run_item 사본을 생략(`JobName.archivesRunItems`). 원형 보존 보장은 타입 테이블이 승계, 하루 적재 절반 감소(실측 626MB/1.25GB). 타입 테이블 없는 잡(SIMILAR 등)·신규 잡은 기본 무조건 아카이브 유지. 기존 중복분(~2.1GB)은 클라우드 이사 덤프에서 제외로 정리 예정 | feat/skip-duplicate-run-items 브랜치 |
| 2026-07-19 | **대시보드 퍼널에 백필 제외 가드 관측 반영 (PR #49 후속)** — 무거운 집계를 후보+"성숙 풀"(04 뷰 자격에서 제때 크롤 가드만 뺀 모수) 1문장으로 확장해 가드 제외분(≈늦크롤 백필)을 수치로 노출, 가드 창 파라미터(pin·slack — app_setting 현재값)를 퍼널 meta에 표기. 수집→후보 낙차(운영 2.7만→1.9천)가 백필 MVP 제외 정책임이 화면에서 읽히게 | feat/dashboard-backfill-guard 브랜치 |
| 2026-07-19 | **어드민 대시보드 재설계 (태스크 I 개편)** — 운영 첫날 피드백(단계 추적 불가·비용 카드 무의미·자동/수동 구분 불가)으로 관측 대시보드 전환. 잡→ProgressReporter(analyze 경계 인터페이스)→JobProgressRegistry 진행률, RunHistory 인메모리 피드(이력 DB 없음 유지), PipelineStatsService(빠른 동기+무거운 비동기 30분 캐시), ScheduleInfo(크론 다음 발화 — base는 시스템 존, @Scheduled 해석과 정합·표시만 KST). JobCostEstimator 삭제. 리뷰에서 '다음 예정' 9시간 존 오차 잡아 수정 | [specs/2026-07-19-analytics-dashboard-design.md](docs/superpowers/specs/2026-07-19-analytics-dashboard-design.md) |
| 2026-07-19 | **이메일 소유권 인증 구현(6.17 [TBD] 해소)** — 가입 전 강제(스텝5), 6자리 코드(TTL 10분·오입력 5회), Resend HTTPS 발송(키 미설정 시 로깅 폴백 + 기동 로그, connect 5s/read 10s 타임아웃), 서버 상태 방식(V7 `email_verifications`, verified 30분·가입 성공 시 1회 소비). signup 검증 순서에 403 EMAIL_NOT_VERIFIED 삽입(429→가입 코드→필드→이메일 인증→409). 운영 개통은 Resend 도메인 인증(DNS SPF/DKIM) + RESEND_API_KEY 등록 필요 — 프론트 배선(REST 전환) 전 배포 시 운영 signup은 인증 선행 없이는 403 | [specs/2026-07-18-email-verification-design.md](docs/superpowers/specs/archive/2026-07-18-email-verification-design.md) |
| 2026-07-19 | **운영 서버에 raw DB 상주 컨테이너 신설 + e2e 실데이터 이전** — 오라클 운영 compose에 `postgres-raw`(crawler DB, 루프백 5433, 계정 .env `RAW_DB_*`) 추가. 07-17 일회성 LLM 실행(e2e-*)에 올라가 있던 raw 실데이터 2.4GB(콘텐츠 27,093·인플루언서 12,837)를 postgres-raw로, 분석 결과 public 스키마(계정 12,638·콘텐츠 16,686·LLM 분석 account 77/content 88·landing_stats)를 운영 analysis DB로 이전(구 114건 미러 대체, `app` 스키마·세션은 보존 — e2e의 검증용 유저 3건은 미이관). 운영 /v1/contents 실데이터 응답 검증 완료. was의 raw 접근 금지 규율은 유지(같은 compose 네트워크지만 접속 정보 미주입). 미결: ~~backup.sh는 analysis만 백업(raw 백업 여부)~~(07-20 해소), e2e-* 컨테이너 정리 | deploy/compose.yaml |
| 2026-07-18 | **LLM 스택 Gemini 3.1 Flash-Lite 전환 (태스크 L)** — 골드셋 40건 실측(Opus 기준 mainCategory 90%·adType 98%·subCat Jaccard 0.62·브랜드 88%, Haiku 4.5보다 우수·5.5배 저렴)으로 전 분석 축 통일. ②속성+③종합은 통합 1콜(`ContentInsightPort` — Anthropic은 기존 어댑터 2콜 컴포지트로 보존해 app_setting `analytics.llm-provider` 롤백 경로), 문구 프롬프트에 절제 규칙(`LlmGuard` — 표본 3건 미만 단정 금지·조언 금지·수치 인용, 골드셋 문구 검증 통과본) 필수. 이원 운영: 일상=무료 키(`GEMINI_API_KEY`) 동기+RPM 페이싱, 429/일한도 소진은 에러 아닌 배치 이월 / 백필 2만 건=유료 키(`GEMINI_API_KEY_PAID`) Batch one-shot(`analytics.backfill-submit`→`-collect`, ~$9). ①판정은 크롤러 BeautyJudge 포트 뒤 Gemini 어댑터(팀 프롬프트·파서 재사용, `crawler.beauty.judge` 기본 gemini). 댓글 분류는 MVP 휴면이라 Anthropic 유지. 구모델(2.5)은 신규 API 키에서 404 — 3.1이 유일 선택지 | [plans/archive/2026-07-18-gemini-llm-stack.md](docs/superpowers/plans/archive/2026-07-18-gemini-llm-stack.md) |
| 2026-07-18 | **설계-구현 전수 감사 + 문서 드리프트 정비** — 백엔드·프론트 specs 35건 전수 대조 결과 백엔드는 무동작 스텁 0건·트랙 표와 실체 일치. 정비: §5 G 표현 명확화(이메일 **소유권 인증**(6.17)은 [TBD] 미구현 — G의 "이메일 인증"은 이메일+비밀번호 로그인 의미), 대체·현행과 어긋난 spec 7건 상태 헤더 갱신(detail-source-selector 🗄 등), `/coverage` 진입점 제거로 고아가 된 `PostDemoRepository.analyzedPosts()` 죽은 쿼리 삭제(`/posts/{shortCode}` 페이지 자체는 URL 직접 접근으로 잔존). 주요 잔여 갭은 프론트 측: 마스터 비밀번호 백도어(celfit-front core.ts — 제거 대기), 이메일 인증 실구현(설계 착수), REST 배선(PR #18 draft) | 감사 세션 리포트 |
| 2026-07-18 | **/coverage 매트릭스를 celfit-front 실소비 필드 기준으로 재정의** — 기준 코드는 celfit-front 배포본(origin/main). 구 content-ranking 카드 14행을 프론트가 실제 렌더·필터에 쓰는 /v1 필드 27행(카드·필터 6.1 → 드로어 리포트 6.3 → 인플루언서 6.4/6.5)으로 교체. 타입에만 있고 UI 미소비인 필드(email·external_link)와 /v1 미사용 미러(content_metric_snapshots — 단 metric_captured_at 원천이라 coverage.sql 골격 가드는 유지)는 제외. 구 산출물 content_ranking 행은 본 쿼리에서 분리 조회(테이블 부재에도 매트릭스 생존 — was는 폴백 행, coverage.sql은 to_regclass DO 블록). 매트릭스 정의 쌍(CoverageRepository ↔ analytics/check/coverage.sql) 컨벤션 유지 | feat/coverage-v1-fields 브랜치 |
| 2026-07-17 | **분석 뷰 신 스키마 재구축 설계 확정 (A2)** — 신 크롤러는 상세 수집 없이 열거만 하므로(raw_post_detail 소멸) 캡션·지표 소스를 raw_media_page clips 아이템·SELF_GQL 내장 타임라인 노드로 교체(플랫 뷰 체인, base 층에 평탄화 뷰 2종 신설). 서빙 모수는 뷰티 인플루언서만(QUALIFIED ∧ beauty ∧ ¬company, 1,496계정), 미러 계약은 형태 유지+우아한 공백(피드 광고 false·카테고리 믹스 0행 — B4 캡션 분류가 대체 소스), 04_analysis_candidates 신설(캡션 선분석 Haiku+Batch 입구 — 숙성 가드 3일·캡션 필수까지 뷰 담당, 분석됨 대조·상한은 Java). 기반은 PR #30 머지 후 develop | [specs/2026-07-17-analytics-views-new-schema-design.md](docs/superpowers/specs/archive/2026-07-17-analytics-views-new-schema-design.md) |
| 2026-07-17 | **analytics 어드민 UI + 스케줄러 골격 설계 확정 (태스크 I 신설)** — analytics를 상주 웹 서버(8082)로 전환, 크롤러 어드민 패턴 이식(`/ui` 잡 버튼 4종 + LLM 예상 비용 카드 + LogBuffer 로그 패널 + 잡별 락·비동기 트리거). 실행 이력 DB 테이블은 두지 않음(로그로 충분 — 사용자 확정). 스케줄러는 크롤러 동일 게이트(`analytics.schedule.enabled`, 기본 off) 골격만. `mirror-on-startup` 기본 false로 전환하되 cloud push는 one-shot CLI 보존(cloud 프로파일만 true) | [specs/2026-07-17-analytics-admin-ui-design.md](docs/superpowers/specs/2026-07-17-analytics-admin-ui-design.md) |
| 2026-07-17 | **로그인 월 + 가입 코드 도입** — 제품 구조 변경(사용자 확정): 모든 조회는 로그인 필수, 가입은 단일 공용 코드 필수. SecurityConfig를 화이트리스트로 전환(기본 authenticated, 열린 경로는 /v1/auth·/v1/events/gate(익명 측정 유지)·/v1/stats(랜딩 통계 — P3와 병합 시 추가, 로그인 전 랜딩이 소비)·/health·swagger(로컬)만) — /v1 읽기 4종·구 /api·내부 페이지·프로필 이미지 전부 잠금, 401 계약은 기존 유지(/v1 envelope). 가입 코드는 `app.app_setting`(V6) `signup.code`와 trim 정확 일치, 불일치·미설정 403 INVALID_SIGNUP_CODE(fail-closed — 빈 값 시드로 배포되며 운영자 UPDATE로 개통). 레거시 /api/auth/signup은 코드 우회 뒷문이라 폐쇄(인증 입구 /v1 일원화) | [specs/2026-07-17-login-wall-signup-code-design.md](docs/superpowers/specs/archive/2026-07-17-login-wall-signup-code-design.md) |
| 2026-07-17 | **P3 랜딩 통계 개통 + 유사 콘텐츠 제외 확정** — GET /v1/stats(스펙 6.20)를 분석 층 1행 뷰·미러(landing_stats V32)로 서빙, 강한 HTTP 캐시(1시간). **모수는 마이크로 구간 계정(팔로워 3천~5만)과 그 콘텐츠로 통일** — 랜딩 카피·스펙 분포 합계 100과 일치(수집 114 중 55). 조회수는 릴스만(회신표 #16). 분포 %·합계 100 보정은 was 표현 계층(최대 잔여). updatedAt은 미러 실행 시각. **유사 콘텐츠(스펙 6.2)는 제품 고려 대상이 아니라 구현하지 않기로 확정** — 스펙 6.2·회신표 #3은 미구현으로 남김 | [plans/archive/2026-07-17-p3-landing-stats.md](docs/superpowers/plans/archive/2026-07-17-p3-landing-stats.md) |
| 2026-07-17 | **정적분석 Error Prone 도입** — 전 모듈(4개) 컴파일에 Error Prone 단독 적용(1인 팀 시그널/노이즈 기준으로 SpotBugs 조합 대신 선택). ERROR 등급만 빌드 실패로 걸고 WARNING은 비활성(`disableAllWarnings`), 한국어 테스트 메서드명 컨벤션과 충돌하는 `UnicodeInCode`만 체크 해제. 초기 지적은 전 모듈에서 실질 1건(SecurityConfig `csrfToken.get()` 반환값 무시 — 의도적 지연 발급 해제 호출이라 `unused` 관용구로 정리, baseline/suppress 파일 불필요). 컴파일 단계에 걸리므로 CI(PR #22)의 `./gradlew test`가 그대로 정적분석 체크가 된다 | [PR #25](https://github.com/subtle-madness/hypenow-backend/pull/25) |
| 2026-07-17 | **GitHub Actions CI 도입** — develop 대상 push·PR마다 `./gradlew test` 전체 실행(.github/workflows/ci.yml). Testcontainers는 러너 기본 Docker로 충분, 외부 키 불필요. Gradle 캐시는 gradle/actions/setup-gradle(develop push에서 쓰기, PR은 읽기). SQL 하니스는 제외 — 뷰가 실DB(V6 시점) 스키마를 전제해 리포 마이그레이션(V8+ influencer 개편)과 불일치, 프레시 DB 재현 불가 확인(§8에 블로커 기록) | .github/workflows/ci.yml |
| 2026-07-17 | **P2 운영 배포 + 세션 쿠키 SameSite=Lax 확정** — P2(가입·로그인·/v1/me·저장·레이트리밋)를 api.hypenow.io에 배포, 운영 curl E2E(가입 201→me 200→로그아웃 204→재로그인 200, 쿠키·CSRF 왕복)와 로그인 레이트리밋(분당 10 초과 429) 실측 통과. 미결이던 prod 쿠키 SameSite는 **Lax로 확정**(사용자 확인) — www↔api는 hypenow.io 하위 same-site라 전송 손실 없고 스펙과 일치, CSRF 방어 이중화. 테스트 계정은 검증 후 DB 삭제 | [PR #23](https://github.com/subtle-madness/hypenow-backend/pull/23) |
| 2026-07-17 | **Swagger(springdoc-openapi) 도입 — prod 미노출** — was에 springdoc-openapi-starter-webmvc-ui 3.0.3(Boot 4 지원 라인) 추가, `paths-to-match=/v1/**`로 스펙 v1 표면 17경로만 자동 문서화(구 /api·내부 페이지 제외). **정본은 프론트 API 스펙 문서** — Swagger는 서버 시그니처 확인용 보조 문서라 컨트롤러 어노테이션 없이 자동 스캔만. prod에서는 api-docs·swagger-ui 비활성(사용자 결정) — 문서는 로컬·개발 전용. UI /swagger-ui · 스키마 /v3/api-docs | [PR #26](https://github.com/subtle-madness/hypenow-backend/pull/26) |
| 2026-07-15 | **P2 서비스 데이터 정렬 개통** — /v1 인증·계정·저장·이벤트를 스펙 계약으로. HttpSession→Spring Session JDBC(app.spring_session, 세션 목록·개별 로그아웃, hypenow-session 30일 슬라이딩). 세션 principal은 안정 형상(userId·email — CredentialsContainer로 해시 미영속). users 프로필 15필드(V3), 저장 memo(V4), gate_events(V5). 세션 노출 id는 sha256 alias, 타 세션 삭제는 404 은닉. 프로필 이미지 로컬 저장. 레이트리밋 인메모리(분당 스윕). 게이트 이벤트는 CSRF 면제(익명 첫 방문자가 XSRF 쿠키 선행 없이 이벤트를 쏘도록 — append-only 측정 로그라 표적 가치 없음, 완전 익명 curl E2E 403→204 확인) | [plans/archive/2026-07-15-p2-service-data-alignment.md](docs/superpowers/plans/archive/2026-07-15-p2-service-data-alignment.md) |
| 2026-07-15 | **P1 V1 읽기 API 개통** — `/v1` envelope 계약으로 리더보드·콘텐츠 AI 리포트·인플루언서 프로필/리포트 4종 서빙. hypeScore 스펙 5.4 산식(0~100) 재정의 — 피드는 views 부재로 팔로워 ER 축 대체 산식(cbrt(axis²×fresh)×100, 축=min(min(ER,0.3)/0.10,1)), 산식은 `analytics.hype_score()` SQL 함수 단일 원천. 유통사 필터는 `beauty_distributors.slug` 사전 해석, 카테고리 확장 매칭은 `beauty_taxonomy` SQL 처리. email은 미수집 null 확정, 매핑 단계 405/미존재 경로 404는 envelope 미적용(수용) | [plans/archive/2026-07-15-p1-v1-read-api.md](docs/superpowers/plans/archive/2026-07-15-p1-v1-read-api.md) |
| 2026-07-15 | **프론트 API 스펙 v1 전체 채택** (fit 6.18 제외·보류) — `/v1` prefix + envelope 계약을 was 정본으로, 기존 `/api/*`와 병존 후 전환. 분석 윈도우는 스펙과 12로 정렬(develop 기본값 그대로). 인증은 G 구현 유지+확장: HttpSession→Spring Session JDBC(세션 목록·개별 로그아웃), 쿠키 `hypenow-session` 슬라이딩 30일, same-site 전제(도메인 확보로 Vercel rewrite 동일 오리진 — 아래 배포 행 참조). hypeScore는 스펙 5.4 산식(0~100)으로 재정의(현행 원값 방식 대체). 트랙 P1(V1 읽기)→P2(서비스 데이터 정렬)→P3(stats·유사도) 분해 | [specs/2026-07-15-hypenow-api-spec-alignment-design.md](docs/superpowers/specs/2026-07-15-hypenow-api-spec-alignment-design.md) |
| 2026-07-15 | **was+DB 오라클 배포 체계 + 도메인 hypenow.io** — 배포 범위는 was+analysis DB만(크롤·분석은 로컬 유지, 미러가 SSH 터널로 push). 오라클 A1 무료(도쿄, 2/12) + docker compose 3컨테이너(postgres 루프백/was/caddy HTTPS), 이미지 GHCR multi-arch로 타사 30분 이사 가능 구조. 일일 pg_dump(서버 7일)+rclone Google Drive(30일) 백업. 도메인 확보: 프론트 `www.hypenow.io`(Vercel) / API `api.hypenow.io` — 프론트 연동은 Vercel rewrite로 같은 오리진화(CSRF 쿠키), prod CORS는 www.hypenow.io만. was `prod`·analytics `cloud` 프로파일 신설 — Flyway `*:missing` 완화는 dev 기본값으로 국한(§8 해소) | [specs/2026-07-15-oracle-deploy-design.md](docs/superpowers/specs/2026-07-15-oracle-deploy-design.md) |
| 2026-07-15 | **태스크 G: 서비스 데이터 완료** — `app` 스키마(was 소유 Flyway, 이력 `app.flyway_schema_history`) + 이메일+비밀번호 인증(Spring Security DaoAuthenticationProvider·BCrypt·세션 쿠키, 미인증 401 고정) + 저장 2종(인플루언서 후보 상태·메모 부분 갱신 upsert / 콘텐츠 북마크 멱등 upsert). 상태 어휘는 was가 생산자로 확정: `reviewing·contact_planned·collaborating`. CSRF는 XSRF-TOKEN 쿠키 + **SpaCsrfTokenRequestHandler**(지연 발급 해제 + raw 헤더 허용 — 기본 Xor 핸들러만으론 SPA가 첫 쓰기 전 쿠키를 못 받고 raw 값도 403, 실 curl E2E에서 발견). CORS는 WebConfig GET-only를 걷어내고 SecurityConfig `CorsConfigurationSource`로 일원화(allowCredentials). 저장 기술은 JdbcClient 유지(기존 관용구·최소 의존). 확장점: spring-session-jdbc(현 인메모리 세션)·운영 HTTPS 쿠키(Secure·SameSite=None). 같은 태스크의 딴 갈래였던 후보 관리 단독 구현은 닫힌 [PR #13](https://github.com/subtle-madness/hypenow-backend/pull/13)에 보존 — app V1 충돌·후보 기능 중복으로 이 설계(07-14 사용자 확정)로 일원화 | [plans/archive/2026-07-14-task-g-service-data.md](docs/superpowers/plans/archive/2026-07-14-task-g-service-data.md) |
| 2026-07-16 | **프로필 404 = 소프트 딜리트(InfluencerStatus.DELETED)** — 계정 단위 404(삭제·개명)를 페처(SELF·HIKER_MOBILE)가 `Execution.notFound`로 표면화하고, qualify·collect·resnapshot이 해당 인플루언서를 DELETED로 전환. 데이터는 보존하되 모든 선정 쿼리(상태 필터)에서 자동 제외돼 매 실행 재시도·재과금이 끊긴다. Hiker 404는 전용 `NotFoundException`(ApifyException 하위)으로 구분 — 릴스의 기존 404 처리(수확 완료 마킹)는 유지. collect는 방문 트랜잭션 롤백과 분리하기 위해 run 루프(트랜잭션 밖)에서 저장 | feat/beauty-captions 브랜치 |
| 2026-07-16 | **비뷰티 재검 파이프라인 — RESNAPSHOT 잡 신설 + rejudge 선정 좁힘** — 캡션 유무 실험(60계정×4판정)에서 캡션이 비뷰티→뷰티로만 안정적으로 뒤집음(안정 판정의 ~10%, 비뷰티의 ~30%)을 확인. RESNAPSHOT은 캡션 없는 소스(HIKER_MOBILE·DATALIKERS)가 최신인 CLAUDE 비뷰티 판정분을 로컬 GQL로 재수집(비공개는 요청 없이 스킵, `resnapshot.batch-limit` 기본 200), beauty rejudge는 "판정 후 재료가 갱신된 비뷰티"로 선정을 좁힘(뷰티 판정분은 재검 안 함 — 역방향 뒤집힘 미관측). 재수집 완료 계정은 최신 스냅샷이 SELF_GQL이 되어 자연 소진 | feat/beauty-captions 브랜치 |
| 2026-07-16 | **뷰티 판정 3분류 — 비뷰티 / 뷰티 인플루언서 / 뷰티 회사** — `beauty_company`(V14) 신설, Claude 판정을 class(INFLUENCER/COMPANY/NOT_BEAUTY) 출력으로 전환. **회사는 명단 리스트업 전용** — 수집(collect)·릴스(reels)·유사발굴(similar) 대상에서 전부 제외(명시적 회사만 제외, NULL은 인플루언서 취급). 기존 판정분은 V14가 인플루언서(false)로 백필 — rejudge가 회사를 정정. 대시보드 BEAUTY_COMPANY 타일·명단 3버튼(뷰티/회사/뷰티 아님) 추가 | feat/beauty-captions 브랜치 |
| 2026-07-15 | **collect 프로필 실패(401 등) = 방문 실패·재시도, Hiker 피드 폴백 제거** — 프로필 원형이 방문의 유일한 재료(팔로워 추이 + 내장 피드 12개)이므로, 프로필을 못 찍으면 유료 피드 폴백으로 방문을 "성공" 처리하지 않고 북키핑 미갱신으로 남겨 다음 실행 재시도(추이 스냅샷 구멍·헛돈 방지). collect는 미디어 페처 의존이 사라져 정상·실패 경로 모두 피드 별도 요청 0 | feat/beauty-captions 브랜치 |
| 2026-07-15 | **collect 분리 — 게시물을 위한 프로필 수집(COLLECT) / 릴스 수집(REELS)** — 유료 HikerAPI 구간(릴스 1페이지)을 별도 REELS 잡으로 분리해 무료 구간(SELF 프로필 + 내장 피드 12개)과 독립 운용. REELS는 `last_reels_at` 북키핑(V13)·`reels.batch-limit`(기본 10)·주기는 collect와 공유, pk 미보유는 해석 없이 스킵 — **계정당 정확히 Hiker 1요청** 보장. content upsert 규칙은 ContentUpserter로 공유 | [specs/2026-07-15-collect-reels-split-design.md](docs/superpowers/specs/2026-07-15-collect-reels-split-design.md) |
| 2026-07-15 | **collect는 뷰티 계정 전용** — 수집 대상을 QUALIFIED 전체에서 `beauty=true`로 좁힘(비뷰티·미판정은 방문 안 함). 파이프라인이 발굴→판정→**뷰티 판정**→수집으로 확정되고 대시보드도 뷰티 판정 그룹(뷰티/비뷰티/미판정)·READY(뷰티만) 분리 표기. 같은 세션에서 뷰티 판정 재료에 최근 게시물 캡션(계정당 5개·100자) 추가, `beauty.batch-limit`(기본 500) 신설, 계정별 판정 로그 추가 | feat/beauty-captions 브랜치 |
| 2026-07-14 | was에 검증용 내부 페이지 2종 — `/coverage`(필드 커버리지 라이브 추적)·`/posts/{shortCode}`(게시물 드로어 시안형 상세 데모). 분석 결과 읽기 전용, 태스크 D(API)와 별개의 데모/점검 화면. 데모 세션의 VLM base64 전환은 B3 개통(아래 행)과 동일 결론으로 합류 | [specs/2026-07-14-was-coverage-page-design.md](docs/superpowers/specs/archive/2026-07-14-was-coverage-page-design.md) |
| 2026-07-14 | **E에 C2 카피 additive 조립 완료** — `account_analyses` 계정별 최신 1행(계약 record `AccountAnalysis`)을 report의 tagline·summary·trend.note·chart.note·contentMix.traits·ads.headline·activity.paceNote로 서빙. 미러와 SQL 조인 없이 was 코드 조합(§4-4), traits(jsonb)는 was 매핑 계층 파싱. 이력 없으면 카피 전부 null(블록 형태 유지), adHeadline은 이력 있어도 null 허용. ads.brands는 캡션 분류 후속(§8)까지 필드 부재 유지 | [specs/2026-07-14-c2-account-llm-design.md §1](docs/superpowers/specs/2026-07-14-c2-account-llm-design.md) |
| 2026-07-14 | **캡션 분류 + B3 숙성 가드 (B4)** — 캡션 5종(광고 구분·카테고리·브랜드·제품·유통사)을 별도 잡이 아닌 기존 속성 콜 전환(캡션 항상·썸네일 생존 시만 첨부, 병합은 모델 안에서)으로. 어휘는 analysis DB `beauty_taxonomy`·`beauty_distributors`(V30 시드)로 이동 — BeautyTaxonomy는 로더 스냅샷, 프롬프트·sanitize 동일 원천 유지, main_category CHECK는 sanitize로 이관. `detected_products jsonb`([{name,brand}]) 신설. 분석 대상에 게시 후 3일 숙성 가드(`analytics.analyze-maturity-days`) | [specs/2026-07-14-caption-classification-design.md](docs/superpowers/specs/archive/2026-07-14-caption-classification-design.md) |
| 2026-07-14 | **게시물 지표 +3일 고정 구현(정정 ③)** — `v_contents` 지표를 업로드 +3일 이후 **가장 이른** 스냅샷으로 고정(키 `analytics.metric-pin-days` 기본 3 — B3 숙성 가드의 "게시 후 3일"과 같은 기준, 키 공유 권장). 고정 후보 없으면 최신 폴백(구크롤러 조기 수집 잔재 5건 보호, 개편 크롤러는 3일 미경과 미수집이라 소멸 예정). 메타(썸네일·캡션)는 최신 유지(서명 URL ~4일 만료 대응). 적용은 `v_contents`만 — 계정 집계(01·10)·B3 기준선(03)은 최신 기준 유지, 미러 DDL·record 무변경. 실데이터 137건 전행 동일 확인(회귀 0). D3·H의 end_date as-of는 이력 조회로 공존 — 매일 재크롤 개시 후 정합은 §8 | [PR #14](https://github.com/subtle-madness/hypenow-backend/pull/14) |
| 2026-07-14 | **MVP 범위·데이터 정책 정정 4건** — ① 크롤링 개편의 "최근 3개월"은 **초기 확보(백필) 시작 범위**이고 지표 윈도우와 무관 — 계정 지표 윈도우는 **최근 24개**(개수 기반 유지, `analytics.recent-window` 12→24). ② **댓글은 수집·분석 모두 MVP 제외** — B2·`content_comments` 경로 신규 유입 없음(구현 보존). 댓글 외 LLM 산출(콘텐츠 감지·종합 텍스트·계정 카피)은 전부 MVP 포함, 게시물 상세 드로어도 MVP 유지(댓글 분석 탭만 데이터 부재). ③ **게시물 지표는 업로드 +3일 시점 고정** — `contents`가 최신 스냅샷을 따라가는 구조 폐기, 재크롤 스냅샷은 이력 보존(인플루언서 상세 조회 참조용). ④ **캡션 LLM 산출 5종 확정: 광고 구분·카테고리·브랜드·제품·유통사** — 항목 목록은 수정 용이 구조로(하드코딩 대신 설정/데이터 기반) | 2026-07-14 방향 정리 세션 |
| 2026-07-14 | **태스크 E: 인플루언서 상세 API 계약 확정** — 응답 = profile(accounts ⊕ account_summaries, accounts 부재 시 표시 필드 null) + report(C1 지표 전달). 주 리소스는 account_summaries(부재 404). 표현 조립 이행: 경과일 24h 단위·isActive=14일 미만·lastAdNote 문구("마지막 광고 오늘"/"N일 전")·광고 strip(시계열 순 bool). comparison은 organic/ad 평균 한쪽이라도 null이면 블록 null. LLM 카피 7종(summary·trend.note·traits·headline·brands·paceNote·tagline)은 필드 부재 → C2 additive | [plans/2026-07-14-task-e-influencer-api.md](docs/superpowers/plans/archive/2026-07-14-task-e-influencer-api.md) |
| 2026-07-14 | **크롤링 구조 개편 방향 확정** — 인플루언서 리스트를 먼저 확보 → 계정별 최근 3개월 게시물 크롤 → 매일 신규 게시물만 추가 크롤(기존 게시물 재크롤은 조회수 등 지표 갱신 수준, 콘텐츠 재분석 없음). 게시물 분류는 discovery 키워드 대신 **caption 감지**로 가는 방향. 파생 후속 3건(윈도우 기간 전환·B3 숙성 가드·캡션 분류 태스크)은 §8 | [specs/2026-07-14-c2-account-llm-design.md §6](docs/superpowers/specs/2026-07-14-c2-account-llm-design.md) |
| 2026-07-14 | **태스크 C2 설계 확정** — AccountReport 카피 7종을 계정당 LLM 1콜로 생성, `account_analyses`(V20 — V11에서 renumber, B3 잔여분 선점 충돌) 이력 INSERT. 재분석 = 신규 즉시 / stale(새 게시물)+**쿨다운 7일**(매일 크롤 대비 비용 가드). adHeadline은 광고 비교 데이터 있을 때만. 계약 record `AccountAnalysis` 신설 — 분석 층 테이블도 생산자가 record로 조립·소비하면 §4-4 쌍 성립 | [specs/2026-07-14-c2-account-llm-design.md](docs/superpowers/specs/2026-07-14-c2-account-llm-design.md) |
| 2026-07-14 | **B3 VLM 잔여분 개통** — F-2 스파이크(실 8건) 전 항목 채택: 입력은 URL 불가(인스타 CDN을 Anthropic이 robots.txt로 거부)→직접 다운로드+base64. `content_analyses.detected_distributors jsonb` 신설(유통사 감지 — 어휘 올리브영/다이소 고정). **분류 어휘 = celfit-front 배포본 verbatim 계약**: main_category 영문 slug 6종(CHECK 추가), sub_categories = 중분류+소분류 한글 라벨 배열(프론트가 배열 포함으로 매칭), detected_product_categories = 소분류 라벨 — 단일 원천은 analytics `BeautyTaxonomy`(프롬프트+sanitize 공용). 썸네일 서명 URL ~4일 만료 대응: 분석 대상 수집 최신순 + HEAD 프리체크(만료는 VLM만 NULL). 게이트 on 실행으로 실데이터 7건 채움 확인 | [plans/archive/2026-07-14-task-b3-vlm-remainder.md](docs/superpowers/plans/archive/2026-07-14-task-b3-vlm-remainder.md) |
| 2026-07-14 | **as-of 선택 규칙 확정 + 서빙 트랙 신설(D3·H)** — celfit-front 실동작 확인: 랭킹 집계 기간 = **게시일(postedAt) 범위 필터**(URL `start_date`·`end_date`), "그 기간 화면"의 지표 시점 = **end_date**. 상세·목록 모두 `captured_at ≤ end_date(KST 하루 끝)` 중 최신 스냅샷으로 지표 구성, 그 시점 스냅샷이 없는 콘텐츠는 화면에 부재(목록에서 필터링, 상세는 404). §5에 D3(상세 as-of)·H(랭킹 목록 API — 프론트 URL 파라미터가 사실상 확정 계약) 신설 | 프론트 URL 계약·기간 필터 실측 (2026-07-14 세션) |
| 2026-07-14 | **캠페인 추천 피봇 검토 후 취소** — "브리프 제출→인플루언서 추천+근거"로의 전환을 07-13~14 검토(구조 설계·어휘 계약 계획까지 작성)했으나 기존 방향(콘텐츠 랭킹+상세 분석 MVP) 유지로 결정. 검토 산출물은 develop 미머지 — 닫힌 PR #5·로컬 브랜치 `docs/campaign-recommendation-pivot`에 보존, 본 문서 기준 태스크 트랙(§5)은 변동 없음 | 닫힌 [PR #5](https://github.com/subtle-madness/hypenow-backend/pull/5) |
| 2026-07-13 | **태스크 D2: 상세 API에 B2·B3 산출물 additive 확장** — comments.items[].aiCategory(분류 LEFT JOIN, 미분류 null) + analysis 블록(content_analyses 1행: AI 텍스트·기준선 스냅샷·카테고리 맥락·VLM·댓글 진정성, 미분석 null). 읽기 record는 was 로컬(분석 층 소유 테이블은 공유 형태 미성립 — §4-4), jsonb는 실 JSON 구조로 서빙. 릴스 개별 바 차트는 인플루언서 상세(E) 소관. as-of 서빙은 `content_metric_snapshots` 미러(별도 세션 분리) 후 was에서 | [plans/2026-07-13-task-d2-analysis-block.md](docs/superpowers/plans/archive/2026-07-13-task-d2-analysis-block.md) |
| 2026-07-13 | C1은 **celfit-front 실계약(AccountReport) 기준**으로 구현 — v4 목업 지표(중앙값·히트율·변동성·구간포지션 등) 폐기. 계정 평균 ER은 **followers 분모**(`avg_er_pct`, 게시물 ER의 views 분모와 공존), 기준 지표 폴백 `metric`('views'\|'likes')은 데이터에 확정 | [specs/2026-07-13-c1-account-detail-design.md](docs/superpowers/specs/2026-07-13-c1-account-detail-design.md) |
| 2026-07-13 | B1 잔여분 `content_metric_snapshots` 미러 개통 — base 뷰에 이력 노출(`v_base_detail_history`) 추가, 서빙은 최신(`contents`)/이력(스냅샷) 분리 완성. was의 as-of 조회(태스크 D) 재료 | [plans/2026-07-13-task-b1-snapshot-mirror.md](docs/superpowers/plans/archive/2026-07-13-task-b1-snapshot-mirror.md) |
| 2026-07-12 | **태스크 D: 상세 API 계약 확정** — 응답 블록=소스 테이블 1:1(post/account/comments), 참여율=(좋아요+댓글)/조회수(피드는 조회수 NULL이라 null)·경과일은 was 표현 조립, 댓글은 수집분 전체 서빙(좋아요순). LLM 블록(모달 우측 탭 5종·댓글 ai_category)은 필드 부재→B2·B3 산출물의 additive 확장은 후속 태스크. as-of 규칙은 스냅샷 미러 도입 시로 보류 유지. CORS `/api/**` GET(localhost:3000·celfit-front.vercel.app) | [plans/2026-07-12-task-d-post-detail-api.md](docs/superpowers/plans/archive/2026-07-12-task-d-post-detail-api.md) |
| 2026-07-12 | LLM 코드 모듈 소속 = analytics 확정 (포트/어댑터, 테스트는 fake). 댓글 분류 배치 개통 — 기본 게이트 off, 비용 가드 app_setting | [plans/2026-07-12-task-f-b2-llm-comment-classification.md](docs/superpowers/plans/archive/2026-07-12-task-f-b2-llm-comment-classification.md) |
| 2026-07-12 | 게시물 **중복 크롤링 도입** — 지표 스냅샷 누적. 분석 층 서빙을 최신/이력으로 분리(`contents` = 최신, `content_metric_snapshots` = 시점별, B1에서 구현). as-of 선택 규칙은 D에서 | [specs/2026-07-12-analytics-data-layer-design.md](docs/superpowers/specs/2026-07-12-analytics-data-layer-design.md) |
| 2026-07-12 | **미러를 명시적 타입 기반으로 재설계**(뷰 SQL=계산 / Flyway DDL=저장 / 공유 record=자바 그릇, TRUNCATE+INSERT, 컬럼 대조 가드) — 기존 제네릭 미러 폐기. **계약 모듈 `contract-analysis` 신설**(생산자+소비자 쌍 성립). 모듈 공유 원칙(§4-4) 확정. **기존 analytics 구현(뷰 소스·하니스·미러 코드) 전체 초기화 — 백지 재구축** | [specs/2026-07-12 §8](docs/superpowers/specs/archive/2026-07-12-detail-analysis-design.md) |
| 2026-07-12 | 3-tier 확정: 미러=tier 경계(필수), LLM=분석 층 소속, 태스크 A~G 분해. **서비스 데이터**(로그인·후보 관리 등 was가 쓰는 앱 데이터)는 분석 결과와 스키마 분리(`app`), 물리 분리 고려 | [specs/2026-07-12-detail-analysis-design.md](docs/superpowers/specs/archive/2026-07-12-detail-analysis-design.md) |
| 2026-07-10 | 상세 분석 확정안(드로어 v3·인플루언서 v4) + 구현 계획 초안 3건(현재는 참고 자료) | [plans/2026-07-10-*](docs/superpowers/plans/) |
| 2026-07-09 | 모노레포 통합(crawler/analytics/was), was 랭킹 대시보드, 미러 도입 | [plans/2026-07-09-monorepo-migration.md](docs/superpowers/plans/archive/2026-07-09-monorepo-migration.md) |
| 2026-07-09 | 분석 = SQL 뷰 방식(A안), `analytics` 스키마, 더미 시드 검증 | [specs/2026-07-09-analytics-catalog-design.md](docs/superpowers/specs/archive/2026-07-09-analytics-catalog-design.md) |
| 2026-07-09 | 제품 방향: 분석 단위 = 크리에이터, 마이크로인플루언서 발굴 | [specs/2026-07-09-influencer-analysis-decisions.md](docs/superpowers/specs/archive/2026-07-09-influencer-analysis-decisions.md) |
| 2026-07-07 | crawler: Apify 원형(raw) 적재 + discover→qualify→aggregate 3단계 | [specs/2026-07-07-crawler-design.md](docs/superpowers/specs/archive/2026-07-07-crawler-design.md) |
