# CB1 — 배치 1회용 가입 코드

- **소속 트랙군**: 클로즈베타 전환 트랙 — 2026-07-19 프론트 요청서(초대코드 가입 전용 + 도입문의 접수)
- **의존**: —
- **상태**: ✅ ([PR #53](https://github.com/subtle-madness/hypenow-backend/pull/53) 머지 완료 07-19)

## 내용

V8 `signup_codes`(채널별 발급) + 가입 트랜잭션 원자 선점(SignupService) + `POST /v1/auth/signup-code/verify`(관문 사전 검증) + 단일 공용 코드(app_setting) 즉시 폐기 + 코드 생성 스크립트(deploy/scripts)
