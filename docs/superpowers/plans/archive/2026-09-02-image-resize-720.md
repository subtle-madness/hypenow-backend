# 서빙 이미지 리사이즈 720px·q70 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 아카이브 잡이 저장하는 서빙 이미지를 최장변 720px·JPEG q70으로 축소해 GCS 용량(−46%)과 증가 속도(−50%)를 꺾는다. 스펙: [2026-09-02-image-resize-720-design.md](../specs/2026-09-02-image-resize-720-design.md)

**Architecture:** `ImageDownloader`를 감싸는 `ImageResizer` 데코레이터를 analytics·monitoring 두 모듈에 각각 두고(모듈 간 Java 공유 금지 — `ImageDownloader` 전례대로 복제), 배선 지점(`JobConfig`·`ImageArchiveConfig`)에서 래핑만 한다. 잡 코드는 무수정. 기존분은 로컬 맥에서 sips 병렬 백필 스크립트로 일회 처리한다.

**Tech Stack:** 순수 JDK(ImageIO·Graphics2D — 의존성 추가 없음), JUnit5, bash+python3+sips+gcloud(백필).

## Global Constraints

- 리사이즈 정책(스펙 §2): 최장변 **720px 초과분만** 축소, JPEG **q0.70**, 결과가 원본보다 크거나 디코드 불가(webp 등)·예외 시 **원본 바이트 그대로 반환**. ≤720px 재인코딩 금지.
- 모듈 간 Java 공유 금지 — analytics·monitoring에 같은 클래스를 **복제**한다(ARCHITECTURE §4, `ImageDownloader` 전례).
- 테스트는 모듈 단위로만 실행: `./gradlew :analytics:test --tests ...` / `./gradlew :monitoring:test --tests ...` (전체 `./gradlew test`는 PR 직전에만).
- `ImageResizer` 단위 테스트는 Testcontainers 불필요(플레인 JUnit) — `DOCKER_HOST` 없이도 돈다.
- 커밋 메시지 한국어, prefix `feat(analytics):`/`feat(monitoring):`/`feat(deploy):` 식.
- Cache-Control 계약: `thumb/`=`public, max-age=31536000, immutable`, `monitor-*`=`public, max-age=86400` (deploy/README §5-2) — 백필 덮어쓰기 시 보존 필수.

---

### Task 1: analytics `ImageResizer` (TDD)

**Files:**
- Create: `analytics/src/main/java/com/celfit/analytics/archive/ImageResizer.java`
- Test: `analytics/src/test/java/com/celfit/analytics/archive/ImageResizerTest.java`

**Interfaces:**
- Consumes: `ImageDownloader`(기존) — `Downloaded fetch(String url)`, `record Downloaded(byte[] bytes, String contentType)`.
- Produces: `public static ImageDownloader ImageResizer.wrap(ImageDownloader delegate)` — Task 2가 배선에 사용. `static byte[] shrink(byte[] original)` — 패키지 프라이빗, 테스트 전용.

- [ ] **Step 1: 실패하는 테스트 작성**

`analytics/src/test/java/com/celfit/analytics/archive/ImageResizerTest.java`:

```java
package com.celfit.analytics.archive;

import static org.assertj.core.api.Assertions.assertThat;

import java.awt.image.BufferedImage;
import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import java.util.Random;
import javax.imageio.IIOImage;
import javax.imageio.ImageIO;
import javax.imageio.ImageWriteParam;
import javax.imageio.ImageWriter;
import javax.imageio.stream.MemoryCacheImageOutputStream;
import org.junit.jupiter.api.Test;

/** 리사이즈 데코레이터 계약 — 스펙 2026-09-02 §2·§5. */
class ImageResizerTest {

	/** 그라데이션 JPEG 생성 — 압축 여지가 있는 자연스러운 콘텐츠 근사. */
	private static byte[] jpeg(int w, int h, float quality) throws Exception {
		BufferedImage img = new BufferedImage(w, h, BufferedImage.TYPE_INT_RGB);
		for (int y = 0; y < h; y++) {
			for (int x = 0; x < w; x++) {
				img.setRGB(x, y, (x % 256) << 16 | (y % 256) << 8 | ((x + y) % 256));
			}
		}
		return encode(img, quality);
	}

	private static byte[] encode(BufferedImage img, float quality) throws Exception {
		ImageWriter writer = ImageIO.getImageWritersByFormatName("jpg").next();
		ImageWriteParam param = writer.getDefaultWriteParam();
		param.setCompressionMode(ImageWriteParam.MODE_EXPLICIT);
		param.setCompressionQuality(quality);
		ByteArrayOutputStream out = new ByteArrayOutputStream();
		writer.setOutput(new MemoryCacheImageOutputStream(out));
		writer.write(null, new IIOImage(img, null, null), param);
		writer.dispose();
		return out.toByteArray();
	}

	@Test
	void 초과분은_720으로_축소되고_바이트가_줄어든다() throws Exception {
		byte[] original = jpeg(1440, 1080, 0.9f);
		byte[] shrunk = ImageResizer.shrink(original);
		assertThat(shrunk.length).isLessThan(original.length);
		BufferedImage decoded = ImageIO.read(new ByteArrayInputStream(shrunk));
		assertThat(Math.max(decoded.getWidth(), decoded.getHeight())).isEqualTo(720);
		assertThat(decoded.getWidth()).isEqualTo(720); // 1440x1080 → 720x540 비율 유지
		assertThat(decoded.getHeight()).isEqualTo(540);
	}

	@Test
	void 이하는_원본_바이트를_그대로_반환한다() throws Exception {
		byte[] original = jpeg(640, 640, 0.9f);
		assertThat(ImageResizer.shrink(original)).isSameAs(original);
	}

	@Test
	void 디코드_불가_바이트는_원본을_그대로_반환한다() {
		byte[] notAnImage = "webp라고 치자".getBytes();
		assertThat(ImageResizer.shrink(notAnImage)).isSameAs(notAnImage);
	}

	@Test
	void 재인코딩이_더_크면_원본을_유지한다() throws Exception {
		// 초고압축 노이즈 원본 — q70 재인코딩이 원본보다 커지는 케이스 재현
		BufferedImage noise = new BufferedImage(900, 900, BufferedImage.TYPE_INT_RGB);
		Random rnd = new Random(42);
		for (int y = 0; y < 900; y++) {
			for (int x = 0; x < 900; x++) {
				noise.setRGB(x, y, rnd.nextInt(0xFFFFFF));
			}
		}
		byte[] original = encode(noise, 0.05f);
		byte[] result = ImageResizer.shrink(original);
		// 720 축소 후에도 q70 노이즈가 q5 원본보다 크면 원본 유지가 계약이다.
		// (혹시 더 작아지면 축소본 채택도 계약상 정상 — 어느 쪽이든 결과가 원본보다 크면 안 된다)
		assertThat(result.length).isLessThanOrEqualTo(original.length);
	}

	@Test
	void wrap은_축소_시_contentType을_jpeg로_바꾸고_아니면_원본_Downloaded를_보존한다() throws Exception {
		byte[] big = jpeg(1440, 1440, 0.9f);
		ImageDownloader.Downloaded bigDownloaded = new ImageDownloader.Downloaded(big, "image/webp");
		ImageDownloader wrapped = ImageResizer.wrap(url -> bigDownloaded);
		ImageDownloader.Downloaded result = wrapped.fetch("http://x");
		assertThat(result.contentType()).isEqualTo("image/jpeg");
		assertThat(result.bytes().length).isLessThan(big.length);

		byte[] small = jpeg(320, 320, 0.9f);
		ImageDownloader.Downloaded smallDownloaded = new ImageDownloader.Downloaded(small, "image/webp");
		ImageDownloader wrappedSmall = ImageResizer.wrap(url -> smallDownloaded);
		assertThat(wrappedSmall.fetch("http://x")).isSameAs(smallDownloaded);
	}
}
```

- [ ] **Step 2: 실패 확인**

```bash
./gradlew :analytics:test --tests "com.celfit.analytics.archive.ImageResizerTest"
```

기대: `ImageResizer` 심볼 없음으로 **컴파일 실패**.

- [ ] **Step 3: 구현**

`analytics/src/main/java/com/celfit/analytics/archive/ImageResizer.java`:

```java
package com.celfit.analytics.archive;

import java.awt.Graphics2D;
import java.awt.RenderingHints;
import java.awt.image.BufferedImage;
import java.io.ByteArrayInputStream;
import java.io.ByteArrayOutputStream;
import javax.imageio.IIOImage;
import javax.imageio.ImageIO;
import javax.imageio.ImageWriteParam;
import javax.imageio.ImageWriter;
import javax.imageio.stream.MemoryCacheImageOutputStream;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

/**
 * 서빙 이미지 720px·q70 축소 데코레이터 (스펙 2026-09-02).
 *
 * <p>최장변 720px 초과분만 축소한다 — IG CDN 원본은 이미 고압축이라 동일 해상도 재인코딩은
 * 실측 +30% 역효과(스펙 §2). 결과가 원본보다 크거나 디코드 불가(webp 등)·예외면 원본을
 * 그대로 반환한다 — 리사이즈는 최적화이지 아카이브 성패 조건이 아니다.
 *
 * <p>monitoring 모듈에 동일 클래스가 복제돼 있다 — 모듈 간 Java 공유는 계약 모듈만
 * 허용이라 {@code ImageDownloader} 전례를 따른다. 수정 시 양쪽을 함께 고칠 것.
 */
public final class ImageResizer {

	private static final Logger log = LoggerFactory.getLogger(ImageResizer.class);

	static final int MAX_EDGE = 720;
	static final float QUALITY = 0.70f;

	private ImageResizer() {
	}

	/** 다운로더 데코레이터 — 축소가 일어난 경우에만 contentType을 image/jpeg로 바꾼다. */
	public static ImageDownloader wrap(ImageDownloader delegate) {
		return url -> {
			ImageDownloader.Downloaded original = delegate.fetch(url);
			byte[] shrunk = shrink(original.bytes());
			return shrunk == original.bytes() ? original
					: new ImageDownloader.Downloaded(shrunk, "image/jpeg");
		};
	}

	/** 축소 시 새 JPEG 바이트, 아니면(이하·디코드 불가·역효과·예외) 원본 배열 그대로. */
	static byte[] shrink(byte[] original) {
		try {
			BufferedImage src = ImageIO.read(new ByteArrayInputStream(original));
			if (src == null) {
				return original; // webp 등 ImageIO 미지원 포맷 — 정상 폴백, 로그 없음
			}
			int edge = Math.max(src.getWidth(), src.getHeight());
			if (edge <= MAX_EDGE) {
				return original;
			}
			double scale = (double) MAX_EDGE / edge;
			int w = Math.max(1, (int) Math.round(src.getWidth() * scale));
			int h = Math.max(1, (int) Math.round(src.getHeight() * scale));
			BufferedImage dst = new BufferedImage(w, h, BufferedImage.TYPE_INT_RGB);
			Graphics2D g = dst.createGraphics();
			g.setRenderingHint(RenderingHints.KEY_INTERPOLATION,
					RenderingHints.VALUE_INTERPOLATION_BILINEAR);
			g.drawImage(src, 0, 0, w, h, null);
			g.dispose();
			byte[] jpeg = encodeJpeg(dst);
			return jpeg.length < original.length ? jpeg : original;
		} catch (Exception e) {
			log.warn("이미지 축소 실패 — 원본 유지: {}", e.toString());
			return original;
		}
	}

	private static byte[] encodeJpeg(BufferedImage img) throws Exception {
		ImageWriter writer = ImageIO.getImageWritersByFormatName("jpg").next();
		ImageWriteParam param = writer.getDefaultWriteParam();
		param.setCompressionMode(ImageWriteParam.MODE_EXPLICIT);
		param.setCompressionQuality(QUALITY);
		ByteArrayOutputStream out = new ByteArrayOutputStream();
		writer.setOutput(new MemoryCacheImageOutputStream(out));
		writer.write(null, new IIOImage(img, null, null), param);
		writer.dispose();
		return out.toByteArray();
	}
}
```

- [ ] **Step 4: 통과 확인**

```bash
./gradlew :analytics:test --tests "com.celfit.analytics.archive.ImageResizerTest"
```

기대: 5개 전부 PASS.

- [ ] **Step 5: 커밋**

```bash
git add analytics/src/main/java/com/celfit/analytics/archive/ImageResizer.java \
        analytics/src/test/java/com/celfit/analytics/archive/ImageResizerTest.java
git commit -m "feat(analytics): 서빙 이미지 720px·q70 축소 데코레이터 — 초과분만, 실패·역효과 시 원본 유지"
```

---

### Task 2: analytics 배선

**Files:**
- Modify: `analytics/src/main/java/com/celfit/analytics/config/JobConfig.java` (imageArchiveJob 빈, ~190행)

**Interfaces:**
- Consumes: Task 1의 `ImageResizer.wrap(ImageDownloader)`.
- Produces: 없음(말단 배선).

- [ ] **Step 1: 배선 수정**

`JobConfig.imageArchiveJob`의 마지막 return에서 다운로더만 래핑한다:

```java
		return new ImageArchiveJob(rawJdbcTemplate, analysisDataSource,
				store, ImageResizer.wrap(ImageDownloader.http()), settings, reporter);
```

(변경 전: `store, ImageDownloader.http(), settings, reporter`)

- [ ] **Step 2: 기존 잡 테스트로 회귀 확인**

```bash
./gradlew :analytics:test --tests "com.celfit.analytics.archive.*"
```

기대: `ImageArchiveJobTest` 포함 전부 PASS (잡 계약 무변경 — 데코레이터는 바이트만 바꾼다).
주의: 이 머신 로컬 도커는 **Docker Desktop이라 `DOCKER_HOST` 미설정이 정답** — CLAUDE.md의
colima export 안내는 이 머신엔 해당 없음(메모리 08-09). Testcontainers 대량 실패가 나면
`DOCKER_HOST`가 설정돼 있는지부터 확인하고 해제할 것.

- [ ] **Step 3: 커밋**

```bash
git add analytics/src/main/java/com/celfit/analytics/config/JobConfig.java
git commit -m "feat(analytics): 이미지 아카이브 다운로더에 720px 축소 배선"
```

---

### Task 3: monitoring `ImageResizer` 복제 (TDD)

**Files:**
- Create: `monitoring/src/main/java/com/celfit/monitoring/image/ImageResizer.java`
- Test: `monitoring/src/test/java/com/celfit/monitoring/image/ImageResizerTest.java`

**Interfaces:**
- Consumes: monitoring `ImageDownloader`(기존, analytics와 동일 시그니처).
- Produces: `public static ImageDownloader ImageResizer.wrap(ImageDownloader delegate)` — Task 4가 사용.

- [ ] **Step 1: 테스트 복제**

Task 1의 `ImageResizerTest.java`를 그대로 복사하되 패키지만 바꾼다:
`package com.celfit.monitoring.image;` — 나머지 코드는 Task 1 Step 1과 문자 그대로 동일
(`ImageDownloader`가 이 패키지에도 동일 시그니처로 존재).

- [ ] **Step 2: 실패 확인**

```bash
./gradlew :monitoring:test --tests "com.celfit.monitoring.image.ImageResizerTest"
```

기대: 컴파일 실패(`ImageResizer` 없음).

- [ ] **Step 3: 구현 복제**

Task 1 Step 3의 `ImageResizer.java`를 그대로 복사하되:
- 패키지를 `package com.celfit.monitoring.image;`로.
- 클래스 javadoc의 복제 안내 문장을 반대 방향으로: "analytics 모듈에 동일 클래스가 복제돼
  있다 — 수정 시 양쪽을 함께 고칠 것."

- [ ] **Step 4: 통과 확인**

```bash
./gradlew :monitoring:test --tests "com.celfit.monitoring.image.ImageResizerTest"
```

기대: 5개 전부 PASS.

- [ ] **Step 5: 커밋**

```bash
git add monitoring/src/main/java/com/celfit/monitoring/image/ImageResizer.java \
        monitoring/src/test/java/com/celfit/monitoring/image/ImageResizerTest.java
git commit -m "feat(monitoring): 서빙 이미지 720px·q70 축소 데코레이터 복제 — analytics와 동일 계약"
```

---

### Task 4: monitoring 배선

**Files:**
- Modify: `monitoring/src/main/java/com/celfit/monitoring/config/ImageArchiveConfig.java` (51·61·72·83·94·105·116행의 7곳)

**Interfaces:**
- Consumes: Task 3의 `ImageResizer.wrap(ImageDownloader)`.
- Produces: 없음(말단 배선).

- [ ] **Step 1: 7곳 일괄 래핑**

`ImageArchiveConfig.java`의 `ImageDownloader.http()` 7곳을 전부
`ImageResizer.wrap(ImageDownloader.http())`로 바꾼다(import 추가:
`import com.celfit.monitoring.image.ImageResizer;`). 7곳 전부인지 확인:

```bash
grep -c 'ImageResizer.wrap(ImageDownloader.http())' \
  monitoring/src/main/java/com/celfit/monitoring/config/ImageArchiveConfig.java   # → 7
grep -c 'ImageDownloader.http()' \
  monitoring/src/main/java/com/celfit/monitoring/config/ImageArchiveConfig.java   # → 7 (전부 래핑 안쪽)
```

- [ ] **Step 2: 모듈 회귀 확인**

```bash
./gradlew :monitoring:test --tests "com.celfit.monitoring.image.*"
```

기대: `BrandPostThumbnailArchiveJobTest` 등 기존 이미지 잡 테스트 포함 전부 PASS.

- [ ] **Step 3: 커밋**

```bash
git add monitoring/src/main/java/com/celfit/monitoring/config/ImageArchiveConfig.java
git commit -m "feat(monitoring): 이미지 잡 7종 다운로더에 720px 축소 배선"
```

---

### Task 5: 백필 스크립트 (로컬 맥 전용)

**Files:**
- Create: `deploy/scripts/backfill-image-resize.py`

**Interfaces:**
- Consumes: 없음(독립 스크립트 — gcloud CLI 인증·sips·python3 필요, 로컬 맥 전용).
- Produces: 없음. 사용법: `backfill-image-resize.py <prefix> <cache-control> [--apply]`
  (기본 드라이런 — `--apply` 없으면 덮어쓰기 생략, 예상 절감만 집계).

설계 주의: 객체당 gcloud 프로세스를 띄우면 33만 회 × 프로세스 기동 ~1초로 수십 시간이
된다 — **200개 배치**로 `gcloud storage cp <url...> <dir>` 한 번에 받고(내부 병렬 전송),
sips는 로컬 루프, 업로드도 배치 한 번(같은 프리픽스는 Cache-Control 계약값이 동일해
배치 플래그로 안전)으로 gcloud 기동을 프리픽스당 ~3천 회로 줄인다.

- [ ] **Step 1: 스크립트 작성**

`deploy/scripts/backfill-image-resize.py`:

```python
#!/usr/bin/env python3
"""GCS 서빙 이미지 720px·q70 일괄 백필 — **로컬 맥 전용**(sips 사용), 서버 아님.

스펙: docs/superpowers/specs/2026-09-02-image-resize-720-design.md §4

사용:
  ./backfill-image-resize.py thumb "public, max-age=31536000, immutable"          # 드라이런
  ./backfill-image-resize.py thumb "public, max-age=31536000, immutable" --apply  # 실제 덮어쓰기
  ./backfill-image-resize.py monitor-brand-post "public, max-age=86400" --apply

정책(잡 ImageResizer와 동일): 최장변 720 초과분만 축소, 결과가 크면 원본 유지 — 자연
멱등이라 중단 후 재실행 안전(처리된 객체는 치수 검사에서 자동 통과). Cache-Control은
프리픽스 계약값을 인자로 받아 배치 업로드에 그대로 붙인다(deploy/README §5-2).
잠자기 방지는 호출 측에서: caffeinate -i ./backfill-image-resize.py ...
"""
import shutil
import subprocess
import sys
import tempfile
from pathlib import Path

BUCKET = "gs://hypenow-images"
BATCH = 200


def dims(path: Path) -> int:
    """최장변 픽셀. 측정 실패는 0(→ 통과 처리)."""
    try:
        out = subprocess.run(
            ["sips", "-g", "pixelWidth", "-g", "pixelHeight", str(path)],
            capture_output=True, text=True, timeout=30).stdout
        vals = [int(line.split()[-1]) for line in out.splitlines()
                if "pixelWidth" in line or "pixelHeight" in line]
        return max(vals) if len(vals) == 2 else 0
    except Exception:
        return 0


def main() -> None:
    if len(sys.argv) < 3:
        sys.exit("사용법: backfill-image-resize.py <prefix> <cache-control> [--apply]")
    prefix, cache_control = sys.argv[1], sys.argv[2]
    apply = "--apply" in sys.argv[3:]

    urls = subprocess.run(
        ["gcloud", "storage", "ls", f"{BUCKET}/{prefix}/"],
        capture_output=True, text=True, check=True).stdout.split()
    print(f"대상 {len(urls)}개 (모드: {'APPLY' if apply else '드라이런'})")

    shrunk = passed = kept = failed = 0
    before = after = 0
    for i in range(0, len(urls), BATCH):
        batch = urls[i:i + BATCH]
        with tempfile.TemporaryDirectory(prefix="img-backfill.") as tmp:
            dl, up = Path(tmp) / "dl", Path(tmp) / "up"
            dl.mkdir(); up.mkdir()
            # 배치 다운로드 — 개별 실패는 배치 전체를 죽이지 않게 check 없이
            subprocess.run(["gcloud", "storage", "cp", *batch, str(dl)],
                           capture_output=True)
            for url in batch:
                f = dl / url.rsplit("/", 1)[1]
                if not f.exists():
                    failed += 1
                    continue
                size = f.stat().st_size
                if dims(f) <= 720:
                    passed += 1
                    continue
                out = up / f.name
                r = subprocess.run(
                    ["sips", "-Z", "720", "-s", "format", "jpeg",
                     "-s", "formatOptions", "70", str(f), "--out", str(out)],
                    capture_output=True)
                if r.returncode != 0 or not out.exists():
                    failed += 1
                    continue
                if out.stat().st_size >= size:
                    kept += 1
                    out.unlink()
                    continue
                shrunk += 1
                before += size
                after += out.stat().st_size
            outs = list(up.iterdir())
            if apply and outs:
                subprocess.run(
                    ["gcloud", "storage", "cp", *map(str, outs), f"{BUCKET}/{prefix}/",
                     "--content-type=image/jpeg", f"--cache-control={cache_control}"],
                    capture_output=True, check=True)
        done = min(i + BATCH, len(urls))
        print(f"  {done}/{len(urls)} — 축소 {shrunk} · 통과 {passed} · 유지 {kept} · 실패 {failed}", flush=True)

    gb = 2 ** 30
    print(f"축소 {shrunk}개: {before / gb:.2f}GB → {after / gb:.2f}GB (절감 {(before - after) / gb:.2f}GB)")
    print(f"통과(≤720px) {passed} · 역효과 유지 {kept} · 실패 스킵 {failed}"
          + ("" if apply else " · 드라이런(덮어쓰기 0건)"))


if __name__ == "__main__":
    if not shutil.which("sips"):
        sys.exit("sips 없음 — 이 스크립트는 macOS 전용이다")
    main()
```

작성 후 실행 권한:

```bash
chmod +x deploy/scripts/backfill-image-resize.py
```

- [ ] **Step 2: 소형 프리픽스로 드라이런 리허설 (읽기 전용 — 언제든 안전)**

```bash
caffeinate -i deploy/scripts/backfill-image-resize.py monitor-author "public, max-age=86400"
```

기대: 배치 진행 로그가 200개 단위로 찍히고, 마지막에 축소/통과/유지/실패 집계 + "드라이런"
표기. 덮어쓰기 0건. `monitor-author/`는 ~0.2GB·수천 개라 몇 분 안에 끝난다.

- [ ] **Step 3: 커밋**

```bash
git add deploy/scripts/backfill-image-resize.py
git commit -m "feat(deploy): GCS 서빙 이미지 720px 백필 스크립트 — 로컬 맥 전용·드라이런 기본·자연 멱등"
```

---

### Task 6: 문서·PR

**Files:**
- Modify: `DECISIONS.md` (표 맨 위에 행 추가)
- Move: `docs/superpowers/plans/2026-09-02-image-resize-720.md` → `docs/superpowers/plans/archive/`

**Interfaces:** 없음.

- [ ] **Step 1: DECISIONS.md 행 추가**

표 맨 위(`|---|---|---|` 바로 아래)에:

```markdown
| 2026-09-02 | **서빙 이미지 720px·q70 축소 — 잡 데코레이터 + 기존분 백필** — GCS `hypenow-images` 50GB 초과 알람 대응(수명주기 배제 확정): FE 최대 렌더 488px(플레인 img — 저장 바이트가 1:1 서빙) 실측 + 상세 패널 마크업 복제 A/B 화질 승인 후, 최장변 720 초과분만 JPEG q70 축소(동일 해상도 재인코딩은 실측 +30% 역효과라 금지, 커지거나 실패 시 원본 유지). `ImageDownloader` 데코레이터라 잡 무수정 — analytics·monitoring 복제(공유 금지 전례). 기존분 48.8GB는 로컬 맥 sips 백필(자연 멱등·드라이런 게이트, **비가역** — CDN 원본 만료). 실측 예상: 버킷 51→28GB(−46%), 증가 3→1.5GB/일 | [스펙](docs/superpowers/specs/2026-09-02-image-resize-720-design.md) |
```

- [ ] **Step 2: plan 문서 아카이브 이동 (세션 위생 규약)**

```bash
git mv docs/superpowers/plans/2026-09-02-image-resize-720.md docs/superpowers/plans/archive/
```

- [ ] **Step 3: 모듈 테스트 최종 확인 후 커밋·푸시·PR**

```bash
./gradlew :analytics:test :monitoring:test
git add DECISIONS.md docs/superpowers/plans
git commit -m "docs: 이미지 720px 축소 결정 기록 + plan 아카이브"
git push -u origin feat/image-resize-720
gh pr create --base develop --title "feat: 서빙 이미지 720px·q70 축소 — 잡 데코레이터 + 백필 스크립트" \
  --body "스펙: docs/superpowers/specs/2026-09-02-image-resize-720-design.md
- analytics·monitoring \`ImageResizer\` 데코레이터(잡 무수정, 초과분만·실패 시 원본 유지)
- 백필 스크립트(로컬 맥·드라이런 기본·멱등) — 실행은 운영 배포 후 별도
- 실측 예상: 버킷 51→28GB(−46%), 증가 3→1.5GB/일

🤖 Generated with [Claude Code](https://claude.com/claude-code)"
```

---

## 머지 후 런북 (코드 밖 — 계획 실행 범위 아님, 운영 배포 확인 후 수동)

1. develop→staging→main 승격, 운영 배포 확인.
2. 다음 새벽 잡 후 신규 객체 샘플 치수 확인:
   `gcloud storage cp gs://hypenow-images/thumb/<새 객체>.jpg /tmp/x.jpg && sips -g pixelWidth /tmp/x.jpg` → ≤720.
3. 본 백필 실행(로컬 맥):
   ```bash
   caffeinate -i deploy/scripts/backfill-image-resize.py thumb "public, max-age=31536000, immutable" --apply
   caffeinate -i deploy/scripts/backfill-image-resize.py monitor-brand-post "public, max-age=86400" --apply
   ```
4. `bucket_used_gb` 정시 게시로 ~28GB 확인 + FE 랭킹 상세 패널·브랜드 모니터링 카드 눈 확인.
